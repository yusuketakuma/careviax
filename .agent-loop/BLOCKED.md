# BLOCKED.md — Items Awaiting Human / External Input

**Purpose.** Append-only register of work that the autonomous loop is NOT permitted to
complete on its own and which requires a human decision or an external dependency before
it can move. Keeping these out of `PATCH_INBOX.md` prevents the loop from spinning on
things it can never self-resolve.

**Hard rule (per §15 — always lands here, no exceptions).** Any task touching:

- **auth** (Cognito / NextAuth / session / MFA),
- **security** (RLS policy changes, secrets, encryption keys, token signing),
- **destructive migration** (drop/alter column, data backfill that loses information),
- **prod deploy** (Amplify deploy / release to production),

is cross-referenced here and CANNOT be auto-landed by a Supervisor. It waits for explicit
human approval / external credentials regardless of how green the gates are.

> **2026-06-28 — billing/payments restriction LIFTED (user directive).** The user removed
> the billing/算定/payments hard-stop: billing logic is now treated the **same as any other
> domain code** — it proceeds through the normal dual maker/checker loop + objective gates,
> with **no special human-approval gate**. Existing rows below that were gated **solely** on
> billing/算定 are therefore unblocked and re-enter the normal flow. Rows gated **also** on
> auth / security / prod-deploy / destructive-migration STAY blocked on those grounds (a
> billing fix that also changes authorization or runs a destructive migration is still gated
> by the surviving category). auth/security/destructive-migration/prod-deploy remain §15.

**How it is used in the loop.**

- When a task hits one of the categories above, or stalls on something the loop cannot
  produce (external creds, a vendor decision), a Supervisor appends a row with
  `status: blocked` semantics here and stops working that task.
- The loop continues on other unblocked tasks; it re-checks `unblock_condition` each cycle.
- When the `unblock_condition` is satisfied (human says go, creds arrive), the row is
  annotated and the task re-enters the normal flow (REVIEW → VERIFY).
- Mirrors the project blocked-marker convention: external-dependency blocks are tracked
  here; monitoring/WIP counting stays separate (cc:WIP), per project memory.

**Run context.** Initial run id: `RUN-20260620-001`. Cycle 0, idle, next_action: bootstrap.

**Rules.**

- Append-only. Resolve by annotating `unblock_condition` (e.g. `RESOLVED 2026-06-21: …`),
  not by deleting the row.
- `needs` ∈ {`human_approval`, `external_creds`, `destructive_migration`, `prod_deploy`}.
- `since` is ISO-8601 local (Asia/Tokyo) when it became blocked.
- `unblock_condition` — the concrete, checkable event that lets it move.

## Schema

| task_id | reason | needs | since | unblock_condition |
| ------- | ------ | ----- | ----- | ----------------- |

## Blocked

| task_id                              | reason                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | needs                           | since      | unblock_condition                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| gbrain-embeddings                    | careviax imported (keyword-searchable) but NO semantic embeddings exist anywhere in the brain (natural-language `gbrain query` returns empty on every source). Enabling semantic recall is blocked on TWO independent constraints: (1) **Agent egress hard-block** — Claude Code's classifier hard-blocks the agent from sending private repo docs to an external embedding API (Voyage/OpenAI); in-chat user approval cannot clear it. (2) **Schema dimension lock** — the Postgres brain schema is fixed to `text-embedding-3-large` (OpenAI, 3072d); `gbrain config set embedding_model` is a no-op, switching to Voyage (1024d) needs the `docs/embedding-migrations.md` schema migration first. | human_approval + external_creds | 2026-06-20 | User-driven only: the USER runs `gbrain embed` themselves (their action, not the agent) with a key matching the schema — either (a) an OpenAI key (3072d, schema-compatible), or (b) migrate the schema to Voyage 1024d then embed with the Voyage key. **RESOLVED 2026-06-20**: USER switched the embedding provider to a **local Ollama model** (`ollama:mxbai-embed-large`, 1024d, `http://localhost:11434`). Both constraints cleared — (1) egress: a local model sends nothing to OpenAI/Voyage, no data leaves the Mac; (2) dimension: the brain schema is now 1024d and the default source (which holds careviax docs) reports **embed coverage 100%**. `gbrain query`/`search` verified returning cosine-scored results. No further user action required. |
| F-20260620-008-control-plane-runtime | The Control Plane MVP intentionally adds docs/config only. Runtime enforcement remains unavailable for denied-path blocking, automatic budget/cost stop, automatic secret/SAST scan, golden eval mutation protection, dependency/workflow approval automation, branch/PR/merge automation, shadow/canary execution, and immutable incident storage. Claiming those as enforced would create false governance assurance.                                                                                                                                                                                                                                                                              | human_approval                  | 2026-06-20 | Open a separate implementation task with explicit human approval for the specific runtime surface. The task must include a validator/controller design, tests proving denied-path and high-risk approval enforcement, secret/PHI redaction proof, rollback behavior, and peer-reviewed updates to `CONTROL_PLANE_CONFIG.yml`, `GATE_CONFIG.md`, and the affected ledgers. Until then, supervisors apply the config manually and mark runtime enforcement as deferred.                                                                                                                                                                                                                                                                                             |

<!-- APPEND NEW ROWS BELOW. auth/security/destructive-migration/prod-deploy ALWAYS land here (§15). billing/payments NO LONGER auto-gated as of 2026-06-28 (user lifted the billing restriction — normal loop applies). -->

| RUN-20260622-001-medical-ui-gate-stabilization | Codex hit the loop hard-stop after repeated focused set-audit final approval conflict Playwright timeouts. The latest failed DOM showed set-audit progress 0/3 and disabled approval/checklist controls after set→set-audit navigation/hydration. Further safe progress likely requires Claude ownership or a narrow expanded lock for `src/components/features/dispense-workbench/*`; Codex must not keep blind-retrying or edit outside granted locked paths. | human_approval | 2026-06-22T09:33:50+09:00 | RESOLVED when claude-lead either takes ownership of the dispense-workbench hydration/write-handler root cause or grants Codex a narrow expanded lock, followed by a passing focused set-audit final approval conflict Playwright run. |

> Resolution note 2026-06-22T10:26:08+09:00: resolved as test-side inside Codex's granted locked paths, with no product-code lock expansion required. Focused set-audit conflict E2E passed after preserving SPA phase-tab navigation and asserting the final approval `plan_id`; focused persistence and mobile set-audit smoke also passed. Remaining work is peer review and the combined-run caveat recorded in `.agent-loop/STATE.md`, not this blocker.

> Resolution note 2026-06-22T10:32:14+09:00: combined-run caveat is resolved. The desktop set-audit final approval Playwright command passed both conflict and persistence cases together in one worker. This blocker remains closed; current wait is normal peer review, not a product-code or lock-expansion blocker.

| RUN-20260622-001-medical-ui-gate-prescription-intake-timeout | Final `pnpm medical-ui:e2e:gate` is blocked by `/api/prescription-intakes` returning 500 under the billing/PCA/prescription guardrail test. Evidence: Playwright expected 400 but received 500 at `tools/tests/e2e-billing-pca-prescription-guardrails.spec.ts:249`; Next dev log reports Prisma interactive transaction expiration at `workflowException.findFirst` in `src/server/services/prescription-intake-service.ts`; direct minimal authenticated fetch returned the expected 400 but took 33.7s. Current Codex lock forbids `src/server/**` and `src/app/api/**`, so Codex cannot product-fix this without a new owner/lock decision. | human_approval | 2026-06-22T12:52:14+09:00 | RESOLVED when claude-lead either takes/lands the product-code performance/stability fix or grants Codex a narrow expanded lock for `src/server/services/prescription-intake-service.ts` / `src/app/api/prescription-intakes/route.ts`, followed by a passing focused prescription guardrail E2E and a controlled full `pnpm medical-ui:e2e:gate`. |

| gbrain-db-embedding-dim-mismatch-768-vs-1024 | gbrain DB/semantic-index writeback fails with an embedding dimension mismatch (DB schema expects 768, provider now emits 1024). Observed 2026-06-22 by codex-lead writing the slice1 ReviewFinding: the file-plane write under `/Users/yusuke/brain/projects/careviax/...` succeeds (and `gbrain lint` passes), but the `gbrain put` DB/index insert is rejected, so semantic `gbrain query`/`search` recall stays stale for newly-written CareViaX memories. NOT a regression of the 2026-06-20 ollama 1024d switch per se — the DB schema/index appears pinned to 768 while the active provider emits 1024. Loop continues on file-plane recall (slug-path files) meanwhile. | human_approval + external_creds | 2026-06-22T06:18:00Z | RESOLVED when the human realigns the gbrain DB embedding dimension to the active provider (re-create/migrate the index to 1024d for `ollama:mxbai-embed-large`, or point the provider back to a 768d model) and re-runs `gbrain embed`/reindex so `gbrain put` DB inserts succeed. Until then, supervisors keep writing to the file-plane and record memory_id slugs in STATE; do not claim semantic DB recall is current. |
| rls-force-nonsuperuser-proof | F-20260624-003 Cycle C adds the `createScopedTxRunner` RLS-context short-tx seam (`app.current_org_id` etc. applied via `set_config` inside each scoped `$transaction`). The unit suite proves **structural RLS context application only** (reads flow through the injected `tx`; the 8 `set_config` writes run on the handed-out client). It does NOT prove that PostgreSQL **FORCE RLS** denies a non-superuser cross-org timeline rows: the E2E DB role `ph_os` is a **superuser** and superusers BYPASS FORCE RLS, so a cross-org isolation assertion run under `ph_os` would pass vacuously and overclaim tenant isolation. The `it.skip`-guarded test in `rls.test.ts` records this honestly; PATCH evidence is worded "structural RLS context proof only … does NOT claim Postgres-enforced tenant isolation." | human_approval + external_creds | 2026-06-24 | **RESOLVED (2026-07-03 承認レーン W1-8 で実装、commit はメインループ).** A dedicated non-superuser role `ph_os_app` (NOSUPERUSER NOBYPASSRLS) is provisioned by `tools/scripts/setup-rls-test-role.sql` (+ `pnpm db:e2e:rls-proof-role`), and the former `it.skip` in `src/lib/db/rls.test.ts` is now an env-gated `it` (runs only when `RLS_PROOF_DATABASE_URL` is set; skips otherwise). Connecting AS that non-superuser it proves, on a real PostgreSQL engine under FORCE ROW LEVEL SECURITY, that cross-org rows are denied: SELECT sees only the in-context org, cross-org UPDATE affects 0 rows, cross-org INSERT is rejected by WITH CHECK, and no-context is fail-close. An anti-vacuity guard asserts the connecting role is NOT superuser/BYPASSRLS (so mis-pointing the URL at `ph_os` fails loudly instead of passing vacuously). The CI `phos-aurora-rls-gate` job provisions the role and runs the proof on every push. Verified green locally 2026-07-03 against the W1-7-migrated e2e DB (5433). ~~RESOLVED when a dedicated non-superuser Postgres role (subject to FORCE RLS) plus a cross-org seed fixture are provisioned for the test DB, and an isolation test connecting as that non-superuser proves the cross-org timeline rows are denied.~~ |

<!-- §15 human-gate candidates from the 2026-06-24 ultracode 51-screen 主要UI research (ops/MAINUI_RESEARCH_SYNTHESIS.json). High-ROI but NOT auto-landable: each touches auth/billing/security/prod (audit-attribution)/destructive-data per §15. Surfaced for explicit human approval; non-gated candidates proceed via the normal dual-maker loop. -->

| mainui-workbench-real-data-default | Research C1/#1 (BLOCKER): the shared DispensingWorkbench (/dispense,/audit,/set,/set-audit) defaults to MOCK seed patients (山田花子 等) in production (NEXT_PUBLIC_WORKBENCH_USE_REAL_DATA unset → USE_MOCK true; adapter.ts:59-65) and audit OK/NG/保留 writes are MOCK_WRITE_NOOP. Flipping to real data changes who/what is recorded as dispensed/audited = audit-attribution + prod behavior. | human_approval (prod / audit-attribution) | 2026-06-24 | RESOLVED when a human approves enabling real-data default (or an explicit empty/error state when the flag is unset) for the 4 workbench screens, with the change going through maker/checker + objective gates and a verified non-mock audit trail. **RESOLVED 2026-06-24:** human approved full real-data (read+write) for the 4 workbench screens via AskUserQuestion. Landed maker/checker'd + objective gates: Slice 1A real-data default + opt-out seam + per-phase patient filter (`c6381067`); Slice 1 UI/UX honest loading/error/empty states, retry, no seed fallback (`2a08802d`). Non-mock audit trail verified by Slice 4 attribution teeth (`aa91f085`): dispense-results/dispense-audits/set-audits writes attribute exclusively to ctx.userId; a forged client dispensed_by/audited_by is ignored (mutation of the route to trust the client value reds the test). Real-data screens fail closed (empty/error state, no seed/mock fallback). |
| mainui-workbench-operator-identity | Research #3 (major): workbench status bar + calBarMeta show HARDCODED operator names (調剤者 山田花子 / 監査者 佐々木健) and 接続:オンライン / fixed セット完了 timestamps even on real data; the route returns real dispenser/auditor but the UI ignores them. Correct operator identity is an auth/session-derived, audit-attribution surface. | human_approval (auth / audit-attribution) | 2026-06-24 | RESOLVED when a human approves wiring real session/assignee identity + real connection/offline + real evidence timestamps into the workbench chrome, maker/checker + gates, no fabricated attribution. **RESOLVED 2026-06-24:** human approved operator-identity wiring via AskUserQuestion. Landed maker/checker'd + objective gates (Slice 3, `d74bf88e`): status bar shows the real recorded dispenser (dispense_results.dispensed_by) and the current operator (the API auditor = ctx.userId viewer, surfaced under an honest 「操作者：」 label — never under a completed 「監査者：」 nor folded into recorded audit attribution), connection via useNetworkOnline (HeaderSyncStatus parity) with a new AA --wb-status-offline token, and calBarMeta fail-closed to '—'. Fabricated names (山田花子/佐々木健), hardcoded 接続:オンライン, and the fixed セット完了 timestamp are removed; reviewer-audit verified zero fabricated attribution and that the viewer is not presented as the recorded auditor. Real set/audit operator names in calBarMeta remain a follow-up (currently honest '—'). |
| mainui-billing-aggregation-correctness | Research #8/#15 (major, billing): facility-standards empty requirements_status {} returns claim_status='claimable' (誤算定可); billing 摘要テンプレ count groups all template_types; 疑義 KPI counts all while table caps 10. These drive 算定 (reimbursement) correctness. | human_approval (billing) | 2026-06-24 | RESOLVED when a human approves the billing/算定 aggregation fixes (claimable gating on empty requirements, correct template-type母集団, 疑義 count/cap reconciliation) with tests proving correct counts and no over-claim. |
| F-20260625-billing-evidence-afterhours-tz | Backend scout (claude) CONFIRMED a §15 算定 bug: `src/server/services/billing-evidence/core.ts:241` `resolveAfterHoursVisitCategory` classifies 夜間/深夜/休日加算 from `visitDate.getHours()/getMinutes()/getSeconds()` (server-LOCAL tz). `visit_date` is a full `DateTime` UTC instant (`prisma/schema/visit.prisma:195`). The same file already has the correct JST idiom `japanCivilMonthParts` (core.ts:263-268: `new Date(t + JAPAN_TIME_ZONE_OFFSET_MS)` then `getUTC*`); the classifier deviates. On a UTC production server (AWS Lambda/Amplify default TZ=UTC) the hour bands are off by 9h → both under- and over-claiming of after-hours 加算. The existing test (core.test.ts:1499, local-time `new Date(2026,2,20,22,0,0)`) passes ONLY because the Vitest runner is Asia/Tokyo, masking the bug. Full evidence: `.agent-loop/research/billing-evidence-afterhours-tz-20260625.md`. | human_approval (billing / 算定) | 2026-06-25 | RESOLVED when a human approves converting after-hours classification to Japan civil time via the existing JST offset idiom (NOT bare getUTCHours, which would classify by UTC), with tz-INDEPENDENT tests (construct the stored instant in UTC) proving correct 夜間/深夜/休日 classification with no over-claim and no dropped 加算, plus a review of any upstream day-of-week/holiday determination for the same server-tz dependence. Do NOT auto-land — 算定 correctness. |
| mainui-evidence-photo-capture | Research C10/#10 (BLOCKER, security/prod): work-evidence photos (セット前/後/監査完了) are static hatch placeholders with no capture/upload/display; 3省2ガイドライン作業証跡 requires real S3 (Object Lock) presigned capture. New feature touching storage/security + prod. | human_approval (security / prod / storage) | 2026-06-24 | RESOLVED when a human approves the S3 presigned evidence-photo capture/upload/display feature, with security review (Object Lock, scoped presign, PHI handling) + maker/checker + gates. |
| mainui-data-explorer-audit-and-no-harddelete | Research #13 (major, prod): /admin/data-explorer PATCH edits lack audit-log records and it performs hard deletes. Audit by Default + destructive-data concern. | human_approval (prod / destructive-data) | 2026-06-24 | RESOLVED (2026-07-03 承認レーンで実装、commit はメインループ) — HG-1: (1) PATCH 成功時に `data_explorer.record_updated` 監査ログを記録 (変更列名のみ, PHI 値は残さない)。(2) DELETE ハンドラを追加し hard delete を全面禁止: read-only モデル/global は 403 (read-only), 編集可でも soft-delete 列を持たなければ 403 (delete-forbidden), soft-delete 列を持つ編集可モデルのみ当該列 NOW() の soft-delete + 監査 (現状該当モデルなし→常に 403)。(3) モデル別 mutability ポリシーを `READ_ONLY_MODELS` 定数化 (医療記録/調剤・監査証跡/患者臨床/同意・交付の法的記録/請求根拠・契約) し根拠コメント明記、マスタ/設定系のみ編集可。テスト: read-only PATCH/DELETE 403・編集可更新で監査行1件・hard delete SQL 不発。 |

> Addendum 2026-06-25: backend hardening triage found a narrower admin Data Explorer mutation-scope question inside this same human gate. Generic PATCH can mutate high-risk medical/audit/file rows such as `FileAsset`, `SetAudit`, `SetBatch`, `MedicationCycle`, `DispenseTask`, `DispenseResult`, `PrescriptionIntake`, and `PrescriptionLine`. Auto-marking these models read-only changes admin recovery/product operations, while leaving them mutable keeps a high-power prod mutation surface without audit-by-default. Do not auto-land a read-only list or partial mutation policy; human approval must decide the model-level mutability policy together with audit-log/no-hard-delete semantics before implementation.

| F-20260625-set-audit-photo-binding-invariant | Backend/medical-safety triage found that `set-audits` photo evidence validation currently accepts same-org `FileAsset` rows by id, `purpose='set-photo'`, completed/uploaded status, and `storage_key` prefix. The upload purpose is org-scoped and does not bind the asset to the same set plan, medication cycle, or patient. A status-only tightening is unsafe: it can still attach another patient's set-photo evidence to the wrong audit. Fixing this correctly requires a domain/security decision on the binding invariant and likely file/storage/API shape changes. | human_approval (security / medical-safety / storage / audit-attribution) | 2026-06-25 | RESOLVED when a human/domain owner approves the required evidence-photo binding invariant (for example plan/cycle/patient or an equivalent auditable relation), any needed migration/backfill strategy, and maker/checker gates proving wrong-plan/wrong-patient photo ids are rejected without weakening existing upload security. |
| F-20260625-dispense-results-patch-authz | Security scout (claude) CONFIRMED an authorization asymmetry: `PATCH /api/dispense-results/[id]` (route.ts:90-91) gates only on `requireAuthContext` with NO permission, while the sibling create `POST /api/dispense-results` gates on `permission:'canDispense'` (route.ts:851). Access is otherwise scoped by `buildMedicationCycleAssignmentWhere`, which returns null (no restriction) for `ORG_WIDE_ACCESS_ROLES = {owner,admin,pharmacist,pharmacist_trainee,clerk}`. Since `clerk.canDispense=false` but clerk is org-wide, a clerk can PATCH-edit a dispense result's actual_quantity/drug_name/discrepancy_reason/carry (算定+clinical fields) despite being unable to create one. Only guards are org/assignment scope (clerk bypasses) + the rejected-audit precondition. Full evidence + triage of the other 4 scout findings (1 false positive, 1 cosmetic, 1 intentional read, 1 policy): `.agent-loop/research/dispense-results-patch-authz-20260625.md`. | human_approval (auth / 算定 / clinical-record-integrity) | 2026-06-25 | RESOLVED (2026-07-03 承認レーンで実装、commit はメインループ)。`PATCH /api/dispense-results/[id]` は POST と同一の `requireAuthContext(req, { permission: 'canDispense' })` ゲートを持つ（`src/app/api/dispense-results/[id]/route.ts:121-126`、HEAD 済）。GET は `canDispense || canAuditDispense || canReport` でゲート済（同 route.ts:82-88）で clerk の org-wide 参照は canReport 経由で意図的に維持。回帰テスト `src/app/api/dispense-results/[id]/route.test.ts` に clerk/driver/external_viewer の 403 と owner/admin の 200 を追加し、組織横断(assignment 迂回)の clerk を権限ゲートのみが止めることを実証（23 tests green）。旧: RESOLVED when a human approves adding `permission:'canDispense'` to the PATCH (mirroring the gated POST) and decides whether the GET read should also be gated (clerk org-wide read may be intentional), with permission tests proving clerk + any non-canDispense role is 403'd on the edit while pharmacist/owner/admin still succeed. Do NOT auto-land — authorization change. |
| mainui-settings-compliance-ranges | Research #16 (major, security): admin-settings numeric compliance settings lack min/max enforcement (client Input + server Zod). Misconfiguration risk on compliance-affecting thresholds. | human_approval (security / config-integrity) | 2026-06-24 | RESOLVED (2026-07-03 承認レーンで実装、commit はメインループ, HG-2)。`SettingCatalogItem`/`SettingValueItem` に `min`/`max` を追加し3省2GL準拠のレンジを定義（`session_timeout_minutes` 5〜30分、`audit_log_retention_days` 365〜3650日、`password_min_length` 12〜128文字）。共用検証 `getSettingRangeError`（src/lib/admin/settings-catalog.ts, SSOT）を server/client 双方から再利用。server: `PATCH /api/settings`（src/app/api/settings/route.ts）が保存直前にレンジ外を検出し `VALIDATION_ERROR`(400) で拒否、GET の読み取りは既存値がレンジ外でも許容（保存時のみ拒否）。client: `settings-content.tsx` の `SettingRow` に `min`/`max` 属性＋インラインエラー表示、フォーム編集モードはレンジ外の間保存ボタンを無効化、JSON編集モードは送信直前に再検証。境界値テスト: src/lib/admin/settings-catalog.test.ts、src/app/api/settings/route.test.ts（4/5/30/31, 364, 11, 非数値の境界）、settings-content.test.tsx（レンジ外表示＋保存無効化→修正で解除）で計25 tests green。旧: RESOLVED when a human approves enforcing validated compliance ranges (client + server Zod) on the relevant settings, with tests. |
| mainui-jobs-error-log-redaction | Research #18 (major, security): /admin/jobs renders raw error_log (take:50) — risk of leaking sensitive internals; needs safe structured summary + bounded fetch + register missing handlers. | human_approval (security / redaction) | 2026-06-24 | RESOLVED (2026-07-03 承認レーンで実装、commit はメインループ, HG-3)。`/api/jobs` GET が `error_log` を返さず、既存の固定 redaction 済み文言のみから成る `error_summary`（`error_name`＝status/retry_count から分類した安全な固定ラベル、`occurred_at`＝completed_at??started_at??created_at、`message`＝既存の固定文言 'エラーが記録されています'）を返すよう変更（src/app/api/jobs/route.ts）。`error_name`/`message` はいずれも DB の error_log 本文を一切参照せず、status/retry_count/日時のみから導出 — 将来 unsanitized な error_log が書き込まれても画面には絶対に生テキストが出ない設計（defense-in-depth）。fetch は既存の `take: 50` を `RECENT_JOB_RUN_WINDOW` 定数として明示（counted-list: 古いジョブ種別は window 外だと「未実行」表示になり得ることをコメントで明記）。FE（jobs-dashboard-content.tsx）は生 `error_log` の描画を全廃し、テーブルセル・展開行・対応必要カードすべてで構造化サマリ（種別/ジョブ/発生時刻/リトライ）＋固定メッセージを表示、展開行に「詳細な生ログが必要な場合は CloudWatch を参照してください（本画面には表示されません）」の案内文言を追加。redaction テスト: route.test.ts に token/password/患者名を含む unsanitized error_log を注入してもレスポンス本文に一切現れないことを検証するテストを追加、jobs-dashboard-content.test.tsx に構造化サマリ描画＋CloudWatch 案内文言のテストを追加（計 10 tests green、focused vitest + eslint --max-warnings=0 + prettier --check + colors:check + boundaries:check 実行済み）。**スコープ外で発見した関連ギャップ（未対応、フォローアップ推奨）**: `[jobType]/route.ts` の `JOB_HANDLERS` には `daily-initial-home-visit-assessment` / `daily-pca-pump-rental-overdue` / `daily-pca-pump-return-inspection-pending` / `drug-master-freshness-check` の4ジョブ種別が登録済みだが、`/api/jobs` の `JOB_DEFINITIONS`（admin/jobs 一覧）に未掲載で管理画面から状態確認・再実行ができない（UI到達性ギャップ、redaction とは無関係のため本スライスでは追加していない）。旧: RESOLVED when a human approves the redacted/structured error-log summary + bounded query + handler registration, with redaction proof. |
| mainui-incidents-and-permission-affordance | Research #14/#21 (major, auth): incidents needs status/severity display + status-change controls + create flow (compliance記録), and pharmacy-sites/institutions show admin-only actions to non-admins (always-403). Both are authorization-affordance surfaces. | human_approval (auth) | 2026-06-24 | RESOLVED (2026-07-03, HG-4)。**(1) incidents 画面**: バックエンド(`createIncidentReport`/`updateIncidentReport`, POST/PATCH route, canAdmin ステータスガード, 監査ログ)は既存実装済みだったため FE のみ追加。`incidents-content.tsx` に severity/status を `StateBadge`(一覧行+詳細ヘッダ)で表示、ステータス変更は `canAdmin` を持つロールのみ Select(PATCH `{status}`)、非管理者は読み取り専用バッジのみ(サーバ側 canAdmin ガードと一致、403 になる操作を出さない)。作成フローは「新規記録」ボタン→Sheet(表題必須+重大度+発生日)→POST。純関数(ラベル/バッジロール/ペイロード生成)は `incidents-form.ts` に追加し `incidents-form.test.ts` で検証。**(2) pharmacy-sites/institutions**: 新規登録/編集/削除(institutions)、編集/保険設定(pharmacy-sites、GET含め全操作が API 側 canAdmin 必須)を非管理者ロールに表示しない(hide、disable+tooltip ではない)。`useAuthStore` の `currentUser.role` + `hasPermission(role, 'canAdmin')` で判定、role が未確定(null)の場合も fail-closed で非表示。institutions は列自体(actions column)を条件生成、pharmacy-sites は既存の 44px touch-target(`h-11 sm:h-11 sm:min-h-[44px]`)クラスに変更なし。permission テスト: pharmacist/clerk/owner/null の4パターンを institutions/pharmacy-sites 両方に追加。テスト: focused vitest 6 files 77 green (incidents-content/incidents-form/institutions-content/pharmacy-sites-content/incident-reports route/[id] route)、eslint --max-warnings=0、prettier --check、colors:check、boundaries:check 実行済み。旧: RESOLVED when a human approves the incident status/severity management + role-gated action affordance (hide/disable for non-admins or page guard + role-linked nav), maker/checker + gates with permission tests. |
| mainui-notification-os-bridge-redaction | Codex Discover #5 (security/privacy): the OS notification bridge risks sending raw in-app title/message/link to the browser Notification API while server external notifications are redacted. PHI egress concern. | human_approval (security / privacy / PHI) | 2026-06-24 | RESOLVED (2026-07-03, HG-5): OS/プッシュ層への PHI egress をクライアント・サーバー双方で遮断。**(1) クライアント OS ブリッジ**: `redactNotificationForOsBridge` (src/lib/notifications/os-bridge-redaction.ts) を新設し、notification-bell の maybeShowBrowserNotifications で OS ブリッジ(Notification API)へ渡す前に redact。固定タイトル「PH-OS 通知」+種別ベースの汎用本文(例「新しい緊急通知があります」)+汎用ランディング `/notifications` のみを渡し、raw な title/message/link(患者名・本文・患者 ID 付きディープリンク)は破棄。**(2) サーバー Web Push** (レビュー指摘で追加対応): `dispatchNotificationEvent` の Web Push payload (src/server/services/notifications.ts) は title/body を `buildExternalNotificationContent` で汎用化していたが `link: input.link ?? null` に患者ディープリンク(`buildPatientHref` 由来の `/patients/<patient_id>/...` = PHI)が残り、サードパーティ push 基盤(FCM/Mozilla/Apple)経由で送信され sw.ts が OS 通知の data.url として保存していた。link も共有定数 `OS_BRIDGE_LANDING_URL`('/notifications') へ redact し、患者 ID をプッシュ基盤へ渡さないよう修正(クライアント OS ブリッジと同一 SSOT)。**注**: 前回の maker note「リポジトリ内に web-push 送信側は存在せず dormant」は誤り — notifications.ts:389 の `webpush.sendNotification` は `getWebPushEnabled()`(VAPID鍵あり)でゲートされ jobs/runner.ts の dispatch パイプラインに配線された live path であり、sw.ts の push handler はこの経路に給電される。in-app 通知センター表示は従来どおり raw を保持。redaction テスト: src/lib/notifications/os-bridge-redaction.test.ts(クライアント汎用化)、src/server/services/notifications.test.ts の 'redacts Web Push payloads...' を患者 ID リンクが payload に現れないアサートへ更新(生 `/patients/patient_1` を弾く)。commit はメインループ。 |

<!-- §15 GATED findings from ultracode workflow w4hljwto2 (2026-06-25 backend audit, 33 confirmed). Full triage: .agent-loop/research/workflow-findings-20260625.md. These touch billing/算定 (§15), schema migration (§15), or cross-org RLS/tenant-isolation (security §15) → human approval before any fix. NON-gated routes→codex, services/lib→claude proceed via the dual-maker loop. -->

| WF-20260625-rls-cross-org-daily-jobs | ultracode audit flagged 8 daily-job queries that read across ALL orgs with no org_id filter and not wrapped in withOrgContext: conferences.ts:19-30 (conference notes), followups.ts:56-69/99-122/153-166 (contact logs / residences / self-reports), daily-prescription-original-retention.ts:32-58 (intakes + fax originals), cleanup.ts:10-16 (abandoned QR drafts), compliance-expiry.ts:28-35 (facility/credential/consent expiry). AMBIGUITY: these may be BY-DESIGN system-level cron jobs intended to sweep every tenant once, OR a tenant-isolation leak that should iterate per-org via withOrgContext. Cannot auto-fix: adding org_id to a system job could silently drop all-but-one org's processing; leaving it could leak/process cross-tenant PHI. Real security impact = §15. | human_approval (security / RLS / tenant-isolation) | 2026-06-25 | RESOLVED when a human/supervisor decides per job whether cross-org read is intended (mark by-design + document) or a leak (rewrite to iterate per-org with withOrgContext + RLS), then each rewrite goes through maker/checker + objective gates with a cross-org isolation test. |
| WF-20260625-billing-aggregation-correctness | ultracode audit flagged billing/算定 (reimbursement) correctness defects: billing-evidence/core.ts:1657/1662-1672 (singleBuilding uses monthly buildingPatientCount instead of per-visit tier — wrong 算定 tier), core.ts:309-310 (isClaimableOutcome excludes delivery_only but monthly/weekly counts include it — count/claim mismatch), billing-evidence/check route:136-141 (rejectionCount '返戻' without billing_month constraint — cross-month overcount), billing-candidates/close route:49-68 (transmitClaimsExportForClose does not verify billing_domain matches candidateIds — wrong-domain claim transmit), jobs/daily/billing.ts:8-12 (billingEvidence.findMany missing org_id). Billing/算定 = §15. | human_approval (billing / 算定) | 2026-06-25 | RESOLVED when a human approves each billing fix with tests proving correct counts/tiers and no over-claim; landed via maker/checker + objective gates. |
| WF-20260625-schema-finalized-schedule-unique-orgid | ultracode audit: prisma/schema/visit.prisma:305-310 VisitScheduleProposal.finalized_schedule_id has a single-column @unique and an FK referencing VisitSchedule [id] only (not [id, org_id]); VisitSchedule lacks @@unique([id, org_id]). Cross-tenant reference is currently prevented only by app-layer withOrgContext, not DB-level. Fixing requires a schema change + DB migration (composite @@unique([org_id, finalized_schedule_id]) + composite FK + add VisitSchedule @@unique([id, org_id])). Destructive/structural migration = §15. | human_approval (destructive-migration / schema) | 2026-06-25 | RESOLVED (2026-07-03 承認レーンで実装、commit はメインループ) — W1-12f: prisma/schema/visit.prisma に VisitSchedule @@unique([id, org_id]) + VisitScheduleProposal 複合FK (finalized_schedule_id, org_id)->VisitSchedule(id, org_id) + @@unique([finalized_schedule_id, org_id]) を追加。migration 20260703093000_harden_visit_schedule_proposal_finalized_tenant_fk（先頭に cross-tenant/dangling + uniqueness collision の fail-close guard DO ブロック、rollback SQL をコメント + runbook §3.1 に記載）。旧 ON DELETE SET NULL は複合FK+NOT NULL org_id のため ON DELETE RESTRICT へ（保護強化）。e2e DB deploy 済・drift なし、visit-schedule-proposals(199) + visit-schedules/rls-contract(303) green。Prisma 1:1 制約で @@unique の列順は [finalized_schedule_id, org_id]（一意性は同値）。 |
| WF-20260625-set-derivations-daycount-rounding | ultracode audit (P1): src/lib/dispensing/set-derivations.ts:47-51 deriveSlotMarks uses Math.round((end-start)/86_400_000)+1 for inclusive day count, while diffInclusiveDays:214 uses Math.floor(...)+1. On exact N.5-day diffs the two disagree, and slot-mark ('set' vs 'partial') can diverge from the completion_gate day count (line 306). Borderline §15: the day-count feeds set-completion which can affect 一包化/set 算定. Surfaced for judgment rather than auto-fixed. | human_approval (算定-adjacent / completion-gate) | 2026-06-25 | RESOLVED when a human confirms the day-count change (standardize both to Math.floor) does not alter 算定/billing outcomes (or approves the billing impact), landed via maker/checker + gates with a test pinning the N.5-day boundary. |

| F-20260625-runtime-tz-prod-env | The JST runtime-TZ guard (`assertRuntimeTimezone` in src/lib/env/assert-env.ts, wired in instrumentation.ts) only DETECTS a non-JST runtime: it `console.warn`s by default and fails fast only when production AND `ENFORCE_APP_TZ` is set. Actually guaranteeing the production process runs in Asia/Tokyo is a prod-deploy/env change, not auto-landable. The codebase's civil-time logic (date-boundary.ts date keys, `@db.Date` day boundaries, dispensing/billing today + after-hours classification) ASSUMES server TZ=JST; AWS Amplify/Lambda default TZ is UTC, which shifts every day boundary 9h. Full rationale: `.agent-loop/research/jst-runtime-timezone-decision-20260625.md`. Related 算定 deviation tracked separately as F-20260625-billing-evidence-afterhours-tz. | human_approval (prod_deploy / env) | 2026-06-25 | RESOLVED when a human (1) sets `TZ=Asia/Tokyo` on the production runtime (Amplify build/Lambda env) and confirms `getTimezoneOffset() === -540` in prod, then (2) enables `ENFORCE_APP_TZ=1` so the guard fails fast on any future TZ regression. Until then the guard stays warn-only and supervisors must not claim prod is TZ-guaranteed. |

> Addendum 2026-06-25 (security_critic REQUEST_CHANGES, consciously waived): a late security review flagged the warn-only-by-default behavior as a P1 fail-open — a prod container with NODE_ENV=production but no TZ/ENFORCE_APP_TZ (Dockerfile/workflow set neither) keeps serving with date-key/billing/day-boundary drift. This is the SAME residual this row tracks. The requested "production fail-closed by DEFAULT" is NOT auto-landed because (1) prod is currently not TZ=Asia/Tokyo, so a default-throw would CRASH the prod container on next boot — converting a pre-existing silent drift into a hard outage with no deploy-side TZ set first (§15 prod-deploy), and (2) the user's explicit directive was non-breaking warn + OPT-IN fail-fast (ENFORCE_APP_TZ) + prod TZ env via this BLOCKED/human gate. The guard does NOT introduce the drift (the code already assumed JST; it was fully silent before) — it makes it observable. Observability was strengthened in the same effort (commit follows d1c9cab0): production logs at `console.error` (not warn) and instrumentation.ts reports a non-JST runtime to Sentry, so the misconfig is ALERTABLE without crashing prod. The fail-closed default flips automatically the moment the human satisfies this row's unblock_condition (set prod TZ + ENFORCE_APP_TZ=1).

| OPDAY-S2-operating-hours-migration | 稼働日カレンダー基盤 S2: 新規 `PharmacyOperatingHours` テーブル + `BusinessHoliday` の nullable open/close 列 + **2本の partial unique index**（`UNIQUE(org_id,date,holiday_type) WHERE site_id IS NULL` / `UNIQUE(org_id,date,site_id,holiday_type) WHERE site_id IS NOT NULL`, raw SQL）+ migration 前の既存 duplicate 検査/解消 + seed。**追加のみ・非破壊**（drop/alter なし）。PLAN docs/operating-day-calendar-plan.md §2.4/§6。schema migration = §15。 | human_approval (schema-migration) | 2026-06-27 | **RESOLVED 2026-06-27**: ユーザーが S2 migration を明示承認（"S2 migration の承認は私が出すので進めて"）。条件: ①追加のみ非破壊を厳守 ②duplicate 解消は実 duplicate のある場合のみ・削除前に件数ログ＋レビュー ③maker/checker + objective gate ④**ローカル dev DB への適用＋migration/SQL レビューまでが S2 の範囲。本番 RDS への適用は別の prod_deploy ゲート**（F-20260625-runtime-tz-prod-env と同様、本番反映は別承認）。実装は Codex（DB レーン）、checker は Claude。 **LANDED 2026-06-27**: S2=f2353f7c（claude APPROVED、全 gate green、Codex がローカル dev DB へ db execute 適用 exit0 + カタログ検証、重複 preflight 0/0 で削除なし）。本番 RDS は未適用・prod_deploy ゲート維持。 |

| RR-OBS-20260703-MFA1-auth-mfa-recovery-safe-log | codex separate-track 提案: src/app/api/auth/mfa/recovery/route.ts の restore-failure ログを console.error→共有 safe logger へ移行(observability のみ・挙動変更なし主張)。auth MFA recovery は §15 hard stop (auth/security) 領域のため fable は LOCK を DENY し human gate へ退避。機微点: MFA recovery code/秘密値が error/context 経由でログへ乗らないことの構造的保証が必要。 | human_approval (auth / security) | 2026-07-03 | RESOLVED when a human approves the auth-path logging change. 承認時の必須条件: recovery code・TOTP secret・session token が logger context/error にいかなる経路でも乗らないことをテストで固定(negative assert)、b33c71b8/e8027e51 と同じ safe-log 契約(error_name のみ抽出)、opus 独立レビュー + gate green。実装は codex 予約済み。 |
