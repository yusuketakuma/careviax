# PATCH_INBOX.md — Incoming Changes-Requested Queue

**Purpose.** Append-only queue of `changes_requested` items raised during peer review
(`REVIEW_LOG.md`) or objective gating (`VERIFY_LOG.md`) that are now awaiting the _owner_
(the maker who authored the patch). Every requested change has a single owner and an
explicit resolution — nothing falls on the floor.

**How it is used in the loop.**

- A `changes_requested` verdict in `REVIEW_LOG.md`, or any `fail` gate in `VERIFY_LOG.md`,
  creates exactly one row here with `status = open`.
- The owner addresses it in their own lane (Claude = UI/UX + main impl under
  `src/app/(dashboard)/**`, `src/components/**`; Codex = backend/perf/refactor/test).
  LOCK the path via agmsg before editing; drain inbox before commit; stage only own files.
- When fixed, the owner sets `status = addressed` and writes a one-line `resolution`
  (commit sha / what changed). The reviewer then re-reviews → new `REVIEW_LOG.md` row.
- `status = wontfix` requires a `resolution` justifying it; if it is policy-blocked
  (auth/billing/payments/security/destructive migration/prod deploy) it ALSO goes to
  `BLOCKED.md`, not silently dropped here.
- Only Supervisors write here. Subagents/workers never touch agmsg or these logs directly.

**Run context.** Initial run id: `RUN-20260620-001`. Cycle 0, idle, next_action: bootstrap.

**Rules.**

- Append-only for new items; `status`/`resolution` of an existing row may be updated in place
  (it is a live queue), but `item_id`, `task_id`, `from`, `requested_changes` are immutable.
- `item_id` format: `PI-<NNN>` (zero-padded, monotonic), e.g. `PI-001`.
- `from` is the reviewer who raised it: `claude-lead` | `codex-lead`.
- `status` ∈ {`open`, `addressed`, `wontfix`}.

## Schema

| item_id | task_id | from | requested_changes | status | resolution |
| ------- | ------- | ---- | ----------------- | ------ | ---------- |

## Queue

| item_id | task_id                    | from       | requested_changes                                                                                                                                                                                                                                                                                                       | status    | resolution                                                                                                                                                                                                                                                                                                                                                                     |
| ------- | -------------------------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| PI-001  | scaffold-review (2986725b) | codex-lead | GATE_CONFIG.md classifies full `pnpm test` (~8k Vitest) as cheap/every-slice; would stall the loop or be ignored. Make unit-test gate = targeted Vitest every slice, full `pnpm test` before done/merge or periodic.                                                                                                    | resolved  | c8580b23 split GATE_CONFIG into "unit test (targeted)" cheap/every-slice + "unit test (full)" heavy/before-done; policy §2.4 updated. codex-lead APPROVED 2026-06-20T11:53:52+09:00.                                                                                                                                                                                           |
| PI-002  | F-20260620-007             | codex-lead | `/statistics` patch/rev6 plan needed contract reconciliation and permission hardening: preserve raw 64 recon → exact 23 navigable manifest with `/admin/jobs`, exact manifest tests, malformed-2xx/stale-refetch/raw-error/org-hydration tests, page `canViewDashboard` gate, and per-surface permission filtering.     | open      | Rev7 reviewed 2026-06-20T21:43+09:00 → CHANGES_REQUESTED. Remaining: align KPI client schema with raw dispensing-stats API or an API-lane envelope, fix clerk-support to canViewDashboard, fix report-delivery to canSendCareReport, add exact 23-entry manifest/provenance assertions, and add StatisticsPage integration coverage. Claude ACKed valid and is preparing rev8. |
| PI-003  | F-20260620-009             | codex-lead | Command palette rev2 required: hide stale rows and disable Enter when query drops below MIN_CHARS; avoid AppShell Escape shortcut swallowing dialog Escape while palette is open; align visible search copy to active categories only and keep prescription/contact deferred until minimal backend contracts are wired. | addressed | Rev3 addressed the stale-actionability, Escape, and copy issues; codex rechecked focused Vitest 107/107 plus typecheck/no-unused/eslint/prettier/build GREEN. New rev3 findings are tracked separately in PI-004.                                                                                                                                                              |
| PI-004  | F-20260620-009             | codex-lead | Rev3 must not land while active palette categories call full patient/proposal/report list APIs and over-fetch PHI/metadata; minimal-shell routes must not create invisible palette open state; the modal needs a named close control for touch screen-reader users.                                                     | open      | Claude ACKed valid and is preparing rev4: UI #2/#3 fixes plus PHI-bearing categories deferred so active palette is drug-only; backend minimal projections handed off as F-20260620-012.                                                                                                                                                                                        |

<!-- APPEND NEW ITEMS BELOW. item_id is monotonic (PI-001, PI-002, ...). -->

| PI-005 | RUN-20260622-001-medical-ui-gate-stabilization | claude-lead | `patients-board.tsx` must not keep a local `READABLE_STATUS_BADGE_CLASSES` map that forks the State Color SSOT. Promote the readable badge classes to `src/lib/constants/status-tokens.ts`, restore patients-board to `spec.badgeClassName`, and update the StateBadge unit contract to assert foreground text plus token-colored ring/tint. | addressed | Addressed 2026-06-22T10:45+09:00 and Claude re-review approved: `STATUS_TOKENS[*].badgeClassName` now owns the readable foreground + role-colored ring/tint contract; patients-board local map deleted; `state-badge.test.tsx` asserts the new contract for all 7 roles. Focused Prettier, ESLint, StateBadge unit, PatientsBoard unit, `git diff --check`, and no-emit tsc passed. Final full medical-ui gate remains separate. |
| PI-006 | RUN-20260622-001-medical-ui-gate-stabilization | codex-lead | Final medical-ui gate must not be released while `/api/prescription-intakes` can return 500 for the unconfirmed-injection guardrail under E2E load. Resolve the Prisma interactive transaction timeout/performance issue or produce an owner-approved mitigation, then rerun the focused prescription guardrail E2E and controlled full `pnpm medical-ui:e2e:gate`. | addressed | Addressed by commit `97ece552` after human/Claude decision to fix the code-level transaction scope root cause without migration: case-target blocked prescription intakes now run guardrails before creating a `MedicationCycle`, preserving the 400 + `blocked_lines` contract and avoiding orphan cycles. Validation evidence recorded in `.codex/ralph-state.md` / `CODEX_GOAL_PROGRESS.md`: service unit 20/20, prescription-intakes route unit 31/31, focused prescription guardrail E2E 1/1 under 5s with no cycle-count increase, full billing/PCA/prescription guardrail E2E 4/4, medical-ui preflight, changed-file Prettier/ESLint/diff-check, and full typecheck. The later one-piece `medical-ui:e2e:targeted` interruption is tracked as runner/environment caveat, not a product 500 regression. |
| PI-007 | F-20260622-001-slice1 | codex-lead | Rev1 admin select a11y tests under-lock the approved behavior. Add `sm:min-h-[44px]` assertions alongside `min-h-[44px]` for all four migrated Select triggers, and extend the alert type/severity select test through save so the `/api/drug-alert-rules` POST body proves the changed `alert_type` and `severity` are persisted. | addressed | Rev2 approved by codex-lead 2026-06-22T14:56:16+09:00. Test-only strengthening closes both requirements: four Select triggers assert both 44px classes, and alert-rules save payload asserts `alert_type=narcotic` plus `severity=critical`. Focused Vitest 11/11, scoped ESLint, scoped Prettier, and scoped `git diff --check` passed. |
| PI-008 | F-20260622-001-slice2 | codex-lead | Rev1 capacity plan must include the loading skeleton layout update and make the DOM-order component test mandatory. Option A is approved as direction, but implementation should not start until the plan says loading follows KPI → attention → two charts and a new component test locks `今すぐ見るべきこと` before the chart sections. | addressed | Addressed by slice2 implementation and approved rev3 2026-06-22T15:18:38+09:00: loaded layout is KPI grid → `今すぐ見るべきこと` → 2 chart grid; loading layout is KPI skeletons → attention skeleton → 2 chart skeletons; component test locks loaded order and loading mirror. |
| PI-009 | F-20260622-001-slice2 | codex-lead | Rev1 capacity patch tests must lock the approved implementation more tightly. Strengthen the DOM-order test so `今すぐ見るべきこと` follows the full KPI grid (for example, after the last KPI heading `緊急余力` or a stable KPI-grid container), and add loading-state regression coverage for KPI skeletons → attention skeleton → two chart skeleton groups. | addressed | Rev2 addressed the full-KPI anchor and added loading-state coverage but missed explicit `attention(1)`. Rev3 approved 2026-06-22T15:18:38+09:00 after adding `expect(attention.childElementCount).toBe(1)`; focused Vitest 3/3, scoped ESLint, scoped Prettier, and scoped `git diff --check` passed. |
| PI-010 | F-20260622-001-slice3 | codex-lead | Document-templates PageSection plan must lock the h2/h3 hierarchy and its tests tightly: wrap template-management and delivery-rules as PageSection h2, convert nested CardTitle subpanels to real h3 with `asChild`, demote TemplateBodyEditor inner headings to h3, align its region naming, make the parent TemplateBodyEditor mock prop-aware instead of static, and scope heading assertions so duplicate parent/child headings cannot satisfy the wrong test. | addressed | Rev4 approved by codex-lead 2026-06-22T15:58:00+09:00. The plan now includes PageSection h2 wrappers, nested CardTitle asChild h3 panels, TemplateBodyEditor h3/aria-labelledby cleanup, scoped heading assertions, no duplicate child h2 sentinel, and a field-validating TemplateBodyEditor parent mock that fails unless it receives the expected `template_1` / `主治医報告 基本` / `content.sections[0] === summary` fixture contract. |
| PI-011 | F-20260622-002-slice4a | codex-lead | Drug-masters select migration plan rev1 must preserve clear-to-unselected behavior for コピー元拠点 and テンプレート, preserve the first-site effective target default, clear/disable copy source when target site changes to prevent same source/target state, strengthen local MockSelect to forward aria-label/aria-labelledby and include empty/sentinel items, assert min-h plus sm:min-h 44px trigger classes, and handle 44px popup-item touch targets. | addressed | Rev5 approved 2026-06-22T17:17:27+09:00. Plan now requires complete target-site reset, mandatory templatePreview and bulkPreview stale-clear tests, captured original SelectItem className checks, explicit clear labels, existing focused-red fix, and full-width-parens delete name. PATCH_REVIEW must verify implementation matches this contract. |
| PI-012 | F-20260622-002-slice4a | codex-lead | Rev2 drug-masters patch must close the remaining stale-preview safety/test gaps before landing: update latest-context refs synchronously or use an equivalent token/reducer so stale dry-run onSuccess cannot pass before passive `useEffect` flushes; include `copyOverwrite` in copy/template dry-run request context and stale checks; and make tests exercise mutationFn request-context stamping instead of manually injecting request context into onSuccess fixtures. | open | Rev3 closed the original mutationFn-stamping and `copyOverwrite` gaps, but codex-lead sent CHANGES_REQUESTED 2026-06-22T18:33:58+09:00 because stale-guard refs became the safety SSOT while reset paths such as template search, template delete success, and bulk import success still clear state without synchronously clearing refs. Claude ACKed valid 2026-06-22T18:35:31+09:00 and is implementing rev4 with setter helpers plus regressions. |
