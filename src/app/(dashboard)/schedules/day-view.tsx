'use client';

/**
 * Schedule day view component.
 * Full implementation is planned for Phase 1a-2 (visit scheduling).
 */
export function ScheduleDayView() {
  return (
    <div className="flex items-center justify-center rounded-lg border border-dashed border-border py-16">
      <p className="text-sm text-muted-foreground">スケジュール表示は準備中です</p>
    </div>
  );
}
