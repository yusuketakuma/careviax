import { OperationsInsightsContent } from './operations-insights-content';

export const metadata = {
  title: '在宅業務の動きを見る — PH-OS',
};

export default function OperationsInsightsPage() {
  return <OperationsInsightsContent />;
}
