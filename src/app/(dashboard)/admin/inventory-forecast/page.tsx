import { InventoryForecastContent } from './inventory-forecast-content';

export const metadata = {
  title: '在庫と定期処方の予測 — PH-OS',
};

export default function InventoryForecastPage() {
  return <InventoryForecastContent />;
}
