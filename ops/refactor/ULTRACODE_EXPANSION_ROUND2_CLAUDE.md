# Ultracode Expansion — Round 2 (Claude neighbor sweep) — 2026-07-02

確定 CE01-CE19 を種にした近接領域(兄弟ファイル/二次consumer/同パターン網羅)の loop-until-dry 掃引。6 finder × 敵対検証。既存 F01-F78 および round-1 CE01-CE19 と dedup 済み。
**レビュー台帳。実装しない(ユーザー指示: 相互レビューのみ)。** flag=auth/security/RLS/migration/PHI は BLOCKED.md のハードストップ級 → 人間承認まで着手不可。

- raw: 38 / confirmed-novel: 33 / refuted: 3 / dup: 2
- byClass: {"missing-rls-tenant-isolation": 10, "rls-ssot-drift": 9, "false-empty / false-zero (unread isError collapses a safety/ops gate open)": 1, "concurrency-check-then-act": 3, "perf-unbounded-findMany / load-all-to-use-one": 1, "tz-date-boundary": 5, "offline-reliability": 2, "false-empty": 1, "perf-unbounded-findMany / load-all-to-use-newest-per-group": 1}

## ⚠️ 最重要クラスター: RLS テナント分離 (compliance / 3省2ガイドライン)

CE04 の隣接調査で **19件のRLS所見** が出た。2系統:

- **missing-rls-tenant-isolation**: org_id NOT-NULL の org-scoped テーブルが migration にも SSOT にも **RLS が一切ない** → DB層の defense-in-depth backstop が欠落(live leak には app層の org_id 抜けが別途必要だが、repo の FORCE-RLS 方針が全org-scoped表に要求するもの)。
- **rls-ssot-drift**: migration では ENABLE+POLICY+FORCE 済みだが `prisma/rls-policies.sql`(SSOT/監査証跡/再provision元/contract test allowlist) に **0行** → 完全migration済み本番DBは保護されるが、SSOT provision環境・監査artifact・contract testでdrift。
- Claude 独立スポット検証済: N01(PatientPackagingProfile=RLS皆無), N02(FacilityContact), N03(JahisSupplementalRecord) は grep で確認。

凡例: N##=round-2番号。score=5軸合計。~CExx=どの種の隣接か。

## N01 [22] missing-rls-tenant-isolation [flag] — PatientPackagingProfile (patient PHI) has NOT-NULL org_id but zero RLS in any migration or SSOT

- File: `prisma/schema/patient.prisma:373` (neighbor of CE04)
- Scores: safety 4 / effect 3 / verify 5 / scope 5 / efficiency 5 = **22**
- Reachability: Model migrated (CREATE TABLE present), NOT-NULL org_id, actively queried in src (2 files). No RLS ENABLE/FORCE/policy anywhere.
- Evidence: PatientPackagingProfile has org\*id String (NOT NULL, Organization-owned patient data). grep across prisma/migrations/\** finds NO `ENABLE/FORCE ROW LEVEL SECURITY` and NO tenant policy for "PatientPackagingProfile"; it is absent from the 60-table failsafe array in 20260328234500*rls_context_failsafe/migration.sql and absent from prisma/rls-policies.sql (0 ALTER TABLE / 0 POLICY lines). It is not in the SSOT's documented no-RLS list (rls-policies.sql:458-509 lists only Drug\*, LabelDictionary, IntegrationJob). Queried per-tenant in app code (patientPackagingProfile.\* in 2 files).
- Failure scenario: rls-policies.sql header states RLS covers 'all multi-tenant tables', and CLAUDE.md mandates RLS as defense-in-depth backstop for app-layer org filters. A patient-packaging query that omits `org_id: ctx.orgId` (or runs the module-level prisma client outside withOrgContext) returns another tenant's patient packaging/medication data with NO DB-layer block — the second defense layer is entirely absent for this PHI table.
- Suggested fix: Add ENABLE + tenant_isolation policy (org_id = public.app_enforced_org_id()) + FORCE RLS in a new migration and mirror into prisma/rls-policies.sql.
- Verifier (adversarial): Verified in code: PatientPackagingProfile (prisma/schema/patient.prisma:373) has NOT-NULL org_id and is patient PHI, yet grep across all of prisma/ (migrations + rls-policies.sql) for the table combined with ROW LEVEL/POLICY/ENABLE/FORCE returns zero matches. The table was created (20260328235900) after the failsafe migration (20260328234500), so it was never in the failsafe array (line 67 lists PatientMcsLink, not this table), and no later RLS-hardening migration (e.g. 20260608150000 hardened sibling PatientLabObservation) ever added it. rls-policies.sql:3 claims coverage for 'all multi-tenant tables' and its no-RLS list (459,509) only excepts global Drug/dictionary tables, so this is an omission not a design choice. Distinct from CE04: CE04's tables (PatientSelfReport/CommunityActivity) are actually ENABLEd in migration 20260329020500 and only drift on FORCE; this table has NO RLS at all in the DB — different, worse root cause. Not in F01-F78 or CE01-CE19. Nuance: current read paths reach the profile via the org-scoped Patient relation, so a live cross-tenant leak needs a secondary app-layer miss; this is a genuine missing defense-in-depth backstop the repo's FORCE-RLS pattern mandates for every org-scoped PHI table.

- [ ] **Codex cross-review**:

## N02 [22] rls-ssot-drift [flag] — rls-policies.sql SSOT entirely omits Facility, FacilityContact, and ExternalProfessional (no ENABLE/policy/FORCE), though their creating migration ENABLEs+FORCEs all three org-scoped tables — leaves facility-contact PII with no RLS if provisioned from the SSOT

- File: `prisma/rls-policies.sql:459` (neighbor of CE04)
- Scores: safety 4 / effect 3 / verify 5 / scope 5 / efficiency 5 = **22**
- Reachability: SSOT is authoritative per compliance docs; uncaught by contract test.
- Evidence: Migration ...add_facility_and_external_masters/migration.sql:103 (ENABLE) / :114 (FORCE) protect "FacilityContact"; prisma/rls-policies.sql has 0 lines for it.
- Failure scenario: SSOT-provisioned environment leaves FacilityContact (facility contact PII: names/phones) unprotected; not covered by rls-policy-contract.test.ts allowlist.
- Suggested fix: Add ENABLE+policy+FORCE for FacilityContact to SSOT.
- Verifier (adversarial): Verified: prisma/migrations/20260328234500_add_facility_and_external_masters/migration.sql creates FacilityContact with org_id NOT NULL and PII (name/phone/email/fax), ENABLEs RLS (:103), adds tenant_isolation policy on org_id (:104-106), and FORCEs RLS (:114). prisma/rls-policies.sql (the documented re-applyable SSOT, cited as compliance evidence EV-SRC-03) has 0 occurrences of FacilityContact — and also 0 for Facility and ExternalProfessional; the whole migration's RLS block was never ported. The RLS contract test allowlist (src/tools/rls-policy-contract.test.ts:31-74) omits all three, so the drift is uncaught. This is a genuine neighbor of CE04 but a distinct, more severe defect: CE04's tables are present in the SSOT with ENABLE+policy and only lack FORCE (owner-bypass only); here the tables are entirely absent, so SSOT-provisioned environments get no tenant isolation at all. Reachability is conditional (production uses the migration chain, which forces these tables; SSOT matters for standalone provisioning and as an auditor-facing compliance artifact), matching CE04's accepted profile. Classification=flag (RLS/PHI/security).

- [ ] **Codex cross-review**:

## N03 [22] rls-ssot-drift [flag] — SSOT omits JahisSupplementalRecord entirely though migration ENABLEs+FORCEs it (prescription PHI)

- File: `prisma/rls-policies.sql:459` (neighbor of CE04)
- Scores: safety 4 / effect 3 / verify 5 / scope 5 / efficiency 5 = **22**
- Reachability: SSOT authoritative per compliance docs.
- Evidence: Migration 20260421090000_jahis_supplemental_records/migration.sql:40 (ENABLE), :41 (policy jahis_supplemental_record_org_isolation), :43 (FORCE). prisma/rls-policies.sql has 0 lines for "JahisSupplementalRecord".
- Failure scenario: JAHIS supplemental prescription records (dispensing/prescription PHI). SSOT-provisioned/RLS-reset environment leaves it unprotected — cross-tenant prescription data disclosure; uncaught by contract test.
- Suggested fix: Add ENABLE+policy+FORCE for JahisSupplementalRecord to SSOT.
- Verifier (adversarial): Code-confirmed: prisma/rls-policies.sql (the re-appliable RLS SSOT, header line 4 'Usage: Run via psql or as a Prisma migration') contains ZERO references to JahisSupplementalRecord (grep -c "Jahis" = 0), yet its sole migration 20260421090000_jahis_supplemental_records/migration.sql:42-45 ENABLEs RLS, creates the org_isolation policy (org_id = current_setting('app.current_org_id')), and FORCEs RLS. The model is live (prescription.prisma:587) and holds org-scoped prescription/dispensing PHI (patient_id, payload JSONB, raw_line), reachable via src/app/api/qr-scan-drafts and prescription-intakes routes. NOT a duplicate of CE04: CE04 covers PatientSelfReport/CommunityActivity which have ENABLE+policy but merely lack FORCE; this table is entirely absent from the SSOT (no ENABLE/policy/FORCE), a distinct and more severe omission requiring its own three statements — CE04's fix does not remediate it. No jahis match in either dedup source file. Reachability is conditional (SSOT-provisioning or app_user table ownership; a fully-migrated live DB is protected by the migration), mirroring CE04's conditional impact.

- [ ] **Codex cross-review**:

## N04 [22] rls-ssot-drift [flag] — SSOT omits PharmacyCooperationMessage entirely though migration ENABLEs+FORCEs it

- File: `prisma/rls-policies.sql:459` (neighbor of CE04)
- Scores: safety 4 / effect 3 / verify 5 / scope 5 / efficiency 5 = **22**
- Reachability: SSOT authoritative per compliance docs.
- Evidence: Migration 20260619223000_add_pharmacy_cooperation_message_threads/migration.sql:111 (ENABLE) / :116 (FORCE) for "PharmacyCooperationMessage"; prisma/rls-policies.sql has 0 lines for it.
- Failure scenario: Individual cooperation messages unprotected in an SSOT-provisioned DB — cross-tenant leak of clinical cooperation content; uncaught by contract test.
- Suggested fix: Add ENABLE+policy+FORCE for PharmacyCooperationMessage to SSOT.
- Verifier (adversarial): Code-confirmed: grep of prisma/rls-policies.sql returns 0 matches for PharmacyCooperationMessage (and 0 for PharmacyCooperationMessageThread), while migration 20260619223000_add_pharmacy_cooperation_message_threads/migration.sql:111-116 ENABLEs + adds tenant_isolation policy + FORCEs RLS for PharmacyCooperationMessage (and :104-110 for the Thread). The SSOT is fully explicit per-table with no dynamic FOR-ALL-TABLES loop (verified), so both org-scoped clinical-cooperation tables are entirely absent = genuine SSOT-vs-migration drift. This is a real neighbor of CE04 but NOT a duplicate: CE04 covers PatientSelfReport/CommunityActivity missing only FORCE (ENABLE+policy present); this candidate is a total omission of different tables (no RLS whatsoever in SSOT), a strictly more severe leak vector requiring a separate fix (add full ENABLE+policy+FORCE block). Same conditional reachability as CE04 (prod uses the migration chain which forces it; risk only if a DB is provisioned directly from rls-policies.sql per its documented psql-usage header). Note: candidate's cited line 459 is incorrect (that line is in the Drug Domain comment block; the table is absent everywhere), but the substantive claim holds.

- [ ] **Codex cross-review**:

## N05 [22] rls-ssot-drift [flag] — SSOT omits SavedView entirely though migration ENABLEs+FORCEs it

- File: `prisma/rls-policies.sql:459` (neighbor of CE04)
- Scores: safety 4 / effect 3 / verify 5 / scope 5 / efficiency 5 = **22**
- Reachability: SSOT authoritative per compliance docs.
- Evidence: Migration 20260614120000_wave2_design_fidelity_contract/migration.sql:43 (ENABLE) / :48 (FORCE) for "SavedView"; prisma/rls-policies.sql has 0 lines for it.
- Failure scenario: SavedView (per-tenant saved filters/views, may embed patient identifiers in filter params) unprotected in SSOT-provisioned DB; uncaught by contract test.
- Suggested fix: Add ENABLE+policy+FORCE for SavedView to SSOT.
- Verifier (adversarial): Code confirms every claim. Migration prisma/migrations/20260614120000_wave2_design_fidelity_contract/migration.sql ENABLEs (:43), policies (:45), and FORCEs (:48) RLS on "SavedView", but grep of prisma/rls-policies.sql returns zero lines for SavedView — it is entirely absent (no ENABLE, no tenant_isolation policy, no FORCE). SavedView is org-scoped (prisma/schema/saved-view.prisma:5 org_id String required; @@unique([org_id,user_id,scope,name])) and its filters Json field holds opaque per-screen filter params that can embed patient identifiers. The contract test src/tools/rls-policy-contract.test.ts:59-74 only checks a fixed allowlist that does not include SavedView, so the drift is uncaught. Reachability is conditional (live prod applies the migration chain which forces SavedView; the SSOT file only governs the psql<rls-policies.sql provisioning path documented in its own header at line 4, or the app_user-owns-table case) — the same conditional profile CE04 was accepted under. Not a duplicate of CE04: CE04 enumerated a closed set of two tables (PatientSelfReport, CommunityActivity) with ENABLE-present/FORCE-missing; SavedView is a distinct third table that is entirely absent (no RLS at all in the SSOT path — worse: cross-tenant for all roles, not just owner-bypass), and fixing CE04 does not fix SavedView.

- [ ] **Codex cross-review**:

## N06 [21] missing-rls-tenant-isolation [flag] — VisitScheduleOverride is org-scoped (NOT-NULL org_id) but has zero RLS in any migration, rls-policies.sql, or the fail-close failsafe — the only visit-schedule-family table without a DB tenant-isolation backstop

- File: `prisma/schema/visit.prisma:372` (neighbor of CE04)
- Scores: safety 3 / effect 3 / verify 5 / scope 5 / efficiency 5 = **21**
- Reachability: Migrated, NOT-NULL org_id, 4 query files.
- Evidence: org_id String NOT NULL. No RLS in migrations/failsafe; 0 lines in rls-policies.sql. Queried in 4 src files.
- Failure scenario: VisitScheduleOverride query missing org_id exposes another tenant's visit scheduling overrides with no DB backstop.
- Suggested fix: Add ENABLE+policy+FORCE RLS.
- Verifier (adversarial): Code-confirmed: VisitScheduleOverride (prisma/schema/visit.prisma:372) has NOT-NULL org_id (tenant-scoped) but ZERO row-level security anywhere — grep for the table name in prisma/rls-policies.sql returns 0 matches, and no migration (baseline, 20260326120000_workflow_schedule_ops creating migration, or the 20260328234500_rls_context_failsafe list) ever issues ENABLE/POLICY/FORCE ROW LEVEL SECURITY for it. Every sibling in the family (VisitSchedule rls-policies.sql:145/536, VisitScheduleProposal 446/583, VisitScheduleProposalBatch 451/584) has full RLS; Override is the sole omission, and the failsafe list includes only 'VisitSchedule' (migration line 51). Reachable: queried in src/server/services/home-care-ops.ts:738, src/app/api/visit-schedules/[id]/reschedule/route.ts:321/339/349/622, .../reschedule/approve/route.ts:52/104, .../[id]/route.ts:911/922. Mitigation I checked: all current call sites explicitly filter org_id in the app layer (home-care-ops:740, approve:54/107), so this is a defense-in-depth DB-backstop gap rather than a live cross-tenant leak — but the backstop is genuinely absent. Not a duplicate of CE04 (which is FORCE-only SSOT-vs-migration drift on tables that DO have ENABLE+policy); this is total RLS absence on a different table, a distinct (file, root-cause). Not present in F01-F78.

- [ ] **Codex cross-review**:

## N07 [21] missing-rls-tenant-isolation [flag] — VisitScheduleContactLog has NOT-NULL org_id but no RLS anywhere

- File: `prisma/schema/visit.prisma:343` (neighbor of CE04)
- Scores: safety 4 / effect 3 / verify 5 / scope 5 / efficiency 4 = **21**
- Reachability: Migrated, NOT-NULL org_id.
- Evidence: org_id String NOT NULL. No RLS in migrations/failsafe; 0 lines in rls-policies.sql.
- Failure scenario: VisitScheduleContactLog query missing org_id leaks another tenant's patient contact-attempt logs with no DB backstop.
- Suggested fix: Add ENABLE+policy+FORCE RLS.
- Verifier (adversarial): Code-accurate and reachable. prisma/schema/visit.prisma:345 defines org_id String (NOT NULL) on VisitScheduleContactLog, a PHI table (patient_id, contact_name, contact_phone, note). grep -c of the table name in prisma/rls-policies.sql = 0, and no migration applies RLS (the creating migration 20260326120000_workflow_schedule_ops enables RLS only on PatientSchedulePreference/PharmacistShiftTemplate, skipping the contact log), so there is no ENABLE/policy/FORCE anywhere — no DB tenant-isolation backstop. Reachable via day-board/route.ts:1064, visit-preparations/[scheduleId]/route.ts:1092, visit-schedule-proposals/[id]/route.ts. Mitigation the candidate understates: the app-layer queries DO filter org_id explicitly (day-board:1064 where org_id: ctx.orgId), so this is a defense-in-depth loss rather than an active leak, matching the flagged/conditional severity of CE04/F79. Not a duplicate of CE04 (that is FORCE-only drift on tables that already have ENABLE+policy) — but the root-cause CLASS (org-scoped PHI table with RLS entirely absent) is identical to F79 (FormularyChangeRequest/FormularyTemplate), just a different table/file; it is a neighbor of F79, not CE04. Novel per the (file, root-cause) tuple since the file/table differs. Sibling VisitScheduleOverride (visit.prisma:372) shares the same gap.

- [ ] **Codex cross-review**:

## N08 [21] rls-ssot-drift [flag] — SSOT omits PatientCondition entirely though migration ENABLEs+FORCEs it (medical PHI)

- File: `prisma/rls-policies.sql:459` (neighbor of CE04)
- Scores: safety 4 / effect 3 / verify 5 / scope 5 / efficiency 4 = **21**
- Reachability: SSOT authoritative per compliance EV-SRC-03; contract test does not guard this table.
- Evidence: Migration 20260329022000_add_patient_conditions_table/migration.sql:26 (ENABLE), :30 (FORCE), with policy using public.app_enforced_org_id() at :27-29. prisma/rls-policies.sql has 0 lines for "PatientCondition".
- Failure scenario: PatientCondition holds patient diagnoses/conditions (要配慮個人情報). An SSOT-provisioned or RLS-reset environment leaves this medical PHI table fully unprotected, enabling cross-tenant disclosure of patient conditions; not in rls-policy-contract.test.ts allowlist.
- Suggested fix: Add ENABLE + app_enforced_org_id policy + FORCE for PatientCondition to SSOT.
- Verifier (adversarial): Code confirms the claim. prisma/migrations/20260329022000_add_patient_conditions_table/migration.sql ENABLEs (line 26), defines a tenant_isolation policy with public.app_enforced_org_id() (lines 27-29), and FORCEs (line 30) RLS on "PatientCondition". prisma/rls-policies.sql (740 lines) contains 0 occurrences of "PatientCondition" — the table is entirely absent from the re-appliable SSOT (whose documented usage is `psql < rls-policies.sql`, header line 4). src/tools/rls-policy-contract.test.ts:59-74 allowlist does not cover PatientCondition, so no test guards the gap. Not a duplicate of CE04: CE04 concerns PatientSelfReport/CommunityActivity being present in the SSOT but omitted only from the FORCE block (a FORCE-bypass that requires app_user table ownership); this candidate is a different table completely missing from the SSOT, so an SSOT-provisioned/reset env gets NO RLS whatsoever on PatientCondition (not even the policy), a stronger unconditional cross-tenant PHI disclosure. Same file as CE04 but the (file, root-cause) tuple differs (entirely-missing vs missing-FORCE-only). F82 mentions PatientCondition only re a CDS-context gap. Classification flag: RLS/PHI/security danger-zone; real-world reachability is conditional on the SSOT-provisioning path (prod applies migrations which already FORCE the table), matching accepted CE04's conditional profile.

- [ ] **Codex cross-review**:

## N09 [21] rls-ssot-drift [flag] — SSOT omits PharmacyCooperationMessageThread entirely though migration ENABLEs+FORCEs it

- File: `prisma/rls-policies.sql:459` (neighbor of CE04)
- Scores: safety 4 / effect 3 / verify 5 / scope 4 / efficiency 5 = **21**
- Reachability: SSOT authoritative per compliance docs.
- Evidence: Migration 20260619223000_add_pharmacy_cooperation_message_threads/migration.sql:104 (ENABLE) / :109 (FORCE) for "PharmacyCooperationMessageThread"; prisma/rls-policies.sql has 0 lines for it.
- Failure scenario: Inter-pharmacy cooperation message threads (may contain patient-linked clinical discussion). SSOT-provisioned DB leaves it unprotected cross-tenant; uncaught by contract test.
- Suggested fix: Add ENABLE+policy+FORCE for PharmacyCooperationMessageThread to SSOT.
- Verifier (adversarial): Confirmed by reading both files. prisma/rls-policies.sql (740 lines) has 0 references to PharmacyCooperationMessageThread or its sibling PharmacyCooperationMessage — the tables are entirely absent (no ENABLE, no policy, no FORCE). The creating migration prisma/migrations/20260619223000_add_pharmacy_cooperation_message_threads/migration.sql ENABLEs (:104,:111), adds tenant_isolation policy (:106), and FORCEs (:109,:116) both, and both carry org_id NOT NULL (:11,:30). PharmacyCooperationMessage.body is free-text 1-4000 chars (:34,:39-41) in patient-linked (share_case_id/visit_request_id) inter-pharmacy clinical cooperation — PHI-bearing. SSOT header (:4) documents re-applyability via psql, so provisioning from the SSOT yields NO RLS at all on these org-scoped tables — strictly worse than CE04 (whose tables keep ENABLE+policy, missing only FORCE). NOVEL vs CE04: different tables and a more complete omission; the round-2 rls-neighbors mission (ultracode-expansion-round2-workflow.mjs:71) explicitly asks for exhaustive SSOT/migration drift diffing excluding only CE04's two tables. Reachability is conditional (real prod provisions via migration chain that already FORCEs; exploit needs SSOT-based provisioning or app_user table ownership) — same low-but-genuine posture as the confirmed CE04. The cited line 459 is only the adjacent Drug-domain section; the defect is absence, not that line.

- [ ] **Codex cross-review**:

## N10 [20] false-empty / false-zero (unread isError collapses a safety/ops gate open) [fix] — admin/realtime 「今すぐ見る運用シグナル」top panel false-zeros workflow-derived safety/ops KPIs on query error, while the same file guards the identical lower KPIs

- File: `src/app/(dashboard)/admin/realtime/page.tsx:208` (neighbor of CE01)
- Scores: safety 3 / effect 3 / verify 5 / scope 5 / efficiency 4 = **20**
- Reachability: Reachable on every /admin/realtime mount (admin/realtime/page.tsx) whenever the workflow endpoint fails independently of notifications; workflowQuery.isError is a real, already-consumed signal in the same component (lines 222, 338).
- Evidence: workflowQuery = useRealtimeQuery (line 99) feeds `workflow = workflowQuery.data?.data` (line 128). The top section `<section aria-labelledby="realtime-operation-signals">` (「今すぐ見る運用シグナル」= 'operational signals to check right now', lines 168-220) is rendered UNCONDITIONALLY and shows three workflow-derived counts with `?? 0`/`?? []` fallbacks: priorityWorkbenchItems.length (line 201, derived from `workbenchItems = workflow?.unified_workbench ?? []` at line 130 → filter high/urgent at 136), `workflow?.route_control.pending_override_requests ?? 0` (line 208, 変更承認待ち), and `workflow?.workflow_exceptions.open ?? 0` (line 215, 未処理例外). NONE of these top-section values check workflowQuery.isError. The developer explicitly guards the SAME KPIs in the lower 'ルート・例外 KPI' section: line 222 `{workflowQuery.isError ? <ErrorState/> : (...)}` with comment line 223 'ワークフロー取得失敗時は KPI を 0 (false-zero) 表示せず' — and that guarded branch re-renders pending_override_requests / emergency_impact_items / `workflow_exceptions.open ?? 0` (line 256). So on a workflow fetch error the lower section correctly shows an ErrorState + 再読み込み, but the top 'signals to check right now' panel silently shows 高優先ワークベンチ 0 / 変更承認待ち 0 / 未処理例外 0.
- Failure scenario: A supervisor opens /admin/realtime while /api/dashboard/workflow?view=realtime returns 5xx / RLS failure / times out (SSE-backed useRealtimeQuery → workflowQuery.isError=true, exactly as the lower section relies on). The top 運用シグナル panel renders 高優先ワークベンチ 0, 変更承認待ち 0 (pending route-change approvals), 未処理例外 0 (open workflow exceptions) as an all-clear, so pending override/reschedule requests and open workflow exceptions — including emergency-impact routing items — appear to be zero when the data actually failed to load. The lower ErrorState is easy to miss because the top 'check right now' summary reads green/zero.
- Suggested fix: Gate the top 「今すぐ見る運用シグナル」workflow-derived cards on workflowQuery.isLoading/isError the same way the lower 'ルート・例外 KPI' section is (line 222) — render an ErrorState / placeholder instead of `?? 0`/`?? []` for priorityWorkbenchItems, pending_override_requests, and workflow_exceptions.open when the workflow query has not succeeded.
- Verifier (adversarial): Code at src/app/(dashboard)/admin/realtime/page.tsx confirms the claim: the top 「今すぐ見る運用シグナル」panel (lines 169-220) renders unconditionally and shows three workflow-derived KPIs with ?? 0 / ?? [] fallbacks and no workflowQuery.isError guard — priorityWorkbenchItems.length (201, derived from workflow?.unified_workbench ?? [] at 130), pending_override_requests ?? 0 (208), workflow_exceptions.open ?? 0 (215). On a fresh-mount workflow fetch error, workflow (= workflowQuery.data?.data, line 128) is undefined, so all three show 0. The identical KPIs are explicitly false-zero-guarded in the lower section (line 222, comment line 223 「KPI を 0 (false-zero) 表示せず」), proving the omission is unintentional intra-file inconsistency. Reachable in prod whenever /api/dashboard/workflow?view=realtime fails. Partial mitigation exists (an ErrorState renders directly below at 222, and the SSE 再接続中 indicator at 176-186 signals connection loss), lowering but not removing the false-zero. Novel: F78 covers the notification-merge helper (67-97), a different root cause; CE01/CE02 are different files.

- [ ] **Codex cross-review**:

## N11 [20] missing-rls-tenant-isolation [flag] — FormularyTemplate has NOT-NULL org_id but no RLS anywhere

- File: `prisma/schema/drug.prisma:246` (neighbor of CE04)
- Scores: safety 3 / effect 3 / verify 5 / scope 4 / efficiency 5 = **20**
- Reachability: Migrated, NOT-NULL org_id, 6 query files.
- Evidence: org_id String NOT NULL. No RLS in migrations/failsafe; 0 lines in rls-policies.sql. Queried in 6 src files.
- Failure scenario: FormularyTemplate query missing org_id exposes another tenant's formulary configuration with no DB backstop.
- Suggested fix: Add ENABLE+policy+FORCE RLS.
- Verifier (adversarial): Code-confirmed: FormularyTemplate (prisma/schema/drug.prisma:246-259) has org_id String NOT-NULL plus @@unique([org_id,name]) and @@index([org_id,created_at]), so it is unambiguously org-scoped, not a global master. grep of prisma/rls-policies.sql for "FormularyTemplate" returns 0 lines, and its creating migration (20260527233800_add_formulary_templates/migration.sql) has no ENABLE/policy/FORCE ROW LEVEL SECURITY — total RLS absence. It is NOT covered by the intentional-global exemption note at rls-policies.sql:458 (that note lists org_id-less masters DrugMaster/DrugPackage/DrugInteraction). Its same-domain sibling PharmacyDrugStock (the source table) gets full ENABLE(463)+policy(464)+FORCE(575), proving RLS is the expected posture; FormularyTemplate and FormularyChangeRequest were both added in the later 2026-05-27 migration batch and never wired into the RLS SSOT — a whole-feature oversight. Reachable via src/app/api/pharmacy-drug-stock-templates routes. Novel vs CE04: CE04 is ENABLE-present-but-FORCE-missing drift on PHI tables; this is total RLS absence on a config table — different (file, root-cause). Not in F01-F78 (0 formulary matches). Honest caveat: all current queries explicitly filter org_id (route.ts:30,73,106; [id]:11; apply:148,151,207,255), so no active leak today — this is a missing defense-in-depth DB backstop (the 二重防御 model CLAUDE.md mandates), and data is org business config, not PHI, so harm is conditional and lower than CE04.

- [ ] **Codex cross-review**:

## N12 [20] missing-rls-tenant-isolation [flag] — FacilityUnit is a NOT-NULL org_id tenant table with no RLS anywhere (migrations + SSOT), unlike its parent Facility and siblings — missing DB tenant-isolation backstop

- File: `prisma/schema/organization.prisma:399` (neighbor of CE04)
- Scores: safety 3 / effect 3 / verify 5 / scope 4 / efficiency 5 = **20**
- Reachability: Migrated, NOT-NULL org_id.
- Evidence: org_id String NOT NULL. No RLS in migrations/failsafe; 0 lines in rls-policies.sql. Note the parent Facility IS forced in migrations (see separate finding) but the child FacilityUnit is unprotected.
- Failure scenario: FacilityUnit query missing org_id exposes another tenant's facility unit/ward structure with no DB backstop.
- Suggested fix: Add ENABLE+policy+FORCE RLS.
- Verifier (adversarial): Code-verified: FacilityUnit (organization.prisma:399, org_id NOT NULL per migration 20260330140000) has zero RLS anywhere — grep for FacilityUnit in prisma/rls-policies.sql returns 0 lines, and no migration adds ENABLE/POLICY/FORCE ROW LEVEL SECURITY to it. Parent Facility is both ENABLE'd and FORCE'd (mig 20260328234500:98,113) and siblings Residence (rls-policies.sql:35-36,515) and FacilityContact (:103) are protected, making FacilityUnit the lone unprotected org-scoped child — clearly an oversight, not spec. The rls-policy-contract.test.ts allowlist does not include FacilityUnit, so nothing waives it. Distinct from CE04 (FORCE-only drift on PatientSelfReport/CommunityActivity, which retain ENABLE+policy) and absent from F01-F78 → NOVEL. Severity is moderated to defense-in-depth: every current call-site (units/route.ts:25, units/[unitId]/route.ts, facility-reference.ts:55) explicitly filters org_id, so a cross-tenant leak requires a future query that omits the filter; and the data is non-PHI facility/ward structure, not patient data.

- [ ] **Codex cross-review**:

## N13 [20] rls-ssot-drift [flag] — SSOT omits ExternalProfessional entirely though migration ENABLEs+FORCEs it

- File: `prisma/rls-policies.sql:459` (neighbor of CE04)
- Scores: safety 4 / effect 4 / verify 5 / scope 3 / efficiency 4 = **20**
- Reachability: SSOT authoritative per compliance docs.
- Evidence: Migration ...add_facility_and_external_masters/migration.sql:108 (ENABLE) / :115 (FORCE) protect "ExternalProfessional"; prisma/rls-policies.sql has 0 lines for it.
- Failure scenario: SSOT-provisioned DB leaves ExternalProfessional (external doctors/care-team member PII) unprotected across tenants; uncaught by contract test.
- Suggested fix: Add ENABLE+policy+FORCE for ExternalProfessional to SSOT.
- Verifier (adversarial): Verified: migration 20260328234500_add_facility_and_external_masters/migration.sql:108/109/115 ENABLEs, policies, and FORCEs RLS on ExternalProfessional (org_id NOT NULL, holds external doctor/care-team PII per schema organization.prisma:445-471), while prisma/rls-policies.sql has 0 references to it (grep -c = 0) — absent from both the ENABLE and FORCE sections. This is real SSOT drift: a DB provisioned/re-applied from rls-policies.sql leaves ExternalProfessional (and siblings Facility/FacilityContact/PrescriberInstitution, also absent) with no tenant isolation. Distinct from CE04, which covers PatientSelfReport/CommunityActivity that ARE present in the SSOT (ENABLE+policy) but only miss the FORCE block — different tables and a different (more severe: fully absent) root cause. Reachability is conditional (prod uses prisma migrate deploy where the migration protects the table; no migration invokes rls-policies.sql), so it warrants flag not a live fix — consistent with how CE04 was classified. Not a duplicate of any F-entry or CE-seed.

- [ ] **Codex cross-review**:

## N14 [19] missing-rls-tenant-isolation [flag] — BillingRule is an org-scoped NOT-NULL multi-tenant table with zero RLS (no ENABLE/FORCE/policy) — defense-in-depth gap; all ~6 current query sites still filter org_id so harm is latent, not a live leak

- File: `prisma/schema/admin.prisma:38` (neighbor of CE04)
- Scores: safety 4 / effect 3 / verify 5 / scope 3 / efficiency 4 = **19**
- Reachability: Migrated, NOT-NULL org_id, 11 query files. Zero RLS.
- Evidence: BillingRule.org_id String NOT NULL. No RLS ENABLE/FORCE/policy in any migration, not in failsafe array, 0 lines in rls-policies.sql. Queried in 11 src files (billing logic).
- Failure scenario: A billing-rule query missing org_id (11 call sites, billing-critical) exposes/applies another tenant's fee configuration with no DB isolation, producing wrong billing for the wrong tenant.
- Suggested fix: Add ENABLE+policy+FORCE RLS in migration and SSOT.
- Verifier (adversarial): Verified in code: prisma/schema/admin.prisma:38-40 defines BillingRule with NOT-NULL org_id and org-scoped indexes (@@unique([org_id, ssot_key]), @@index([org_id])), making it multi-tenant data. Grepping prisma/rls-policies.sql and every migration returns ZERO ENABLE/FORCE ROW LEVEL SECURITY or CREATE POLICY for BillingRule, whereas its sibling BillingCandidate in the same file has both ENABLE (rls-policies.sql:342) and FORCE (567). The rls-policies.sql header states RLS covers 'all multi-tenant tables', so the absence is a genuine deviation, not an intentional global/reference table. This is a real defense-in-depth tenant-isolation gap consistent with confirmed seed CE04 (missing-FORCE) and F79 (total-absence on Formulary tables). Two corrections to the candidate: (1) 'Queried in 11 src files' is inflated — only ~6 real Prisma query sites exist; (2) all current query sites filter org_id: ctx.orgId (e.g. billing-evidence/check/route.ts:154-160,236-243; master-hub/route.ts:216-221; stats:56; analytics:101), so there is NO live cross-tenant leak today — the harm is the missing DB backstop, worsened by stats/analytics using the raw prisma. client without withOrgContext. Not a duplicate: F79 and CE04 concern different tables (FormularyChangeRequest/FormularyTemplate, PatientSelfReport/CommunityActivity); BillingRule appears in neither findings doc (grep returned nothing).

- [ ] **Codex cross-review**:

## N15 [19] rls-ssot-drift [flag] — SSOT prisma/rls-policies.sql omits base tables "Facility" and "FacilityContact" entirely (no ENABLE/POLICY/FORCE) though their migration ENABLEs+FORCEs RLS — SSOT-provisioned envs leave facility master data cross-tenant readable

- File: `prisma/rls-policies.sql:459` (neighbor of CE04)
- Scores: safety 3 / effect 2 / verify 5 / scope 5 / efficiency 4 = **19**
- Reachability: SSOT is referenced as authoritative RLS snapshot in compliance/ops docs; contract test does not guard this table.
- Evidence: Migration 20260328234500_add_facility_and_external_masters/migration.sql:98 (ENABLE) and :113 (FORCE) protect "Facility", but prisma/rls-policies.sql has 0 ALTER TABLE / 0 POLICY lines for "Facility" (verified grep). Unlike CE04's tables, Facility is not even ENABLEd in the SSOT.
- Failure scenario: rls-policies.sql is the documented RLS SSOT (compliance EV-SRC-03 in docs/compliance/three-ministry-guideline-mapping.md, ops/SYSTEM_MAP.md). A DR/test/fresh environment or 'reset RLS' provisioned from this file leaves Facility (with facility addresses, contacts) fully unprotected — cross-tenant leak. The rls-policy-contract.test.ts allowlist (src/tools/rls-policy-contract.test.ts:31-74) does not cover Facility, so drift is uncaught.
- Suggested fix: Add ENABLE + tenant_isolation policy + FORCE RLS blocks for Facility to prisma/rls-policies.sql to match the migration.
- Verifier (adversarial): Code-confirmed: base tables "Facility" and "FacilityContact" are org-scoped (organization.prisma:369/:422, org_id NOT NULL per migration 20260328234500/migration.sql:30) and their migration ENABLEs (:98/:103) + FORCEs (:113/:114) RLS, yet prisma/rls-policies.sql (the documented RLS SSOT, header :1-6) contains ZERO ENABLE/POLICY/FORCE lines for either (grep count 0; only unrelated FacilityVisitBatch :150/:537 and FacilityStandardRegistration :320/:563 match). Other later-migration org-scoped tables (PharmacyOperatingHours, ExternalAccessGrant) ARE present in the SSOT, so this is genuine drift, not a blanket exclusion. Facility is queried live with org_id filters (admin/facilities/route.ts:39-101, [id]/route.ts:127, master-hub/route.ts:162) holding facility addresses/phone/fax/contacts. NOT intentional: org_id is NOT NULL and the live migration forces RLS. NOT a duplicate: CE04 covers PatientSelfReport+CommunityActivity, which ARE ENABLEd+policied in the SSOT and only miss FORCE; this is a different table pair AND a stronger root-cause (entire absence from SSOT → no isolation at all if provisioned from the file, vs FORCE-only owner-bypass). Not present in F01-F78 (F41 = facility-contacts route duplication, unrelated). Reachability is conditional/low, same as the accepted CE04: live DB uses the migration chain that already applied FORCE; the drift only bites a fresh/DR/test provisioning run from rls-policies.sql alone.

- [ ] **Codex cross-review**:

## N16 [18] concurrency-check-then-act [flag] — visit-billing-candidates generate: canUpdateExistingCandidate billing-lock check is not re-asserted in the update where — overwrites already-invoiced candidate billing data

- File: `src/app/api/visit-billing-candidates/route.ts:384` (neighbor of CE05 (id-only update where without re-asserting the guard state))
- Scores: safety 3 / effect 3 / verify 4 / scope 4 / efficiency 4 = **18**
- Reachability: POST /api/visit-billing-candidates (batch generate) and the invoice-creation service run in separate default-isolation (Read Committed) withOrgContext transactions; no FOR UPDATE / Serializable / re-asserted where guards the candidate. Reachable by regenerate concurrent with invoicing, or two overlapping generate runs.
- Evidence: Line 293-307 batch-reads existingCandidates selecting billing_status + invoice_items (take 1) INSIDE the tx. The per-record loop (312) gates with canUpdateExistingCandidate() (382/418), which returns true only when billing_status ∈ {'candidate','excluded'} AND invoice_items.length===0 (def at 124-132). When canUpdate, line 384 (and the unique-constraint retry at 420) does tx.visitBillingCandidate.update({ where: { id_org_id: { id, org_id } }, data: candidateData }) — the where re-asserts NEITHER billing_status NOR invoice_items:none, so the gate is advisory only. The sibling pharmacyVisitRequest.updateMany at 434-441 correctly re-asserts status:{in:allowedFrom}, and pharmacy-invoices.ts:643-652 locks candidates atomically via updateMany({where:{billing_status:{in:['candidate','confirmed']}, invoice_items:{some:{invoice_id}}}, data:{billing_status:'invoiced'}}) — proving the id-only update here is the outlier. candidateData contains recomputed billing amounts/breakdown.
- Failure scenario: A pharmacist runs POST /api/visit-billing-candidates (regenerate) for a billing month while, concurrently, pharmacy-invoices.ts creates an invoice over the same partner-visit records. tx-A reads existingCandidates at line 293 (billing_status='candidate', no invoice_items → canUpdate=true), then iterates records doing per-record contract lookups (a real time window). tx-B (invoicing) commits: it links PharmacyInvoiceItem rows to the candidate and flips billing_status to 'invoiced'. tx-A then reaches line 384 and its id-only update overwrites the now-invoiced candidate's calculation_breakdown/amounts, so the persisted invoice references a candidate whose billing figures were silently mutated after invoicing — billing-integrity corruption that the canUpdate lock was written to prevent.
- Suggested fix: Change lines 384/420 to tx.visitBillingCandidate.updateMany({ where: { id, org_id, billing_status: { in: ['candidate','excluded'] }, invoice_items: { none: {} } }, data: candidateData }) and treat count!==1 as skippedLockedCount (already the fall-through), then re-fetch for the response.
- Verifier (adversarial): Verified in src/app/api/visit-billing-candidates/route.ts: existingCandidates batch-read (290-307) inside the interactive withOrgContext tx, canUpdateExistingCandidate (124-132) gates on billing_status∈{candidate,excluded} AND invoice_items.length===0, but the update at 384 (and retry 420-423) uses where:{id_org_id:{id,org_id}} with NO re-assertion of billing_status/invoice_items — a genuine check-then-act. The per-record loop's await tx.pharmacyContractVersion.findFirst (313) is a real time window. A concurrent invoice tx (pharmacy-invoices.ts:643-653 updateMany flips billing_status→invoiced and links invoice_items) can commit inside that window; the id-only update then overwrites the now-invoiced candidate's billing_status/amount_snapshot/is_billable, corrupting billing integrity. Sibling correctness proofs: pharmacyVisitRequest.updateMany (434-441) re-asserts status:{in:allowedFrom} and the invoicing updateMany re-asserts status+invoice_items, showing the id-only update is an oversight. Reachable via regenerate POST concurrent with invoice-draft creation on the same billing month (both canManageBilling, separate Read-Committed transactions). Test route.test.ts:508 only characterizes the read-time-stale case, not the race, so it does not lock the id-only where as intentional. Novel: not F44 (cases transition), F71 (workflow-exceptions/dispense-tasks/patient-share activate), F83/CE05 (FormularyChangeRequest), or CE06 (dispense-results) — new file and new lock object (VisitBillingCandidate billing-lock via invoice_items).

- [ ] **Codex cross-review**:

## N17 [18] missing-rls-tenant-isolation [flag] — PharmacySiteInsuranceConfig has NOT-NULL org_id but no RLS anywhere

- File: `prisma/schema/organization.prisma:165` (neighbor of CE04)
- Scores: safety 3 / effect 3 / verify 5 / scope 4 / efficiency 3 = **18**
- Reachability: Migrated, NOT-NULL org_id, 3 query files.
- Evidence: org_id String NOT NULL. No RLS in any migration/failsafe array; 0 lines in rls-policies.sql. Queried in 3 src files.
- Failure scenario: Insurance-config query without org_id filter leaks another pharmacy/tenant's insurance billing configuration with no DB backstop.
- Suggested fix: Add ENABLE+policy+FORCE RLS in migration and SSOT.
- Verifier (adversarial): Verified: prisma/schema/organization.prisma:165-192 defines PharmacySiteInsuranceConfig with org_id NOT NULL (line 167) and @@unique([org_id,...]) — genuine org-scoped tenant billing config. Grep across prisma/rls-policies.sql and all migration.sql files returns ZERO RLS statements (ENABLE/FORCE/CREATE POLICY) for this table; it is also absent from the failsafe array (20260328234500) and the RLS contract test (src/tools/rls-policy-contract.test.ts:31-74). Parent PharmacySite IS RLS-protected (baseline:1994,2107) but RLS does not cascade through FK, so the child has no DB backstop. All current app queries filter by org_id (route.ts:40,68; billing-runtime-context.ts:189), so this is a defense-in-depth gap with no active leak today — identical class to F79 (P0 FIX) which flags FormularyChangeRequest/FormularyTemplate for the same missing-RLS root cause. NOT a duplicate: CE04 = SSOT/migration drift on PatientSelfReport/CommunityActivity; F79 = Formulary tables; this is a distinct table with total-absence root cause and no F-entry mentions it.

- [ ] **Codex cross-review**:

## N18 [18] perf-unbounded-findMany / load-all-to-use-one [fix] — Print hub downloads the entire org's SetPlan table via unbounded GET /api/set-plans to pick one plan

- File: `src/app/api/set-plans/route.ts:142` (neighbor of F76 (dispense-queue unbounded org-wide findMany) + CE11 (load-all-to-use-newest-per-group))
- Scores: safety 2 / effect 3 / verify 5 / scope 4 / efficiency 4 = **18**
- Reachability: Reachable: GET /api/set-plans authenticatedGET path; the print hub (reports/print) invokes it unfiltered via useQuery(enabled: needsSetPlan). Confirmed consumer at print-hub-content.tsx:122-136.
- Evidence: parseSetPlanListQuery makes both cycle_id and patient_id OPTIONAL (route.ts:98-109); when neither is supplied the where clause is just { org_id, ...assignmentWhere } and tx.setPlan.findMany (route.ts:142) runs with orderBy created_at desc and NO take. The live consumer print-hub-content.tsx:125 calls fetch('/api/set-plans') with NO query params, then pickPrintSetPlan (print-hub.shared.ts:293-308) reduces the whole array to select ONE plan (longest target-period span, tie-break newest created_at). So the FE materializes every SetPlan row the org has ever created just to render a single print document.
- Failure scenario: An org that has run for a year accumulates thousands of SetPlan rows (one+ per medication cycle period, schema prisma/schema/prescription.prisma). Every time a pharmacist opens the print hub for a document type that needs a set plan (needsSetPlan), the server serializes and the browser downloads the org-wide SetPlan list (with packaging_method_ref/cycle includes), then throws all but one away. Payload and query cost grow without bound as history accumulates; nothing is paginated or windowed.
- Suggested fix: Require a cycle_id or patient_id filter for the list (or add pagination/take), and have the print hub pass the specific patient/cycle it is printing so the server returns at most that patient's plans; pickPrintSetPlan then reduces a bounded set.
- Verifier (adversarial): Verified at source: set-plans/route.ts:142 findMany has no take; where (132-137) uses only org_id + optional cycle_id/patient_id + assignmentWhere, and parseSetPlanListQuery (98-109) makes both filters optional. print-hub-content.tsx:125 fetches /api/set-plans with no params (enabled: needsSetPlan), and pickPrintSetPlan (print-hub.shared.ts:293-308) reduces the entire org-wide array to a single plan. This is a genuine load-all-to-use-one / unbounded-findMany pattern, reachable when a pharmacist opens the print hub for set_instruction/medication_calendar/medication_label. Distinct file and root-cause from F43 (admin/capacity/route.ts:70, dashboard KPI) — grep of both dedup files shows no entry for api/set-plans/route.ts or print-hub. Novel neighbor of F76/CE11. Classification fix (read-only BFF perf; no schema/auth/billing change). Caveat: purely a perf/efficiency finding, not a medical false-negative, so safety harm is low.

- [ ] **Codex cross-review**:

## N19 [17] tz-date-boundary [fix] — Home-care feature summary builds all day-windows with date-fns startOfDay(new Date()) (server-local) against instant + @db.Date columns

- File: `src/server/services/home-care-ops.ts:444` (neighbor of F66 (daily-helpers server-local startOfDay) / CE03)
- Scores: safety 3 / effect 3 / verify 4 / scope 3 / efficiency 4 = **17**
- Reachability: Imported by patients/[id]/route.ts and visit-preparations/[scheduleId]/route.ts (getHomeCareFeatureSummary / getPatientHomeCareFeatureSummary).
- Evidence: Line 1 imports startOfDay from 'date-fns' (server-local). getHomeCareFeatureSummary:444 and getPatientHomeCareFeatureSummary:1069 do today=startOfDay(new Date()); upcomingWindow=addDays(today,7); shortWindow=addDays(today,3). `today` is then used as gte/lte against scheduled*date (DateTime @db.Date, visit.prisma:115) at 552/1187, and against DateTime \_instant* columns next_review_date (patient.prisma:322, no @db.Date) at 665/679, expires_at (261, instant) at 641/1233, expiry_date (patient.prisma:288, instant) at 714/1244, refill_next_dispense_date & prescription_expiry_date (prescription.prisma:177/181, instant) at 727/730. date-boundary.ts mandates todayUtcRange for @db.Date and japanDayInstantRange for instant columns.
- Failure scenario: On UTC prod, startOfDay(new Date()) = UTC-midnight. For instant columns the correct JST day-start is prior-day 15:00Z, so the window is skewed ~9h year-round; for @db.Date scheduled_date, during 00:00-08:59 JST `today` resolves to yesterday's sentinel, so 'upcoming visits/reviews/refills/expiries' over-include one past day and drop the far edge. e.g. a management-plan review due today or a refill due today can fall outside the today..+7 window shown on the home-care dashboard.
- Suggested fix: Use todayUtcRange(now) for @db.Date scheduled_date, and japanDayInstantRange(now).gte for the instant columns (next_review_date/expiry_date/expires_at/refill_next_dispense_date/prescription_expiry_date).
- Verifier (adversarial): home-care-ops.ts:444/1069 compute today=startOfDay(new Date()) (date-fns, server-local; import at line 1) and use it as a gte bound against true-instant DateTime columns: expires_at (641), expiry_date (714, patient.prisma:288), refill_next_dispense_date/prescription_expiry_date (727/730, prescription.prisma:177/181 no @db.Date), next_review_date (patient.prisma:322). date-boundary.ts:4-16,100-138 mandates japanDayInstantRange for instant columns and todayUtcRange for @db.Date. assert-env.ts:99-134 confirms JST is NOT enforced (ENFORCE_APP_TZ gated behind still-BLOCKED F-20260625, comment says prod is 'not-yet-JST') so prod runs UTC — the same premise F65/F66/CE03 were accepted under. Under UTC the instant-column windows start 9h late, dropping items due JST 00:00-09:00 today (missed management-plan reviews, refills, prescription/consent expiries) on the home-care dashboard. Reachable via patients/[id]/route.ts and visit-preparations/[scheduleId]/route.ts. Distinct file+root-cause from F66 (daily-helpers.ts) and F65 (next-day.ts); home-care-ops.ts appears in CE03 only as an is_current reader. NOTE: the candidate's @db.Date scheduled_date sub-claim is wrong for current UTC prod (startOfDay UTC == correct sentinel), but the instant-column defect is real and the suggested fix already splits the two correctly.

- [ ] **Codex cross-review**:

## N20 [17] tz-date-boundary [fix] — External shared upcoming-visits query uses server-local startOfDay against @db.Date scheduled_date

- File: `src/server/services/external-access.ts:811` (neighbor of CE03/F06 server-local day boundary)
- Scores: safety 2 / effect 2 / verify 5 / scope 5 / efficiency 3 = **17**
- Reachability: external-access.ts is consumed by external-access/route.ts, external-access/shared.ts, patients/[id]/route.ts (external grant visit view).
- Evidence: Line 4 imports startOfDay from 'date-fns'. Line 811: visitSchedule.findMany where scheduled_date: { gte: startOfDay(new Date()) }. scheduled_date is DateTime @db.Date (visit.prisma:115/172, UTC-midnight-of-JST sentinel), but startOfDay(new Date()) on UTC prod = UTC-midnight of the UTC civil date.
- Failure scenario: During 00:00-08:59 JST on UTC prod, startOfDay(new Date()) = yesterday's UTC midnight = yesterday's @db.Date sentinel, so the externally-shared 'upcoming visits' list for the patient/family includes yesterday's already-past visit until 09:00 JST.
- Suggested fix: Use todayUtcRange(new Date()).gte (utcDateFromLocalKey(japanDateKey())) for the scheduled_date lower bound.
- Verifier (adversarial): Verified: external-access.ts:4 imports startOfDay (date-fns, server-local); line 811 uses it as the gte lower bound for scheduled_date, which is DateTime @db.Date (visit.prisma:115,172) stored as the UTC-midnight sentinel of the JST civil date. date-boundary.ts:3-16 explicitly names scheduled_date as a @db.Date column that MUST use utcDateFromLocalKey/todayUtcRange and warns that local/instant midnight lands on 前日15:00 UTC. On UTC prod during 00:00-08:59 JST, startOfDay(new Date()) = UTC-midnight of the UTC civil date = yesterday's JST-date sentinel, so the externally-shared upcoming-visits list includes yesterday's already-past visit until 09:00 JST. Reachable via GET /api/external-access/[token]/route.ts:24 -> buildExternalAccessPayload (line 713) -> scope.visit_schedule block (791-823). Novel vs CE03 (cockpit route.ts created_at carryover), F06 (capacity route.ts setPlan), F66 (shared daily-helpers), F62 (next-day.ts jobs): same bug class, different file/consumer/line = a genuine seed neighbor. Harm is low (over-includes one past-day visit of the grant's own authorized patient; no cross-patient leak or auth bypass; ≤9h daily window). Classification=fix: one-line where-clause date-boundary correction, no auth/scope/PHI-visibility change.

- [ ] **Codex cross-review**:

## N21 [17] offline-reliability [fix] — Offline evidence-photo drafts auto-sync is page-scoped to /capture only — captured-offline photos never replay on reconnect elsewhere (CE12 neighbor, different queue)

- File: `src/lib/offline/evidence-drafts.ts:349` (neighbor of CE12)
- Scores: safety 3 / effect 3 / verify 5 / scope 3 / efficiency 3 = **17**
- Reachability: Reachable on any offline-capture-then-navigate flow, which is the primary mobile home-visit use case the feature exists for.
- Evidence: setupEvidenceAutoSync (evidence-drafts.ts:349) registers a window 'online' listener that calls syncEvidenceDrafts, but its ONLY caller is capture-content.tsx:127 inside a page-scoped useEffect (teardown on unmount). The p0_48 photo drafts live in the separate evidenceDrafts table (offline-db.ts:77) and are NOT part of the syncQueue that query-provider's global reconnect (CE12) also ignores. grep confirms no global setupEvidenceAutoSync/syncEvidenceDrafts anywhere except capture-content.tsx. Also nothing (e.g. createRecord in visit-record-form) re-triggers evidence sync after the visit record is created.
- Failure scenario: Pharmacist opens /visits/[id]/capture offline, snaps evidence photos (saveEvidenceDraft stores them, toast promises '端末に保存しました' and the module comment/UX promises '戻ったら自動で送信します'), then navigates to the record form or any other page BEFORE reconnecting. When the network returns, the global reconnect handler only refetches react-query data and never calls syncEvidenceDrafts; the capture page's 'online' listener was torn down on navigation. The encrypted photos sit in IndexedDB unsent indefinitely until the user manually re-opens that exact capture page and a reconnect fires. Compounds when photos were captured before the visit record existed (result.skipped, retryCount not incremented): even after the record is created online, nothing re-attempts the skipped drafts.
- Suggested fix: Register evidence auto-sync (and a reconnect drain) in a global provider/root-provider alongside the visit-record sync, keyed by orgId, rather than only inside capture-content.
- Verifier (adversarial): Code confirms the claim. setupEvidenceAutoSync (evidence-drafts.ts:349-358) registers a window 'online' listener; its ONLY prod caller is capture-content.tsx:127 inside a useEffect whose teardown removes the listener on unmount (line 357). grep confirms setupEvidenceAutoSync/syncEvidenceDrafts appear in prod only in capture-content.tsx — no global provider/layout/root drains the evidenceDrafts table. The immediate save-time sync is gated on navigator.onLine (capture-content.tsx:148), so offline captures depend entirely on the page-scoped reconnect listener. UI text (capture-content.tsx:337 '戻ったら自動で送信します') and the module comment (evidence-drafts.ts:348) promise auto-send on reconnect, so the page-scoped mounting is an oversight, not intentional. The skipped-draft detail is also accurate: syncEvidenceDraftsOnce (evidence-drafts.ts:318-321) increments result.skipped and continues WITHOUT bumping retryCount when no visit record exists, and no code re-triggers syncEvidenceDrafts after the record is later created. This is a genuine neighbor of CE12, not a duplicate: CE12 targets query-provider.tsx:28 / sync-engine.ts processSyncQueue draining the visit-record syncQueue via visit-record-form's listener; this is a distinct module (evidence-drafts.ts), a distinct IndexedDB table (evidenceDrafts), and a distinct page-scoped listener. It is explicitly not the excluded src/phos/api/offlineEvidenceQueue (F54). No F-entry covers this exact auto-sync path. Reachable via the real /visits/[id]/capture dashboard route in the primary mobile home-visit workflow.

- [ ] **Codex cross-review**:

## N22 [17] false-empty [fix] — Notifications page's '未同期' warning row is gated on global pendingSyncCount that is never bootstrapped there — unsynced records silently hidden (CE13 neighbor, secondary consumer)

- File: `src/app/(dashboard)/notifications/notifications-content.tsx:177` (neighbor of CE13)
- Scores: safety 3 / effect 3 / verify 4 / scope 3 / efficiency 4 = **17**
- Reachability: Reachable on any direct/fresh navigation to /notifications with pending offline drafts (mobile reload, deep link, tab restore).
- Evidence: NotificationsContent reads pendingSyncCount from the global useOfflineStore (notifications-content.tsx:75) and gates the unsynced row/subtitle on it: showUnsyncedRow = pendingSyncCount > 0 (line 177), subtitle '未同期 ${pendingSyncCount} 件。通信が戻ったら自動で再送します' (line 248). grep confirms this file has NO useEffect, NO refreshSyncCount/refreshSyncState/getPendingSyncCount call — it never hydrates the store. Per CE13 the offline store's pendingSyncCount is only ever refreshed on the record/offline-sync pages, so on a fresh load or direct navigation to /notifications it is the initial 0.
- Failure scenario: User saves a visit record offline (enqueueForSync populates the IndexedDB syncQueue), then reloads/opens /notifications directly. pendingSyncCount is 0 in the freshly-created store, so showUnsyncedRow is false and the entire '未同期 N件 / 通信が戻ったら自動で再送します' category and subtitle never render — the user is told there is nothing pending even though unsynced medical records persist in IndexedDB. The 'unsynced' category tab likewise shows an empty state.
- Suggested fix: Add a global one-time bootstrap of the offline store (refreshSyncCount) in a shared provider, or call refreshSyncCount() in a useEffect within NotificationsContent so the unsynced row reflects real IndexedDB state.
- Verifier (adversarial): Verified in-code: notifications-content.tsx:75 reads pendingSyncCount from useOfflineStore; line 177 gates showUnsyncedRow on pendingSyncCount>0, line 248 subtitle and line 226 chip count both derive from it. Read the full file — no useEffect and no refreshSyncCount/refreshSyncState call, so the page never hydrates the store. offline-store.ts:29 inits pendingSyncCount:0 with a non-persisted zustand store; grep confirms refreshSyncCount/refreshSyncState are invoked ONLY from visit-record-form.tsx and offline-sync-content.tsx, with no layout/provider bootstrap. On any fresh load of /notifications (reload/deep-link/bookmark) with pending IndexedDB drafts, pendingSyncCount stays 0, so the '未同期 N件 / 通信が戻ったら自動で再送します' row and the unsynced chip count are entirely suppressed — a false-empty hiding unsynced medical records. Reachable in prod. Not intentional (line 32 comment shows the row is meant to surface offline-store state). NOT a duplicate of CE13: CE13 is the app-header consumer (app-header.tsx, offline-store.ts:29); this is a distinct file (notifications-content.tsx:177), a different secondary consumer with a different symptom (suppressed actionable warning row), which is exactly the secondary-consumer neighbor being hunted. Shares CE13's underlying root cause but the (file, symptom) tuple differs.

- [ ] **Codex cross-review**:

## N23 [17] perf-unbounded-findMany / load-all-to-use-newest-per-group [fix] — dispense-workbench patient list loads each patient's entire SetPlan history to keep only the newest per patient

- File: `src/server/services/dispense-workbench-patients.ts:207` (neighbor of CE11 (inventory-forecast load-all-intakes-newest-per-patient) + F43 (unbounded setPlan.findMany))
- Scores: safety 4 / effect 2 / verify 4 / scope 4 / efficiency 3 = **17**
- Reachability: Reachable: called from listDispenseWorkbenchPatients (dispense-workbench-patients.ts:132) which backs GET /api/dispense-workbench/patients (src/app/api/dispense-workbench/patients/route.ts:8) — the reception/chouzai workbench patient list, a hot screen.
- Evidence: hydrateLatestSetPlans (dispense-workbench-patients.ts:196-243) runs prisma.setPlan.findMany({ where: { org_id, cycle.patient_id: { in: patientIds } }, orderBy: [created_at desc, id desc] }) with NO take (line 207-229), then the loop at line 231-237 keeps only the first row seen per patient (`if (!latestPlanByPatient.has(patientId)) latestPlanByPatient.set(...)`). Only latest_set_plan_id / latest_set_plan_cycle_id (one row per patient) is ever consumed. This is the exact CE11 shape (load all history, use newest-per-group).
- Failure scenario: listDispenseWorkbenchPatients returns up to MAX_CYCLES=500 patients (route GET /api/dispense-workbench/patients). For each of those patients hydrateLatestSetPlans loads ALL of their SetPlan rows across all history, so a patient with N accumulated set plans contributes N rows; the query reads up to 500\*N rows to produce 500 results. Cost grows unbounded with per-patient set-plan history even though the workbench only needs the single newest plan pointer.
- Suggested fix: Use a window query (ROW_NUMBER() OVER (PARTITION BY cycle.patient_id ORDER BY created_at DESC) with rn=1) or per-patient findFirst, mirroring how patient-status-tracker.ts:223 was already converted to a bounded ROW_NUMBER query.
- Verifier (adversarial): Verified src/server/services/dispense-workbench-patients.ts:207-229: setPlan.findMany filters org_id + cycle.patient_id in [up to 500 patients] + assignmentWhere, orders created_at desc / id desc, with NO take. The loop at 231-237 keeps only the newest row per patient (if(!latestPlanByPatient.has(patientId))), and only latest_set_plan_id/cycle_id (one per patient) is consumed at 239-243. So the query reads every historical SetPlan for those patients to produce one pointer each — the exact CE11/F43 load-all-use-newest-per-group anti-pattern. Reachable from listDispenseWorkbenchPatients:132 (fires when includeSetPlan || phase in set/set-audit) backing GET /api/dispense-workbench/patients (route.ts:35-45, canDispense). MAX_CYCLES=500 bounds patient count but not per-patient history depth. Novel: neither findings doc references this file/function; F43 is a different file (admin/capacity/route.ts) with an added KPI-inflation semantic bug, CE11 is inventory-forecast intakes. Not intentional — the comment documents newest-per-patient intent but not the unbounded read; no take/window bound. Pure efficiency defect (result stays correct), no medical false-negative or wrong data.

- [ ] **Codex cross-review**:

## N24 [16] tz-date-boundary [fix] — Delivery-analytics monthly window and month-bucket use date-fns startOfMonth (server-local) against created_at instant

- File: `src/server/services/report-reminders.ts:163` (neighbor of F22/F63 (server-local monthly windows) + CE10 (conference month))
- Scores: safety 2 / effect 3 / verify 5 / scope 2 / efficiency 4 = **16**
- Reachability: care-reports/analytics/route.ts and care-reports/reminders/route.ts and jobs/daily/reports.ts all consume report-reminders.
- Evidence: Line 2 imports startOfMonth/subMonths from 'date-fns' (server-local). computeDeliveryAnalytics:162-163 currentMonth=startOfMonth(now); rangeStart=startOfMonth(subMonths(currentMonth,months-1)) then filters deliveryRecord where created_at gte rangeStart (166-170) — created_at is DateTime instant (@default(now()), communication.prisma). formatMonth:23-24 uses getFullYear()/getMonth() (server-local) and is applied to startOfMonth(report.created_at) at 113 (reminder dedupe key reportMonth) and to sent_at/created_at at 277 (monthly bucket key).
- Failure scenario: On UTC prod a report created 2026-07-01 05:00 JST (=2026-06-30 20:00Z) yields formatMonth='2026-06', so its reminder dedupe key and monthly analytics bucket are attributed to June not July; likewise the rangeStart window boundary is off by 9h, mis-including/excluding boundary deliveries in the 6-month care-report analytics and potentially emitting a duplicate response-followup reminder across the month edge.
- Suggested fix: Derive month keys via japanDateKey(now).slice(0,7) and build the window with japanMonthInstantRange for the created_at filter and month bucketing/dedupe.
- Verifier (adversarial): Code matches claim: report-reminders.ts:2,162-163,170 use date-fns server-local startOfMonth against DeliveryRecord.created_at (DateTime instant, communication.prisma model DeliveryRecord); formatMonth:22-24 (getFullYear/getMonth) is applied to instant created_at/sent_at at :113 (reminder dedupe reportMonth) and :276-277 (monthly bucket). date-boundary.ts:15-16,116-121 mandates japanMonthInstantRange for instant month windows. TZ enforcement is warn-only/opt-in (assert-env.ts:101-103), so UTC prod produces real ~9h drift. Reachable via care-reports/analytics, care-reports/reminders, and jobs/daily/reports. Novel vs F77 (same file, different root cause: unbounded OR branch) and distinct file from F22/F63/CE10. Harm is analytics mis-bucketing + a duplicate (not suppressed) followup task at month edges — a correctness defect, not a missed safety alert.

- [ ] **Codex cross-review**:

## N25 [16] offline-reliability [fix] — Offline evidence-photo drafts that hit MAX_EVIDENCE_SYNC_RETRIES are stuck forever — no reset / dead-letter / manual retry path (F53 neighbor, different table)

- File: `src/lib/offline/evidence-drafts.ts:298` (neighbor of F53)
- Scores: safety 3 / effect 4 / verify 4 / scope 3 / efficiency 2 = **16**
- Reachability: Reachable via any 3 consecutive upload failures; MAX is only 3 so a single flaky-network window can trip it.
- Evidence: syncEvidenceDraftsOnce selects only drafts with retryCount below MAX_EVIDENCE_SYNC_RETRIES (evidence-drafts.ts:298-305, filter also in evidence-drafts.shared.ts:50). On each failure retryCount is incremented (line 327-330). Unlike the visit-record syncQueue which has resetFailedSyncQueueRetries (sync-engine.ts:484) surfaced via the offline-sync page 'すべて再試行' button, there is NO reset function for evidenceDrafts anywhere. The gallery (evidence-gallery-content.tsx) '再試行' at line 215 only refetch()es the list query — it does not call syncEvidenceDrafts or reset retryCount. listEvidenceDraftSummaries (line 61) shows drafts with retryCount aboveOrEqual 0, so a maxed-out draft is still displayed as 未同期 forever.
- Failure scenario: An evidence photo fails to upload 3 times (transient S3/presign/network errors while the record exists). retryCount reaches 3. From then on pickSyncableEvidenceDrafts excludes it permanently; reconnects, new captures, and the gallery '再試行' button never re-attempt it. The photo remains encrypted in IndexedDB, permanently counted as 未同期 (feeds unsyncedPhotoCount/mobilePendingSyncCount badge in visit-record-form.tsx:1301), with zero user-facing recovery — the medical evidence is silently lost to the server and the '未同期' badge sticks forever.
- Suggested fix: Add a resetFailedEvidenceDraftRetries() (mirroring sync-engine.ts:484) and wire the gallery/offline-sync '再試行' controls to reset retryCount and call syncEvidenceDrafts, plus a discard path for un-recoverable photos.
- Verifier (adversarial): Verified against code: evidence-drafts.ts:298-304 selects only retryCount<MAX_EVIDENCE_SYNC_RETRIES (=3, shared.ts:7) and :327-330 only increments retryCount, never resets; deletes occur only on success (:324). Repo-wide grep confirms NO reset/requeue/discard exists for the evidenceDrafts table, whereas the sibling syncQueue has resetFailedSyncQueueRetries (sync-engine.ts:484) wired to the offline-sync 'すべて再試行' button. All three sync triggers (auto-sync online capture-content.tsx:127, immediate post-capture :149, gallery '再試行' → refetch only at evidence-gallery-content.tsx:215) funnel through syncEvidenceDraftsOnce, so a retryCount>=3 draft is never re-attempted by any path. listEvidenceDraftSummaries filters retryCount>=0 (evidence-drafts.ts:62-66,:77) so the dead draft stays visible as 未同期 forever and feeds unsyncedPhotoCount/mobilePendingSyncCount badge + VisitUnsyncedEvidenceBanner (visit-record-form.tsx:1300-1304,1538). Reachable via 3 genuine upload failures (missing visit-record is skipped not failed). Not intentional: sibling queue ships reset+retry-all UI. NOVEL neighbor of F53: F53 is src/phos/api/offlineEvidenceQueue.ts (pendingEvidence table); this is the distinct src/lib/offline/evidence-drafts.ts (evidenceDrafts table) — same dead-letter pattern, different file+table per the neighbor-hunt dedup rule, and absent from CE01-CE19. Correction: unlike F53 there is no visit-completion blocking gate, so harm is silent PHI-evidence loss + permanent 未同期 banner, not a hard completion lock; and it takes 3 distinct failed passes, not a single window.

- [ ] **Codex cross-review**:

## N26 [15] tz-date-boundary [fix] — conference-sync proposeVisitSchedule no-discharge path derives next-visit proposed_date via getUTC\* of new Date() (current instant), off-by-one in the JST 00:00-08:59 window (sibling of CE16, different file/source-date; manifests in UTC and JST runtimes)

- File: `src/server/services/conference-sync.ts:790` (neighbor of CE16 (conference-data-sync +7 proposed date getUTCDate))
- Scores: safety 2 / effect 2 / verify 4 / scope 3 / efficiency 4 = **15**
- Reachability: ConferenceSyncService (conference-sync.ts) imported by conference-notes/[id]/generate-report/route.ts:12.
- Evidence: conference-sync.ts:789-801: proposedDate = targetDischargeDate ? new Date(targetDischargeDate) : new Date() (current instant); proposedDate.setDate(getDate()+ (targetDischargeDate?3:7)); then proposedDateOnly = Date.UTC(proposedDate.getUTCFullYear(), getUTCMonth(), getUTCDate(), 12,...) stored into VisitScheduleProposal.proposed_date (DateTime @db.Date, visit.prisma:285).
- Failure scenario: On UTC prod in the no-discharge-date path, at 05:00 JST new Date() UTC date is the prior JST civil day, so proposed_date is stored one civil day earlier than the JST-intended +7, producing an off-by-one suggested next-visit date for conferences processed in the 00:00-08:59 JST window.
- Suggested fix: Compute the proposed civil date via japanDateKey(proposedDate) → utcDateFromLocalKey (or addUtcDays on todayUtcRange) instead of getUTC\* of the raw instant.
- Verifier (adversarial): conference-sync.ts:789-801 mixes local getDate()/setDate() with UTC getUTCFullYear/Month/Date extraction in the no-discharge next-visit proposal path (new Date() current instant + 7 days). Empirically verified: a pre_discharge note processed at 05:00 JST stores proposed_date one civil day earlier than the JST-intended +7 (Jul 8 vs Jul 9), in BOTH UTC and Asia/Tokyo runtimes; 15:00 JST processing is correct. Reachable via generate-report route -> generateReportDraft -> proposeVisitSchedule (gated note_type==='pre_discharge', line 383-384) with a next_visit_plan section and no target_discharge_date. Not intentional (comment only explains +7 default; no test locks it). Novel: CE16 is the sibling file conference-data-sync.ts:698 keyed on note.conference_date; this is a different file/line/source-date, and no F01-F78 entry references conference-sync.ts. Low harm: staff-reviewed proposal (proposed/pending), a suggested date, not a safety alert or billing window.

- [ ] **Codex cross-review**:

## N27 [15] concurrency-check-then-act [fix] — pca-pump-rentals PATCH reads rental status in a separate transaction then updates by id only — duplicate return-inspection maintenance events + lost update on rental status

- File: `src/app/api/pca-pump-rentals/[id]/route.ts:235` (neighbor of CE05 (id-only update without re-asserting prior status) — status machine with adoption-style side effects)
- Scores: safety 3 / effect 2 / verify 4 / scope 3 / efficiency 3 = **15**
- Reachability: PATCH /api/pca-pump-rentals/[id]; reachable via double-submit/retry or two concurrent editors. Two independent default-isolation transactions with no re-assert/lock make the tx1→tx2 window wide.
- Evidence: existing (including status, pump_id, pump.status) is read in a FIRST withOrgContext transaction at lines 40-61. A SECOND withOrgContext transaction (147-327) computes returningNow = nextStatus==='returned' && existing.status!=='returned' (151), nextInspectionStatus (152), then at line 235 does tx.pcaPumpRental.update({ where: { id }, data: rentalUpdateData }) — id-only where, NO status re-assertion and no re-read of the rental inside the tx. All downstream side effects derive from the stale tx1 existing.status: the return_inspection pcaPumpMaintenanceEvent.create (254-271), syncPcaRentalAccessoriesFromReturnInspection (248), and the pcaPump.update status side effects (303-321). The unique-constraint catch (328-333) only guards the 'open rental per pump' invariant, not double-processing the same rental id.
- Failure scenario: A pharmacist double-submits (or two staff act on) the same rental whose status is 'active', both requesting status='returned' with an inspection result. Both tx1 reads observe existing.status='active' ⇒ returningNow=true for both. Both enter the update tx; the id-only pcaPumpRental.update serializes but both apply, and each independently runs pcaPumpMaintenanceEvent.create('return_inspection') and (if accessory_checklist present) syncPcaRentalAccessoriesFromReturnInspection — producing duplicate return-inspection maintenance/audit rows and a last-writer-wins clobber of rental fields (returned_at, return_inspection_status, inspected_by), plus a racing pcaPump.status between 'available'/'maintenance'/'rented'.
- Suggested fix: Read/lock the rental inside the update tx (FOR UPDATE or re-fetch status) and make the write tx.pcaPumpRental.updateMany({ where: { id, org_id, status: existing.status }, data }) with a count!==1 conflict; gate the maintenance-event/accessory/pump-status side effects on that claim.
- Verifier (adversarial): Code confirms: existing (status/pump_id/pump.status) is read in a first withOrgContext tx (route.ts:40-61); the second tx computes returningNow off that stale read (151) and updates by id only (235) with no status re-assertion or FOR UPDATE. PcaPumpMaintenanceEvent has no unique constraint (pca-pump.prisma:57-79), and the only unique index (PcaPumpRental_one_open_per_pump_idx, migration 20260608103000) covers only status IN (scheduled/active/overdue), so it cannot block double-processing a status='returned' transition. Under Read Committed, two concurrent canAdmin PATCHes requesting returned+passed both observe active in tx1, both skip the conflict check (154 is only for open statuses), and both run pcaPumpMaintenanceEvent.create('return_inspection') → duplicate audit/maintenance rows for a controlled-substance (opioid) PCA pump. The accessory-sync leg is idempotent (upsert, accessories.ts:78-95) and the field 'lost update' is benign in the pure double-submit case, so the load-bearing harm is the duplicate maintenance/audit records — which is real. Novel: the only existing pca-pump seed is a frontend false-empty on the return-inspection query (different file/root-cause); no F-finding covers this backend PATCH concurrency.

- [ ] **Codex cross-review**:

## N28 [15] missing-rls-tenant-isolation [flag] — PackagingMethodMaster has NOT-NULL org_id but no RLS anywhere

- File: `prisma/schema/medication.prisma:171` (neighbor of CE04)
- Scores: safety 3 / effect 2 / verify 5 / scope 2 / efficiency 3 = **15**
- Reachability: Migrated, NOT-NULL org_id, queried in set-plans/master-hub.
- Evidence: org_id String NOT NULL. No RLS in migrations/failsafe; 0 lines in rls-policies.sql. Queried in master-hub/set-plans.
- Failure scenario: PackagingMethodMaster query missing org_id exposes another tenant's packaging-method master with no DB backstop.
- Suggested fix: Confirm tenant-vs-shared intent; if tenant, add ENABLE+policy+FORCE RLS.
- Verifier (adversarial): Verified: PackagingMethodMaster (prisma/schema/medication.prisma:173) has org_id NOT NULL and is genuinely tenant-scoped, yet has zero RLS anywhere — 0 hits in prisma/rls-policies.sql, its own migration (20260330235500) adds only table+indexes, and the failsafe DO-block array (20260328234500) does not list it (and predates the table). All app-layer consumers (master-hub:204, set-plans:313, packaging-methods route/[id]) do scope by org_id:ctx.orgId, so there is no active cross-tenant leak today; the defect is a missing DB backstop that violates the repo's explicit 二重防御 defense-in-depth model. NOVEL vs CE04 (which is PatientSelfReport/CommunityActivity with ENABLE+POLICY present but FORCE missing — different tables, different root cause: total absence vs FORCE-only drift). Not in F01-F78. The stated failure scenario is latent/hypothetical rather than presently exploitable, which lowers severity but does not refute the missing-RLS root cause.

- [ ] **Codex cross-review**:

## N29 [15] missing-rls-tenant-isolation [flag] — BusinessHoliday has NOT-NULL org_id but no RLS anywhere

- File: `prisma/schema/organization.prisma:328` (neighbor of CE04)
- Scores: safety 3 / effect 3 / verify 4 / scope 2 / efficiency 3 = **15**
- Reachability: Migrated, NOT-NULL org_id.
- Evidence: org_id String NOT NULL. No RLS in migrations/failsafe; 0 lines in rls-policies.sql.
- Failure scenario: BusinessHoliday query missing org_id returns another tenant's business-calendar rows, potentially mis-windowing scheduling/billing date calculations across tenants with no DB backstop.
- Suggested fix: Add ENABLE+policy+FORCE RLS.
- Verifier (adversarial): Verified at prisma/schema/organization.prisma:330 that BusinessHoliday has org_id String (NOT NULL) and is org-scoped. Confirmed NO RLS anywhere: absent from prisma/rls-policies.sql (grep empty), no ENABLE/POLICY/FORCE in any migration for BusinessHoliday, and NOT in the app_enforced_org_id failsafe table array (20260328234500_rls_context_failsafe). Oversight is strongly evidenced because sibling tables in the same migrations got full RLS: PatientSchedulePreference and PharmacistShiftTemplate (20260326120000 lines 374-384) and PharmacyOperatingHours (20260627090000 lines 72-77). Not a duplicate of CE04, which is a FORCE-vs-ENABLE drift for PatientSelfReport/CommunityActivity (tables that DO have RLS); this is total absence on a distinct table. Caveat: every current consumer (business-holidays route.ts:49, visit-schedule-planner.ts:1470, home-care-ops.ts:663, workflow-dashboard-queries.ts:923, billing-evidence/core.ts:1381) explicitly filters org_id, so the stated cross-tenant-read failure scenario is not actively reachable today; the confirmed defect is the missing DB defense-in-depth backstop on an org-scoped table, matching the CE04-class flag pattern.

- [ ] **Codex cross-review**:

## N30 [14] tz-date-boundary [fix] — Nav handoff badge counts the wrong day's board (server-local todayDateKey vs JST @db.Date shift_date)

- File: `src/server/services/nav-badges.ts:81` (neighbor of CE08/@db.Date-key + handoff-board japanDateKey convention)
- Scores: safety 3 / effect 3 / verify 4 / scope 2 / efficiency 2 = **14**
- Reachability: GET /api/nav-badges (route.ts:5-8 → buildNavBadgePayload → countHandoffBadge); also imported by handoff-board/route.ts:11. Gated on canReport.
- Evidence: todayDateKey() (nav-badges.ts:20-26) builds 'YYYY-MM-DD' from server-local now.getFullYear()/getMonth()/getDate(), then countHandoffBadge (line 81) does shiftDate=toDateOnly(todayDateKey()) and queries handoffItem where board.shift_date === shiftDate. shift_date is DateTime @db.Date (prisma/schema/communication.prisma:366). The actual board page uses the JST-correct helper: handoff-board/route.ts:91 defaults the board date with japanDateKey(). So badge and board disagree.
- Failure scenario: On UTC prod (Amplify) at 08:30 JST 2026-07-02, UTC now = 2026-07-01T23:30Z, so todayDateKey='2026-07-01' → badge counts YESTERDAY's handoff board while the board page (japanDateKey) shows 2026-07-02. During the whole 00:00-08:59 JST window the 責任移転/相談/連絡 unread badge reflects the wrong civil day (stale count or hidden new items).
- Suggested fix: Replace todayDateKey() with japanDateKey() (import from @/lib/utils/date-boundary), keeping toDateOnly/utcDateFromLocalKey.
- Verifier (adversarial): countHandoffBadge (nav-badges.ts:81) builds the handoff-board lookup key via todayDateKey() (lines 20-26), which uses server-local now.getFullYear()/getMonth()/getDate(); toDateOnly (28-30) then produces a UTC-midnight Date compared against board.shift_date (DateTime @db.Date, communication.prisma:366). The sibling board page defaults its date with the JST-correct japanDateKey() (handoff-board/route.ts:91), so on a UTC runtime the badge and the board disagree during the 00:00-08:59 JST window (UTC now still on the previous civil day). date-boundary.ts:11-16 explicitly documents server-local midnight as the wrong pattern and japanDateKey (line 35) as the TZ-independent helper, confirming this is an overlooked inconsistency, not spec. Reachable via GET /api/nav-badges and the badge=1 branch (handoff-board/route.ts:96), both gated on canReport. Novel: no F01-F78 / CE01-CE19 entry references nav-badges/todayDateKey/handoff badge. Effect is contingent on a non-JST (UTC) runtime; route tests toggling process.env.TZ and the existence of the date-boundary helper family show the codebase does not assume Asia/Tokyo runtime.

- [ ] **Codex cross-review**:

## N31 [14] rls-ssot-drift [flag] — SSOT omits UatFeedback entirely though migration ENABLEs+FORCEs it

- File: `prisma/rls-policies.sql:459` (neighbor of CE04)
- Scores: safety 3 / effect 3 / verify 5 / scope 2 / efficiency 1 = **14**
- Reachability: SSOT authoritative per compliance docs.
- Evidence: Migration 20260328234500_add_uat_feedback/migration.sql:19 (ENABLE) / :22 (FORCE) for "UatFeedback"; prisma/rls-policies.sql has 0 lines for it.
- Failure scenario: UatFeedback (user feedback, may include screenshots/free text with PHI) unprotected in SSOT-provisioned DB; uncaught by contract test.
- Suggested fix: Add ENABLE+policy+FORCE for UatFeedback to SSOT.
- Verifier (adversarial): Verified: migration 20260328234500_add_uat_feedback/migration.sql ENABLEs (line 19), CREATEs tenant_isolation policy (line 20-21) and FORCEs (line 22) RLS on org-scoped table "UatFeedback" (org_id NOT NULL, free-text feedback, checked_items JSONB). prisma/rls-policies.sql (740 lines, 104 ENABLE statements) contains zero references to UatFeedback (grep -ni uat empty). SSOT header claims coverage of "all multi-tenant tables" and is a provisioning path (psql or Prisma migration), so provisioning from SSOT leaves UatFeedback with no RLS. The contract test src/tools/rls-policy-contract.test.ts only checks a hardcoded table list that excludes UatFeedback, so the drift is uncaught. Distinct from CE04, which covers PatientSelfReport/CommunityActivity that ARE in the SSOT but missing only FORCE; here the whole table is absent — different table and more severe. Neighbor, not duplicate. Omission is drift (migration forces it), not intentional.

- [ ] **Codex cross-review**:

## N32 [13] concurrency-check-then-act [fix] — pca-pumps PATCH: open-rental / pending-inspection guards read in-tx without FOR UPDATE, then pump status updated by id only — pump can be flipped to 'available' while a rental is active

- File: `src/app/api/pca-pumps/[id]/route.ts:117` (neighbor of CE05 (check-then-act, id-only update omits the guard state))
- Scores: safety 2 / effect 2 / verify 4 / scope 2 / efficiency 3 = **13**
- Reachability: PATCH /api/pca-pumps/[id] concurrent with a rental activation on the same pump (separate route/tx). Narrower cross-entity interleaving than the other findings.
- Evidence: Inside one withOrgContext tx, line 72-93 findFirst reads status, rentals(status='returned',return_inspection_status='pending') and \_count.rentals(status ∈ {scheduled,active,overdue}); lines 95-100 reject status changes that would leave the pump non-'rented' while open rentals exist, or set 'available' with a pending return inspection. Line 117 tx.pcaPump.update({ where: { id }, data: { ...status } }) uses id-only where with no re-assertion of the counted rental state; findFirst does not FOR UPDATE, so the guard is a stale snapshot under Read Committed.
- Failure scenario: Admin A PATCHes pump P to status='available' (findFirst sees 0 open rentals). Concurrently the pca-pump-rentals route activates a new rental on P (sets pump 'rented' + creates an open rental) and commits between A's findFirst (72) and A's update (117). A's id-only update then writes status='available' onto a pump that now has an active rental, violating the open_rental invariant the guard enforces — pump inventory shows 'available' while rented.
- Suggested fix: SELECT ... FOR UPDATE the pump (or its open rentals) at the top of the tx, or re-count open rentals atomically and use updateMany with a guard that fails the write when open rentals appeared; return the existing open_rental validation error on count!==1.
- Verifier (adversarial): Code claims verified: pca-pumps/[id]/route.ts:72-93 does a non-locking findFirst snapshot (open-rental \_count + pending-inspection), and line 117 does an unconditional id-only tx.pcaPump.update that never re-asserts that state — while the sibling rental-creation route (pca-pump-rentals/route.ts:200-208) defends its pump transition atomically via updateMany where status='available' + count!==1. This asymmetry is the real defect: under Read Committed, if the rental POST commits (pump available→rented + open rental created) between PATCH's findFirst and its update, PATCH's blind write clobbers 'rented' back to the payload status (available/maintenance/retired), violating exactly the open_rental invariant the line 95 guard intends. Reachable via two concurrent canAdmin ops (status change racing rental creation) on the same pump. Severity is capped by the partial unique index (migration 20260608103000, WHERE status IN scheduled/active/overdue) which blocks a second open rental, so the double-booking cascade is prevented and residual harm is a pca_pump.status field inconsistency (inventory display) rather than a suppressed medical alert. Not a duplicate: no F01-F78 entry (only unrelated pca daily-job timezone findings) and no CE01-CE19 seed (CE01 is an FE false-empty in pca-pumps-content.tsx) covers this file+root-cause.

- [ ] **Codex cross-review**:

## N33 [13] missing-rls-tenant-isolation [flag] — NotificationRule (org_id NOT NULL, admin.prisma:24) has no RLS in schema or any migration while ~80 sibling org-scoped tables get ENABLE+FORCE — DB-layer tenant isolation absent (app-layer org_id filters still present, so no active leak)

- File: `prisma/schema/admin.prisma:22` (neighbor of CE04)
- Scores: safety 3 / effect 2 / verify 4 / scope 2 / efficiency 2 = **13**
- Reachability: Migrated, NOT-NULL org_id, 3 query files.
- Evidence: org_id String NOT NULL. No RLS in migrations/failsafe; 0 lines in rls-policies.sql. Queried in 3 src files.
- Failure scenario: NotificationRule query missing org_id filter routes/exposes another tenant's escalation/notification config with no DB isolation.
- Suggested fix: Add ENABLE+policy+FORCE RLS.
- Verifier (adversarial): Code-confirmed structural facts: prisma/schema/admin.prisma:24 declares org_id as NOT NULL (tenant-scoped), yet NotificationRule has zero RLS anywhere — 0 references in prisma/rls-policies.sql and no ENABLE/FORCE/POLICY in any migration (baseline migration.sql:101-1218 only creates the table + 3 indexes), while ~80 sibling org-scoped tables get ENABLE+tenant_isolation and ~130 get FORCE. Not in any intentional-exclusion list; it is a genuine multi-tenant table, so this is a real DB-layer tenant-isolation (defense-in-depth) gap of the same class the seeds track. NOT a duplicate: CE04 is FORCE-drift on PatientSelfReport/CommunityActivity (tables that already have ENABLE+policy); this is a DIFFERENT table with a total-absence root cause, and no F01-F78 entry covers it. Caveat that lowers real-world severity: the candidate's literal failure scenario ('query missing org_id filter') is NOT present in current code — every query site filters org_id at the app layer (route.ts:39, [id]/route.ts:38/73-116, notifications.ts:280-283), so there is no active cross-tenant leak today; the RLS absence only bites if app_user owns the table (FORCE bypass) or a future query omits the filter. Same low-reachability profile under which CE04 was confirmed, and the missing-RLS fact itself is proven, not speculative — hence CONFIRMED as a defense-in-depth finding, not REFUTED.

- [ ] **Codex cross-review**:
