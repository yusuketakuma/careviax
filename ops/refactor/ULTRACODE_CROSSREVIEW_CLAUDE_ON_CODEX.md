# Ultracode Cross-Review — Claude reviews Codex F79-F89 — 2026-07-02

Claude が Codex の独立候補 F79-F89 を **別視点で敵対再検証＋隣接拡張**（read-only、実装なし）。
結果: reviewed 11 / CONFIRMED 9 / PARTIALLY 2 / REFUTED 0 / 新規neighbor 13。
**0 refuted** = Codex の F79-F89 は全て実在確認。以下に Claude の独立検証・severity補正・訂正を記す。

## Part A: Claude の F79-F89 独立検証

### F79 — CONFIRMED [flag] (Claude score 21: s4/e4/v5/sc3/ef5) neighbors=0

- 独立検証: Verified independently by reading code. prisma/schema/drug.prisma:224 (FormularyChangeRequest.org_id String NOT NULL) and :246 (FormularyTemplate.org_id String NOT NULL) are both genuinely org-scoped (org-scoped indexes; FormularyTemplate has @@unique([org_id,name])), not global masters. grep for 'formulary' in prisma/rls-policies.sql = 0 hits; the drug-domain block (rls-policies.sql:463-476) covers only PharmacyDrugStock and ServiceArea, and the FORCE block ends at those two. Neither table appears in the failsafe array (prisma/migrations/20260328234500_rls_context_failsafe/migration.sql grep = 0). src/tools/rls-policy-contract.test.ts:31-74 is a hardcoded per-migration allowlist, not a comprehensive assertion, so it never catches these — the real systemic root cause. AGREE with Codex on the missing-RLS fact for BOTH tables. DISAGREE on severity: all current consumers filter org_id explicitly (src/app/api/pharmacy-drug-stock-requests/route.ts:43,129,170; [id]/route.ts:41,95), so this is a latent defense-in-depth backstop gap on org business-config data (not PHI, not a live leak), not an active exposure. FormularyTemplate overlaps my prior N11; FormularyChangeRequest is the newly-added half and is equally unprotected.
- **Codexへの訂正**: Codex scores safety 5 / effect 5, which contradicts the scoring convention every N-series missing-RLS finding used (N11 scored the same FormularyTemplate table safety 3 as latent defense-in-depth). I re-score safety 4 / effect 4 to match: no live cross-tenant leak (all query sites filter org_id), and the data is org business config, not PHI. Also, F79's evidence cites the failsafe migration line ':28' as if adjacent — I confirmed neither formulary table appears in that failsafe array at all (grep = 0).

### F80 — CONFIRMED [flag] (Claude score 23: s5/e4/v5/sc4/ef5) neighbors=1

- 独立検証: Independently re-verified by reading the code. POST /api/external-access grant creation is gated by permission:'canReport' (src/app/api/external-access/route.ts:658). pharmacist_trainee has canReport:true, canVisit:true, canManagePatientSharing:false (src/lib/auth/permission-matrix.ts:67-80; asserted in src/lib/auth/**tests**/permissions.test.ts:106). The only additional gate is validateExternalAccessScopeForRole (src/server/services/external-access.ts:317-350), where SENSITIVE_SCOPE_PERMISSIONS (lines 72-77) maps medication_list/allergy_info/visit_schedule -> canVisit. Because trainee has canVisit, a trainee passes both the route guard and the per-scope check and can mint a bearer/OTP external-access grant exposing medication_list/allergy/visit_schedule PHI to an outside party, despite not holding canManagePatientSharing. This is inconsistent with every other patient-sharing lifecycle route, all of which gate on canManagePatientSharing (patient-share-cases/route.ts:364,559; pharmacy-partnerships/route.ts:203; pharmacy-contracts/route.ts:409; confirmed by grep). I also confirmed the external_sharing consent that POST requires (route.ts:471-484) is itself creatable by a trainee: consent-records POST is gated by canVisit (src/app/api/consent-records/route.ts:241) and its schema accepts consent_type 'external_sharing' (line 43), so the whole consent->grant chain is self-serviceable by a trainee without canManagePatientSharing. Agreement with Codex on root cause and files. Correction: Codex's spec 'trainee/clerk POST returns 403 before token/grant creation' is imprecise — clerk (canVisit:false, canSendCareReport:false) is already blocked today because every supported scope key requires canVisit or canSendCareReport and empty scope is rejected (external-access.ts:127-133); clerk stops at scope validation (kind:'permission' -> forbidden), not the route guard. The genuinely exploitable actor is pharmacist_trainee with medication_list/allergy_info/visit_schedule scope (currently 201, should be 403). The correct fix (gate POST with canManagePatientSharing) would also 403 clerk at the route level.
- **Codexへの訂正**: Codex's test/failure framing overstates clerk exposure: clerk cannot issue any currently-supported scope grant today (all scope keys require canVisit or canSendCareReport, both false for clerk; empty scope rejected at external-access.ts:127-133), so clerk is already blocked at scope validation. The actual exploitable role is pharmacist_trainee (canVisit:true) issuing medication_list/allergy_info/visit_schedule grants. effect is closer to 4 than 5 since it requires a valid accessible patient and an active external_sharing consent (though a trainee can create that consent via canVisit).

### F81 — CONFIRMED [fix] (Claude score 23: s5/e4/v5/sc4/ef5) neighbors=2

- 独立検証: Re-verified independently in src/server/cds/checker.ts. AGREE with Codex: (1) checkInteractions:532-536 builds medCodes only from masterByMedId.get(m.id)?.yj_code and returns [] when medCodes.length===0, so a current med with drug_master_id null is silently excluded from interaction detection. (2) checkDuplicates:614-632 — a code-resolved prescription line (line.drug_code set) never falls back to name matching (explicit continue at 631), and unresolved current meds only match via the name path at 634-636 which requires !masterByMedId.has(m.id); so a code-resolved new line vs an unresolved current med produces no duplicate alert. (3) masterByMedId is populated only for resolved current meds (1694-1701) and no code path emits any data-quality/identity-unresolved alert for current meds — I confirmed the ONLY identity data-quality alert is resolvePrescriptionLineDrugIdentitiesForCds:215 (buildPrescriptionLineDrugIdentityMismatchAlert) which covers prescription LINES on mismatch only, never current meds. Type confirms drug_master_id/drug_code are string|null (lines 22-23), so drug_master_id=null current meds are reachable (hand-entered/import-failed). Pharmacist sees a clean CDS result. Minor correction to Codex framing: the interaction/duplicate misses on an unidentifiable drug are partly inherent (you cannot reliably code-match an unresolved drug), so the true defect is the MISSING data-quality alert, not the match logic itself — which is exactly the suggested fix. classification=fix agrees (self-contained additive alert; no auth/RLS/schema/migration touched).

### F82 — PARTIALLY_CONFIRMED [flag] (Claude score 20: s5/e4/v4/sc3/ef4) neighbors=2

- 独立検証: Core claim CONFIRMED independently. src/app/api/cds/check/route.ts:41-49 fetches only {id, patient_id} from the cycle and calls checkDispenseAlerts(orgId, cycleId, patient_id). src/server/cds/checker.ts:1704-1707 is the sole patient fetch and selects only birth_date + allergy_info (type PatientForChecks at 703-706 confirms exactly these two fields). Repo-wide grep shows PatientCondition/condition_type are never referenced anywhere in src/server/cds/ or src/app/api/cds/, so active problem-list conditions (glaucoma, myasthenia gravis) cannot become patient-specific contraindication alerts. Contraindication text is emitted only as generic severity:'info' (checker.ts:751-757), never matched against the patient's actual conditions — agreeing with Codex. CORRECTIONS to Codex's expansion hint: (a) renal function is NOT absent — checkRenalDoseAdjustment (checker.ts:1402-1418) already fetches latest egfr from PatientLabObservation and does dose-band matching, so 'renal absent' is refuted; (b) pregnancy is a genuine gap but at the DATA-MODEL level: PatientConditionType enum (prisma/schema/patient.prisma:97-100) is only disease|problem, Patient has gender but no pregnancy/lactation flag, and grep finds zero pregnancy/妊娠 references in src/server/cds/ — differs in root cause from the PatientCondition wiring gap. PARTIALLY_CONFIRMED because the finding is real but the fix is clinical-rule/data-model design work (Codex itself warns against naive string matching), not a mechanical code change; hence flag.
- **Codexへの訂正**: Codex's hint that renal function is absent from CDS input is incorrect: checkRenalDoseAdjustment (checker.ts:1402-1418) already pulls latest eGFR from PatientLabObservation and matches dose bands. Pregnancy IS absent, but as a data-model gap (no pregnancy/lactation status on Patient or in PatientConditionType), not a failure to pass existing structured data.

### F83 — CONFIRMED [fix] (Claude score 22: s4/e4/v5/sc4/ef5) neighbors=2

- 独立検証: Re-read all cited code independently. CREATE side: pharmacy-drug-stock-requests/route.ts:127-136 does prisma.formularyChangeRequest.findFirst({org_id,site_id,drug_master_id,status:'pending'}), returns conflict at 140-145 if found, then unconditionally create() at 167-179. drug.prisma:243 confirms FormularyChangeRequest_pending_guard_idx is @@index (map:), NOT @@unique — so two concurrent POSTs for the same site/drug both read null and both create duplicate pending rows (agrees with Codex). DECISION side: [id]/route.ts:40-46 findFirst + status!=='pending' guard runs OUTSIDE the transaction (tx opens at 83); approve path upserts PharmacyDrugStock (87-111) marking the drug adopted, then update({where:{id:request.id}}) at 114-122 uses an id-only where with NO status:'pending' re-assertion and no FOR UPDATE; audit written per call at 124-139. Concurrent approve+reject (or double-submit) both pass line 44, both enter the tx, the approve-path stock upsert persists as adopted while a racing reject flips status='rejected' by id (last-writer-wins), leaving formulary/inventory divergence + two contradictory audit rows (agrees with Codex and my own CE05). F83 correctly SUPERSETS CE05: CE05 only covered the decision route; F83 adds the truly-missing unique constraint on the create side. Sibling pca-pump-rentals/route.ts:200-208 (updateMany claim on status:'available' + count!==1) and pharmacy-partnerships/[id]/activate use the correct atomic pattern, proving the id-only update and non-unique index are oversights. Correction to Codex scoring: this is formulary/inventory data-integrity, not a patient-facing medical false-negative or PHI leak, so safety is 4 not 5; effect/verify/scope/efficiency align.

### F84 — CONFIRMED [flag] (Claude score 22: s5/e4/v5/sc3/ef5) neighbors=1

- 独立検証: Re-verified by reading the code, not Codex's summary. (1) consent-records/route.ts:304-317 performs the active-duplicate guard with prisma.consentRecord.findFirst on {org_id, patient_id, consent_type, is_active:true} BEFORE the create, and the create runs in a separate withOrgContext transaction at :353-373 — the read is not inside that tx and nothing serializes read+create, so two concurrent/retried/double-submit POSTs each observe no duplicate and each insert is_active:true. (2) patient.prisma:276-304 confirms ConsentRecord's only uniqueness is @@unique([id, org_id]); there is NO partial/filtered unique over active (org_id, patient_id, consent_type), so the DB cannot reject the second active row. Agreements with Codex on all four cited locations. Independently confirmed the consumers pick the basis with findFirst+orderBy: management-plans.ts:104-121 findActiveVisitConsent (orderBy obtained_date desc) and external-access/route.ts:172-186 findActiveExternalSharingConsent (orderBy obtained_date desc, select id/case_id) — with two active rows the chosen consent is order-dependent, and same-obtained_date ties are non-deterministic. Additional corroboration Codex did not state: the revoke path [id]/revoke/route.ts:122-139 uses updateMany filtered by single id, so revoking one active row leaves the sibling duplicate active — directly realizing the 'later revoke affects one row while another remains active' scenario. Failure scenario fully holds; medical/PHI harm is real (external_sharing consent basis and visit_medication_management billing/workflow gate can key off a stale/wrong active row).
- **Codexへの訂正**: No material inaccuracy. Codex cited management-plans.ts:110 and external-access:174 as consumers — confirmed correct (the findFirst calls are at :110 and :174 respectively). Scope=3 is fair: the fix spans a schema migration (partial unique + duplicate cleanup) plus a 409 conflict path in the POST handler. Note the read consumers degrade somewhat gracefully via orderBy, but the revoke-one-leaves-one behavior means the integrity violation is durable, not transient.

### F85 — CONFIRMED [flag] (Claude score 20: s4/e3/v5/sc3/ef5) neighbors=2

- 独立検証: Independently re-verified by reading the code, not Codex's summary. POST (src/app/api/patients/[id]/insurance/route.ts:186-217): the overlap guard is a tx.patientInsurance.findFirst using buildPatientInsuranceOverlapWhere (line 188) followed by tx.patientInsurance.create (line 204), both inside withOrgContext. PUT ([insuranceId]/route.ts:195-237): same findFirst-then-update shape. withOrgContext (src/lib/db/rls.ts:85-89) runs prisma.$transaction with NO isolationLevel argument, so Postgres default READ COMMITTED applies — a snapshot read does not block a concurrent transaction's insert, and there is no predicate/exclusion lock. I confirmed prisma/schema/patient.prisma:452-481 has only @@index entries (no @@unique, no exclusion constraint) on PatientInsurance, so nothing at the DB level rejects two overlapping active rows. buildPatientInsuranceOverlapWhere (src/lib/patient/insurance-overlap.ts:20-49) correctly models open-ended interval overlap with is_active:true and public_program_code dimensioning, so the query logic is right — the defect is purely the check-then-act race, exactly as Codex states. Agreements: TOCTOU race real, no DB backstop, both POST and PUT affected, billing/public-fund selection non-deterministic. Minor corrections to Codex scoring: effect is closer to 3 than 4 (the race window inside a single interactive tx is narrow and requires near-simultaneous writes to the same patient+insurance_type; concurrent edits of one patient's insurance by two staff are plausible but not high-frequency), and safety is 4 not 5 (this is data-integrity/billing corruption, not a direct suppressed medical safety alert — copay_ratio/public-fund ambiguity harms billing correctness, one tier below CDS false-negatives like F48).
- **Codexへの訂正**: Codex scored safety 5 / effect 4. On independent read I put safety at 4 (data-integrity/billing corruption via ambiguous copay_ratio/public-program selection, one tier below a suppressed medical CDS alert) and effect at 3 (the POST/PUT race window sits inside a single interactive transaction and needs near-simultaneous writes to the same patient+insurance_type; plausible but not high-frequency). Everything else in Codex's claim — no DB constraint, READ COMMITTED, both POST and PUT affected, non-deterministic billing selection — is accurate. The overlap where-builder itself is correct; the defect is exclusively the check-then-act pattern.

### F86 — CONFIRMED [flag] (Claude score 24: s5/e4/v5/sc5/ef5) neighbors=2

- 独立検証: Re-verified by reading the code. src/app/api/admin/webhooks/route.ts:108-154: POST generates a 32-byte hex webhook signing secret, persists only the encrypted form (secret:null, ...encryptedSecret at 125-126), and returns the raw plaintext `secret` once in the 201 body via `success({ data: registration }, 201)` (150,154). src/lib/api/response.ts:18-20 confirms `success()` calls `NextResponse.json(data,{status})` and sets NO Cache-Control/Pragma headers — so the 201 carrying the HMAC signing secret has default cacheability. src/lib/api/sensitive-response.ts:1-11 is the correct helper (Cache-Control: private, no-store, max-age=0 + Pragma: no-cache) but this route never imports it (import list 1-15 has no sensitive-response). grep confirms admin/webhooks/route.ts is NOT among the ~250 routes using withSensitiveNoStore. Agreement with Codex on all points; the 503 encryption-error branch (113-117) is also plain and equally uncovered. Classification=flag (security: credential leakage). Scores unchanged from Codex.

### F87 — PARTIALLY_CONFIRMED [flag] (Claude score 18: s2/e2/v5/sc4/ef5) neighbors=1

- 独立検証: I read the actual code, not Codex's summary. AGREE: prescriber-institutions/suggestion/route.ts:20-27 returns via success() with NO withSensitiveNoStore, unlike every sibling (external-professionals/suggestions/route.ts:58, conference-notes/participant-suggestions/route.ts:73, consent-records/route.ts:222, management-plans/route.ts:105, billing-preview/route.ts:83, prescriber-institutions/route.ts). So patient-contextual clinical metadata is cacheable — this no-store gap is REAL (F86-class). DISAGREE with the P1 IDOR/access framing: permission-matrix.ts grants canReport=true to exactly owner/admin/pharmacist/pharmacist_trainee/clerk (driver/external_viewer=false, permissions.test.ts:82-119 confirms), and those five are precisely ORG_WIDE_ACCESS_ROLES (visit-schedule-access.ts:23-29). buildCareCaseAssignmentWhere/buildPatientAssignmentWhere return null (bypass) for all of them (lines 88-94,135-141), so the 'stronger guard' canAccessCareCase/canAccessPatient in external-professionals/suggestions/route.ts:19-33 is a NO-OP for every reachable caller — there is no assignment a canReport user can violate; org-wide access is intended policy (visit-schedule-access.ts:16-22). Cross-tenant is blocked by explicit org_id filter (prescriber-institutions.ts:109). DISAGREE with the mismatch claim: findLatestPrescriberInstitutionSuggestion (prescriber-institutions.ts:113-116) AND's case_id and patient_id in the SAME cycle relation filter, so a mismatched pair matches no cycle and returns null — consistency is already enforced. Net: only the no-store hygiene gap survives; the authz/IDOR core is refuted.
- **Codexへの訂正**: Codex over-scoped this to P1 authz/IDOR (safety 5/effect 4 = 23). The IDOR is not reachable: all canReport roles have org-wide access by design, so the sibling's access helper is a no-op and grants no extra protection; the mismatched patient/case scenario already returns empty because the helper AND's both ids on the same cycle. The only real defect is the missing withSensitiveNoStore on patient-contextual clinical metadata — a minor caching-hygiene gap equivalent to F86, not a P1 access-control break. Severity should be downgraded accordingly.

### F88 — CONFIRMED [flag] (Claude score 22: s4/e4/v5/sc4/ef5) neighbors=0

- 独立検証: Re-verified in care-reports/route.ts. careReportQuerySchema (route.ts:113,550-565) allows patient_id and q together with no mutual-exclusion check. Clobber confirmed at route.ts:688-692: object literal spreads patient_id scalar, then later spreads patient_id in matchedPatientIds when query present; object-spread lets the later key win, replacing the scalar. matchedPatientIds (route.ts:640-661) is an org-wide name/kana search not restricted to patientId, so patient_id=A and q matching B returns patient B reports. Same defect in palette branch (690/692). Within-org (org_id 689 and accessWhere 725 still applied), not cross-tenant. Name mapping (744-746, 805-814) resolves from returned reports, so results are self-consistent for the wrong patient, not a same-name mislabel.</independentReason>
  <corrections>Not cross-tenant: org_id (689) and accessWhere (725) remain applied, so leakage is within the same org. Not a mislabel of patient A; the explicit patient_id=A scope is silently discarded and another patient reports returned with that patient own name. Cross-tenant and same-name framing is overstated; severity otherwise accurate.</corrections>
  </invoke>

### F89 — CONFIRMED [flag] (Claude score 19: s4/e3/v4/sc4/ef4) neighbors=0

- 独立検証: Independently re-verified both parts by reading the code. QR part: qr-scan/page.tsx:168-171 catch collapses ANY fetch failure into setPatients([])+setPhase('matched'), which is byte-identical to a genuine no-match. The matched-phase UI (860-871) then shows "該当する患者が見つかりませんでした", and critically the 新規患者登録 CTA (goToNewPatient) at 880-883 is rendered UNCONDITIONALLY (outside the selectedPatient/candidates/empty branch), so it is offered even when the "empty" is actually a 500/network error → duplicate patient record for an already-existing patient. CONFIRMED. Capture part: shutter disabled only by saving||patientLoading (capture-content.tsx:321); patientLoading=!orgId||patientQuery.isPending (214), so on query ERROR (isPending=false, data=null) the shutter is enabled while the banner reads "患者情報を取得できませんでした" (240-243), and persistCapturedImage saves patientId: patientContext?.patientId ?? undefined (136). CONFIRMED as behavior. CORRECTION to Codex's severity framing: on sync, uploadEvidenceDraft resolves the target purely from scheduleId→visit*record_id (evidence-drafts.ts:185-198, presign body visit_record_id:225, PATCH 277) and NEVER reads the draft.patientId — so undefined patientId does NOT misroute evidence to the wrong patient; the harm is consent/UX (capturing under an unconfirmed-patient banner), not cross-patient PHI misattribution. Also note capture/page.tsx:34-38 can seed a truthy initialPatientContext with patientId:null (schedule with no case*), which via initialData:66 makes isPending=false and reproduces the same enabled-shutter-with-null-patient state from the server side — same root cause, part of F89. Support (not contradiction): the sibling patient-match flows already implement exactly Codex's suggested fix — prescription-intake-form.tsx:1930-1936 shows an explicit "「該当なし」ではなく取得エラーです。誤った患者で登録しないよう再読み込みしてください。" and qr-drafts/[id]/page.tsx:756-769 distinguishes case fetch-error from empty and 804-811 blocks confirm when patient_id unresolved. qr-scan is the lone laggard, so the fix direction is repo-consistent.
- **Codexへの訂正**: Codex's capture claim "permits evidence capture without a confirmed patient identity" overstates the data-integrity risk: the offline draft's patientId is never used on sync — uploadEvidenceDraft binds the photo to the visit via scheduleId→visit_record_id (evidence-drafts.ts:185-198, 225, 277), so there is no cross-patient misroute; the real harm is consent/UX (capturing under an unconfirmed-patient banner), which lowers the capture-part effect. The QR part is the more material harm (duplicate-patient creation on masked fetch error). Also: the 新規患者登録 CTA at qr-scan/page.tsx:880-883 is rendered unconditionally, so Codex's "exposes 新規患者登録" is correct but it is not inside the empty branch — it always shows, which is why the error-collapsed empty is dangerous.

## Part B: Claude が Codex 候補を拡張して見つけた新規 neighbor (X01-X13)

### X01 [flag] authz/PHI-disclosure — GET /api/external-access enumerates all org external-sharing grants under canReport, not canManagePatientSharing

- File: `src/app/api/external-access/route.ts:412` (neighbor of F80, confidence medium)
- Evidence: The list handler is gated by permission:'canReport' (route.ts:411-414) and returns every org external-access grant with granted_to_name, granted_to_contact_masked, patient name/name_kana, scope, expiry and self-report summary (route.ts:386-408). clerk and pharmacist_trainee have canReport:true but canManagePatientSharing:false (permission-matrix.ts:81-96, 67-80). Distinct handler/operation from the POST creation bug F80 cites (417-660): read/enumeration of external-sharing lifecycle state. Every other patient-sharing lifecycle route uses canManagePatientSharing, so this GET should too.
- Failure scenario: A clerk (canManagePatientSharing:false) calls GET /api/external-access and enumerates who currently has external bearer/OTP access to each patient's PHI across the org (granted_to_name, masked contact, patient identity, scope, expiry) even though the role is not permitted to manage patient sharing.
- Reachability: Directly reachable by any authenticated clerk/trainee session with a single GET request; no preconditions beyond org membership.

### X02 [flag] medical-safety-false-negative — CDS allergy cross-check silently skips hand-entered prescription lines (drug_code null), suppressing name-based allergy match with no data-quality alert

- File: `src/server/cds/checker.ts:928` (neighbor of F81, confidence high)
- Evidence: checkAllergyReactions:927-928 does `for (const line of prescriptionLines) { if (!line.drug_code) continue; }`. resolvePrescriptionLineDrugIdentitiesForCds (177-219) only sets drug_code=master.yj_code when drug_master_id resolves; a fully hand-entered line (drug_master_id null AND drug_code null) passes through unchanged (early return line 208). The name-based allergy cross-match at 947-964 (`line.drug_name.includes(allergy.drug_name)` with `!allergyDrugCode || hasSameYjIngredientPrefix(...)`) needs only drug_name, so it WOULD fire — but the line-928 continue skips it entirely. No data-quality alert is emitted for the skipped line (buildPrescriptionLineDrugIdentityMismatchAlert only fires on drug_master_id-resolved MISMATCH, not fully-unresolved lines).
- Failure scenario: A pharmacist hand-enters a prescription line 'アモキシシリンカプセル' with no drug_master_id and no drug_code for a patient whose allergy_info records drug_name 'アモキシシリン'. The name-substring allergy match would normally fire (allergyDrugCode empty → the `!allergyDrugCode` branch), but checkAllergyReactions skips the line at 928 and returns no allergy alert. Pharmacist sees a clean allergy check for a drug the patient is allergic to — an allergy false-negative, the highest-harm CDS direction.
- Reachability: Reachable via /api/cds/check (checkDispenseAlerts) whenever a prescription line has drug_master_id null and drug_code null (hand entry, or import that failed code resolution) and the patient has a name-only allergy entry. Same input class as F81.

### X03 [flag] medical-safety-false-negative — Fully-unresolved prescription lines are dropped from ALL CDS checks with no identity-unresolved data-quality alert (prescription-line twin of F81)

- File: `src/server/cds/checker.ts:208` (neighbor of F81, confidence high)
- Evidence: resolvePrescriptionLineDrugIdentitiesForCds emits a data-quality alert ONLY on a drug_master_id-resolved code MISMATCH (207-219: `if (!line.drug_master_id) return line;` at 208 → no alert; `if (!master) return line;` at 211 → no alert; alert only at 215 when lineDrugCode && lineDrugCode !== master.yj_code). Every downstream line-based check then does `if (!line.drug_code) continue`: checkInteractions:571, checkDuplicates keys off master/drug_code, checkNarcoticFlags:1051, checkHighRiskDrugs:1133, checkPackageInsertAudit (contraindication):733, checkAllergyReactions:928. So a line with drug_master_id null AND drug_code null is silently excluded from interaction, duplicate, narcotic, high-risk, contraindication, and allergy checks with zero 'identity unresolved' signal to the UI.
- Failure scenario: An import or hand entry produces a prescription line with drug_name only (drug_master_id null, drug_code null). checkDispenseAlerts runs all 10+ checks; every one skips this line, and resolvePrescriptionLineDrugIdentitiesForCds emits no data-quality alert for it. The pharmacist receives a clean CDS bill for a drug that was never actually checked against interactions, contraindications, or allergies — identical harm to F81 but on the prescription-line (new drug) side, which is the more directly clinically relevant side.
- Reachability: Reachable via /api/cds/check whenever a prescription line fails identity resolution (no master id, no yj code). resolvePrescriptionLineDrugIdentitiesForCds already builds the dataQualityAlerts array, so adding an unresolved-line alert is a contained additive fix.

### X04 [fix] medical false-negative / missing-data fail-open — checkRenalDoseAdjustment fails silent-clean when eGFR unrecorded — no coverage notice, unlike sibling monitoring check

- File: `src/server/cds/checker.ts:1418` (neighbor of F82, confidence high)
- Evidence: checkRenalDoseAdjustment returns [] immediately when the patient has no eGFR PatientLabObservation (checker.ts:1416-1418: const egfr = latestEgfr?.value_numeric ?? null; if (egfr === null) return alerts;) — it exits before even fetching the renal-dose package inserts, emitting NO data-quality/coverage alert. Its sibling in the SAME file, checkMonitoringAlerts, does the opposite: when PT-INR (1573-1580) or K (1599-1606) is missing it pushes an explicit info alert ('...の直近値が未記録です'). This is exactly the 'absent data should produce a coverage notice rather than a misleading clean bill' pattern Codex recommends in F82, applied inconsistently across sibling checks.
- Failure scenario: A home-visit elderly patient is prescribed a renally-cleared narrow-therapeutic-index drug (e.g. a DOAC or digoxin) whose DrugPackageInsert has dosage_adjustment_renal populated, but the patient has never had an eGFR lab recorded (common for home-visit populations). CDS silently returns zero renal alerts, giving the pharmacist a clean bill with no indication that renal dosing could not be assessed.
- Reachability: Reached on every CDS run via checkDispenseAlerts -> checkRenalDoseAdjustment (checker.ts:1742-1744), gated only by managedAlertStates.get('renal_dose'); needs no condition data to trigger.

### X05 [flag] medical false-negative / arbitrary truncation of safety text — Package-insert contraindication/adverse/elderly alerts truncated via slice(0,3)/slice(0,2)+100 chars in unsorted order — clinically relevant entries silently dropped

- File: `src/server/cds/checker.ts:751` (neighbor of F82, confidence medium)
- Evidence: Contraindications are capped at the first 3 entries, each truncated to 100 chars (checker.ts:751 contraindications.entries.slice(0, 3), 755 item.text.slice(0, 100)); serious adverse effects capped at 2 (775,779); elderly precautions capped at 2 (797,801). The parser readPackageInsertTextEntries (checker.ts:250-279) preserves raw stored/object-key order with NO clinical-priority sorting, so the retained 'first 3' are arbitrary. No alert signals that entries were dropped (contrast the malformedCount data-quality path at 740-749).
- Failure scenario: A drug's package insert lists 5 contraindications where the clinically relevant one (e.g. severe renal impairment, a specific comorbidity) is the 4th/5th entry, or the relevant clause sits past the first 100 characters of a long entry. The pharmacist never sees it because slice(0,3)/slice(0,100) drops it with no notice.
- Reachability: checkPackageInsertAudit is always enabled (checker.ts:1745-1746) and runs on every CDS check for any prescribed drug with a DrugPackageInsert.

### X06 [fix] concurrency/check-then-act create guarded only by non-unique findFirst — CommunicationRequest reply-request/queue-draft dedup is read-before-create outside the tx with no unique constraint — concurrent double-submit creates duplicate open reply requests / queue drafts

- File: `src/app/api/communication-requests/route.ts:546` (neighbor of F83, confidence high)
- Evidence: route.ts:546-568 findFirst({request_type,patient_id,case_id,recipient_role,related_entity_type,related_entity_id,status:{in:ACTIVE_REPLY_REQUEST_STATUSES}}) then returns conflict('この相手への返信依頼は既に起票されています') — an explicit dedup intent. route.ts:579-601 does the same for queue drafts (returns reused_existing_draft). Both findFirst run on the bare prisma client OUTSIDE the create transaction (create is a separate withOrgContext at 604-607). communication.prisma:100-102 shows CommunicationRequest has only @@index([org_id]), @@index([status]), @@index([related_entity_type,related_entity_id]) — NO @@unique over the dedup tuple. Same root-cause class as F83 create side (create-if-not-exists guarded only by non-atomic read + no unique constraint).
- Failure scenario: A pharmacist double-submits (or two staff act) the same reply-request for one report/recipient. Both findFirst at 546 read null (no existing open request), both pass the guard, both create() at 607 — producing duplicate open CommunicationRequest rows for the same recipient/entity, defeating the stated '既に起票されています' guard and cluttering the multidisciplinary comms queue with contradictory duplicates.
- Reachability: POST /api/communication-requests when related_entity present and request_type in DUPLICATE_GUARDED_REPLY_REQUEST_TYPES, or a communication-queue draft; reachable by any authorized author via double-submit/retry.

### X07 [fix] concurrency/check-then-act create guarded only by non-unique reads — VisitScheduleProposal duplicate-open-proposal / active-schedule guards are check-then-act findFirst/findMany with no unique constraint backing them

- File: `src/app/api/visit-schedule-proposals/route.ts:1200` (neighbor of F83, confidence medium)
- Evidence: Inside the tx (opens ~1137), route.ts:1163-1184 findFirst(visitSchedule) → error 'duplicate*active_schedule', and 1200-1224 findMany(visitScheduleProposal {case_id, finalized_schedule_id:null, proposal_status in OPEN*...}) → error 'duplicate_open_proposal', matching the explicit conflict('同一ケース・同一日付・同一担当薬剤師の未確定候補が既に存在します') at route.ts:1849-1859. These are read-then-create with no FOR UPDATE / atomic claim; VisitScheduleProposal (visit.prisma:270+) has no @@unique on (org_id,case_id,proposed_date,proposed_pharmacist_id,proposal_status). Distinct file and root-cause from F83; the existing F-findings on this route are FE error-envelope and billing-preview perf, not this race.
- Failure scenario: Two proposal-generation requests with DIFFERENT idempotency keys for the same case/date/pharmacist run concurrently; both pass the findMany duplicate-open check at 1200 and both create proposals for the same route cell, producing duplicate unfinalized visit proposals for one case/date/pharmacist that feed downstream scheduling and billing candidates. (Partial mitigation: same idempotency_key is deduped via the batch findUnique at 1137, so only cross-key races slip through — hence medium confidence.)
- Reachability: POST /api/visit-schedule-proposals generation path with distinct idempotency keys under concurrency.

### X08 [flag] active-row-uniqueness-not-constrained / concurrent-write race — ManagementPlan 'single approved plan per case' invariant is not DB-constrained and its supersede step is not concurrency-safe

- File: `src/app/api/management-plans/[id]/route.ts:246` (neighbor of F84, confidence medium)
- Evidence: In the approve handler, within one withOrgContext tx: :246-260 updateMany sets the target draft->approved, then :263-273 updateMany supersedes all OTHER approved plans for the same case (status:'approved', id:{not: freshPlan.id} -> 'superseded'). The existence of this supersede step proves the intended invariant is 'at most one approved plan per case'. But ManagementPlan (prisma/schema/patient.prisma model, ~line 281) has no partial unique over (org_id, case_id) where status='approved', and Prisma's default READ COMMITTED gives no serialization. Two concurrent approvals of two different draft plans for the same case (plan1, plan2): each reads/approves its own row (different ids, no write conflict); each then runs the supersede 'approved where id != self'. If they commit near-simultaneously, neither supersede sees the other's not-yet-committed approve, so BOTH remain status='approved'. findCurrentManagementPlan (management-plans.ts:128-150) then relies on findFirst+orderBy(effective_from desc, version desc, approved_at desc) to disambiguate — a defensive read that silently masks the broken invariant, while GET list and PDF surfaces can show two approved plans as authoritative. Reachable via approve on /api/management-plans/[id] when a case has >=2 drafts (versioning supported) and two approvals race, or via double-submit; gate consumers are billing/visit-workflow paths.
- Failure scenario: Two pharmacists (or a double-submit) approve two different draft management-plan versions for the same case at the same time. Both transactions commit with status='approved' because each supersede query ran before the other's approve was visible. The case now has two approved management plans with no DB guard; the visit-workflow/billing gate (evaluateVisitWorkflowGate) keys off whichever findCurrentManagementPlan's orderBy happens to rank first, and list/PDF views present conflicting authoritative plans.

### X09 [flag] concurrency-check-then-act / date-range-overlap not serialized — Pharmacy-site insurance-config effective-period overlap check runs OUTSIDE the transaction (findMany-then-create/updateMany), so concurrent configs with overlapping effective_from/effective_to but different revision_codes both commit

- File: `src/app/api/pharmacy-sites/[id]/insurance-configs/route.ts:88` (neighbor of F85, confidence high)
- Evidence: POST reads overlappingConfigs via prisma.pharmacySiteInsuranceConfig.findMany at route.ts:88 — using the GLOBAL prisma client, entirely OUTSIDE the withOrgContext transaction that opens at line 110 — filters with rangesOverlap (a genuine open-ended interval overlap test, pharmacy-site-insurance-config.ts:59-68), rejects at line 104 when overlapping && !auto_close_overlaps, else auto-closes the read-time overlaps via updateMany (line 112) and creates (line 122). The @@unique on prisma/schema/organization.prisma:189 is only ([org_id, site_id, insurance_type, revision_code]); it does NOT constrain date-range overlap across DIFFERENT revision_codes, so no DB backstop exists for the interval invariant. The sibling PUT ([configId]/route.ts:48) has the identical shape: prisma.findMany overlap check outside the tx (line 48), tx.update inside (line 70). Worse than F85, whose read at least sits inside the tx.
- Failure scenario: Two admins concurrently POST insurance configs for the same site + insurance_type='medical' with different revision_codes (e.g. '2024' and '2024b') whose effective_from/effective_to ranges overlap. Both requests run the findMany overlap check at line 88 before either commits, so each sees only the other's absence, passes the rangesOverlap gate (line 103), and both create rows. Neither auto-close updateMany closes the other's just-inserted row (it only closes rows read before the tx). Result: two active insurance-billing configs cover the same date for the same site+insurance_type, making which revision's config applies for a given billing date non-deterministic — the exact billing/data-integrity harm class as F85.
- Reachability: POST /api/pharmacy-sites/[id]/insurance-configs (canAdmin, route.ts:48) and PUT [configId]/route.ts:48 (canAdmin). Two admins, or a double-submit/retry, registering revisions on the same site+insurance_type in overlapping periods.

### X10 [fix] concurrency-check-then-act / duplicate-insert not serialized — BusinessHoliday duplicate check is findFirst-then-create with no unique constraint, so concurrent POSTs create duplicate holiday rows for the same date

- File: `src/app/api/business-holidays/route.ts:104` (neighbor of F85, confidence medium)
- Evidence: The duplicate guard reads existing via prisma.businessHoliday.findFirst at route.ts:104 (org_id + date + site_id + holiday_type) OUTSIDE the withOrgContext tx, returns validationError at line 114 if found, then creates inside the tx at line 118. prisma/schema/organization.prisma:328-344 shows BusinessHoliday has NO @@unique — only @@index([org_id, date]) and @@index([site_id, date]) — so the DB cannot reject a duplicate. Under READ COMMITTED (rls.ts:85, no isolationLevel) two concurrent POSTs both read 'no existing' and both create. Distinct root-cause from N29 (which flags BusinessHoliday's MISSING RLS, a tenant-isolation gap) — this is a check-then-act duplicate-insert race.
- Failure scenario: An admin double-submits (or two admins add) the same holiday for date D, site S, holiday_type T at nearly the same moment. Both transactions run the findFirst at line 104 before either commits, both find nothing, both insert. The calendar ends up with two identical BusinessHoliday rows for the same day, which downstream schedule/day-board and billing-evidence date logic (which read these rows) can double-count or render inconsistently. Lower harm than the insurance neighbors (scheduling metadata, not billing amounts), hence medium confidence.
- Reachability: POST /api/business-holidays (canAdmin, route.ts:147). Double-submit/retry or two admins adding the same holiday date concurrently.

### X11 [flag] security/credential-leak (missing no-store on secret response) — MFA TOTP setup endpoint returns the shared TOTP secret without no-store (cacheable)

- File: `src/app/api/me/mfa/setup/route.ts:19` (neighbor of F86, confidence high)
- Evidence: POST returns `NextResponse.json({ secretCode: result.SecretCode, otpauthUri: 'otpauth://totp/PH-OS:...?secret=' + result.SecretCode ... })` (lines 19-24). result.SecretCode is the Cognito TOTP shared secret from associateTotpForAccessToken (line 14). This is emitted via a bare NextResponse.json with NO Cache-Control/Pragma — the route does not import withSensitiveNoStore (imports at 1-4), and it is absent from the ~250-route withSensitiveNoStore list. The secret is embedded twice (secretCode field AND inside otpauthUri). Same root cause as F86: a one-time long-lived credential returned in a cacheable response.
- Failure scenario: A user enrolls MFA; the 201 response carrying the TOTP shared secret (and the full otpauth:// provisioning URI) is retained by browser cache, an intermediary/debugging proxy, or a shared-workstation artifact. Anyone who later reads that cache obtains the TOTP seed and can generate valid MFA codes indefinitely — full MFA bypass for that account. Higher-severity than F86 because a TOTP seed is a persistent authenticator, not a single webhook HMAC.
- Reachability: Authenticated POST /api/me/mfa/setup, reached by any user during MFA enrollment (guarded only by session+accessToken at lines 8-11).

### X12 [flag] security/credential-leak (missing no-store on secret response) — MFA verify endpoint returns one-time recovery codes without no-store (cacheable)

- File: `src/app/api/me/mfa/verify/route.ts:68` (neighbor of F86, confidence high)
- Evidence: After successful TOTP verification the handler issues MFA recovery codes (`const recoveryCodes = await issueMfaRecoveryCodes(userId)`, line 66) and returns them via `return success({ ok: true, recoveryCodes })` (line 68). success() (src/lib/api/response.ts:18-20) sets no cache headers, and this route does not import/use withSensitiveNoStore (imports 1-8). Recovery codes are MFA-bypass credentials. Same root-cause class as F86 but distinct file, and distinct from the mfa/setup neighbor.
- Failure scenario: During MFA enrollment the response body containing the plaintext recovery codes is cached by the browser/intermediary/proxy or left as a shared-workstation artifact. An attacker reading that cache obtains the account's MFA recovery codes and can bypass MFA at login (recovery codes are consumed at /api/auth/mfa/recovery to disable TOTP). Persistent credential leak.
- Reachability: Authenticated POST /api/me/mfa/verify, hit by every user completing MFA setup (session+accessToken guard at lines 34-38).

### X13 [flag] PHI-cacheability / missing no-store — prescriber-institutions/[id] GET returns patient names (PHI) without no-store cache header

- File: `src/app/api/prescriber-institutions/[id]/route.ts:105` (neighbor of F87, confidence high)
- Evidence: GET (route.ts:55-106) returns success({ data: toResponse(item) }) with NO withSensitiveNoStore wrapper. toResponse (lines 41-49) maps recent*prescriptions to include patient_id, case_id and patient_name (intake.cycle.case*.patient.name), pulled from prescription_intakes include (lines 77-100). success() does not set Cache-Control: private, no-store (per F86). Only gated by permission canReport (line 57). Every sibling clinical route wraps responses in withSensitiveNoStore; this detail route does not.
- Failure scenario: A canReport user (owner/admin/pharmacist/trainee/clerk) opens a prescriber-institution detail page; the JSON response contains real patient names linked to that institution and is served with a cacheable default (no private/no-store), so shared/intermediary caches or browser back-forward cache can retain PHI on shared devices — a stronger PHI exposure than the F87 suggestion route which returns no patient names.
- Reachability: Reachable GET on existing admin/master detail endpoint; requires only canReport (org-wide roles).
