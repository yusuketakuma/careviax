# Ultracode Expansion — Round 1 (Claude workflow) — 2026-07-02

拡張スキャン by Claude: 16 finder レンズ × 敵対的 per-candidate 検証。既存 F01-F78 と dedup 済み（重複0）。
**このファイルはレビュー台帳。実装はしない（ユーザー指示: 相互レビューのみ）。** Codex は各項目を別視点から敵対検証＋隣接バグへ拡張し、`ops/refactor/ULTRACODE_EXPANSION_ROUND1_CODEX.md` に返す。

- raw candidates: 22 / confirmed-novel: 19 / refuted: 3 / dup-of-F01-78: 0
- byLane: {"FE": 4, "BE": 10, "DB": 1, "X": 4} / byClass: {"false-empty": 2, "tz-date-boundary": 7, "db-rls": 1, "concurrency": 2, "perf-n+1": 2, "offline-reliability": 3, "dead-code-dupe": 1, "fe-be-contract": 1}

凡例: CE=Claude候補番号。score= safety+effect+verify+scope+efficiency(各1-5)。classification fix=実装安全 / flag=人間/計画レビュー要(auth/billing/security/破壊的migration/PHI/danger-zone)。

## CE01 [23] false-empty [fix] — PCA-pump 返却検品待ち list and its re-rental safety gate (pendingInspectionPumpIds) silently collapse to empty when returnInspectionRentalsQuery errors, because only .data is read and the error is never surfaced unlike the two sibling queries.

- File: `src/app/(dashboard)/admin/pca-pumps/pca-pumps-content.tsx:957` (lane FE, finder fe-falseempty-admin)
- Scores: safety 5 / effect 4 / verify 5 / scope 5 / efficiency 4 = **23**
- Reachability: Reachable in production via the admin PCA-pump ledger page src/app/(dashboard)/admin/pca-pumps/page.tsx (loads PcaPumpsContent). The returnInspectionRentalsQuery runs on every mount for any org and can fail independently of the pumps/rentals queries because it hits a distinct endpoint with its own key ['pca-pump-rentals', orgId, 'return-inspection-pending'].
- Evidence: returnInspectionRentalsQuery (lines 489-502) fetches rentals with status=returned&inspection_status=pending. Its result is defaulted `?? []` at lines 523-529 and the render at lines 957-958 keys only off length: `returnInspectionRentals.length === 0 ? <p>返却検品待ちはありません。</p> : ...`, with the header badge at 947-953 showing 検品待ち 0件. Nowhere in the file is `returnInspectionRentalsQuery.isError`/`isLoading` read. This is the ONLY one of the four queries without an error surface: pumpsQuery (line 1041) and rentalsQuery (line 998) both pass `errorMessage={...isError ? '…を取得できませんでした' : undefined}` to DataTable, and pca-pumps-content.test.tsx:637-654 locks that correct sibling behavior (asserts 'PCAポンプ台帳/貸出一覧を取得できませんでした' on error) while only the EMPTY (not error) path is tested for the inspection list (test lines 618-622). Additionally pendingInspectionPumpIds is derived from this same list at lines 530-533 and is used as a safety gate: line 540 excludes those pump ids from `availablePumps` (rental dropdown) and line 848 disables the メンテ/整備完了 button for them.
- Failure scenario: The /api/pca-pump-rentals?status=returned&inspection_status=pending request fails (5xx/network/RLS) while the other queries succeed. The page shows '返却検品待ちはありません。' and '検品待ち 0件', so the pharmacist believes no returned PCA opioid-infusion pumps await the mandatory return inspection (cleaning / operation check / accessory verification) and skips it. Worse, pendingInspectionPumpIds becomes an empty Set, so a returned-but-uninspected pump is no longer excluded from the rental dropdown (line 540) and its メンテ button re-enables (line 848) — an uninspected PCA pump can be re-rented to the next patient.
- Suggested fix: Surface returnInspectionRentalsQuery.isError (and isLoading) in the 返却検品待ち PageSection instead of collapsing to '返却検品待ちはありません。' — e.g. render an ErrorState with 再試行 when isError, and do not treat an errored/loading query as a trustworthy empty set when computing pendingInspectionPumpIds (fail closed: block re-rental / メンテ while the inspection list is unknown).
- Verifier (adversarial) reason: Re-read pca-pumps-content.tsx myself. grep shows returnInspectionRentalsQuery only ever reads .data (lines 525/528); .isError/.isLoading never read. Render at line 957 keys only on returnInspectionRentals.length; badge 947-953 shows '検品待ち 0件'. pendingInspectionPumpIds (530-533) is a genuine safety gate: line 540 excludes pumps from availablePumps rental dropdown, lines 845-854 disable メンテ/整備完了 and set the '返却検品が未完了' title — both collapse to open when the Set is empty on query error. Sibling queries surface errors: rentalsQuery line 998, pumpsQuery line 1041, and pca-pumps-content.test.tsx locks that contract ('passes pump inventory/rental failures to DataTable instead of showing a false empty table'); only the EMPTY (returnInspectionEmptyMock) path is tested for the inspection list, not the error path, and the '0件は偽シグナル回避' comment is about state-color not error handling — so not intentional. Reachable via admin PCA-pump ledger page on every mount; distinct endpoint/key can fail independently. grep of ULTRACODE_FINDINGS for 'pca' returns only unrelated daily-job timezone findings (267/426/429), no pca-pumps false-empty entry.

- [ ] **Codex cross-review**: (別視点の敵対検証結果 CONFIRM/REFUTE + 隣接拡張候補をここに追記)

## CE02 [22] false-empty [fix] — visit-record-detail: visitPreparationSnapshot fetch failure silently drops billing/readiness blockers, flipping the post-visit checklist to falsely 'complete'

- File: `src/app/(dashboard)/visits/[id]/visit-record-detail.tsx:565` (lane FE, finder fe-falseempty-workflow)
- Scores: safety 5 / effect 4 / verify 5 / scope 4 / efficiency 4 = **22**
- Reachability: Rendered by the /visits/[id] visit detail page (VisitRecordDetail) for any visit record whose schedule.id is set; the query is a normal always-live react-query fetch (enabled once orgId and record.schedule.id resolve), so any transient 5xx/offline on /api/visit-preparations triggers it in production.
- Evidence: L565-576 the visitPreparationSnapshot useQuery destructures only {data, isLoading} — isError is never read. L610-619 visitPreparationPack = snapshot?.data.pack, then billingBlockers: visitPreparationPack?.billing_blockers ?? [] and intakeInitialTransitionExpected feed buildHomeVisit2026ReadinessItems. In src/lib/visits/home-visit-2026-evidence.ts L329-336 blockerItems are built ONLY from billingBlockers (each required:true, done:false); with billingBlockers=[] they vanish. L620-622,682 completedHomeVisit2026Count === requiredHomeVisit2026Items.length becomes true once the always-incomplete blocker items are gone, so the 訪問薬剤管理の確認 item flips to done + description '…揃っています'. L694-695 billingBlockerCount / billingBlockers also collapse to 0/[] for PostVisitWorkflowPanel. Crucially L630 workflowDataError = careReportsError||billingCandidatesError||residualsError deliberately OMITS the prep error, and the L924 '一部を取得できませんでした' banner never fires; L954 the care-team panel is gated only on visitPreparationLoading (not error) so it also renders empty. Sibling counter-examples: the residuals query in the SAME file (L557-559) explicitly throws on !res.ok with the comment '取得失敗を空配列に潰すと…区別できないため'; and per F11's verifier note visit-record-form.tsx fail-closes this very /api/visit-preparations endpoint (ErrorCard + 再読み込み), while the detail page does not.
- Failure scenario: A pharmacist opens /visits/[id] for a visit whose schedule has real billing_blockers (e.g. missing 請求根拠). /api/visit-preparations/{scheduleId} returns 500 (or times out). billing_blockers?? [] makes the blocker checklist items disappear; the '訪問薬剤管理の確認' readiness row shows done + '該当時の加算根拠が揃っています', completedHomeVisit2026Count equals the (now smaller) required total so medicationManagementComplete=true, billingBlockerCount=0 in the post-visit workflow, the care-team panel renders empty, and NO 取得失敗 banner appears — the visit looks fully report/billing-ready when a required blocker actually exists.
- Suggested fix: Destructure isError (and refetch) from the visitPreparationSnapshot query and include it in workflowDataError (extend the L924 banner copy + retry to call refetch), or gate the readiness/care-team panels so a prep-fetch failure shows an explicit 取得失敗 state instead of computing readiness from '?? []'/'?? 0' defaults — mirroring the residuals fail-close pattern already in this file.
- Verifier (adversarial) reason: Re-read visit-record-detail.tsx L565-576 (prep query destructures only {data,isLoading}; queryFn throws on !res.ok at L572 but isError is never consumed), L610-619 (billing_blockers ?? [] feeds readiness), L630 (workflowDataError omits prep error), L682/L694-695 (checklist flips done + billingBlockerCount→0), L924 banner never fires, L954 care-team panel gated only on loading. home-visit-2026-evidence.ts L329-336 confirms blockerItems come ONLY from billingBlockers (done:false,required:true) so [] makes them vanish. Sibling residuals query L557-559 has explicit anti-false-empty comment + throw; careReports L715-717 guards !careReportsError — the prep query is the sole unguarded consumer. Test suite visit-record-detail.test.tsx (titled 'no false-empty workflow') L120-128 parametrizes the other three errors but mocks the prep query with isError:false and never tests its failure, confirming oversight not spec. F11 is a different file/query (visit-record-form schedule/CDS) and even cites this file as the correct-pattern exemplar; no F01-F78 touches visitPreparationSnapshot.

- [ ] **Codex cross-review**: (別視点の敵対検証結果 CONFIRM/REFUTE + 隣接拡張候補をここに追記)

## CE03 [22] tz-date-boundary [fix] — cockpit carryover task count compares created_at (DateTime instant) against server-local midnight (setHours), over-counting today's early-morning tasks as 繰り越し during the JST 00:00-09:00 window on the current UTC prod runtime

- File: `src/app/api/dashboard/cockpit/route.ts:157` (lane BE, finder be-tz-boundary-routes)
- Scores: safety 4 / effect 3 / verify 5 / scope 5 / efficiency 5 = **22**
- Reachability: GET /api/dashboard/cockpit (requireAuthContext, permission canViewDashboard, route.ts:130-137); carryoverCount feeds the cockpit response consumed by the handoff/dashboard workspaces.
- Evidence: The route deliberately migrated the @db.Date query to the helper (`const todayRange = todayUtcRange(now)` line 155, used for scheduled_date line 253 and shift.date line 317) but kept `const localTodayStart = new Date(now); localTodayStart.setHours(0, 0, 0, 0);` (lines 157-158) for the carryover count `prisma.task.count({ where: { ..., created_at: { lt: localTodayStart } } })` (line 297). created_at is a DateTime instant; date-boundary.ts:15-16 mandates japanDayInstantRange for instant columns.
- Failure scenario: On a UTC runtime localTodayStart = today 00:00Z = today 09:00 JST. A task created this morning at e.g. 07:00 JST (created_at = yesterday 22:00Z, before localTodayStart) is counted as a carryover (繰り越し) task even though it was created today (JST). The carryover KPI on the cockpit is inflated during the JST 00:00-09:00 window.
- Suggested fix: Replace localTodayStart with japanDayInstantRange(now).gte for the created_at < today comparison, matching the @db.Date migration already done in the same handler.
- Verifier (adversarial) reason: Re-read route.ts:157-158 (localTodayStart via setHours) and :297 (created_at: { lt: localTodayStart } in task.count carryover query). Same handler migrated @db.Date columns to todayUtcRange (line 155) but left created_at on local midnight. date-boundary.ts:12-16 mandates japanDayInstantRange for DateTime/instant columns; japanDayInstantRange exported at line 104. Production is confirmed UTC, not JST: assert-env.ts:119-135 shows the JST runtime guard is non-fatal and only throws under ENFORCE_APP_TZ, which is BLOCKED pending the prod TZ env change (F-20260625-runtime-tz-prod-env). Under UTC, setHours(0,0,0,0)=today 00:00Z=09:00 JST, so a task created 07:00 JST today (22:00Z yesterday) is miscounted as carryover. Test at route.test.ts:369-386 runs under TZ=Asia/Tokyo and only pins the JST value (2026-06-11T15:00:00.000Z), which equals japanDayInstantRange(now).gte, so it does not lock the buggy UTC behavior. Not a duplicate: F06 is a different file/column (admin/capacity/route.ts:34, updated_at), F14/F27 are FE error handling, F30 is payload-type drift. The F06 verifier CONFIRMED the identical bug class with the same reasoning, corroborating this one.

- [ ] **Codex cross-review**: (別視点の敵対検証結果 CONFIRM/REFUTE + 隣接拡張候補をここに追記)

## CE04 [22] db-rls [flag] — rls-policies.sql SSOT omits FORCE ROW LEVEL SECURITY for PatientSelfReport and CommunityActivity, which their own migration forces — drift leaves two org-scoped PHI tables unprotected if provisioned from the SSOT file or when app_user owns the table

- File: `prisma/rls-policies.sql:226` (lane DB, finder db-rls-orgscope)
- Scores: safety 4 / effect 3 / verify 5 / scope 5 / efficiency 5 = **22**
- Reachability: GET/PATCH /api/patient-self-reports/[id] and community-activity endpoints read these tables; provisioning path is `psql < prisma/rls-policies.sql` per the file header.
- Evidence: PatientSelfReport (rls-policies.sql:226-229) and CommunityActivity (231-234) get ENABLE ROW LEVEL SECURITY + tenant_isolation policy but are the ONLY two org-scoped tables absent from the FORCE ROW LEVEL SECURITY block at lines 514-590 (which forces ~130 tables, e.g. Patient:514, PrescriptionLine:526, MedicationProfile:554). The block header (511-513) states its purpose: 'Force RLS for app_user role (bypass for superuser/migration role)'. Both tables DID receive FORCE in their creating migration (prisma/migrations/20260329020500_add_missing_communication_tables/migration.sql:38 and :68), so the drift is between the migration history and the re-applyable SSOT file. PatientSelfReport holds patient-submitted symptom/condition reports (PHI); reachable via src/app/api/patient-self-reports/[id]/route.ts.
- Failure scenario: If a DB environment is provisioned by running rls-policies.sql (the file's documented 'Usage: Run via psql or as a Prisma migration', line 4) rather than the full migration chain, or if app_user is ever the owner of these two tables, then ENABLE-only RLS is bypassed for the table owner while FORCE would have blocked it — permitting cross-tenant reads of patient self-reports and community activity, whereas every sibling org-scoped table stays protected.
- Suggested fix: Add `ALTER TABLE "PatientSelfReport" FORCE ROW LEVEL SECURITY;` and `ALTER TABLE "CommunityActivity" FORCE ROW LEVEL SECURITY;` to the FORCE block in prisma/rls-policies.sql so the SSOT matches the migrations and all other org-scoped tables.
- Verifier (adversarial) reason: Re-read prisma/rls-policies.sql:226-234: PatientSelfReport and CommunityActivity get ENABLE ROW LEVEL SECURITY + tenant_isolation policy. grep for 'FORCE ROW LEVEL SECURITY' scoped to those two names in the SSOT returns empty, while their ENABLE-block siblings ExternalAccessGrant and TracingReport ARE in the FORCE block (rls-policies.sql:511-590). The creating migration migration.sql:38 and :68 DOES force both tables, proving drift between migration history and the re-applyable SSOT file. grep of ops/refactor/ULTRACODE_FINDINGS_20260702.md for rls-policies/PatientSelfReport/CommunityActivity = 0 matches, so not a duplicate. Impact is conditional (FORCE only matters when app_user owns the table, or when re-provisioning from this file rather than the migration chain that already applied FORCE to the live DB), which is why real-world reachability is low — but the SSOT-vs-migration drift on PHI tables is genuine and code-confirmed.

- [ ] **Codex cross-review**: (別視点の敵対検証結果 CONFIRM/REFUTE + 隣接拡張候補をここに追記)

## CE05 [20] concurrency [fix] — pharmacy-drug-stock-requests decision route (FormularyChangeRequest) is check-then-act: tx update uses id-only where without re-asserting status='pending', so concurrent approve/reject leaves stock upserted as adopted while status flips to rejected (last-writer-wins) plus duplicate audit rows

- File: `src/app/api/pharmacy-drug-stock-requests/[id]/route.ts:114` (lane BE, finder be-concurrency)
- Scores: safety 4 / effect 3 / verify 4 / scope 5 / efficiency 4 = **20**
- Reachability: PATCH /api/pharmacy-drug-stock-requests/[id] with permission canAdmin (route.ts:146), from the formulary-change approval admin UI. Concurrency via two admins or double-submit/retry.
- Evidence: Line 40 `const request = await prisma.formularyChangeRequest.findFirst(...)` runs OUTSIDE the transaction; line 44 `if (request.status !== 'pending') return conflict('この申請はすでに処理済みです')` is the only guard. The transaction opens at line 83; line 87-111 upserts PharmacyDrugStock (adoption side effect); line 114-122 does `tx.formularyChangeRequest.update({ where: { id: request.id }, data: { status: approve?'approved':'rejected', decided_by_id, decision_note, decided_at } })` with an id-only where — no `status: 'pending'` re-assertion and no FOR UPDATE. Line 124-139 writes an audit entry per call. Contrast pharmacy-partnerships/[id]/activate/route.ts:124-150 which uses `updateMany({ where: { ..., status: { in: ['draft','suspended'] } } })` + `count !== 1` conflict.
- Failure scenario: Two admins (permission canAdmin) act on the same pending formulary change request simultaneously — or one admin double-submits — one choosing approve and the other reject. Both read status='pending' at line 40, both pass the line 44 guard, both enter the transaction. The approve path upserts PharmacyDrugStock (marks the drug adopted), the reject path does not; both updates flip the request status by id (last writer wins). Final state can be status='rejected' while the PharmacyDrugStock was already upserted as adopted, plus two conflicting audit rows for the same request — an inconsistent formulary/inventory state.
- Suggested fix: Move the status check into an atomic `tx.formularyChangeRequest.updateMany({ where: { id, org_id, status: 'pending' }, data: {...} })` and return the 処理済み conflict when count!==1, performing the PharmacyDrugStock upsert only after the claim succeeds; or FOR UPDATE the request row at the top of the transaction.
- Verifier (adversarial) reason: Re-read route.ts:40-46 (findFirst + status guard OUTSIDE the tx) and route.ts:83-122 (tx opens at 83; PharmacyDrugStock upsert at 87-111 approve-only; formularyChangeRequest.update at 114-122 uses id-only where with NO status:'pending' re-assertion). Confirmed the check-then-act race: two concurrent canAdmin PATCHes (or a double-submit) both read status='pending', both pass line 44, both enter the tx; the approve upsert persists as adopted while a racing reject flips status to 'rejected' by id (last-writer-wins), producing formulary/inventory divergence plus two conflicting audit rows (124-139). Sibling pharmacy-partnerships/[id]/activate/route.ts:124-150 uses the correct atomic updateMany({where:{id,org_id,status:{in:[...]}}}) + count!==1 conflict, proving the id-only update is an oversight. Test route.test.ts:274-284 only characterizes the current non-concurrent guard, does not lock the id-only where as intentional. Not in F44 (cases/[id]/transition), not in F71's enumerated files (workflow-exceptions/dispense-tasks/patient-share-cases) — this is FormularyChangeRequest in a new file; NOVEL per DEDUP RULE.

- [ ] **Codex cross-review**: (別視点の敵対検証結果 CONFIRM/REFUTE + 隣接拡張候補をここに追記)

## CE06 [20] concurrency [fix] — PATCH /api/dispense-results/[id] enforces the version optimistic-lock only in app code; the update where-clause omits version, so concurrent/double-submit corrections of a rejected DispenseResult silently lost-update

- File: `src/app/api/dispense-results/[id]/route.ts:274` (lane BE, finder be-concurrency)
- Scores: safety 4 / effect 3 / verify 4 / scope 5 / efficiency 4 = **20**
- Reachability: PATCH /api/dispense-results/[id] used to correct a rejected (差戻し) dispense result (guarded by latest-audit='rejected' at line 185). Concurrency via two pharmacists on the same rejected task or a double-submit.
- Evidence: Line 144 `existing = await tx.dispenseResult.findFirst(...)` selects `version` (line 160). Line 175 `if (parsed.data.version !== undefined && existing.version !== parsed.data.version) return conflict` is the optimistic-lock check. But line 274-286 does `tx.dispenseResult.update({ where: { id }, data: { ..., version: { increment: 1 } } })` — the where is id-only, it does NOT include `version: existing.version`, so the check at line 175 is advisory and not enforced atomically. The correct in-repo template is patient-self-reports/[id]/route.ts:166-178 which uses `updateMany({ where: { id, ..., version/status }, ... })` + `count !== 1` conflict, and visit-records/route.ts:1610-1627 which re-asserts status in updateMany.
- Failure scenario: Two pharmacists open the same rejected DispenseResult at version=1 and both submit corrections with version:1. Both read version=1 at line 144, both pass line 175, and both run the id-only update; in Read Committed the second update still matches (where id only) and applies with version:{increment} on the post-commit value, so both writes land and one pharmacist's dispensing correction (actual_drug_name/quantity/discrepancy_reason) is silently overwritten despite the version mechanism — a lost update on medical dispensing data.
- Suggested fix: Change the update to `tx.dispenseResult.updateMany({ where: { id, org_id, version: existing.version }, data: { ..., version: { increment: 1 } } })` and return the 更新されています conflict when count!==1, then re-fetch for the response.
- Verifier (adversarial) reason: Re-read src/app/api/dispense-results/[id]/route.ts:144-286. findFirst selects version (line 160), line 175 checks existing.version!==parsed.data.version, but the write at line 274-286 uses where:{id} only with version:{increment:1} — no version re-assertion, a genuine check-then-act lost update. I verified the cycle-level optimistic lock does NOT rescue it: the id-only dispenseResult.update row-lock serializes the second txn after the first commits, so it reads a fresh cycle version (cycle-transition.ts:108-137) and does not conflict; and even a VersionConflictError is caught at line 303-305 and RETURNED (not thrown), which commits the Prisma interactive transaction (rls.ts withOrgContext $transaction) with the overwriting dispenseResult write already applied. Reachable via PATCH /api/dispense-results/[id] (canDispense, line 185 rejected-audit gate) with two pharmacists or a double-submit — both pass line 175, later write silently overwrites the earlier dispensing correction. Not intentional: the version field + line-175 check show optimistic-lock intent; siblings (F44 cases/[id]/transition verifier cites visit-records/route.ts:1585-1605 and patient-self-reports updateMany+count!==1) establish the atomic convention. Not a dup: F44=cases transition, F48=qr-allergy JSON append, F71b=dispense-TASKS self-assign — all different file/root-cause.

- [ ] **Codex cross-review**: (別視点の敵対検証結果 CONFIRM/REFUTE + 隣接拡張候補をここに追記)

## CE07 [19] tz-date-boundary [fix] — Workflow dashboard overdueRequests KPI (fetchWorkflowCoreData, workflow-dashboard-queries.ts:499) uses due_date:{lt:new Date()} instead of the provided JST day-start `today`, flagging communication requests due today as overdue from 09:00 JST onward.

- File: `src/server/services/workflow-dashboard-queries.ts:499` (lane BE, finder be-tz-boundary-services-jobs)
- Scores: safety 3 / effect 3 / verify 4 / scope 5 / efficiency 4 = **19**
- Reachability: GET /api/dashboard/workflow -> fetchWorkflowDashboardCoreQueries in workflow-dashboard-queries.ts; the overdueRequests field is a headline workflow KPI rendered on every dashboard load for every role.
- Evidence: Line 494-501: prisma.communicationRequest.count({ where: { ..., due_date: { lt: new Date() } } }) maps to `overdueRequests` (result tuple line 414, returned line 996). CommunicationRequest.due_date is DateTime? (prisma/schema/communication.prisma:94) but is populated from a YYYY-MM-DD-only input (route.ts:99-105 optionalDateSchema regex /^\d{4}-\d{2}-\d{2}$/, stored via `new Date(due_date)` at route.ts:623 => UTC midnight of that JST calendar day). The correct-sibling in the SAME function computes overdue visits with `scheduled_date: { lt: today }` (line 542) where `today` is the JST-day-start UTC-midnight sentinel (`today = utcDateFromLocalKey(localDateKey())`, dashboard/workflow/route.ts:80, comment '@db.Date 比較用: ローカル日付の UTC 深夜'). The function already RECEIVES `today` as a parameter (line 403) but line 499 uses `new Date()` instead.
- Failure scenario: A communication request due 2026-07-02 is stored as 2026-07-02T00:00:00.000Z. A pharmacist opens the workflow dashboard at 10:00 JST (=01:00 UTC). `new Date()` = 2026-07-02T01:00:00Z > 2026-07-02T00:00:00Z, so `due_date: { lt: new Date() }` matches and the request is counted in `overdueRequests` even though it is due today, not overdue. This holds for the entire JST business day (09:00-24:00 JST), inflating the overdue KPI and creating false urgency; the sibling overdueVisits count with `lt: today` correctly excludes today.
- Suggested fix: Change line 499 to `due_date: { lt: today }` to match the sibling overdueVisits query and the JST-day-start sentinel already passed into the function.
- Verifier (adversarial) reason: Re-read workflow-dashboard-queries.ts:494-501 (overdueRequests uses due_date:{lt:new Date()}) vs sibling line 542 (overdueVisits uses scheduled_date:{lt:today}); result field surfaced at lines 38/414/996. route.ts:80 confirms today=utcDateFromLocalKey(localDateKey()) = JST day-start UTC midnight (comment line 79). communication.prisma:94 = DateTime?; communication-requests/route.ts:99-105 optionalDateSchema regex /^\d{4}-\d{2}-\d{2}$/ date-only, line 623 stores new Date(due_date)=UTC midnight of the JST calendar day. Under TZ=Asia/Tokyo a due-today request (e.g. 2026-07-02T00:00:00Z) is flagged overdue from 09:00 JST until midnight because new Date() crosses that instant, while lt:today would correctly exclude it — a time-of-day-dependent inflated count inconsistent with the adjacent overdueVisits. No comment/test locks new Date(); commit d55e9269 established JST-day-anchored overdue cutoffs. Dedup grep of ops/refactor/ULTRACODE_FINDINGS_20260702.md finds no overdueRequests/communicationRequest due_date entry (lines 411 and 535 touch this file but are different root causes).

- [ ] **Codex cross-review**: (別視点の敵対検証結果 CONFIRM/REFUTE + 隣接拡張候補をここに追記)

## CE08 [19] tz-date-boundary [fix] — syncMedicationProfiles writes new Date() (current instant) into MedicationProfile.end_date @db.Date; during the 00:00–09:00 JST window the discontinued drug's end_date is stored as the previous civil day (record/display off-by-one, NOT a CDS filter false-negative — all current-med reads gate on is_current)

- File: `src/server/services/prescription-intake-service.ts:1564` (lane BE, finder be-db-date-writes)
- Scores: safety 2 / effect 2 / verify 5 / scope 5 / efficiency 5 = **19**
- Reachability: POST /api/prescription-intakes (route.ts:1097), POST /api/qr-scan-drafts/[id]/confirm (confirm/route.ts:732), and POST /api/prescription-intakes/facility-batch (facility-batch/route.ts:451) all call runPrescriptionIntakePostCreateHooks (prescription-intake-service.ts:1383) → syncMedicationProfiles (:1459) → the discontinue updateMany at :1564.
- Evidence: Line 1562-1565: `await prisma.medicationProfile.updateMany({ where: { id: { in: idsToDiscontinue }, org_id: orgId }, data: { is_current: false, end_date: new Date() } })`. MedicationProfile.end_date is `DateTime? @db.Date` (prisma/schema/prescription.prisma:224; start_date at :223). A @db.Date column stores only the UTC date part, so `new Date()` (a full current instant) is truncated in UTC. The SIBLING writer in the very same function sets start_date via the UTC-midnight sentinel convention: line 1495-1498 `const startDate = line.start_date ? (typeof line.start_date === 'string' ? new Date(line.start_date) : line.start_date) : ...` where line.start_date is a 'YYYY-MM-DD' string, so `new Date('YYYY-MM-DD')` = UTC midnight (correct). The repo's documented convention for a @db.Date 'today' value is utcDateFromLocalKey(localDateKey()) (src/lib/utils/date-boundary.ts), the same convention F07 established for PharmacistShift.date. Contrast also formatUtcDateKey (src/lib/date-key.ts:19) vs local formatDateKey — the column round-trips as UTC midnight.
- Failure scenario: Runtime TZ = Asia/Tokyo (assert-env pins APP_TIMEZONE). A pharmacist confirms a QR-scan draft or registers a prescription intake at, e.g., 2026-06-12 07:00 JST (= 2026-06-11 22:00 UTC). A previously-current drug that is not in the new prescription is discontinued: end_date = new Date() → UTC date part = 2026-06-11 → the @db.Date column stores 2026-06-11 (the PREVIOUS civil day) instead of 2026-06-12. The medication reads back as having ended yesterday, so 'currently taking' / interaction (併用) filters that compare end_date against the UTC-midnight today sentinel treat the drug as ended one day early — a drug-safety false-negative. Outside the 00:00–09:00 JST window the value is correct, so the bug is intermittent and time-windowed (same class as F07/staff-kpi).
- Suggested fix: Write the UTC-midnight sentinel for 'today' instead of a full instant: `end_date: utcDateFromLocalKey(localDateKey())` (import from src/lib/utils/date-boundary.ts), matching the start_date convention in the same function. Add a unit test asserting the stored end_date equals the JST civil day (freeze time inside the 00:00–09:00 JST window).
- Verifier (adversarial) reason: Re-read prescription-intake-service.ts:1552-1567 — line 1564 does `data: { is_current: false, end_date: new Date() }`. end*date is `DateTime? @db.Date` (prescription.prisma:224), which Prisma stores as the UTC date part, so under APP_TIMEZONE=Asia/Tokyo a confirm at e.g. 07:00 JST (22:00 UTC prior day) persists the previous civil day. The sibling start_date write (1495-1499) uses the UTC-midnight sentinel, and date-boundary.ts:4-13 explicitly names medication*\*\_date @db.Date columns and warns local/instant Dates land on 前日15:00 UTC — confirming the discontinue write violates the repo convention (same class the repo already treats as FIX in F07 shift-templates and F47 daily/prescriptions). Reachable via POST /api/prescription-intakes, qr-scan-drafts confirm, and facility-batch → runPrescriptionIntakePostCreateHooks → syncMedicationProfiles, not pinned to a cron time. HOWEVER the candidate's failureScenario is refuted: no reader filters current meds by end_date — cds/checker.ts:1671-1672, medication-profiles/route.ts:130, patients/[id]/route.ts:1002, pdf-medication-record.ts:46, external-access.ts:768, home-care-ops.ts:541/1097 all gate on is_current:true; end_date is only selected for display. The drug is correctly excluded from CDS/interaction checks via is_current=false regardless of end_date, so there is no drug-safety false-negative. Actual harm is an off-by-one on the displayed end date of a discontinued profile (medication record PDF / visit brief) during a 9-hour daily window. Novel vs F07 (different file/column), F09 (same block, root cause = dead code: key matching), F47 (read-side comparison, different file).

- [ ] **Codex cross-review**: (別視点の敵対検証結果 CONFIRM/REFUTE + 隣接拡張候補をここに追記)

## CE09 [18] tz-date-boundary [fix] — ResidualMedicationChart buckets created_at timestamps by UTC calendar day, so residual records recorded 00:00-08:59 JST are misbucketed into the prior day, merging/splitting JST-distinct trend points against the 7-day 減数調剤 line

- File: `src/components/features/patients/residual-medication-chart.tsx:46` (lane FE, finder fe-shared-component)
- Scores: safety 3 / effect 3 / verify 4 / scope 4 / efficiency 4 = **18**
- Reachability: Rendered by src/app/(dashboard)/patients/[id]/medications/medications-content.tsx (patient medications tab); any patient with residual records recorded between 00:00-08:59 JST triggers the misbucketing.
- Evidence: Line 46: `const dateKey = r.created_at.slice(0, 10);` groups ResidualRecord rows by the UTC date prefix of a full timestamp. Contrast the sibling patient-structured-care-panel.tsx:23-27 which deliberately uses getUTCFullYear/Month/Date ONLY because start_date/end_date are date-only @db.Date columns (UTC-midnight); created_at here is a real instant, so slicing its UTC prefix is the wrong axis under Asia/Tokyo runtime. The grouped map then sums excess_days per bucket (lines 47-51) and the chart draws a 7-day 減数調剤 threshold line (lines 177-198).
- Failure scenario: A pharmacist records a residual-medication check at 08:30 JST on 2026-07-02 (created_at = 2026-07-01T23:30:00Z). slice(0,10) yields '2026-07-01', so the point is plotted under 07/01. If another check was recorded 10:00 JST on 2026-07-01, both collapse into a single '2026-07-01' bucket with their excess_days summed, distorting the residual trend and the position relative to the 7-day dose-reduction line.
- Suggested fix: Bucket by JST calendar day using a Tokyo-zoned formatter (e.g. formatInTimeZone(parseISO(created_at), 'Asia/Tokyo', 'yyyy-MM-dd')) instead of created_at.slice(0,10).
- Verifier (adversarial) reason: Re-read residual-medication-chart.tsx:46 — `const dateKey = r.created_at.slice(0, 10)` groups by UTC-date prefix. Confirmed created_at is a DateTime instant (prisma/schema/medication.prisma:75 `created_at DateTime @default(now())`), returned as full ISO timestamps by route.ts:139-152, so the UTC-day boundary lands at 09:00 JST under Asia/Tokyo runtime. Records recorded 00:00-08:59 JST get bucketed into the previous day; JST-distinct days can merge (excess_days summed at lines 47-51) and single JST days can split across UTC buckets, distorting the residual trend vs the 7-day 減数調剤 threshold line (lines 177-198). Reachable: rendered at medications-content.tsx:1377 on the patient medications tab. Not spec: residual-medication-chart.test.tsx only locks URLSearchParams and error-state, no comment justifies UTC grouping; the sibling patient-structured-care-panel uses getUTC\* correctly ONLY because those are @db.Date columns. Not a dup: grep of ULTRACODE_FINDINGS_20260702.md shows the tz-boundary findings (F07/F19/F24/F29/F31/F47/F57) are all other files/root causes.

- [ ] **Codex cross-review**: (別視点の敵対検証結果 CONFIRM/REFUTE + 隣接拡張候補をここに追記)

## CE10 [18] tz-date-boundary [fix] — care-reports/[id]/send builds the conference-month visit window with server-local getFullYear/getMonth (plus end-of-day lte) against instant visit_date, mis-selecting month-boundary visits for billing-evidence upserts on UTC runtimes

- File: `src/app/api/care-reports/[id]/send/route.ts:1715` (lane BE, finder be-tz-boundary-routes)
- Scores: safety 4 / effect 3 / verify 4 / scope 4 / efficiency 3 = **18**
- Reachability: POST /api/care-reports/[id]/send finalize path, when the report links a conference note with a case_id (route.ts:1714); triggers upsertBillingEvidenceForVisit per selected visit.
- Evidence: monthStart = `new Date(conferenceNote.conference_date.getFullYear(), conferenceNote.conference_date.getMonth(), 1)` and monthEnd = `new Date(..getFullYear(), ..getMonth() + 1, 0, 23, 59, 59, 999)` (lines 1715-1728) — server-local, end-of-day + lte instead of lt next-day-start. conference_date is `DateTime` (communication.prisma:229, an instant). This window is applied to `visit_date` (visit.prisma:195, also a DateTime instant) via `gte: monthStart, lte: monthEnd` (lines 1733-1736), and each matched visitRecord is fed to upsertBillingEvidenceForVisit (lines 1746-1751). date-boundary.ts:104-121 requires japanMonthInstantRange for instant-column month windows.
- Failure scenario: On a UTC runtime the window becomes [monthUTC-01T00:00Z, monthUTC-last-day-T23:59:59.999Z] rather than the JST month window. Visits recorded in the JST 00:00-09:00 morning of the month's first day are excluded and visits in the next month's first-day morning are wrongly included, so billing evidence is generated for the wrong set of month-boundary visits when a conference-linked care report is sent.
- Suggested fix: Derive the conference month key via japanDateKey(conferenceNote.conference_date).slice(0,7) and use japanMonthInstantRange(monthKey) with gte/lt for the visit_date filter.
- Verifier (adversarial) reason: Re-read route.ts:1715-1736: monthStart/monthEnd built with conference_date.getFullYear()/getMonth() (server-local) and applied to visit_date via gte:monthStart, lte:monthEnd (end-of-day inclusive rather than lt next-month-start). Confirmed conference_date is DateTime instant (communication.prisma:229) and visit_date is DateTime instant (visit.prisma:195); date-boundary.ts:115-121 japanMonthInstantRange is the correct JST-offset helper for instant month windows. Reachable via POST /api/care-reports/[id]/send finalize path (1702-1714) when a conference note with case_id is linked; each matched visit drives upsertBillingEvidenceForVisit (1746-1751). Not intentional: no local-intent comment, dedicated JST helpers exist, and F60-F66 already lock this class as a real FIX. Not a duplicate: findings file has no conference_date nor care-reports/[id]/send reference; F60/F61 live in billing-evidence/core.ts with different window computations.

- [ ] **Codex cross-review**: (別視点の敵対検証結果 CONFIRM/REFUTE + 隣接拡張候補をここに追記)

## CE11 [18] perf-n+1 [fix] — inventory-forecast fetches every historical prescriptionIntake (with all lines) for next-week cases despite consuming only the newest intake per patient — unbounded scan/payload growing with case age

- File: `src/app/api/admin/inventory-forecast/route.ts:113` (lane BE, finder be-perf)
- Scores: safety 2 / effect 3 / verify 5 / scope 4 / efficiency 4 = **18**
- Reachability: authenticatedGET with permission canAdmin (route.ts:50,219). Triggered by any admin loading the p1_07 inventory-forecast dashboard card.
- Evidence: route.ts:111-137 `prisma.prescriptionIntake.findMany({ where: { org_id: ctx.orgId, cycle: { case_id: { in: caseIds } } }, select: { prescribed_date, created_at, cycle{patient_id}, lines{...9 fields} } })` — NO time bound and NO take. The comment on line 109 says 「最新 1 件の選定は純関数側で行う」. The pure consumer confirms it: src/lib/analytics/inventory-forecast.ts:369-382 `selectLatestIntakeByPatient` keeps only the single newest intake per patientId (max prescribed_date then created_at), and buildInventoryForecast (line 506-508) uses only `latestIntakeByPatient`. So every intake row except the latest-per-patient is loaded and discarded. Sibling correct pattern: dashboard/monthly-stats/route.ts:80-92 first groupBy over a month-bounded window, then fetches only the reduced patient set.
- Failure scenario: An org that has operated for a year has ~12+ prescription intakes per active case (monthly refills), each with multiple `lines`. Every GET /api/admin/inventory-forecast (admin 「在庫と定期処方の予測」 page) enumerates next-week visit cases (dozens–hundreds) and loads their ENTIRE prescription-intake history plus all lines, then throws away everything but the newest intake per patient. DB read volume and payload grow unboundedly with case age while the forecast needs one intake per patient.
- Suggested fix: Either bound the intake window (e.g. prescribed_date >= now()-120d, matching the forecast's relevance horizon) or, better, fetch only the latest intake per case (orderBy prescribed_date desc/created_at desc with a per-case reduction / distinct-on) so both payload and scan cost stay bounded. Consumer output is unchanged because only the latest intake is used.
- Verifier (adversarial) reason: Re-read route.ts:111-137: the prescriptionIntake.findMany is scoped only by org_id and cycle.case_id in caseIds, with no prescribed_date bound and no take. Re-read inventory-forecast.ts:367-383 (selectLatestIntakeByPatient keeps one newest intake per patientId) and 506-509 (buildInventoryForecast uses only latestIntakeByPatient/linesByPatient), confirming all non-latest rows and their lines are loaded then discarded. Reachable via canAdmin GET (route.ts:50,219). route.test.ts:270-286 uses expect.objectContaining and does not lock absence of a bound/take, so it is not intentional spec. Not covered by F43/F55/F73/F76 (different files/queries).

- [ ] **Codex cross-review**: (別視点の敵対検証結果 CONFIRM/REFUTE + 隣接拡張候補をここに追記)

## CE12 [18] offline-reliability [fix] — Offline visit-record sync queue drains only via the record-form's page-scoped 'online' listener; the global reconnect handler (query-provider.tsx:27) refetches queries but never calls processSyncQueue, so records saved offline then navigated away from are not auto-synced despite the '再接続後に自動同期します' toast (manual /offline-sync retry still recovers them).

- File: `src/components/providers/query-provider.tsx:28` (lane X, finder offline-reliability)
- Scores: safety 4 / effect 3 / verify 5 / scope 3 / efficiency 3 = **18**
- Reachability: Standard p0 offline workflow: save visit record while offline (window.navigator.onLine === false branch, visit-record-form.tsx:1210), then leave the page before reconnecting.
- Evidence: The only GLOBAL online-event handler is query-provider.tsx:27-31, which calls `queryClient.refetchQueries({ type: 'active', stale: true })` but never `processSyncQueue`. The sole caller of `setupAutoSync` (the online-event queue drain) is visit-record-form.tsx:661-666, inside a useEffect whose teardown (674-678) removes the 'online' listener when that page unmounts. `processSyncQueue` is otherwise called only from the manual /offline-sync page (offline-sync-content.tsx:154). src/app/sw.ts has no Background Sync / periodicsync registration (only precache/runtime caching + push). The offline save path enqueues via `enqueueForSync('visit_record', payload)` (visit-record-form.tsx:1227) and shows toast 'オフラインで下書きを保存しました。再接続後に自動同期します。' (line 1230). Contrast: this is a real replay path (unlike F54's never-replayed PHOS card queue) but it is bound to one component's lifetime.
- Failure scenario: A pharmacist records a home visit offline on /visits/[id]/record (queue item added, toast promises auto-sync on reconnect), then navigates to the dashboard or any other route (visit-record-form unmounts → its 'online' listener is removed). The device regains connectivity elsewhere: query-provider's online handler only refetches React Query caches and does NOT call processSyncQueue, and no service-worker Background Sync exists. The visit record (billing-relevant PHI) sits permanently in IndexedDB unsynced until the user manually opens /offline-sync and taps retry, or happens to be back on a record-form page during an offline→online transition.
- Suggested fix: Drain the sync queue from a global surface: call processSyncQueue in query-provider's handleOnline (or a top-level layout/provider effect that also runs once on mount), and/or register a Service Worker Background Sync so reconnect drains regardless of which page is mounted.
- Verifier (adversarial) reason: Independently verified: query-provider.tsx:26-32 global online handler calls only queryClient.refetchQueries and never processSyncQueue (refetchOnReconnect:false at line 17). Repo grep confirms setupAutoSync/processSyncQueue exist in prod only in visit-record-form.tsx and offline-sync-content.tsx; no layout/provider mounts them globally (find src/app -name layout.tsx + components grep empty). sync-engine.ts:582-605 registers the online listener that drains the queue; its sole prod caller is visit-record-form.tsx:661 inside a useEffect whose teardown (674-678) removes the listener on unmount. Offline branch at visit-record-form.tsx:1210 enqueues (1227) and toasts '再接続後に自動同期します' (1230). sw.ts has no sync/periodicsync (grep empty). offline-sync-content.tsx:154 processSyncQueue is the manual /offline-sync route only. So a visit record saved offline and then navigated away from is not auto-synced on reconnect. Not intentional: no comment/doc/test locks page-scoped auto-sync; the toast promises otherwise. Distinct from F54 (src/phos offlineActionQueue, never-replayed, unmounted UI) — different file, queue, and root cause.

- [ ] **Codex cross-review**: (別視点の敵対検証結果 CONFIRM/REFUTE + 隣接拡張候補をここに追記)

## CE13 [18] offline-reliability [fix] — Offline sync state (pendingSyncCount/lastSyncedAt) is never bootstrapped globally, so the app-header shows green "同期済み" with no pending-drafts badge on any page other than the record/offline-sync pages even when unsynced visit records persist in IndexedDB

- File: `src/lib/stores/offline-store.ts:29` (lane X, finder offline-reliability)
- Scores: safety 4 / effect 4 / verify 3 / scope 3 / efficiency 4 = **18**
- Reachability: Any app session that starts on a route other than /visits/[id]/record or /offline-sync after an offline save; also every non-record page during a normal session.
- Evidence: offline-store.ts initializes `pendingSyncCount: 0` (line 29) and `lastSyncedAt: new Date().toISOString()` (line 34). The only code that refreshes them (`refreshSyncCount`/`refreshSyncState`) is invoked exclusively from visit-record-form.tsx (5s interval, lines 667-672) and offline-sync-content.tsx. app-header.tsx consumes both globally: HeaderSyncStatus renders '同期済み{time}' in green from lastSyncedAt (lines 69,88-97) and HeaderOfflineDrafts mounts the pending-count badge only when `pendingSyncCount > 0` (line 108, comment 102-105). grep confirms no layout/provider calls refreshSyncCount/refreshSyncState. So on a fresh load of e.g. /dashboard, pendingSyncCount stays at its init 0 and lastSyncedAt at init 'now'.
- Failure scenario: User saves a visit record offline, closes/reopens the app (or just loads any non-record page) while online with the queue item still pending: HeaderOfflineDrafts never mounts (pendingSyncCount stuck at init 0 because nothing counts the IndexedDB queue globally) so there is no '未同期 N件' badge, and HeaderSyncStatus shows green '同期済み HH:MM'. The pharmacist is told everything is synced while a billing-relevant visit record remains unsynced in IndexedDB.
- Suggested fix: Bootstrap offline sync state globally: call refreshSyncCount (and periodically) from the dashboard layout or a top-level provider so the pending-drafts badge and '同期済み' status reflect the actual IndexedDB queue on every route.
- Verifier (adversarial) reason: Independently re-read the cited code. offline-store.ts:29 inits pendingSyncCount:0 and line 34 inits lastSyncedAt to new Date() on client. Grep of src/ (tests excluded) confirms refreshSyncCount/refreshSyncState are called ONLY from visit-record-form.tsx (record page) and offline-sync-content.tsx — no layout/provider/app-shell bootstrap. app-header.tsx:88-98 renders green "同期済み HH:MM" from lastSyncedAt when online, and app-header.tsx:111 mounts the pending badge only when pendingSyncCount is truthy — both global consumers. getPendingSyncCount = offlineDb.syncQueue.count() (sync-engine.ts:426-427), a persistent IndexedDB count, so unsynced visit records survive a full reload; zustand store is plain create() with no persist, so a fresh load resets count to 0 and lastSyncedAt to "now". setupAutoSync (online-replay) is also wired only from visit-record-form.tsx, so pending items are not auto-synced off that page either. Therefore on /dashboard (or any non-record page) after an offline save the header falsely shows synced with no badge. Not intentional: the HeaderOfflineDrafts comment (app-header.tsx:102-105) presupposes pendingSyncCount reflects reality, and the record/offline-sync pages do refresh it correctly, making the global header consumer an oversight. Not a duplicate: F52/F53 (offlineEvidenceQueue), F54 (offlineActionQueue) are different files/root causes; no offline-store header-status finding exists.

- [ ] **Codex cross-review**: (別視点の敵対検証結果 CONFIRM/REFUTE + 隣接拡張候補をここに追記)

## CE14 [17] offline-reliability [fix] — enqueueForSync adds a new visit_record queue row per offline save with no scope_id dedup (unlike sibling registerVisitRecordConflict), so a second offline save of the same schedule creates a duplicate POST that self-409s into a spurious server_conflict on reconnect

- File: `src/lib/stores/sync-engine.ts:402` (lane X, finder offline-reliability)
- Scores: safety 3 / effect 3 / verify 4 / scope 4 / efficiency 3 = **17**
- Reachability: Two offline saves of the same visit-record form before reconnecting (the form stays on-page and editable after an offline save).
- Evidence: `enqueueForSync` (sync-engine.ts:402-421) always calls `offlineDb.syncQueue.add(...)` with `scope_id = payload.schedule_id`, with no lookup/replace of an existing pending item for that scope_id. Contrast the sibling `registerVisitRecordConflict` (lines 441-478) which explicitly does `.where('scope_id').equals(...).first()` and updates in place to avoid duplicate rows. On reconnect, processSyncQueueOnce POSTs both rows; the first creates the record + deletes the visitDraft (deleteSyncedQueueItem, 362-369), the second POST returns 409 and is turned into a `server_conflict` (lines 290-305).
- Failure scenario: Offline, a pharmacist saves the visit record, edits a field, and saves again (form is reset-in-place, not navigated away, at visit-record-form.tsx:1229). Two queue rows exist for the same schedule_id. On reconnect the first syncs successfully; the second yields an HTTP 409 that surfaces on /offline-sync as a spurious 'server conflict' the pharmacist must manually resolve for their own duplicate submission.
- Suggested fix: In enqueueForSync, look up an existing non-conflict pending item with the same entityType+scope_id and update it in place (mirroring registerVisitRecordConflict) instead of unconditionally adding a new row.
- Verifier (adversarial) reason: Re-read sync-engine.ts:406 — enqueueForSync unconditionally does offlineDb.syncQueue.add(...) with scope_id=schedule_id and no lookup/replace. The sibling registerVisitRecordConflict (sync-engine.ts:446-450, 472-477) does the scope_id+entityType .first() lookup and updates in place, confirming the dedup pattern is intended and missing here. Caller visit-record-form.tsx:1227-1231 calls enqueueForSync then form.reset(values) (stays on-page, editable, no nav), so a second offline save enqueues a second row for the same schedule_id. On reconnect processSyncQueueOnce (245) POSTs both; first creates the record and deleteSyncedQueueItem (353-372) only deletes its own itemId, leaving the second row; the second POST hits record_conflict at visit-records/route.ts:1098-1103 (existing record, not overwrite) → 409, converted to server_conflict at sync-engine.ts:290-305 requiring manual resolution. verifyQueueItemCurrent (374-382) checks only the item vs itself, so it does not prevent the duplicate. Not covered by F52/F54 (different queues/root causes) or any other F. Confidence tempered only by the multi-step offline flow needed to trigger it.

- [ ] **Codex cross-review**: (別視点の敵対検証結果 CONFIRM/REFUTE + 隣接拡張候補をここに追記)

## CE15 [16] tz-date-boundary [fix] — admin/staff-metrics: parseMonthRange uses server-local month boundaries applied to instant columns (visit_date, created_at), mis-windowing staff KPIs ~9h on UTC prod

- File: `src/app/api/admin/staff-metrics/route.ts:12` (lane BE, finder be-tz-boundary-routes)
- Scores: safety 2 / effect 3 / verify 5 / scope 2 / efficiency 4 = **16**
- Reachability: GET /api/admin/staff-metrics (withAuthContext, permission canAdmin, route.ts:54-62), consumed by the admin staff-metrics dashboard page.
- Evidence: parseMonthRange builds the window with server-local Date constructors: default branch `start: new Date(now.getFullYear(), now.getMonth(), 1)`, `end: new Date(now.getFullYear(), now.getMonth() + 1, 1)` (lines 12-13) and explicit-month branch `start = new Date(year, monthIndex, 1)` / `end = new Date(year, monthIndex + 1, 1)` (lines 31,38). That single range is applied to THREE queries against columns of two storage conventions: `visit_date` (prisma/schema/visit.prisma:195 `visit_date DateTime` — a real instant) at route.ts:117-120, `created_at` (communication.prisma:16 `DateTime @default(now())` — instant) at route.ts:139-142, and `shift.date` (organization.prisma:290 `date DateTime @db.Date` — stored UTC-midnight of the JST date) at route.ts:158-161. date-boundary.ts:15-16,104-121 mandates japanMonthInstantRange for instant columns and utcDateFromLocalKey for @db.Date columns; a single server-local boundary cannot be correct for both. The sibling correct pattern is japanMonthInstantRange (see F62 admin/metrics fix and me/activity-summary/route.ts:27-31).
- Failure scenario: On a UTC runtime (TZ enforcement is opt-in/warn-only per assert-env.ts, established in F30/F62), the month window becomes [2026-06-01T00:00Z, 2026-07-01T00:00Z) instead of the JST window [2026-05-31T15:00Z, 2026-06-30T15:00Z). Visits/reports created in the first 9 hours of the month (JST 00:00-09:00 on the 1st) are dropped from that month, and the last day's late visits (JST 00:00-09:00 on the next month's 1st) are pulled in. Staff monthly_visit_count, report_submission_rate, workload_balance_delta_percent, and the overloaded/underutilized summary are wrong near month boundaries.
- Suggested fix: Use japanMonthInstantRange(range.label) for the visit_date and created_at (instant) filters, and a UTC-midnight month range (utcDateFromLocalKey-based) for the shift.date @db.Date filter; derive the default month via japanDateKey(now).slice(0,7).
- Verifier (adversarial) reason: Re-read src/app/api/admin/staff-metrics/route.ts:8-41 — parseMonthRange uses server-local Date(year, monthIndex, 1) (lines 12-13,31,38). Same range applied to visit_date (visit.prisma:195 DateTime instant) at 117-120, CareReport.created_at (communication.prisma DateTime @default(now()) instant) at 139-142, and PharmacistShift.date @db.Date (organization.prisma:296) at 158-161. Sibling me/activity-summary/route.ts:27-31 correctly uses japanMonthInstantRange for instant + utcMonthDateRange for @db.Date. Grep of ULTRACODE_FINDINGS confirms F62 is admin/metrics/route.ts (different file), F60/F61 billing-evidence, F63-F66 jobs/helpers; staff-metrics not present. Novel per dedup rule (different file). On UTC runtime instant windows shift ~9h, corrupting monthly_visit_count/report_submission_rate/workload_balance_delta near month boundaries.

- [ ] **Codex cross-review**: (別視点の敵対検証結果 CONFIRM/REFUTE + 隣接拡張候補をここに追記)

## CE16 [16] tz-date-boundary [fix] — conference-data-sync derives recurrence BYDAY/week-of-month and the +7 proposed next-visit date from getUTCDay/getUTCDate of the raw conference_date timestamp, off by one day/weekday for conferences held 00:00-08:59 JST

- File: `src/server/services/conference-data-sync.ts:698` (lane BE, finder be-tz-boundary-services-jobs)
- Scores: safety 2 / effect 2 / verify 4 / scope 4 / efficiency 4 = **16**
- Reachability: Conference sync (syncConferenceDerivedData path) creates/updates a VisitScheduleProposal with proposal_reason `conference-recurrence-proposal:<note.id>` (line 696-725). Triggered when a conference note with a service-adjustment pattern has no latest schedule (weekday branch) or whenever a recurrence rule is suggested (proposed-date branch).
- Evidence: deriveRecurringVisitRule (line 218) derives BYDAY via dayCodeFromDate (getUTCDay, line 211) and weekOfMonthFromDate (getUTCDate, line 215) from anchorDate = `latestSchedule?.scheduled_date ?? note.conference_date` (line 692). Line 698-710 builds the proposed next-visit date as `new Date(note.conference_date)` then `setUTCDate(getUTCDate()+7)` normalized via Date.UTC(getUTCFullYear, getUTCMonth, getUTCDate, 12). ConferenceNote.conference_date is a real DateTime carrying a wall-clock time (schema communication.prisma:229 DateTime; validation conference.ts:417 `z.string().datetime()`, test fixture '2026-06-16T09:00:00.000Z'), NOT a @db.Date midnight sentinel. The rest of the codebase converts real timestamps to the JST civil day before extracting calendar parts (e.g. billingMonthForJapanTimestamp / japanCivilMonthParts add +9h in billing-evidence/core.ts:264-277; japanDateKey in date-boundary.ts:35 uses Asia/Tokyo Intl). getUTC\* here bypasses the +9 offset.
- Failure scenario: A conference recorded at 2026-06-16 07:00 JST is stored as 2026-06-15T22:00:00Z. getUTCDay() => Sunday (June 15) instead of Monday (June 16), so the suggested recurring-visit BYDAY is the wrong weekday; proposedDate = getUTCDate()+7 => June 22 instead of June 23. The generated visit-schedule-proposal recurrence and proposed date are off by one day/weekday.
- Suggested fix: Convert note.conference_date to the JST civil day before deriving weekday/week-of-month and the +7 proposed date (e.g. anchor via japanDateKey/japanDayStartInstantFromDateKey or add the +9h offset like billingMonthForJapanTimestamp), rather than getUTCDay/getUTCDate on the raw timestamp.
- Verifier (adversarial) reason: Re-read conference-data-sync.ts:210-216, 690-710: proposedDate (line 698) always uses new Date(note.conference_date)+setUTCDate(+7) and dayCodeFromDate/weekOfMonthFromDate use getUTCDay/getUTCDate on the anchor. conference_date is a real wall-clock DateTime (communication.prisma:229 = TIMESTAMP(3)), captured via datetime-local input (conferences-content.tsx:1692, new Date(x).toISOString() line 977) and displayed as JST HH:mm (lines 1410/1458). Under the pinned Asia/Tokyo runtime, a 07:00 JST conference stores 2026-06-15T22:00:00Z, so getUTCDate=15 → proposed June 22 (should be 23) and getUTCDay=Sunday → BYDAY=SU (should be MO). The codebase convention converts to JST civil day first (japanDateKey, date-boundary.ts:35); this path bypasses the +9 offset. Test fixture (conference-data-sync.test.ts:49) uses 00:00Z = 09:00 JST, the safe window, so no test locks the buggy behavior and no comment marks UTC intentional. Not a duplicate of F01-F78 (no finding touches this file/root cause). Caveat: only manifests for rare early-morning (<09:00 JST) conferences and produces a staff-reviewed proposal, so real-world impact is limited.

- [ ] **Codex cross-review**: (別視点の敵対検証結果 CONFIRM/REFUTE + 隣接拡張候補をここに追記)

## CE17 [15] perf-n+1 [flag] — Daily checkPrescriptionExpiry job scans the entire prescription-intake history every night (no lower date bound, no status filter)

- File: `src/server/jobs/daily/prescriptions.ts:240` (lane BE, finder be-perf)
- Scores: safety 2 / effect 3 / verify 5 / scope 2 / efficiency 3 = **15**
- Reachability: Invoked by the daily job orchestrator/cron (src/server/jobs/daily/\*). runJob('prescription_expiry_check', ...).
- Evidence: prescriptions.ts:240-251 `prisma.prescriptionIntake.findMany({ where: { prescription_expiry_date: { lte: tomorrow } }, include: { cycle: { include: { case_: true } } } })` — the only predicate is `expiry_date <= tomorrow`, i.e. EVERY prescription that has already expired at any point in history plus those expiring tomorrow, with no lower bound and no cycle/status filter, each row eager-loading cycle→case\_. Sibling jobs in the same file bound their windows: checkMedicationDeadlines (line 20-25) adds `schedule_status in [planned,in_preparation,ready]`; checkIntakeToVisitLinkage (line 103-121) uses `gte: today, lte: expiryWindow`; checkRefillPrescriptions (line 57-62) adds `refill_remaining_count > 0`. The intent per the docstring (line 233-235) is 「翌日期限切れ間近」 — only prescriptions expiring tomorrow.
- Failure scenario: In an org with years of prescriptions, the nightly daily-job run loads the full historical set of expired PrescriptionIntake rows (tens of thousands) with cycle+case joins, only to create dedupe-keyed notifications for the near-expiry subset. The scan cost grows unboundedly with data age; every night re-reads all historically-expired prescriptions that will never produce a new notification (dedupe_key `prescription-expiry:${id}` already exists).
- Suggested fix: Add a lower bound matching intent, e.g. `prescription_expiry_date: { gte: startOfToday, lte: tomorrow }` (and optionally restrict to active cycle statuses), so the nightly scan is bounded to actually-near-expiry prescriptions.
- Verifier (adversarial) reason: Re-read src/server/jobs/daily/prescriptions.ts:240-251: checkPrescriptionExpiry's findMany filters only on `prescription_expiry_date: { lte: tomorrow }` — no lower bound, no cycle/status filter — while eager-loading cycle→case\_. Confirmed unbounded: every PrescriptionIntake whose expiry date is at or before tomorrow (the entire historical set of already-expired prescriptions) is re-scanned every night, join cost growing without limit as data ages. The dedupe_key `prescription-expiry:${id}` (line 269) + skipDuplicates (verified daily.test.ts:741) means these historical rows never produce a new notification, so the scan is pure waste. Three siblings in the SAME file bound their windows on the same/analogous fields: checkIntakeToVisitLinkage uses `prescription_expiry_date: { gte: today, lte: expiryWindow }` (lines 115-118), checkMedicationDeadlines adds schedule_status IN filter (line 23), checkRefillPrescriptions adds refill_remaining_count > 0 (line 61) — strong evidence the missing lower bound here is an oversight. Reachable via orchestrator.ts:52 and jobs route map `daily-prescription-expiry` (route.ts:59). Tests mock prescriptionIntakeFindMany so the where-clause is not behavior-locked. NOT a duplicate: same unbounded-findMany class as F76/F77 but in a distinct not-yet-reported file (dedup rule: same class in a new file is novel); F64 is a TZ boundary, unrelated root cause. Classified flag because adding a `gte: today` lower bound would suppress notifications for already-expired prescriptions — a medical-safety semantic change requiring human confirmation, mirroring the FLAG treatment of F76/F77.</reason>
  <parameter name="correctedTitle">checkPrescriptionExpiry nightly job scans all historically-expired PrescriptionIntake rows (only `expiry_date <= tomorrow`, no lower bound / no cycle-status filter) with cycle+case joins, growing unboundedly with data age

- [ ] **Codex cross-review**: (別視点の敵対検証結果 CONFIRM/REFUTE + 隣接拡張候補をここに追記)

## CE18 [14] dead-code-dupe [fix] — trimStringOrUndefined body-normalizer has no SSOT: two independent exported copies in src/lib/validations plus verbatim inline redeclarations across 7+ write-path route handlers (drift-risk dead-code-dupe)

- File: `src/lib/validations/communication-request.ts:7` (lane X, finder dead-code-dupe)
- Scores: safety 2 / effect 2 / verify 4 / scope 3 / efficiency 3 = **14**
- Reachability: Each copy runs inside a POST/PATCH handler that normalizes untrusted JSON body fields before persistence: e.g. src/app/api/patient-self-reports/route.ts (patient self-report intake), src/app/api/care-reports/route.ts (care report create), src/app/api/files/complete/route.ts and presigned-upload (S3 upload metadata). All are reachable via the normal authenticated app UI.
- Evidence: The exact 6-line body `if (value === null || value === undefined) return undefined; if (typeof value !== 'string') return value; const trimmed = value.trim(); return trimmed.length > 0 ? trimmed : undefined;` is defined byte-identically in 7 non-test files: src/lib/validations/communication-request.ts:7 (exported), src/lib/validations/tracing-report.ts:6 (also exported — a second 'canonical' copy that re-declares instead of importing the first), src/app/api/care-reports/route.ts:29, src/app/api/admin/organizations/route.ts:15, src/app/api/patient-self-reports/route.ts:25, src/app/api/files/complete/route.ts:9, src/app/api/files/presigned-upload/route.ts:22. grep of the findings ledger for `trimStringOrUndefined` returns nothing, so this is not F01-F78. This is the same accepted dead-code-dupe class as F42 (evaluatePasswordStrength copied into 3 auth pages) and F35 (formatYen copies), but a larger cluster (7 copies) and notably the would-be SSOT location (src/lib/validations) itself holds two separate exported definitions — strong evidence the duplication is oversight, not intent (a sibling validator re-declared the helper instead of importing it).
- Failure scenario: All 7 copies are currently identical, so no wrong output today; the defect is drift-risk with a medical-data consequence path: request-body normalizers on live write endpoints (care-reports POST, admin/organizations, patient-self-reports POST, files/complete, files/presigned-upload). If one copy is later 'fixed' (e.g. to also strip a zero-width space, normalize NFKC, or treat whitespace-only differently) the other 6 endpoints keep the old normalization, so the same field (e.g. a patient self-report note or care-report body) is persisted with divergent trimming depending on which endpoint the client hits — exactly the drift failure that already materialized in F32 (readString trimmed in one copy, not the other) and F42.
- Suggested fix: Keep one exported trimStringOrUndefined (e.g. promote to src/lib/validations/index or a src/lib/utils/strings module), delete the other 6 definitions, and import from all 7 sites; behavior-preserving, verify with pnpm typecheck + each route's existing test suite.
- Verifier (adversarial) reason: Re-read and confirmed byte-identical exported `trimStringOrUndefined` at src/lib/validations/communication-request.ts:7 and src/lib/validations/tracing-report.ts:6 (neither imports the other), plus verbatim inline redeclarations at care-reports/route.ts:29, admin/organizations/route.ts:15, patient-self-reports/route.ts:25, files/complete/route.ts:9, files/presigned-upload/route.ts:22 — all POST/PATCH body normalizers reachable via the authenticated app. grep of ops/refactor/ULTRACODE_FINDINGS_20260702.md for trimStringOrUndefined/trimString returns zero hits; F32/F35/F41/F42/F72 cover different helpers/files, so NOVEL. No comment/doc/test locks per-file behavior; two exported siblings re-declaring instead of importing evidences oversight, matching the accepted inconsistency/dead-code-dupe class. Honest caveat: all copies are currently identical, so no wrong output today — harm is future drift on medical-data write paths (same profile as accepted F35).

- [ ] **Codex cross-review**: (別視点の敵対検証結果 CONFIRM/REFUTE + 隣接拡張候補をここに追記)

## CE19 [13] fe-be-contract [fix] — MentionInput accumulates mention ids and never removes them when the @mention text is deleted/edited, so posting notifies staff who are no longer mentioned

- File: `src/components/features/comments/mention-input.tsx:71` (lane FE, finder fe-shared-component)
- Scores: safety 2 / effect 3 / verify 3 / scope 2 / efficiency 3 = **13**
- Reachability: MentionInput is the comment composer used by src/components/features/comments/comment-thread.tsx (comments on visits/patients/etc.); reachable whenever a user edits or removes a mention before posting.
- Evidence: insertMention (lines 56-85) only ADDS to mentions: `if (!mentions.includes(staff.id)) onMentionsChange([...mentions, staff.id])` (lines 71-73). handleChange (lines 107-128) re-parses the '@' token to drive the dropdown but never reconciles the mentions[] array against the current text — there is no removal path anywhere. comment-thread.tsx sends this array verbatim to the comments API (`body: JSON.stringify({ ...content, mentions })`, lines 67-72) which drives mention notifications.
- Failure scenario: User types '@田中', selects the suggestion (mentions=['id-tanaka']), then backspaces to delete the whole '@田中 ' text before posting. mentions still contains 'id-tanaka', so on submit the comment notifies/ tags 田中 even though the final comment body no longer mentions them — a spurious notification and a mismatch between visible text and recorded mentions. Same occurs when a mention is replaced by editing.
- Suggested fix: Derive mentions from the current text on change/submit (re-scan for '@name' tokens and map to ids), or drop an id from mentions when its '@name' substring is no longer present in value.
- Verifier (adversarial) reason: Re-read mention-input.tsx:71-73 (append-only insertMention, no removal), handleChange lines 107-128 (parses @-token only for the dropdown, never reconciles mentions[]), and comment-thread.tsx:67-72/82 (sends mentions verbatim, resets to [] only on success — array is meant to reflect current draft). Confirmed downstream impact at src/app/api/comments/route.ts:218-238 where mentions become explicitUserIds for dispatchNotificationEvent (comment_mention notification). Server validation (route.ts:57-70) only checks ids are valid org members, not that they match the text, so it does not mask the bug. mention-input.test.tsx (47-63) tests only the fetch path; nothing locks accumulate-only behavior as intentional. No F-entry references mention/comment composition. Defect is real: deleting/replacing an @mention before posting still notifies and tags the stale staff id, mismatching visible text vs recorded mentions.

- [ ] **Codex cross-review**: (別視点の敵対検証結果 CONFIRM/REFUTE + 隣接拡張候補をここに追記)
