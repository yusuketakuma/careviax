# Ultracode Refactor Candidates — 2026-07-02 (Claude 10-lens scan)

ユーザー指示によるリファクタリング候補の全コードベーススキャン(バグ探索でなく構造改善)。
10レンズ × 敵対検証(独立にgrep/wc再実行して確認)。既知 F01-F89 / CE / N / X / CXR とdedup済み。
**read-only 台帳。実装はスライス化・maker/checker・objective gate 経由で。**

- raw 63 / confirmed 56 / refuted 6 / dup-of-known 4
- byKind: {"dup-helper": 13, "dead-code": 4, "oversized-split": 8, "type-drift": 7, "dup-component": 3, "dup-route-boilerplate": 2, "pattern-inconsistency": 8, "test-harness-dup": 7, "query-helper": 4}

スコア = payoff+safety+verify+scope+priority (各1-5、25満点)。effort S/M/L。

## ハイライト

- **R01 [22]**: DataTable client CSV export が safe-csv の formula-prefix 中和を欠く — 25画面のCSV出力がserver export(7 route全対応済)と非対称。**security隣接・S effort・最優先推奨**
- **R02 [21]**: body-parse+validationError 定型が **~230 route にコピー** → parseJsonBodyOrError 抽出で~1,000行削減
- **R03/R07/R08/R22 dead-code**: route-builder 死鎖(~280行)、home-config(358行)、Yjs collab 鎖(683+1,619行) 等
- **R04/R12/R29/R37/R38 肥大分割**: card-workspace(4,747行)、drug-master-content(4,056行) 等、既存 view-model 抽出前例に沿う
- **R23/R34**: messageFromError/getErrorMessage ternary が **~192コピー/76ファイル**
- **R31**: API route test の mock 前文 **~33,000行重複**

## R01 [22] dup-helper (effort S) — DataTable's client CSV export re-implements quotedCsvRow WITHOUT safe-csv's formula-prefix neutralization — 25 consumer screens export unneutralized CSV while all 7 server export routes are protected

- Anchor: `src/components/ui/data-table.tsx:113` (lens shared-helper-dupes)
- Files: `src/components/ui/data-table.tsx`, `src/lib/csv/safe-csv.ts`
- Scores: payoff 4 / safety 4 / verify 5 / scope 5 / priority 4 = **22**
- Evidence: src/lib/csv/safe-csv.ts:7 quotedCsvCell = neutralizeCsvFormulaPrefix (prefixes ' to cells starting with =+-@\t\r\n) + quote-doubling; it is used by all 7 server CSV export routes (patients/export, prescriptions/export, audit-logs/export, pharmacy-drug-stocks export+template, communication-requests/export, billing-candidates/export). src/components/ui/data-table.tsx:113 toCsvRow duplicates only the quote-doubling half: `values.map((value) => `"${value.replace(/"/g, '""')}"`).join(',')`, used at line 319 for the CSV出力 button rendered by 25 DataTable consumer files. Behavioral drift: a patient/drug name starting with '=' or '-' exports as a live formula from every DataTable screen but is neutralized in every server-side export of the same data. data-table.test.tsx only asserts the export button's enabled/disabled state (lines 87-127), not CSV content, so the drift is not test-locked.
- Proposal: Delete toCsvRow and import quotedCsvRow from '@/lib/csv/safe-csv' (pure, client-safe, no server deps) in data-table.tsx; add one data-table test asserting a leading-'=' cell is exported with the neutralizing apostrophe.
- Payoff: One CSV-serialization SSOT for client and server; closes the CSV-formula-injection asymmetry across 25 screens with a ~5-line diff.
- Risk: Exported CSV cells that start with =+-@ gain a leading apostrophe (that is the intended convergence); no test currently locks the old bytes. Verify safe-csv has no node-only imports (it has none — pure string functions).
- Verifier (adversarial): Re-verified all claims: (1) safe-csv.ts:3-15 quotedCsvCell/quotedCsvRow = formula-prefix neutralization (/^[=+\-@\t\r\n]/ → leading ') + quote-doubling, file has zero imports (pure, client-safe); (2) data-table.tsx:113-115 toCsvRow duplicates only the quote-doubling half verbatim, used at :319 in handleExport for the CSV出力 button; (3) re-ran grep: exactly 7 server export routes import safe-csv (5 via quotedCsvRow/Cell, patients/export and prescriptions/export via minimalCsvRow which also neutralizes — so 'all 7 protected' holds); (4) re-ran consumer count: 25 non-test files import from '@/components/ui/data-table'; (5) data-table.test.tsx lines 91-127 assert only button disabled/enabled state, no CSV content assertions — old bytes not test-locked, so convergence breaks nothing; (6) dedup grep for csv/toCsvRow/safe-csv in ULTRACODE_FINDINGS_20260702.md and ULTRACODE_EXPANSION_MASTER_TARGETS.md returned zero hits. quotedCsvRow accepts CsvCellValue[] ⊇ string[], drop-in signature. Only behavior delta is the intended one (leading apostrophe on =+-@ cells in client exports, matching server exports of the same data), not intentional decoupling.

- [ ] **Codex cross-review**:

## R02 [21] dup-helper (effort M) — Extract parseJsonBodyOrError(req, schema) — the read-body + null-check + safeParse + validationError block is copied ~214 times

- Anchor: `src/lib/api/request-body.ts:11` (lens be-route-boilerplate)
- Files: `src/lib/api/request-body.ts`, `src/lib/api/response.ts`, `src/app/api/comments/route.ts`
- Scores: payoff 5 / safety 4 / verify 5 / scope 3 / priority 4 = **21**
- Evidence: Exact copies counted by grep over src/app/api/\*\*/route.ts: 214 copies of `return validationError('リクエストボディが不正です');` immediately following readJsonObjectRequestBody (plus 9 following readOptionalJsonObjectRequestBody), and 223 copies of `validationError('入力値が不正です', parsed.error.flatten().fieldErrors)` (270 total flatten().fieldErrors lines; drift: 17 sites drop fieldErrors, 4 use ad-hoc detail objects). Canonical 6-line block at src/app/api/comments/route.ts:186-192. The abstraction was already attempted once — src/lib/api/route-builder.ts:48-54 contains this exact block — but it has zero route consumers (see separate dead-abstraction candidate), and no `parse body → response` helper exists in request-body.ts (22 lines, read-only helpers) or validation.ts.
- Proposal: Add to src/lib/api/request-body.ts: `async function parseJsonBodyOrError<T>(req, schema: ZodType<T>): Promise<{ ok: true; data: T } | { ok: false; response: NextResponse }>` producing exactly validationError('リクエストボディが不正です') / validationError('入力値が不正です', flatten().fieldErrors), plus an `Optional` variant for the 9 readOptionalJsonObjectRequestBody sites. Convert call sites mechanically (`const body = await parseJsonBodyOrError(req, schema); if (!body.ok) return body.response;`).
- Payoff: ~200 handlers shrink by 4-5 lines each (~900-1,000 lines deleted), the 17+4 drifted sites converge to one message/fieldErrors shape, and future body-validation changes (e.g. logging rejected payload field names) become a one-file edit instead of a 200-file sweep.
- Risk: Behavior-preserving byte-for-byte for the 214+223 exact copies; the 17 no-fieldErrors sites gain fieldErrors detail in their 400 response (strictly additive, but their route tests may assert the exact details payload — check per slice). Locked by each route's existing route.test.ts asserting 400 code/message on invalid body; comments/route.test.ts and ~250 siblings cover the paths.
- Verifier (adversarial): Re-ran all greps: 233 copies of validationError('リクエストボディが不正です') in src/app/api/\*\*/route.ts (grep -A2 shows 224 immediately follow `await readJsonObjectRequestBody` and 9 follow readOptionalJsonObjectRequestBody — adjacency claim holds; counts grew slightly from the candidate's 214+9). 223 exact copies of validationError('入力値が不正です', parsed.error.flatten().fieldErrors) out of 270 flatten().fieldErrors lines, and exactly 17 drifted validationError('入力値が不正です') no-fieldErrors sites — drift claim exact. 230 route files contain the block; canonical copy re-read at src/app/api/comments/route.ts:186-192. Re-read src/lib/api/request-body.ts (22 lines, read-only helpers, no parse→response helper exists) and src/lib/api/route-builder.ts:48-54, which contains this exact block but whose withValidatedBody has zero consumers outside its own test (grep confirmed only src/lib/api/**tests**/route-builder.test.ts) — a dead prior abstraction attempt, confirming the proposal fills a real gap. Test lock verified: 184 route.test.ts files assert these exact Japanese messages and comments/route.test.ts asserts 400 + VALIDATION_ERROR code. Dedup check: grepped ops/refactor/ULTRACODE_FINDINGS_20260702.md and ULTRACODE_EXPANSION_MASTER_TARGETS.md for route-builder/withValidatedBody/リクエストボディ/request-body — no hit; F32 is unrelated QR readString dupes, CE18 is trimStringOrUndefined. Distinct (file-set, root-cause). Only caveat (already stated in candidate): the 17 no-fieldErrors sites would gain additive fieldErrors detail; safest mechanical conversion leaves those 17 as-is or checks their tests per slice.

- [ ] **Codex cross-review**:

## R03 [21] dead-code (effort S) — Delete the dead route-builder.ts + deprecated auth/middleware.ts abstraction chain (0 production consumers, ~280 lines incl. tests)

- Anchor: `src/lib/api/route-builder.ts:42` (lens be-route-boilerplate)
- Files: `src/lib/api/route-builder.ts`, `src/lib/api/__tests__/route-builder.test.ts`, `src/lib/auth/middleware.ts`, `src/app/api/__tests__/workflow-prescription-to-report.test.ts`, `src/app/api/__tests__/workflow-full-cycle.test.ts`
- Scores: payoff 3 / safety 5 / verify 5 / scope 5 / priority 3 = **21**
- Evidence: src/lib/api/route-builder.ts (80 lines, exports withValidatedBody) has zero importers outside its own test: grep for 'route-builder|withValidatedBody' across src hits only src/lib/api/**tests**/route-builder.test.ts (153 lines). It is the ONLY production importer of src/lib/auth/middleware.ts (46 lines), whose withAuth is explicitly '@deprecated Prefer withAuthContext' (middleware.ts:25) — no route file imports withAuth (grep 'withAuth(' in src/app/api = 0; all 402 route matches are withAuthContext). The only other references are stale vi.mock('@/lib/auth/middleware') blocks at workflow-prescription-to-report.test.ts:130 and workflow-full-cycle.test.ts:224. Its sibling org-reference.ts is NOT dead (validateOrgReferences used directly by 26 route files) and must stay. Not covered by known dead-code findings F18/F25/F26/F40/F56 (all UI components/deps).
- Proposal: Delete route-builder.ts, its test, and middleware.ts; remove the two stale vi.mock('@/lib/auth/middleware') blocks. Keep org-reference.ts untouched. (Alternative if the loop prefers revival: rebase withValidatedBody onto withAuthContext — but the parseJsonBodyOrError candidate covers the same need with a narrower shape, so deletion is the cleaner disposition.)
- Payoff: ~280 lines of unreachable abstraction stop compiling/linting and stop misleading refactor sweeps — an agent adopting 'the body-validation wrapper' today would build on a @deprecated auth path that mutates the request object instead of the ALS/ctx pattern the rest of the repo uses.
- Risk: Pure deletion of unreferenced modules; pnpm typecheck:no-unused + pnpm test verify. Only touchpoint is removing two mock blocks from workflow tests (they mock a module the routes under test no longer import, so removal is inert).
- Verifier (adversarial): Independently re-verified every claim. (1) wc -l: route-builder.ts=80, **tests**/route-builder.test.ts=153, auth/middleware.ts=46 → 279 lines ≈ "~280". (2) grep 'route-builder|withValidatedBody' across src excluding the module+its test: zero hits (exit 1) — no production or test importer. (3) grep 'auth/middleware' across src: exactly 3 hits — route-builder.ts:3 (sole production importer) plus stale vi.mock blocks at workflow-prescription-to-report.test.ts:130 and workflow-full-cycle.test.ts:224; the mocked withAuthMock (defined in hoisted blocks, used only inside those vi.mock factories) is inert since no route under test imports the module. (4) Read middleware.ts: withAuth carries '@deprecated Prefer withAuthContext' at line 25 and mutates the request object (lines 36-41), unlike the repo's ALS/ctx pattern. (5) grep 'withAuth(' in src/app/api: only hit is api-conventions-static.test.ts:25, a convention test that asserts zero routes use withAuth( (expects []), so deletion aligns with an existing test-locked convention; withAuthContext has 590 matches (candidate said 402 — count differs but the 'all routes on withAuthContext' claim holds). (6) grep 'AuthenticatedRequest' elsewhere: all hits are local test helpers named createAuthenticatedRequest, not imports of the type. (7) Dedup: read F18 body in full (13 files, all UI/feature/lib components — soap wizards, error-boundary, pull-to-refresh; route-builder/middleware absent) and grepped both ops/refactor known-findings files for 'route-builder|withValidatedBody|middleware' — zero mentions. Pure deletion of a verified-unreferenced chain, gated by typecheck:no-unused + full test suite; only caveat is the fix must also remove the withAuthMock hoisted definitions (~3 lines/file), which is inert.

- [ ] **Codex cross-review**:

## R04 [21] oversized-split (effort M) — card-workspace.tsx: six self-contained QuickForm components (~1,690 lines) inline in a 4,747-line file whose sibling panels already live in separate files

- Anchor: `src/app/(dashboard)/patients/[id]/card-workspace.tsx:1582` (lens oversized-split)
- Files: `src/app/(dashboard)/patients/[id]/card-workspace.tsx`, `src/app/(dashboard)/patients/[id]/card-workspace.test.tsx`, `src/app/(dashboard)/patients/[id]/patient-contacts-panel.tsx`
- Scores: payoff 4 / safety 4 / verify 5 / scope 4 / priority 4 = **21**
- Evidence: File is 4,747 lines. In-file components: McsCheckLogQuickForm 1582, BillingPaymentProfileQuickForm 1815, BillingCollectionQuickForm 2103, PrescriptionDocumentQuickForm 2511, PrescriptionOriginalManagementQuickForm 2624, ConferenceNoteQuickForm 2904–3309 (~1,690 lines total), plus PatientProfilePanel 3319, PatientFoundationPanel 3396, PatientVisitPreparationPanel 3645, CardTodayPanel 3759 (~490 lines). All mutations live in CardWorkspace itself (useMutation only at 3885–4176 per grep), so the quick forms are props-only — verified no useQuery/useMutation between lines 1582–3309. Directory precedent: patient-contacts-panel.tsx, patient-documents-panel.tsx, patient-care-team-panel.tsx etc. are already separate files with their own tests.
- Proposal: Relocate the six QuickForm components + their FormInput types (1366–1560) to patients/[id]/card-quick-forms/ (one file per form or one card-quick-forms.tsx), and the four display panels to sibling files matching the existing patient-\*-panel.tsx convention. Keep parseConferenceParticipants / buildConferenceStructuredContent exported from the new conference-note module and re-export from card-workspace.tsx (card-workspace.test.tsx:69–70 imports them). CardWorkspace (3820–4747) keeps all hooks/mutations unchanged.
- Payoff: Main file drops ~2,200 lines to ~2,500; quick forms become individually testable (the 3,699-line test file can be split alongside); matches the directory's own established panel-per-file pattern.
- Risk: Pure file relocation with props unchanged — lowest-risk split in the set. Locked by card-workspace.test.tsx (3,699 lines) incl. direct unit tests of buildConferenceStructuredContent/parseConferenceParticipants (lines 2754–2857). memo() wrappers at 3802–3809 must move/stay consistent.
- Verifier (adversarial): Re-ran wc -l (4,747 / 3,699 lines exactly as claimed), re-grepped top-level components (all six QuickForms at lines 1582/1815/2103/2511/2624/2904–3308, panels 3319/3396/3645/3759, memo wrappers 3802–3809), and re-verified hook placement: lines 1582–3309 contain only useState — every useMutation is in CardWorkspace (3885–4176), so quick forms are props-only and relocation is behavior-preserving. Test lock confirmed (test file imports buildConferenceStructuredContent/parseConferenceParticipants at 69–70 with unit tests at 2754–2857; re-export preserves them; only external consumer is page.tsx:3). Directory precedent verified via ls (~15 sibling patient-\*-panel/card files with own tests). Dedup grep of ULTRACODE_FINDINGS_20260702.md and ULTRACODE_EXPANSION_MASTER_TARGETS.md: card-workspace mentions are all bug-class (false-empty isError, formatYen, dead header field), none a file-split — not a duplicate. No React Compiler conflict. Caveat: hot file on a shared refactor branch, coordinate landing per concurrent-edit discipline.

- [ ] **Codex cross-review**:

## R05 [21] dup-helper (effort S) — MHLW open-data importers duplicate an entire CSV helper layer (8 verbatim + 3 near-identical helpers) that belongs in drug-master-import/shared

- Anchor: `src/server/services/medical-institution-master-import.ts:82` (lens shared-helper-dupes)
- Files: `src/server/services/care-service-office-master-import.ts`, `src/server/services/medical-institution-master-import.ts`, `src/server/services/drug-master-import/shared.ts`
- Scores: payoff 3 / safety 5 / verify 5 / scope 5 / priority 3 = **21**
- Evidence: care-service-office-master-import.ts (395 lines) and medical-institution-master-import.ts (371 lines) both import normalizeCell/splitDelimitedLine/fetchBytes from drug-master-import/shared, yet each re-declares verbatim-identical private helpers: stripBom (care:99 / medical:74), normalizePhoneLike (care:103 / medical:82, byte-identical), csvRows (care:109 / medical:88), headerIndex (care:116 / medical:95), readCsvCell (care:120 / medical:103), normalizeOfficeCode==normalizeSourceCode (same body, different name; care:125 / medical:78), singleValueMap (care:268 / medical:257, byte-identical 9-liner), BYTES_PER_MIB (care:13 / medical:14). Near-identical with 1-token drift: normalizePrefectureCodes (care:224 / medical:202 — only the env-var name differs), uniqueByOfficeCode/uniqueBySourceCode (care:252 / medical:246 — same keep-record-with-address merge policy), buildUpdateData (care:282 / medical:267 — same diff-only-changed-fields pattern). Roughly 70-90 duplicated lines across the pair; a third MHLW importer would copy them again.
- Proposal: Extract the generic CSV/open-data helpers (stripBom, csvRows, headerIndex, readCsvCell, normalizePhoneLike, normalizeAsciiCode, singleValueMap, a parameterized normalizePrefectureCodes(envVarName)) into src/server/services/drug-master-import/shared.ts (or a sibling open-data-csv.ts next to it), and import from both services. Keep domain-specific parse/update logic in place.
- Payoff: ~70-90 duplicated lines collapse to one tested module; the phone/prefecture normalization policy for government open-data imports gets a single point of change (next MHLW source reuses instead of forking).
- Risk: Pure move of private pure functions; behavior locked by src/server/services/care-service-office-master-import.test.ts and src/server/services/medical-institution-master-import.test.ts which exercise parse + import end-to-end.
- Verifier (adversarial): Re-ran wc -l (care=395, medical=371, shared.ts=794) and re-read both helper blocks side by side. Verbatim-identical bodies confirmed at claimed lines: stripBom (care:99/medical:74), normalizePhoneLike (care:103/medical:82), csvRows (care:109/medical:88), headerIndex (care:116/medical:95), readCsvCell (care:120/medical:103), singleValueMap (care:268/medical:257), BYTES*PER_MIB (care:13/medical:14), and normalizeOfficeCode (care:125) == normalizeSourceCode (medical:78) with identical body `normalizeCell(value)?.replace(/[^\dA-Za-z*-]/g, '') ?? null`. Near-identical drift confirmed exactly as claimed: normalizePrefectureCodes differs only in env-var name (CARE*SERVICE_MASTER*... vs MEDICAL*INSTITUTION_MASTER*...), uniqueByOfficeCode/uniqueBySourceCode share the same keep-record-with-address merge policy (key composition and [...spread] vs Array.from differ trivially), buildUpdateData shares the diff-only-changed-fields pattern but with different field sets — correctly excluded from the extraction proposal. Both files already import normalizeCell/splitDelimitedLine/fetchBytes from drug-master-import/shared (care:11, medical:12), so the extraction target is the established idiom and shared.ts has no stripBom collision. Behavior is test-locked: care-service-office-master-import.test.ts (6 tests) and medical-institution-master-import.test.ts (5 tests) exercise URL resolution, CSV parse with row-skipping, diff-only updates, prefecture gating, and URL-policy rejection end-to-end. Dedup grep over ops/refactor/ULTRACODE_FINDINGS_20260702.md and ULTRACODE_EXPANSION_MASTER_TARGETS.md for care-service-office/medical-institution-master/stripBom/csvRows/headerIndex/normalizePhoneLike/open-data returned zero hits; distinct file-set and root-cause from F32 (readString) and F41 (facility-contacts route).

- [ ] **Codex cross-review**:

## R06 [21] type-drift (effort S) — CdsAlert declared byte-identically in server/cds/checker.ts and components/features/cds/alert-panel.tsx (client/server import wall workaround)

- Anchor: `src/components/features/cds/alert-panel.tsx:7` (lens type-contract-drift)
- Files: `src/server/cds/checker.ts`, `src/components/features/cds/alert-panel.tsx`, `src/app/api/dispense-results/route.ts`, `src/app/(dashboard)/visits/[id]/record/visit-record-form.tsx`
- Scores: payoff 3 / safety 5 / verify 5 / scope 5 / priority 3 = **21**
- Evidence: Two exported CdsAlert types with identical shape {type: string; severity: 'critical'|'warning'|'info'; message: string; details?: Record<string,unknown>}: server/cds/checker.ts:8-13 and components/features/cds/alert-panel.tsx:7-12. The dup exists because checker.ts imports prisma (line 2) so the client component cannot import it. Consumers split across the copies: api/cds/check/route.ts and api/dispense-results/route.ts use the server copy; visit-record-form.tsx:107 imports the panel copy; dispense-results/route.ts:129 additionally re-inlines the severity union a third time in countCdsAlertsBySeverity.
- Proposal: Move CdsAlert (and a CdsAlertSeverity union) to a new prisma-free src/types/cds.ts; checker.ts, alert-panel.tsx, and dispense-results/route.ts import it. Pure re-home, no field changes.
- Payoff: One canonical type for the clinical-safety alert payload crossing the CDS API; severity additions (e.g. a 4th level) become impossible to half-apply between checker and panel.
- Risk: None beyond import churn; behavior locked by src/server/cds/checker.test.ts, components/features/cds/alert-panel.test.tsx, api/cds/check/route.test.ts, api/dispense-results/route.test.ts.
- Verifier (adversarial): Independently re-read both declarations: src/server/cds/checker.ts:8-13 and src/components/features/cds/alert-panel.tsx:7-12 export byte-identical `CdsAlert` types ({type: string; severity: 'critical'|'warning'|'info'; message: string; details?: Record<string,unknown>}). Confirmed the import-wall root cause: checker.ts:2 imports `@/lib/db/client` (prisma), and alert-panel.tsx is 'use client', so the panel cannot import the server copy. Re-ran grep -rn CdsAlert: consumer split confirmed — visit-record-form.tsx:107 imports the panel copy and types the /api/cds/check response with it (lines 511, 525), while api/cds/check/route.ts:9 and api/dispense-results/route.ts:16 import checkDispenseAlerts from the server copy; so client/server payload sync currently relies on two hand-maintained copies. Third inline copy confirmed at dispense-results/route.ts:129 (countCdsAlertsBySeverity re-inlines the severity union). Bonus not in the candidate: safety-check.shared.ts:40-44 SafetyCdsAlert is a fourth near-copy (same shape minus details) typing the same /api/cds/check response. All four claimed lock tests exist (checker.test.ts, alert-panel.test.tsx, cds/check/route.test.ts, dispense-results/route.test.ts). Refactor is a pure type re-home to a prisma-free src/types/cds.ts — types erase at runtime, so it is trivially behavior-preserving; no React Compiler/hooks interaction. Dedup: grepped ops/refactor/ULTRACODE_FINDINGS_20260702.md and ULTRACODE_EXPANSION_MASTER_TARGETS.md — F11/F73/F81/F82 touch CDS but are bug/perf/coverage findings with different root causes; no existing finding covers the CdsAlert type duplication.

- [ ] **Codex cross-review**:

## R07 [21] dead-code (effort S) — src/lib/dashboard/home-config.ts: all 11 exports have zero references (358 lines)

- Anchor: `src/lib/dashboard/home-config.ts:13` (lens dead-code-round2)
- Files: `src/lib/dashboard/home-config.ts`, `src/lib/dashboard/home-config.test.ts`
- Scores: payoff 3 / safety 5 / verify 5 / scope 5 / priority 3 = **21**
- Evidence: Per-symbol grep across src+tools: DASHBOARD_PIPELINE_STEPS, DASHBOARD_TASK_TABS, DASHBOARD_TASK_TYPE_TO_TAB, DASHBOARD_TAB_FALLBACK_ACTIONS, DASHBOARD_WORKFLOW_LINKS, DASHBOARD_WORKBENCH_LINKS, DASHBOARD_COORDINATION_LINKS, DASHBOARD_ADMIN_LINKS + 3 types = 0 references each outside the file and its 95L test. The live dashboard uses dashboard-cockpit helpers (src/app/(dashboard)/dashboard/dashboard-cockpit.helpers.ts) instead.
- Proposal: Delete home-config.ts and home-config.test.ts (453 lines total). If any href-builder constants are still wanted, they already live in the navigation href-builder module home-config imports from.
- Payoff: 358 prod + 95 test lines removed; removes a stale second 'dashboard navigation/tab config' that silently drifts from the live cockpit config.
- Risk: Low: 0 refs for every export, proven per symbol. Test file is self-referential only.
- Verifier (adversarial): Independently re-ran wc -l (358+95=453 lines) and repo-wide greps: 'home-config' appears only in its own test import (home-config.test.ts:10); all 8 exported constants and 3 exported types have zero references outside the file+test (per-symbol grep over src+tools). No barrel re-export in src/lib/dashboard (only 4 files). home-link-builders.ts, which home-config imports, has 18 other consumers and survives deletion. Live dashboard uses dashboard-cockpit.helpers.ts as claimed. Dedup verified: 'home-config' has 0 hits in ULTRACODE_FINDINGS_20260702.md (checked full F18/F26 dead-code file lists) and ULTRACODE_EXPANSION_MASTER_TARGETS.md — different file-set from F18/F25/F26/F40/F56.

- [ ] **Codex cross-review**:

## R08 [21] dead-code (effort M) — Zero-importer lib/server module sweep: 6 modules, 563 prod + 592 test lines (drop network-status.tsx — already covered by F26)

- Anchor: `src/lib/api/route-builder.ts:1` (lens dead-code-round2)
- Files: `src/lib/api/route-builder.ts`, `src/lib/api/__tests__/route-builder.test.ts`, `src/server/services/health-check.ts`, `src/server/services/health-check.test.ts`, `src/server/services/care-trend.ts`, `src/server/services/care-trend.test.ts`, `src/lib/dispensing/workflow-order.ts`, `src/lib/navigation/recent-operations.ts` 他
- Scores: payoff 3 / safety 5 / verify 5 / scope 5 / priority 3 = **21**
- Evidence: Per-symbol greps, all zero non-test refs: route-builder.ts (80L+153L test) — withValidatedBody used by 0 routes; health-check.ts (104L+161L) — checkDatabase/checkS3/runHealthChecks unused, src/app/api/health/route.ts implements its own inline checks and imports backup-monitor instead; care-trend.ts (168L+155L) — grep 'services/care-trend' finds no non-test importer (its only conceptual consumer, issue-timeline.tsx, is already known-dead F-item; CareTrend types in src/types/visit-brief.ts are an independent definition); workflow-order.ts (64L+37L); recent-operations.ts (68L+41L, recordRecentOperation 0 refs); user-settings.ts (79L+45L, all 5 exports 0 refs incl. USER_NOTIFICATION_SETTINGS_STORAGE_KEY); network-status.tsx (30L, no test — app-shell uses layout/network-status-banner.tsx instead).
- Proposal: Delete all 7 modules + their 5 test files in one sweep commit. Exception decision for route-builder.ts: it is a candidate ADOPTION target for the known route-boilerplate epics — either adopt it there or delete it; keeping it unused is the worst option. health-check.ts deletion could optionally be inverted into refactoring /api/health/route.ts to reuse it, but the route's inline version is the one with production behavior.
- Payoff: ~1,185 lines removed across 12 files; kills two shadow implementations (health-check vs live /api/health, network-status vs live network-status-banner) that invite divergent edits.
- Risk: Low: every export proven 0-ref. user-settings.ts defines a localStorage key — grep confirms 'ph-os:user-notification-settings' string appears nowhere else, so no stored-data reader breaks.
- Verifier (adversarial): Independently re-ran wc -l and per-symbol greps. All prod line counts match exactly (80/104/168/64/68/79/30 = 593). Re-grepped every exported symbol across src/: getDispenseWorkflowPriorityWeight, compareDispenseWorkflowOrder, withValidatedBody, checkDatabase, checkS3, runHealthChecks, computeCareTrend, prependRecentOperation, parseRecentOperationsStorage, normalizeRecentOperations, RECENT_OPERATIONS_KEY, USER_NOTIFICATION_SETTINGS_STORAGE_KEY, DEFAULT_USER_NOTIFICATION_SETTINGS, normalizeUserNotificationSettings, parseUserNotificationSettingsStorage — all zero references outside their own module+test. The one apparent hit (checkS3 in backup-monitor.ts) is the distinct symbol checkS3Versioning defined locally at backup-monitor.ts:233, not an import. Confirmed src/app/api/health/route.ts imports runBackupMonitorChecks from backup-monitor (line 6) with no health-check import — the shadow-implementation claim holds. Confirmed app-shell.tsx:5 imports layout/network-status-banner, and raw localStorage strings 'ph-os:recent-operations'/'ph-os:user-notification-settings' appear nowhere outside the dead modules. No barrel re-exports in src/lib/api, src/server/services, src/lib/dispensing, src/lib/navigation, src/lib/notifications. Two evidence corrections: (1) network-status.tsx DOES have a test (network-status.test.tsx, 38L) — candidate said 'no test'; total test lines are 630 across 7 test files, not ~592 across 5. (2) DEDUP: network-status.tsx is already explicitly listed in known finding F26 (ops/refactor/ULTRACODE_FINDINGS_20260702.md:241 — 'offline/network-status.tsx (30L, superseded by layout/network-status-banner)'), so it must be dropped from this sweep. The remaining 6 modules (563 prod + 592 test lines) appear nowhere in ULTRACODE_FINDINGS_20260702.md or ULTRACODE_EXPANSION_MASTER_TARGETS.md (grepped all 6 names) — F18/F25/F26/F56 cover feature/UI components, this is a distinct lib/server file-set. Deletion is trivially behavior-preserving (zero importers) and mechanically verifiable via typecheck+build.

- [ ] **Codex cross-review**:

## R09 [20] dup-component (effort S) — Near-identical 31-line cockpit rail guard block (skeleton card + ErrorState + WorkspaceActionRail) copy-pasted across 3 workspaces (handoff/schedule byte-identical modulo testid; report-share differs only in copy strings + detail prop) — extract GuardedWorkspaceActionRail

- Anchor: `src/app/(dashboard)/handoff/handoff-workspace.tsx:1439` (lens fe-panel-boilerplate)
- Files: `src/app/(dashboard)/handoff/handoff-workspace.tsx`, `src/app/(dashboard)/reports/report-share-workspace.tsx`, `src/app/(dashboard)/schedules/schedule-team-board.tsx`, `src/components/features/workspace/action-rail.tsx`
- Scores: payoff 3 / safety 4 / verify 5 / scope 5 / priority 3 = **20**
- Evidence: handoff-workspace.tsx:1439-1469, schedule-team-board.tsx:1842-1872 and report-share-workspace.tsx:633-663 each contain a ~31-line block: `cockpitQuery.isLoading || cockpitQuery.isError ? (<div className="rounded-lg border border-border/70 bg-card p-4"> {isLoading ? <Skeleton h-5/h-10/h-20 stack role=status> : <ErrorState variant=server title="稼働状況を取得できませんでした"… action={再試行, refetch}>} </div>) : (<WorkspaceActionRail nextAction=… blockedReasons=… blockedReasonsEmptyLabel="止まっている作業はありません" …>)`. handoff and schedule-team-board copies are identical except data-testid (handoff-action-rail-loading vs schedule-action-rail-loading) and builder args; report-share differs only in aria-label/title copy and an extra detail prop. dashboard-cockpit.tsx:723 has a fourth partial variant. WorkspaceActionRail itself is already shared (src/components/features/workspace/action-rail.tsx, 11 consumer files), only the guard is copy-pasted.
- Proposal: Add a GuardedWorkspaceActionRail (or `railSlot` helper) next to WorkspaceActionRail in src/components/features/workspace/action-rail.tsx taking {isLoading, isError, onRetry, loadingTestId, loadingAriaLabel, errorTitle?, errorDescription?, errorDetail?, ...WorkspaceActionRailProps}; replace the 3 copies (93 lines → 3×~10 lines).
- Payoff: ~60 net lines removed; the recently-fixed fail-open cockpit rail bug class (ULTRACODE F-verifier notes cite handoff as 'the outlier' that missed this guard) becomes structurally impossible for the next workspace screen that adopts the rail.
- Risk: data-testid and aria-label strings are asserted in handoff-workspace.test.tsx / schedule-team-board.test.tsx / report-share-workspace.test.tsx (e.g. handoff-action-rail-loading) — must be passed as props, not hardcoded. These 3 files are hot on the current refactor branch; re-diff before landing per concurrent-edit discipline.
- Verifier (adversarial): Re-read all 3 blocks: handoff-workspace.tsx:1439-1469 and schedule-team-board.tsx:1842-1872 are identical modulo data-testid and builder args; report-share-workspace.tsx:632-663 differs only in aria-label/title/description copy plus a detail prop — all parameterizable. Re-ran grep: WorkspaceActionRail in 12 non-test files (already shared, only guard duplicated). Tests lock behavior: handoff-workspace.test.tsx:513/:527 and schedule-team-board.test.tsx:1104/:1125 assert loading testid + error title; report-share test locks rail rendering (:748-753) and slot order (:466-480) but NOT its loading/error branches (minor caveat). dashboard-cockpit.tsx:723+ re-read: genuinely different (CockpitSkeleton, !data gate, stale banner) — correctly excluded. Pure JSX extraction, no hooks, no React Compiler conflict. Not in the known-findings exclusion list (different file-set and root cause). Title's 'byte-identical across 3' is slightly overstated — corrected.

- [ ] **Codex cross-review**:

## R10 [20] dup-helper (effort M) — blocked_reasons→BlockedReason[] rail mapping duplicated 10× (not 6×) across 4 structurally-identical source types, buildWorkspaceNextAction ~35-line near-copy 2×, and formatAgeLabel alias 10×, despite designated shared home src/lib/workspace/daily-ops-rail.ts having exactly 1 consumer

- Anchor: `src/app/(dashboard)/handoff/handoff-workspace.helpers.ts:316` (lens fe-panel-boilerplate)
- Files: `src/lib/workspace/daily-ops-rail.ts`, `src/app/(dashboard)/handoff/handoff-workspace.helpers.ts`, `src/app/(dashboard)/reports/report-share-workspace.helpers.ts`, `src/app/(dashboard)/visits/visits-today.tsx`, `src/app/(dashboard)/schedules/schedule-team-board.tsx`, `src/app/(dashboard)/settings/operational-policy-content.tsx`
- Scores: payoff 4 / safety 4 / verify 5 / scope 4 / priority 3 = **20**
- Evidence: The 9-line mapping `(cockpit?.blocked_reasons ?? []).map(r => ({id,label,severity,categoryLabel: r.category ?? undefined, ageLabel: formatAgeLabel(r.age_minutes), actionLabel, actionHref}))` exists near-verbatim in 6 places: daily-ops-rail.ts:80 (buildDailyOpsBlockedReasons — the shared lib, consumed only by intake-triage-content.tsx:28), handoff-workspace.helpers.ts:316, report-share-workspace.helpers.ts:109, visits-today.tsx:262 (drift: `category` without `?? undefined`), schedule-team-board.tsx:1818, operational-policy-content.tsx:559. buildWorkspaceNextAction is a ~35-line near-copy in handoff-workspace.helpers.ts:281-313 vs report-share-workspace.helpers.ts:72-107 (drift: report-share adds timeIsoToString and buildScheduleFocusHref(firstVisit.id) while handoff links static '/schedules'), both re-deriving what daily-ops-rail.ts:57 buildDailyOpsNextAction parameterizes via a fallback arg. Each copy also re-aliases formatAgeLabel = formatElapsedLabel (handoff.helpers:176, dashboard-cockpit.helpers.ts:54, daily-ops-rail.ts:18).
- Proposal: Make daily-ops-rail.ts the single source: replace the 5 stray blocked-reasons mappings with buildDailyOpsBlockedReasons, and fold the two buildWorkspaceNextAction copies into buildDailyOpsNextAction by extending its options (visit-time description lookup + per-screen fallback/href), keeping each screen's current strings as passed-in options.
- Payoff: ~90 duplicated lines deleted; the audit-queue/narcotics next-action wording and blocked-reason rendering rules (a compliance-adjacent surface) get one implementation, ending the drift where handoff and report-share already render different hrefs for the same cockpit state.
- Risk: NextAction copies have real behavioral drift (schedule-focus href, visit-time formatting) that must be preserved via options, not silently unified — verify against handoff-workspace.helpers/report-share tests; blocked-reasons dedup is pure. handoff-workspace.helpers.ts and report-share-workspace.helpers.ts have dedicated test files locking current output.
- Verifier (adversarial): Independently re-verified every claim by re-reading the copies and re-running greps. (1) The 9-line blocked_reasons→BlockedReason[] field-by-field mapping is verbatim-confirmed in all 6 claimed locations: daily-ops-rail.ts:83-91 (shared lib buildDailyOpsBlockedReasons, consumed ONLY by intake-triage-content.tsx:28/488 per repo-wide grep), handoff-workspace.helpers.ts:319-327, report-share-workspace.helpers.ts:112-120, visits-today.tsx:262-270, schedule-team-board.tsx:1818-1826, operational-policy-content.tsx:559-567. The candidate actually UNDERCOUNTS: the identical mapping also exists at dashboard-cockpit.tsx:650-658, patients-board.tsx:679-687, billing-check-content.tsx:373-381, master-hub-content.tsx:157-165 — 10 copies total (~90 lines). (2) buildWorkspaceNextAction near-copy confirmed: handoff-workspace.helpers.ts:281-313 vs report-share-workspace.helpers.ts:72-106 are line-for-line identical except the stated drift (report-share:82 uses timeIsoToString and :98 buildScheduleFocusHref(firstVisit.id); handoff:305/311 hardcodes '/schedules'), and daily-ops-rail.ts:57 buildDailyOpsNextAction covers only the audit+fallback branches, so folding needs the proposed options extension. (3) formatAgeLabel=formatElapsedLabel re-alias grep returns 9 copies + daily-ops-rail.ts:18 (candidate cited 3). (4) Behavior lock: daily-ops-rail.test.ts (buildDailyOpsNextAction/BlockedReasons describes at :71/:114), handoff-workspace.test.tsx and report-share-workspace.test.tsx assert the next-action/blocked strings (grep hits 4 and 5) — component-level tests, not the claimed 'dedicated helper test files', a minor inaccuracy. (5) One proposal correction (not a refutation): visits-today/patients-board/billing/master-hub map DIFFERENT source types (VisitPrepBlockedReason/PatientBoardBlockedReason/TodayOpsBlockedReason, category: string non-null) rather than CockpitBlockedReason (category: string|null) — so the visits-today ` category` without `?? undefined` is a type difference, not behavioral drift, and the unified helper must accept a structural reason-array param ({category: string|null,...}[]) instead of DashboardCockpitResponse; all four source types are otherwise field-identical (verified side by side). Not intentional decoupling: daily-ops-rail.ts:9-14 doc comment explicitly designates it the cross-screen shared builder, and it's plain functions (no hooks), so no React Compiler conflict. Dedup check: F14/F27 are the (already-fixed, abde9163) cockpit fail-open BUG findings on the same files — different root cause (error handling, not duplication); no F/CE/N/X item covers this dup-structure.

- [ ] **Codex cross-review**:

## R11 [20] dup-route-boilerplate (effort L) — Adopt withAuthContext (documented @preferred) across ~174 manual route files, folding sensitive no-store + catch-all envelope into wrapper options (also fixes 35 withOrgContext routes lacking request context; anchor example: patients/[id]/visit-constraints/route.ts:99, not visit-schedules reopen)

- Anchor: `src/app/api/comments/route.ts:156` (lens be-route-boilerplate)
- Files: `src/lib/auth/context.ts`, `src/lib/api/sensitive-response.ts`, `src/app/api/comments/route.ts`, `src/app/api/dispense-results/route.ts`, `src/app/api/billing-evidence/check/route.ts`, `src/app/api/visit-schedules/[id]/reopen/route.ts`
- Scores: payoff 5 / safety 4 / verify 4 / scope 3 / priority 4 = **20**
- Evidence: withAuthContext (src/lib/auth/context.ts:324, JSDoc '@preferred Standard wrapper for new and modified Route Handlers') is used by 123 route files, but 173 route files still hand-roll its exact inline expansion: 211 copies of `if ('response' in authResult) return authResult.response` after requireAuthContext(req (247 call sites), 84 copies of `return runWithRequestAuthContext(ctx, ...)`, 164 manual withRoutePerformance wraps in 69 files, and 511 `unstable_rethrow(err)` catch blocks in 219 files (~437 outside wrapped files) that duplicate the wrapper's built-in catch (context.ts:337-352) differing only in the logger event string (e.g. 'dispense_results_post_unhandled_error', dispense-results/route.ts:1073-1081 vs 'comments_get_unhandled_error', comments/route.ts:156-174, a 19-line export-level envelope). On top of that, withSensitiveNoStore is applied manually at 1,193 call sites in 254 files — 293 copies of `return withSensitiveNoStore(await inner(req))` and 295 of `withSensitiveNoStore(internalError())`. Drift between copies is real: of the 173 manual files, only 68 wrap withRoutePerformance and only 66 establish ALS request context; 107 never call runWithRequestAuthContext, and 33 of them call withOrgContext with neither ALS nor an explicit requestContext option (e.g. visit-schedules/[id]/reopen/route.ts:95), so src/lib/db/rls.ts:70-78 fires a `rls_context_missing` security event and the RLS session loses actor/ip metadata on every transaction in those routes.
- Proposal: Extend withAuthContext options with `sensitive?: boolean` (apply SENSITIVE_NO_STORE_HEADERS to every returned response, success and error, matching current outer-wrapper behavior) and `errorEvent?: string` (default 'route_handler_unhandled_error') so the 109 test assertions on per-route event names keep passing unchanged. Then migrate manual routes in per-route slices: `export const GET = withAuthContext(handler, { permission, message, sensitive: true, errorEvent: 'comments_get_unhandled_error' })`, deleting the requireAuthContext triple, the try/catch/logger epilogue, the withRoutePerformance wrap, and the per-response withSensitiveNoStore calls.
- Payoff: Removes roughly 4,000-5,000 lines of copied envelope (293 exports x ~16 lines + 211 x ~8-line prologues), makes withRoutePerformance and PHI-safe 500 handling uniform, and structurally fixes the 33 routes currently running withOrgContext transactions without request context (kills rls_context_missing log noise and restores actor metadata in RLS settings/audit triggers) — the same class of gap in one sweep instead of route-by-route patches.
- Risk: Behavior deltas per route: no-store headers on responses that today lack them if `sensitive` is defaulted wrong (keep it opt-in per current wrapping), logger event names (109 test assertions like comments/route.test.ts:286 lock them — preserved via errorEvent), and the ALS/requestContext restoration changes what withOrgContext logs. Locked by 252 route test files asserting `Cache-Control: private, no-store, max-age=0` plus per-route 500-path tests (e.g. comments/route.test.ts:272,623); migrate incrementally, one route file per commit, gates green each slice.
- Verifier (adversarial): Re-ran every grep: withAuthContext @preferred JSDoc + built-in catch confirmed at src/lib/auth/context.ts:313-356; 168 route files wrapped vs 174 calling requireAuthContext manually (363 total); 211 exact 'response in authResult' copies; 247 requireAuthContext(req sites; 86 return runWithRequestAuthContext; withRoutePerformance 164 mentions/69 files; unstable_rethrow 513 mentions (277 call sites/207 files); withSensitiveNoStore 1,199 mentions/941 call sites/253 files incl. 300 (await...) + 295 (internalError...). Re-read comments/route.ts:156-174 — byte-level copy of the wrapper's catch differing only in event string. RLS gap re-verified as a class: ALS set only in context.ts:337/middleware.ts:43, and 35 route files call withOrgContext with no ALS/requestContext (e.g. patients/[id]/visit-constraints/route.ts:99), triggering rls_context_missing per rls.ts:65-78. Test locks confirmed: 244 test files assert private,no-store; 110 \_unhandled_error event assertions; comments/route.test.ts:286 exact. Two corrections: the candidate's 511/1,193 figures are mention-counts not call sites (277/941 actual), and the cited example visit-schedules/[id]/reopen/route.ts:95 is wrong — it DOES pass {requestContext: ctx} at line 146; use patients/[id]/visit-constraints/route.ts:99 instead. Dedup check: no withAuthContext-adoption item in ULTRACODE_FINDINGS (F01-F89) or EXPANSION_MASTER_TARGETS; EPIC 7 X11-X13 are per-route missing-no-store bugs, different root cause.

- [ ] **Codex cross-review**:

## R12 [20] oversized-split (effort M) — pharmacy-cooperation-workflow-content.tsx: 8 props-only panel components (~1,490 lines, zero hooks) inline in a 3,585-line file — panel-per-file relocation plus extraction of ~10 shared pure helpers/label maps

- Anchor: `src/app/(dashboard)/workflow/pharmacy-cooperation/pharmacy-cooperation-workflow-content.tsx:1113` (lens oversized-split)
- Files: `src/app/(dashboard)/workflow/pharmacy-cooperation/pharmacy-cooperation-workflow-content.tsx`, `src/app/(dashboard)/workflow/pharmacy-cooperation/pharmacy-cooperation-workflow-content.test.tsx`
- Scores: payoff 4 / safety 4 / verify 5 / scope 4 / priority 3 = **20**
- Evidence: File is 3,585 lines (3rd largest in src/). In-file components verified by grep: ShareCasesTable 1113, PatientShareConsentsPanel 1368, VisitRequestsTable 1620, VisitRequestCreatePanel 1751, PartnerVisitRecordDraftPanel 1927, PartnerVisitRecordsTable 2099, CorrectionRequestsPanel 2252, MessageThreadsPanel 2447 (~1,490 lines), plus shared micro-primitives TinyMeta/FieldLabel/NativeSelect/SectionShell/TableFrame/QueryFallback at 1009–1112. Root component PharmacyCooperationWorkflowContent is 2603–3585. Directory has only 3 files (content, test, page).
- Proposal: Move each panel to workflow/pharmacy-cooperation/panels/<name>.tsx and the shared primitives (SectionShell/TableFrame/QueryFallback/NativeSelect/TinyMeta/FieldLabel, 1009–1112) to a shared section-primitives.tsx. Root component and its hooks stay put. QueryFallback is a generic loading/error frame worth checking against the repo's ErrorState/DataTable errorMessage idiom during the move (adopt, don't fork).
- Payoff: Main file drops from 3,585 to ~2,100 lines; each partner-workflow panel (share cases, visit requests, correction requests, message threads) becomes independently reviewable; shared primitives become reusable by the admin pharmacy-cooperation setup screen (1,669 lines, same domain).
- Risk: Props-only relocation. Locked by pharmacy-cooperation-workflow-content.test.tsx (1,894 lines). Verify none of the panels closes over module-level mutable state before moving (they take props per signatures).
- Verifier (adversarial): Re-ran wc -l (3,585 content / 1,894 test; 3rd largest non-test file in src/ behind 5,112 and 4,747), re-grep'd all 15 component definition lines — every claimed line number matches exactly (TinyMeta 1009 ... MessageThreadsPanel 2447, root export at 2603). Independently verified the candidate's own risk condition: awk over lines 1113-2602 shows ZERO useState/useQuery/useMutation in the panel region (all 39 hook sites are in the root), panels take props-only signatures, and no module-level mutable state exists (only types, zod schemas, const maps, pure functions). Tests render only the root component, so file relocation is invisible to the 1,894-line suite. Dedup check: only F35 (formatYen copy at line 700) touches this file — different root cause; no oversized-split epic covers it in the master targets. One correction: relocation is not purely props-only — panels reference ~10 module-level pure helpers (statusLabel x11, formatDateTime x5, statusVariant x4, etc.) that must be exported or extracted to a shared module; still mechanical/behavior-preserving. The QueryFallback→ErrorState idiom adoption should be split out as a separate behavior-affecting step.

- [ ] **Codex cross-review**:

## R13 [20] oversized-split (effort M) — billing-evidence/core.ts (2,297L): move candidate-workbench trio (lines 1964-2297, ~334L) to its own submodule behind the existing 4-line barrel (17 non-test consumers unaffected); upsertBillingEvidenceForVisit is a single ~785-line function (1179-1963) for a later gated slice

- Anchor: `src/server/services/billing-evidence/core.ts:1964` (lens oversized-split)
- Files: `src/server/services/billing-evidence/core.ts`, `src/server/services/billing-evidence/index.ts`, `src/server/services/billing-evidence/core.test.ts`
- Scores: payoff 4 / safety 4 / verify 5 / scope 4 / priority 3 = **20**
- Evidence: core.ts is 2,297 lines while sibling submodules are 121–424 lines (batch.ts 357, candidate-regeneration.ts 121, duplicate-interaction.ts 328, information-provision.ts 424). index.ts is a 4-line `export *` barrel and 21 non-test files import via the barrel, so intra-package moves are invisible to consumers. Seam A: getBillingCandidateWorkbenchSummary (1980), reviewBillingCandidate (2081), closeBillingCandidatesForMonth (2186) + their Tx types (1964, 2165) = lines 1964–2297, a coherent candidate-review-workflow cluster distinct from evidence computation. Seam B: upsertBillingEvidenceForVisit spans 1179–1963 (~785 lines, one function). core.test.ts is 2,074 lines.
- Proposal: Step 1 (S, mechanical): move lines 1964–2297 to billing-evidence/candidate-workbench.ts, re-export via index.ts — zero consumer changes. Step 2 (L, separate slice): decompose upsertBillingEvidenceForVisit into named phase functions (snapshot loading, blocker evaluation via the existing blockerDefinition/listBlockerKeys helpers at 833/915, rule-key calculation, candidate write) within an upsert-evidence.ts submodule, matching the sibling-submodule precedent.
- Payoff: core.ts drops below ~1,970 immediately and toward ~1,200 after step 2; candidate review workflow gets its own test file split from the 2,074-line core.test.ts; the billing hot path becomes navigable.
- Risk: Step 1 is behavior-preserving by construction (barrel re-export), locked by core.test.ts + candidate-regeneration.test.ts. Step 2 touches billing math — sequence AFTER pending F60/F61 (server-local week/cutoff fixes) land, since both edit the same date-window code in this file.
- Verifier (adversarial): Re-ran wc -l: core.ts=2297, siblings batch.ts=357/candidate-regeneration.ts=121/duplicate-interaction.ts=328/information-provision.ts=424, core.test.ts=2074, index.ts=4-line `export *` barrel — all exact. Re-grepped function boundaries: upsertBillingEvidenceForVisit starts 1179 and its single top-level closing brace is at 1963 (awk over 1179-1963 shows exactly one `^}`), so one ~785-line function confirmed. Trio anchors exact: BillingCandidateWorkbenchSummaryTx:1964, getBillingCandidateWorkbenchSummary:1980, reviewBillingCandidate:2081, CloseBillingCandidatesTx:2165, closeBillingCandidatesForMonth:2186 (334 lines to EOF). Read the full trio: it is a self-contained candidate-review-workflow cluster whose only core dependencies (startOfMonth:256, read/writeBillingCandidateWorkflowState:546/568, monthLabel:829) are already exported and nothing else in core.ts calls the trio, so the move is one-directional with no circular import. Helper anchors blockerDefinition:833/listBlockerKeys:915 exact. Consumer count is the one correction: 17 non-test files import via the barrel (candidate said 21), zero import './core' directly, so intra-package moves remain invisible. Move is test-locked at the barrel: src/server/services/billing-evidence.test.ts (1769 lines) imports closeBillingCandidatesForMonth/getBillingCandidateWorkbenchSummary/upsertBillingEvidenceForVisit from './billing-evidence', and core.test.ts covers reviewBillingCandidate+upsert; route tests mock the barrel module so they are unaffected. Dedup: ops/refactor/ULTRACODE_FINDINGS_20260702.md mentions this file only in F60/F61 (server-local week/cutoff BUG fixes, correctly cited as a sequencing constraint, different root-cause class); ULTRACODE_EXPANSION_MASTER_TARGETS.md has zero billing-evidence/split hits. Step 2 (decomposing the 785-line upsert) is genuinely riskier billing math, but the candidate honestly scopes it as a separate L slice gated behind F60/F61 — the confirmable core is step 1.

- [ ] **Codex cross-review**:

## R14 [20] oversized-split (effort M) — cds/checker.ts: 12 independent check\* functions plus clinical-JSON parsers behind a single-function facade — split into check-family submodules

- Anchor: `src/server/cds/checker.ts:1646` (lens oversized-split)
- Files: `src/server/cds/checker.ts`, `src/server/cds/checker.test.ts`, `src/app/api/cds/check/route.ts`, `src/app/api/dispense-results/route.ts`
- Scores: payoff 4 / safety 4 / verify 5 / scope 4 / priority 3 = **20**
- Evidence: checker.ts is 1,752 lines and is the only module in src/server/cds/. Exactly 2 production consumers, both importing only checkDispenseAlerts (api/cds/check/route.ts:9, api/dispense-results/route.ts:16) — a clean facade. Internal seams: clinical-JSON readers 224–370 (readPackageInsertTextEntries, readRenalDoseEntries, readAlertRuleStringArray, matchesAlertRuleCondition); managed-rule scaffolding 371–520 (MANAGED_ALERT_TYPES, DO_PRESCRIPTION_RISK_PROFILES, scopedDrugAlertRuleWhere, resolveManagedAlertTypeStates); 12 check functions: checkInteractions 521, checkDuplicates 607, checkMaxDays 653, checkPackageInsertAudit 708, checkTransitionalExpiry 815, checkAllergyReactions 883, checkNarcoticFlags 1024, checkHighRiskDrugs 1081, checkDoPrescriptionRisk 1200, checkElderlyPIM 1321, checkRenalDoseAdjustment 1402, checkMonitoringAlerts 1504; orchestrator checkDispenseAlerts 1646–1752.
- Proposal: Split into src/server/cds/: clinical-json-readers.ts (224–370), alert-rules.ts (371–520), checks/interaction-duplication.ts (521–707), checks/package-insert.ts (708–882: audit + transitional expiry), checks/patient-risk.ts (883–1485: allergy/narcotic/high-risk/do-prescription/PIM/renal), checks/monitoring.ts (1486–1645). checker.ts keeps CdsAlert type + checkDispenseAlerts facade so both API consumers are untouched; checker.test.ts (1,215 lines) splits along the same families.
- Payoff: The medical-safety hot file becomes 6 focused ~150–400-line modules; the pending EPIC 2 safety work (per-check changes) gets isolated diffs per check family instead of every change churning one 1,752-line file.
- Risk: Behavior-preserving module moves locked by checker.test.ts (1,215 lines). SEQUENCING: F73 (lab-history query), F81/F82 (EPIC 2 false-negative fixes) all edit this file — land those first, then split, to avoid rebasing safety fixes across a file reshuffle. Medical-safety domain: keep the facade signature byte-identical.
- Verifier (adversarial): Re-verified all claims: wc -l confirms checker.ts=1752, checker.test.ts=1215; ls src/server/cds/ shows only these 2 files. Grep confirms exactly 2 production consumers, both importing only checkDispenseAlerts (api/cds/check/route.ts:9, api/dispense-results/route.ts:16) — clean facade. All 12 check\* anchors match exactly (checkInteractions 521, checkDuplicates 607, checkMaxDays 653, checkPackageInsertAudit 708, checkTransitionalExpiry 815, checkAllergyReactions 883, checkNarcoticFlags 1024, checkHighRiskDrugs 1081, checkDoPrescriptionRisk 1200, checkElderlyPIM 1321, checkRenalDoseAdjustment 1402, checkMonitoringAlerts 1504, facade checkDispenseAlerts 1646). Clinical-JSON readers (readNonEmptyText 224 … matchesAlertRuleCondition 344) and managed-rule scaffolding (MANAGED_ALERT_TYPES 381 … resolveManagedAlertTypeStates 447) verified; minor imprecision only: lines 471-520 are do-prescription comparison helpers (buildComparableLineKey/isSamePrescriptionContent/findMatchingPreviousLine/resolveDoPrescriptionRiskProfile), not rule scaffolding, and lines 8-223 (CdsAlert type, allergy YJ-prefix helpers, resolvePrescriptionLineDrugIdentitiesForCds) are unplaced in the proposal — plan details, not refutations. Candidate is actually SAFER than stated: checker.test.ts imports ONLY checkDispenseAlerts (line 66, single top-level describe at line 68, 22 call sites) — the entire 1215-line suite is facade-level, so it locks the split with ZERO test changes needed (the proposed test split is optional, not required). Dedup check: grepped ULTRACODE_FINDINGS_20260702.md and ULTRACODE_EXPANSION_MASTER_TARGETS.md for checker/cds/split/oversized — F73/F81/F82/X02/X03/CXR1-MSR01/MSR02 are all bug/flag fixes INSIDE checker.ts, none proposes a module split; different root-cause, not a duplicate. Sequencing caveat (land F73/F81/F82 EPIC 2 fixes first, then split) is correctly stated in the candidate and is real: EPIC 2 table confirms those edits target this file.

- [ ] **Codex cross-review**:

## R15 [20] pattern-inconsistency (effort M) — buildOrgHeaders adoption stalled halfway: 217 raw `'x-org-id':` literal sites in 74 files still bypass the fail-closed helper built to replace them

- Anchor: `src/lib/api/org-headers.ts:27` (lens shared-helper-dupes)
- Files: `src/lib/api/org-headers.ts`, `src/app/(dashboard)/patients/[id]/edit/patient-edit-content.tsx`, `src/app/(dashboard)/patients/[id]/consent/consent-records-content.tsx`, `src/app/(dashboard)/patients/patients-board.tsx`, `src/app/(dashboard)/patients/[id]/medications/print/page.tsx`
- Scores: payoff 4 / safety 4 / verify 5 / scope 4 / priority 3 = **20**
- Evidence: src/lib/api/org-headers.ts exists explicitly to unify this (its doc comment says 縦スライス内で重複している `headers: { 'x-org-id': orgId, ... }` を統一する) and enforces a fail-closed no-override contract (RangeError on casing collisions). Adoption: ~70 files call buildOrgHeaders/buildOrgJsonHeaders, but 217 raw `'x-org-id':` literal sites remain across 74 non-test files (grep counts). Drift the helper was designed to prevent already exists in the raw sites: patient-edit-content.tsx:166 sends `'x-org-id': orgId ?? ''` (empty tenant header instead of failing closed); consent-records-content.tsx alone hand-writes the Content-Type+x-org-id pair 7 times (lines 261,287,346,585,679,...).
- Proposal: Finish the migration: mechanical sweep replacing `headers: { 'x-org-id': orgId }` → buildOrgHeaders(orgId) and `{ 'Content-Type': 'application/json', 'x-org-id': orgId }` → buildOrgJsonHeaders(orgId), handling the few `orgId ?? ''` sites with an explicit early-return/guard instead of empty-string tenant scope. Then grep-gate: zero raw 'x-org-id' literals outside org-headers.ts and its test.
- Payoff: Tenant-boundary header construction becomes single-sourced and fail-closed everywhere (compliance-relevant: x-org-id is the RLS org scope carrier); 217 sites shrink and future casing/override mistakes throw instead of silently weakening scope.
- Risk: Nearly pure textual substitution; org-headers.test.ts locks helper semantics and each screen's content tests exercise the fetches. The only semantic decision points are the `orgId ?? ''` style sites (behavior today: sends empty header) — convert those deliberately, not blindly.
- Verifier (adversarial): Independently re-ran all greps: 217 raw 'x-org-id': sites in 74 non-test files (3 of the 217 lines are org-headers.ts itself); 82 non-test files already use buildOrgHeaders/buildOrgJsonHeaders. Re-read org-headers.ts lines 1-46: doc comment explicitly states the unification intent and the RangeError fail-closed contract is implemented; anchor line 27 correct. Re-verified patient-edit-content.tsx:166 sends 'x-org-id': orgId ?? '' (only such site) and consent-records-content.tsx hand-writes the Content-Type+x-org-id pair at 261/287/329/346/585/679/781 with zero helper usage. Shape census with filenames preserved: 100 simple sites + 17 JSON pairs + 63 bare multiline keys are mechanical swaps; only ~8 conditional sites plus the ?? '' site need deliberate handling, which the proposal flags. Behavior is test-locked (org-headers.test.ts, per-screen content tests incl. patient-edit-content.fetch.test.tsx, consent-records-content.test.tsx) and the end state is grep-gateable. Dedup check: no x-org-id/org-headers entries in ULTRACODE_FINDINGS_20260702.md or EXPANSION_MASTER_TARGETS.md; distinct file-set/root-cause from all excluded findings. Minor correction: helper adoption is 82 files (candidate said ~70), which does not weaken the claim.

- [ ] **Codex cross-review**:

## R16 [20] dup-helper (effort M) — JST Tokyo-date derivation via Intl formatter re-implemented 4 times (2 key-string producers, 2 parts producers) plus a verbatim UTC-key clone of formatUtcDateKey, with drift in locale and invalid-date handling vs canonical japanDateKey

- Anchor: `src/lib/utils/date-boundary.ts:35` (lens shared-helper-dupes)
- Files: `src/lib/utils/date-boundary.ts`, `src/lib/date-key.ts`, `src/server/services/patient-home-operations.ts`, `src/server/services/patient-detail-timeline-events.ts`, `src/lib/prescription/prescription-date-window.ts`, `src/server/services/patient-mcs.ts`, `src/server/services/conference-data-sync.ts`
- Scores: payoff 4 / safety 4 / verify 5 / scope 4 / priority 3 = **20**
- Evidence: Canonical: japanDateKey (src/lib/utils/date-boundary.ts:35, Intl Asia/Tokyo formatToParts, throws RangeError on missing parts). Re-implementations: (1) patient-home-operations.ts:39-56 TOKYO_DATE_FORMATTER + formatTokyoDateKey (en-US formatter; missing parts would yield 'undefined-undefined-undefined' instead of throwing); (2) patient-detail-timeline-events.ts:295-311 TOKYO_DATE_FORMATTER + formatTokyoDateParts (ja-JP-u-ca-gregory; silently falls back to '0000'/'00'); (3) prescription-date-window.ts:10-25 formatDateKeyInTimeZone (en-CA, throws — parameterized but only ever fed DEFAULT_PRESCRIPTION_TIME_ZONE='Asia/Tokyo'); (4) patient-mcs.ts:448-462 getTokyoCalendarParts (en-CA numeric, '0' fallback) plus a fixed TOKYO_OFFSET_MS reverse conversion at :445. Separately, conference-data-sync.ts:203 formatDateKey is a line-for-line clone of formatUtcDateKey (src/lib/date-key.ts:19) using getUTC\* accessors.
- Proposal: Replace the copies with imports: japanDateKey for the three key-string producers, and formatUtcDateKey for conference-data-sync; for patient-mcs, add a small japanCalendarParts helper next to japanDateKey in date-boundary.ts (it already owns JAPAN_DATE_FORMAT) and reuse it. prescription-date-window keeps its timeZone parameter only if a non-Tokyo caller exists (none found).
- Payoff: The compliance-critical JST business-day key (the F60-F66 bug family's fix vehicle) gets exactly one implementation; the silent '0000-00-00'/'undefined' fallback variants disappear in favor of the canonical fail-fast contract.
- Risk: Invalid-Date inputs change behavior from garbage-key to RangeError at copies (1),(2),(4) — inputs there are DB Date columns so unreachable in practice, but confirm per call-site. Behavior locked by date-boundary.test.ts plus each service's existing tests (patient-home-operations, patient-detail-timeline-events, prescription-date-window all have test files).
- Verifier (adversarial): Re-read every cited copy. Canonical japanDateKey (date-boundary.ts:35-44, en-US Intl Asia/Tokyo, throws RangeError) vs: patient-home-operations.ts:39-56 (en-US formatter, no validation → 'undefined-undefined-undefined'), patient-detail-timeline-events.ts:295-311 (ja-JP-u-ca-gregory, '0000'/'00' fallback), prescription-date-window.ts:10-25 (en-CA, throws; grep confirmed all 3 production callers of validatePrescriptionDateWindow pass no timezone — only Asia/Tokyo ever used), patient-mcs.ts:448-462 (en-CA numeric, '0' fallback, plus TOKYO_OFFSET_MS reverse at :444-446). conference-data-sync.ts:203-208 confirmed as formatUtcDateKey (date-key.ts:19-25) minus assertValidDate, otherwise line-for-line. Dedup: grepped both ops/refactor docs — all existing date findings are wrong-pattern-at-callsite bugs (F60-F66 class, setHours/toISOString.slice), none proposes consolidating the duplicated Tokyo Intl-formatter implementations; not in the exclusion list. Two corrections to the candidate: (1) patient-detail-timeline-events.test.ts does NOT exist (ls confirmed), so that slice is not test-locked — verify via typecheck + consumer tests; (2) copies 2 and 4 produce parts (YYYY/MM/DD display, YYYY-MM-01 month key, numeric calendar math), not key strings, so they need the proposed japanCalendarParts helper rather than japanDateKey directly — the proposal already accounts for this. Behavior change is confined to Invalid-Date inputs (garbage-key → fail-fast), unreachable for the Prisma Date-column inputs at all call sites.

- [ ] **Codex cross-review**:

## R17 [20] type-drift (effort L) — Truncated-list response envelope ({data,total_count,visible_count,hidden_count,truncated}) hand-built in 23 API route files and hand-mirrored in 16 FE files with drift

- Anchor: `src/app/api/drug-alert-rules/route.ts:73` (lens type-contract-drift)
- Files: `src/app/api/drug-alert-rules/route.ts`, `src/app/api/admin/facilities/route.ts`, `src/app/api/admin/external-professionals/route.ts`, `src/app/api/service-areas/route.ts`, `src/app/api/pharmacy-drug-stocks/route.ts`, `src/app/(dashboard)/admin/alert-rules/page.tsx`, `src/app/(dashboard)/admin/alert-rules/signal-tuning-panel.tsx`, `src/app/(dashboard)/admin/vehicles/vehicles-content.tsx` 他
- Scores: payoff 4 / safety 4 / verify 5 / scope 3 / priority 4 = **20**
- Evidence: grep 'visible_count:' src/app/api = 26 build sites across 23 route files (e.g. drug-alert-rules/route.ts:70-84, service-areas/route.ts:79, admin/facilities/route.ts:89+141, pharmacists/route.ts:227); grep 'visible_count?: number' in src/app/(dashboard) = 16 FE files each hand-declaring the envelope type (vehicles-content.tsx:62, facilities-content.tsx:78, alert-rules/page.tsx:46, signal-tuning-panel.tsx:38, schedule-proposals-content.tsx:239, schedule-weekly-optimizer.tsx:129, ...). Drift already present: signal-tuning-panel.tsx:38 declares data?: optional while alert-rules/page.tsx:46 declares data: required plus extra count_basis/filters_applied/limit fields for the same GET /api/drug-alert-rules response. No shared type exists (src/lib/api/response.ts and src/types have no list-envelope type; only per-feature copies in src/types/schedule-day-board.ts:122-137 etc.).
- Proposal: Add TruncatedListResponse<T> (fields: data: T[]; total_count: number; visible_count: number; hidden_count: number; truncated: boolean) to a new src/types/api/list-envelope.ts, plus a buildTruncatedListPayload(rows, totalCount, extras?) helper next to success() in src/lib/api/response.ts. Adopt incrementally: replace the 16 FE hand-copies first (pure type change), then fold the 26 route-side inline builds into the helper.
- Payoff: Deletes ~16 hand-written FE envelope types and ~26 five-line inline builds; kills the existing optional-vs-required drift class; new list routes get the envelope for free and FE/BE can no longer disagree on truncation fields.
- Risk: Type-only for FE (zero runtime change); route-helper adoption changes construction sites and must preserve per-route extras (count_basis, filters_applied, limit). Behavior locked by each route's route.test.ts (e.g. drug-alert-rules, admin/facilities) and FE content tests (vehicles, facilities, alert-rules).
- Verifier (adversarial): Re-ran all greps: 26 'visible_count:' build sites across 23 non-test files in src/app/api (exact match to claim); 17 files (16 non-test) in src/app/(dashboard) hand-declare 'visible_count?: number'. Re-read copies: drug-alert-rules/route.ts:73-84, service-areas/route.ts:76-87, admin/facilities/route.ts:88-95 all hand-build the identical {data,total_count,visible_count,hidden_count,truncated,count_basis,filters_applied,limit} payload (facilities adds hasMore). Drift verified by reading both FE copies for the same GET /api/drug-alert-rules: signal-tuning-panel.tsx:38 has data?: optional with no extras vs alert-rules/page.tsx:46 data: required plus count_basis/filters_applied/limit. Confirmed no shared type: zero total_count/visible_count/truncated hits in src/lib/api/response.ts; only per-feature copies in src/types/schedule-day-board.ts:124,137 and reports-today-workspace.ts:124. Behavior lock: 24 route.test.ts files assert visible_count. Dedup check: 0 'visible_count' matches in ULTRACODE_EXPANSION_MASTER_TARGETS.md; FINDINGS envelope mentions are error-envelope only (F03/F69); F30 is workflow-dashboard type drift with a different file-set/root-cause.

- [ ] **Codex cross-review**:

## R18 [20] dup-helper (effort S) — PrescriptionLine (14 fields) + InquiryRecord (10 fields) copy-pasted across prescriptions FE (2 identical copies each + 1 drifted InquiryRecord; the prescription-history PrescriptionLine variant is a partial overlap, not a superset) despite an existing prescription.shared.ts

- Anchor: `src/app/(dashboard)/prescriptions/prescription-inline-detail.tsx:26` (lens type-contract-drift)
- Files: `src/app/(dashboard)/prescriptions/prescription-inline-detail.tsx`, `src/app/(dashboard)/prescriptions/[id]/prescription-detail-content.tsx`, `src/app/(dashboard)/patients/[id]/medications/medications-content.tsx`, `src/app/(dashboard)/patients/[id]/prescriptions/prescription-history-content.tsx`, `src/app/(dashboard)/prescriptions/prescription.shared.ts`
- Scores: payoff 3 / safety 5 / verify 5 / scope 5 / priority 2 = **20**
- Evidence: PrescriptionLine (16 fields) is byte-identical in prescription-inline-detail.tsx:26 and prescription-detail-content.tsx:44; both files already import CYCLE_STATUS_CONFIG from prescription.shared.ts. InquiryRecord is declared 3x: identical 11-field copies in those two files (:43 / :61) and a drifted copy in medications-content.tsx:96 where result is narrowed to 'changed'|'unchanged'|'pending'|null (others: string | null) and a line: {drug_name, line_number} sub-object is added. A 4th, extended PrescriptionLine variant lives in prescription-history-content.tsx:58 (adds drug_master_id/source_drug_code/drug_resolution_status/updated_at). Server-side has an unrelated internal PrescriptionLine in src/server/cds/checker.ts:19 sharing the name.
- Proposal: Move the canonical PrescriptionLine and InquiryRecord into prescription.shared.ts (the established shared home for these two views); medications-content and prescription-history express their variants as intersections/Pick extensions of the base. Adopt the narrowed result union as canonical if the API guarantees it (updateInquiryRecordSchema in lib/validations/prescription.ts:362 z.enum(['changed','unchanged','pending']) says it does).
- Payoff: Removes ~55 duplicated lines across 4 files and resolves the result-union drift; the inquiry result union becomes single-sourced against the zod schema.
- Risk: Behavior-preserving type move; locked by prescription-inline-detail.test.tsx, prescriptions-workspace.test.tsx, medications-content tests, prescription-history-content.test.tsx.
- Verifier (adversarial): Re-read all five files and re-ran the type-declaration grep. PrescriptionLine is byte-identical (14 fields, not the claimed 16) at prescription-inline-detail.tsx:26 and prescription-detail-content.tsx:44; InquiryRecord is byte-identical (10 fields, not 11) at :43/:61 with the drifted third copy at medications-content.tsx:96 (result narrowed to 'changed'|'unchanged'|'pending'|null plus line sub-object) exactly as claimed. prescription.shared.ts exists (wc -l = 28) and both prescriptions files already import CYCLE_STATUS_CONFIG from it. updateInquiryRecordSchema at lib/validations/prescription.ts:362-363 confirms z.enum(['changed','unchanged','pending']), so the narrowed union is API-backed. All four claimed test files exist. Correction: the prescription-history-content.tsx:58 variant is NOT a pure superset — it adds 9 fields (quantity, unit, source_drug_code_type, start_date, end_date on top of the 4 claimed) and DROPS is_generic_name_prescription, so it needs Omit-based composition or should stay separate; the clean win is the 2+3 identical copies. Dedup: only PrescriptionLine hit in ULTRACODE_FINDINGS is a QR-draft trim-normalization behavior finding (different root cause/file set); no overlap with F30 or other excluded items. Refactor is type-only (zero runtime change), mechanically verified by tsc + 4 existing test files.

- [ ] **Codex cross-review**:

## R19 [20] type-drift (effort S) — DiffReviewRow (11 fields) / DiffReviewChangeType / DiffReview diff-review contract duplicated between the prescriptions API route and its single FE consumer

- Anchor: `src/app/api/patients/[id]/prescriptions/route.ts:43` (lens type-contract-drift)
- Files: `src/app/api/patients/[id]/prescriptions/route.ts`, `src/app/(dashboard)/patients/[id]/prescriptions/prescription-history-content.tsx`
- Scores: payoff 3 / safety 5 / verify 5 / scope 5 / priority 2 = **20**
- Evidence: BE declares DiffReviewChangeType (route.ts:41) and the 12-field DiffReviewRow (route.ts:43-66, with field docs) and serves it as diff_review (route.ts:312, :409). FE re-declares both field-for-field, docs stripped, at prescription-history-content.tsx:108-122, plus DiffReview wrapper at :124. FE uses the type in 4 places (:306 formatDiffReviewDrugCode, :316 isDifferentDrugMaster, :1224 DIFF_CHANGE_TEXT_COLOR record keyed on the union). No drift yet — pure double-maintenance on a clinically sensitive diff payload (追加/中止/変更 labeling).
- Proposal: Extract DiffReviewChangeType, DiffReviewRow, and DiffReview to src/types/api/prescription-diff-review.ts (or src/types/api/ alongside workflow-dashboard.ts); both route and FE import it. CHANGE_LABELS stays in the route.
- Payoff: ~30 duplicated lines removed; change_type union and row fields become compile-linked so a new change kind cannot ship without the FE color/label maps updating (Record<DiffReviewChangeType,...> forces exhaustiveness).
- Risk: Pure type move, zero runtime delta; locked by api route.test.ts and prescription-history-content.test.tsx which both exercise the diff review path.
- Verifier (adversarial): Independently re-verified: (1) BE declares DiffReviewChangeType at src/app/api/patients/[id]/prescriptions/route.ts:41 and DiffReviewRow at :43-66 with JSDoc per field; FE re-declares both field-for-field with docs stripped at prescription-history-content.tsx:108-122 — I diffed the field lists and they match exactly (note: 11 fields, not 12 as candidate claimed: key, drug_name, current_drug_master_id, current_drug_code, previous_drug_master_id, previous_drug_code, change_type, change_label, previous_label, current_label, pharmacist_memo). (2) FE DiffReview wrapper (:124-129, rows/set_impacts/patient_checks/change_count) matches buildDiffReview's return shape at route.ts:234-239 exactly. (3) Payload served as diff_review at route.ts:312 and :409; grep -rl diff_review over src (excl. tests) returns exactly these 2 files, so blast radius is fully bounded. (4) FE uses the union in Record<DiffReviewChangeType,string> at :1224 (exhaustiveness benefit is real) plus :306/:316/:1236-1242/:1407. (5) Precedent confirmed: src/types/api/ exists and contains only workflow-dashboard.ts. (6) Test lock: route.test.ts mentions diff_review 9x, prescription-history-content.test.tsx 4x. (7) Dedup: grep for DiffReview/diff_review/prescription-history in ops/refactor/ULTRACODE_FINDINGS_20260702.md and ULTRACODE_EXPANSION_MASTER_TARGETS.md returns zero hits; F30 is workflow-dashboard, a different file-set/root-cause. Type-only move, zero runtime delta, no React Compiler interaction.

- [ ] **Codex cross-review**:

## R20 [20] test-harness-dup (effort S) — expectSensitiveNoStore assertion copy-pasted in 156 API route test files (~620 lines); extract to shared src/test helper

- Anchor: `src/app/api/visit-schedules/day-board/route.test.ts:93` (lens test-harness-dup)
- Files: `src/app/api/visit-schedules/day-board/route.test.ts`, `src/app/api/patients/export/route.test.ts`, `src/lib/api/sensitive-response.ts`, `src/test/dom-test-utils.ts`
- Scores: payoff 3 / safety 5 / verify 5 / scope 4 / priority 3 = **20**
- Evidence: grep 'function expectSensitiveNoStore' src/app/api --include='\*.test.ts' -l = 156 files, each ~5 lines (~780 duplicated lines). Drift already exists: 148 copies assert the literal 'private, no-store, max-age=0' while 5 copies assert a locally-defined SENSITIVE_NO_STORE constant. Production SSOT is SENSITIVE_NO_STORE_HEADERS in src/lib/api/sensitive-response.ts:1.
- Proposal: Add src/test/response-assertions.ts exporting expectSensitiveNoStore(response: Response) (keep the header literals inside the helper so the test is not a tautology against the prod constant; add one unit test asserting the helper matches SENSITIVE_NO_STORE_HEADERS). Mechanical codemod: replace each local definition with an import. Follows the src/test/dom-test-utils.ts precedent (228 consumers).
- Payoff: ~780 lines deleted; future header-policy changes (e.g. adding a header) touch one assertion instead of 156; eliminates the 148-vs-5 constant drift.
- Risk: Near zero — pure assertion helper, behavior locked by the 156 existing tests passing unchanged. Only risk is a copy with a subtly different expectation; grep shows all 153 Cache-Control assertions use the identical string.
- Verifier (adversarial): Re-ran greps: 156 test files define expectSensitiveNoStore (exact match). Diffed all 156 bodies via awk/uniq: 148 literal-string copies, 5 with locally-defined SENSITIVE_NO_STORE constant (same value, e.g. src/app/api/patients/[id]/route.test.ts:240), 3 lowercase-header variants (behaviorally identical via case-insensitive Headers.get) — all assert identical values. Prod SSOT confirmed at src/lib/api/sensitive-response.ts:1 (SENSITIVE_NO_STORE_HEADERS). src/test/dom-test-utils.ts precedent confirmed with 228 consumers. Dedup grep of both ops/refactor findings docs: only EPIC 7 X11-X13 mention no-store, which is missing-header bugs on prod routes, different file-set and root cause. Minor correction: helper is 4 lines, so ~620-660 duplicated lines, not ~780.

- [ ] **Codex cross-review**:

## R21 [20] test-harness-dup (effort S) — sonner toast mock duplicated in 71 test files with drifting method surfaces (warning/info in only ~9, loading/dismiss/promise in 0)

- Anchor: `src/app/(dashboard)/patients/[id]/card-workspace.test.tsx:32` (lens test-harness-dup)
- Files: `src/app/(dashboard)/patients/[id]/card-workspace.test.tsx`, `src/app/(dashboard)/tasks/tasks-content.test.tsx`
- Scores: payoff 3 / safety 5 / verify 5 / scope 4 / priority 3 = **20**
- Evidence: vi.mock('sonner') in 70 test files. Method-surface drift: success/error stubbed ~45/44 times, warning only 8, info only 6 — most copies omit info/warning/loading/dismiss, so any component that starts calling toast.info under a mock that lacks it throws TypeError at test time and gets patched per-file (same failure class as the navigation mocks).
- Proposal: Add a createToastMock() export (can live in the same src/test/next-mocks.ts or src/test/toast-mock.ts) covering the full sonner surface (success/error/info/warning/loading/dismiss/promise as vi.fn()) plus named spies for assertion; each file's vi.mock('sonner', () => ({ toast: toastMock })) becomes one line.
- Payoff: ~420 lines converge to one fixture; new toast methods never require touching 70 files; assertion spies become uniform (toastErrorMock naming already exists in 10 files, inline vi.fn() in the rest — currently un-assertable in those).
- Risk: Minimal — pure widening of the mocked surface; existing assertions on success/error spies keep working. Behavior locked by the 70 suites.
- Verifier (adversarial): Re-ran greps: 71 test files contain vi.mock('sonner' (candidate said 70); method stubs inside the mock factories count error=63/success=57/warning=9/info=9/loading=dismiss=promise=0, confirming surface drift. Re-read 4+ copies (card-workspace.test.tsx:32 anchor confirmed, patient-labs-card.test.tsx:63, patient-contacts-panel.test.tsx:17, shared-viewer-content.test.tsx:11) — near-identical object literals, not intentional decoupling. toastErrorMock named spies in exactly 10 files as claimed; 12 production files call toast.info/toast.warning vs ~9-11 mocks stubbing them, so the TypeError-then-patch-per-file failure class is real. No bare toast() prod calls, so an object fixture (via vi.hoisted or async factory import) suffices. Dedup grep of ULTRACODE_FINDINGS_20260702.md and ULTRACODE_EXPANSION_MASTER_TARGETS.md for sonner/toast/vi.mock found only the dead-code stale-mock cluster (different root cause) — not a duplicate. Corrections: 71 files not 70; success/error counts ~57/63 not ~45/44; src/test/next-mocks.ts does not exist (src/test/ has only dom-test-utils.ts) so the fixture is a new file. Test-only change, no React Compiler concerns, fully gated by pnpm test.

- [ ] **Codex cross-review**:

## R22 [20] dead-code (effort M) — Dead Yjs collaborative-editing hook/provider chain (4 new files, 683 prod lines + 1,619 test lines) + yjs/y-protocols/y-websocket deps + orphaned room-token-client (179L) — completes known F26 collaboration-trio deletion

- Anchor: `src/lib/hooks/use-yjs-collaboration-room.ts:1` (lens dead-code-round2)
- Files: `src/lib/hooks/use-yjs-collaboration-room.ts`, `src/lib/hooks/use-collaborative-form.ts`, `src/components/features/collaboration/collaborative-textarea.tsx`, `src/components/features/collaboration/cursor-overlay.tsx`, `src/lib/collaboration/yjs-provider.ts`, `src/lib/collaboration/form-yjs-bridge.ts`, `src/components/features/collaboration/presence-avatars.tsx`, `package.json`
- Scores: payoff 4 / safety 4 / verify 5 / scope 4 / priority 3 = **20**
- Evidence: Dependency chain is fully test-only: use-collaborative-form.ts (140L) is imported only by its own test (use-collaborative-form.test.tsx, 1,492L); it is the sole importer of use-yjs-collaboration-room.ts (373L), which is the sole importer of yjs-provider.ts (34L+127L test) and form-yjs-bridge.ts (136L). collaborative-textarea.tsx (143L+57L test) is the sole importer of cursor-overlay.tsx (69L). grep 'PresenceAvatars' matches only presence-avatars.tsx (47L) and its test (91L). grep "from 'yjs'" hits only these files — no live page imports any of them. yjs/y-protocols/y-websocket in package.json:142-144 are imported nowhere else (lib0 already known as F-dead). The LIVE presence feature (presence-api-client.ts, use-presence-heartbeat.ts, collaboration-content.tsx, /api/presence route) does not touch this cluster.
- Proposal: Delete the 7 files + their 4 test files, remove yjs, y-protocols, y-websocket (and known lib0) from package.json dependencies. Keep presence-api-client/use-presence-heartbeat/presence-store which back the live collaboration page. This extends known F-item field-lock-indicator (26L) to the full never-wired collaborative-editing feature.
- Payoff: ~942 prod lines + ~1,767 test lines removed (use-collaborative-form.test.tsx alone is 1,492L of CI time), 3-4 npm deps dropped from install/bundle audit surface; eliminates a misleading half-built realtime-editing feature.
- Risk: Low: zero non-test importers proven per file. If collaborative editing is a planned feature, deletion loses scaffolding — confirm intent; git history preserves it. pnpm typecheck/test/build gate the removal.
- Verifier (adversarial): Independently re-verified: wc -l matches (942 prod: 373/140/143/69/34/136/47; tests 1492/127/57/91=1767). Re-ran importer greps: use-collaborative-form.ts has zero non-test importers and is the sole importer of use-yjs-collaboration-room.ts (:14), which is the sole importer of yjs-provider.ts (:15) and form-yjs-bridge.ts (:16); collaborative-textarea.tsx has zero non-test importers and is sole importer of cursor-overlay.tsx; presence-avatars.tsx referenced only by its test. Repo-wide grep confirms yjs/y-protocols/y-websocket (package.json:142-144) are imported only inside this cluster+tests, with no config refs. Live presence feature (collaboration-content.tsx, card-workspace.tsx, presence-api-client, use-presence-heartbeat, /api/collaboration/room-token) does not touch the cluster; shared yjs-config.ts (16L) is correctly excluded (room-token route imports it directly). Caveats the fixer must handle: (1) presence.test.ts:178-189 readFileSync-locks cursor-overlay.tsx and presence-avatars.tsx paths — remove 2 entries from that live test; (2) room-token-client.ts (179L) becomes orphaned and should join the batch; (3) tools/infra/websocket/README.md:119 documents `npx y-websocket` for local dev (still works without the declared dep). Dedup: 259 of 942 prod lines (presence-avatars, collaborative-textarea, cursor-overlay) already covered by F26; F40 covered lib0 and its verifier explicitly called the yjs family "genuinely used" — this finding refines that. The core new content (683-line hook/provider chain, 1,492L test file, removal of yjs/y-protocols/y-websocket) appears in no known finding, so not a duplicate, but should land merged with F26's collaboration-trio deletion. Deletion is gated by pnpm typecheck/test/build; no React Compiler/hooks concerns (pure removal).

- [ ] **Codex cross-review**:

## R23 [20] dup-helper (effort M) — Extract shared `messageFromError(e, fallback)` — the `X instanceof Error ? X.message : '…'` ternary is copy-pasted 174 times / 66 files (141 of them inside toast.error)

- Anchor: `src/app/(dashboard)/patients/[id]/patient-insurance-card.tsx:623` (lens state-ui-consistency)
- Files: `src/app/(dashboard)/patients/[id]/patient-insurance-card.tsx`, `src/app/(dashboard)/patients/[id]/patient-contacts-panel.tsx`, `src/lib/api/client-json.ts`
- Scores: payoff 4 / safety 5 / verify 5 / scope 3 / priority 3 = **20**
- Evidence: `grep -rnE "instanceof Error \? [a-zA-Z]+\.message :"` over src/app+src/components (excl. tests) = 174 sites / 66 files; the toast-specific form `toast.error(err instanceof Error ? err.message : '固定文言')` = 140 sites / 53 files (e.g. patient-insurance-card.tsx:623,655; patient-contacts-panel.tsx:184). No shared helper exists: grep for showApiErrorToast/toastError/errorToast in src/lib and src/components returns nothing. Distinct from CE18 (trimStringOrUndefined) and from candidate 1 (this is the catch-side unknown→string step, not response reading).
- Proposal: Add `messageFromError(error: unknown, fallback: string): string` (and optionally `toastApiError(error, fallback)`) in src/lib/api/client-json.ts or src/lib/utils, then mechanically rewrite the 174 ternaries to `toast.error(messageFromError(error, '固定文言'))`. Pure 1:1 substitution.
- Payoff: 174 copies of an identical 60-char ternary collapse to one 5-line tested helper; future hardening (e.g. also reading AbortError, mapping DOMException, trimming) becomes a one-place change instead of a 66-file sweep — exactly the adversarial gap (mutable Error.name, non-Error throws) flagged in past reviews.
- Risk: Behavior-preserving by construction (same ternary inside the helper). Existing screen tests asserting toast strings continue to pass since fallback strings are kept verbatim per call site.
- Verifier (adversarial): Re-ran the greps myself: `instanceof Error ? X.message :` in src/app+src/components excluding tests = 174 sites / 66 files (exact match). The toast.error-wrapped form = 141 sites / 54 files (candidate said 140/53; one more, immaterial). Re-read samples (patient-insurance-card.tsx:623,655; patient-contacts-panel.tsx:184; visit-constraints-card.tsx:198; safety-check-content.tsx:418; capture-content.tsx:194) — all are the identical single-expression ternary with only the fallback literal varying, so a `messageFromError(error, fallback)` helper is a pure 1:1 substitution. No existing helper: grep for messageFromError/showApiErrorToast/toastError/errorToast/getErrorMessage in src/lib, src/components, src/hooks returned zero hits. src/lib/api/client-json.ts exists (76 lines, readApiJson) and is a sensible home. Dedup check: grepped both ops/refactor files for "instanceof Error"/"toast.error"/"messageFromError" — only hit is an unrelated body?.error-reading finding, so this is not F35/F42/CE18 or any listed item. Caveat that trims scope slightly: ~33 of the 174 sites are non-toast variants (API routes using `String(err)` fallback, setError calls, `: undefined` detail props like card-workspace.tsx:4215), so the mechanical toast rewrite covers ~141 sites and the remainder need the String(err)/undefined variants or can be left; this doesn't refute the candidate. Behavior-preserving by construction; anchor screens are test-locked (patient-insurance-card.test.tsx exists, 18 test files in patients/[id]/).

- [ ] **Codex cross-review**:

## R24 [20] pattern-inconsistency (effort M) — Converge ~27 hand-rolled cursor-pagination epilogues onto existing buildCursorPage (drop communication-queue.ts — top-N truncation, not pagination)

- Anchor: `src/lib/api/pagination.ts:8` (lens prisma-query-helpers)
- Files: `src/lib/api/pagination.ts`, `src/app/api/medication-cycles/route.ts`, `src/app/api/consent-records/route.ts`, `src/app/api/visit-records/route.ts`, `src/app/api/care-reports/route.ts`, `src/app/api/billing-candidates/route.ts`, `src/app/api/drug-masters/route.ts`, `src/app/api/patients/[id]/prescriptions/route.ts` 他
- Scores: payoff 4 / safety 4 / verify 5 / scope 4 / priority 3 = **20**
- Evidence: buildCursorPage (src/lib/api/pagination.ts:8-21, locked by pagination.test.ts with 8 tests) is adopted by 21 files, but 27 other files still hand-roll the identical epilogue: 47 `take: limit + 1` sites, 31 `.length > limit` checks, 34 `slice(0, limit)` calls. Concrete copies: medication-cycles/route.ts:104-112 (offset cursor `String(offset + limit)`), consent-records/route.ts:195-198 (id cursor `data[data.length-1].id`), visit-records/route.ts:953, care-reports/route.ts:802, billing-candidates/route.ts:226, plus 5 src/server services. Both cursor styles map onto the existing `cursorOf: (row) => string | undefined` parameter.
- Proposal: Route-by-route mechanical adoption: replace `const hasMore = rows.length > limit; const page = hasMore ? rows.slice(0, limit) : rows; nextCursor = ...` with `const { data, hasMore, nextCursor } = buildCursorPage(rows, limit, cursorOf)` in the 27 straggler files (offset-style passes `() => String(offset + limit)`; keep any route-specific post-filtering before the call).
- Payoff: ~27 x 4-6 duplicated lines removed; hasMore/nextCursor off-by-one logic lives in one tested helper; the two cursor dialects (offset vs last-id) become greppable via cursorOf lambdas instead of ad-hoc string math.
- Risk: buildCursorPage clamps limit to >=1 (Math.max(1, trunc)) — identical to routes that already bound limit via parseBoundedInteger(…,1,max), which is all sampled sites; each route has a route.test.ts locking envelope shape (hasMore/nextCursor keys). Do a per-route diff of nextCursor emission for the hasMore=false case (helper emits undefined, matching sampled copies).
- Verifier (adversarial): Independently re-verified: buildCursorPage at src/lib/api/pagination.ts:8-21 with 8 tests in pagination.test.ts (grep -c "it(" = 8); 21 adopter files vs re-counted 47 `take: limit + 1` non-test sites, 28 non-test files with hand-rolled `length > limit`, 37 `slice(0, limit)` sites. Re-read the claimed copies and confirmed near-identical epilogues: medication-cycles/route.ts:104-112 (offset dialect `String(offset + limit)`), consent-records/route.ts:195-198 (id-cursor dialect), visit-records/route.ts:952-960, billing-candidates/route.ts:226+316, patient-service.ts:725/762/818/859, visit-schedule-service.ts:182, data-explorer.ts:503. All sampled routes have route.test.ts locking envelope shape, and buildCursorPage's limit clamp matches parseBoundedInteger(…,1,…) bounds already used at call sites. Dedup grep of ops/refactor/ULTRACODE_FINDINGS_20260702.md and ULTRACODE_EXPANSION_MASTER_TARGETS.md found no server-side pagination-epilogue convergence finding. One correction: src/server/services/communication-queue.ts:876/941 is plain top-N truncation (no hasMore/nextCursor) and must be dropped from the file list; care-reports (conditional take/keyword-scan), data-explorer (ternary hasMore), and contact-profiles (kind==='all' gates) need per-site care rather than blind substitution.

- [ ] **Codex cross-review**:

## R25 [20] pattern-inconsistency (effort M) — ErrorState retry shorthand: 106 call sites hand-build the same action={{label:'再試行/再読み込み', onClick:refetch}} adapter

- Anchor: `src/components/ui/error-state.tsx:18` (lens component-props-api)
- Files: `src/components/ui/error-state.tsx`, `src/app/(dashboard)/tasks/tasks-content.tsx`, `src/app/(dashboard)/patients/[id]/medications/medications-content.tsx`, `src/app/(dashboard)/handoff/handoff-workspace.tsx`, `src/app/(dashboard)/admin/drug-masters/drug-master-content.tsx`, `src/app/(dashboard)/admin/performance/page.tsx`
- Scores: payoff 4 / safety 4 / verify 5 / scope 4 / priority 3 = **20**
- Evidence: 106 occurrences of `action={{ label: '再試行'|'再読み込み', onClick: () => void xQuery.refetch() }}` in 72 non-test files (grep counts: 54 use label '再試行', 52 use '再読み込み' — pure copy drift for the identical retry affordance). Hotspots: medications-content.tsx 5x, performance/page.tsx 5x, handoff-workspace.tsx 4x, prescription-intake-form.tsx 4x. DataTable already models the same concern as first-class props `onRetry?: () => void` + `errorActionLabel = '再読み込み'` (data-table.tsx:84-85,134), so the two highest-traffic error surfaces force two different adapter shapes. Bonus sweep: 75 non-test call sites pass `size="inline"` which is already the default (error-state.tsx:128).
- Proposal: Add `onRetry?: () => void` and `retryLabel?: string` to ErrorState (rendered as the primary action when `action` is absent), mirroring DataTable's onRetry/errorActionLabel naming. Mechanical codemod: `action={{ label: L, onClick: F }}` → `onRetry={F} retryLabel={L}` (or drop retryLabel where L matches the chosen default), keeping each screen's existing label so test assertions stay byte-identical. Optionally drop redundant size="inline" in the same sweep.
- Payoff: Removes ~106 object-literal adapters and the 54/52 label fork; retry becomes one greppable prop across DataTable and ErrorState; future copy unification ('再試行' vs '再読み込み') becomes a 1-line change instead of a 72-file sweep.
- Risk: 71 \*-content.test.tsx files assert the retry-button text ('再試行'/'再読み込み'); preserved by passing retryLabel per call site. error-state.test.tsx locks action rendering (href vs onClick) and must gain onRetry cases. Behavior-preserving if labels are carried over verbatim.
- Verifier (adversarial): Independently re-ran every count. (1) grep `action={{` + retry label in non-.test. tsx = exactly 106 lines across 72 files (cut -d: -f1 | sort -u = 72); label split = 54 '再試行' / 52 '再読み込み' — matches candidate byte-for-byte, pure copy drift for the same affordance. (2) All 106 are onClick-based (grep -c onClick = 106, href = 0), so the proposed onRetry codemod covers 104/106 mechanically; only 2 sites add variant:'outline' (src/app/global-error.tsx:29, src/components/ui/route-error-boundary.tsx:28) and can keep the action prop. (3) Hotspots re-confirmed: medications-content.tsx 5x, admin/performance/page.tsx 5x, handoff-workspace.tsx 4x, prescription-intake-form.tsx 4x. (4) DataTable precedent re-read: src/components/ui/data-table.tsx declares `errorActionLabel?: string; onRetry?: () => void` with default errorActionLabel='再読み込み' — the naming mirror is real. (5) Re-read src/components/ui/error-state.tsx in full: action is a plain {label, href?, onClick?, variant?, size?} object (lines 10-16), size defaults to 'inline' at line 128, and renderAction (74-98) makes an additive onRetry/retryLabel prop trivially compatible (render as primary when action absent). size="inline" bonus: 77 non-test occurrences confirmed (candidate said 75, same ballpark). (6) Behavior lock: 83 .test.tsx files assert 再試行/再読み込み text (candidate said 71 — actual is higher, strengthening the keep-labels-verbatim constraint), and error-state.test.tsx:35-39 locks href-action rendering. (7) Dedup: not in the excluded list (F18/F25/F26/F40/F56/F32/F35/F41/F42/F72/CE18/F30/CXR2-PERF01); grepped ops/refactor/ULTRACODE_FINDINGS_20260702.md — ErrorState/retry appears only as false-empty bug-fix suggestions (different root cause: missing isError handling, not prop-shape duplication); ops/refactor/ULTRACODE_EXPANSION_MASTER_TARGETS.md has zero error/retry/ErrorState entries. Not a React Compiler conflict (additive prop, no memoization change). One design decision the implementer must pin: precedence when both action and onRetry are passed (proposal already says action wins).

- [ ] **Codex cross-review**:

## R26 [19] dup-route-boilerplate (effort L) — Promote external-viewer PanelBody to a shared guarded-query panel component (isLoading→isError(ErrorState+retry)→empty→data ladder)

- Anchor: `src/app/(dashboard)/external/external-viewer-content.tsx:432` (lens fe-panel-boilerplate)
- Files: `src/app/(dashboard)/external/external-viewer-content.tsx`, `src/app/(dashboard)/handoff/handoff-workspace.tsx`, `src/app/(dashboard)/patients/[id]/medications/medications-content.tsx`, `src/app/(dashboard)/billing/partner-cooperation/partner-cooperation-billing-content.tsx`, `src/app/(dashboard)/admin/operating-hours/operating-hours-content.tsx`, `src/app/(dashboard)/admin/drug-masters/drug-master-content.tsx`, `src/app/(dashboard)/admin/data-explorer/data-explorer-content.tsx`, `src/app/(dashboard)/admin/realtime/page.tsx` 他
- Scores: payoff 4 / safety 4 / verify 4 / scope 3 / priority 4 = **19**
- Evidence: Local PanelBody at external-viewer-content.tsx:432-473 (42 lines) already encodes the exact target shape (Skeleton stack → ErrorState variant=server size=inline + 再試行 → emptyText → children) but is private to one file. Repo-wide (excluding tests): 84 inline `isError ? (` ternary sites across 45 files under src/app/(dashboard) + src/components; 35 of them are full isLoading?(…):isError?(<ErrorState…refetch>):… ladders across 22 files (measured avg region 50 lines; scaffold portion ~12-20 lines each), plus 8 if-return ladders in 8 more files. 91 hand-rolled `role="status" aria-label="…読み込み中"` skeleton blocks duplicate what SkeletonRows (src/components/ui/loading.tsx:14) provides. Copies drift: handoff-workspace.tsx:553-566 uses Skeleton h-12 stack + description-only ErrorState; operating-hours-content.tsx:359-367 uses <Loading /> + no title; handoff:1326-1337 adds title+detail. Top repeat offenders: handoff-workspace (4 ladders), partner-cooperation-billing (3), medications-content (3), drug-masters/data-explorer/operating-hours/realtime/report-share/schedule-team-board (2 each).
- Proposal: Move PanelBody (renamed e.g. QueryPanelBody) from external-viewer-content.tsx:432 to src/components/ui/query-panel-body.tsx with props {isLoading, isError, isEmpty?, loadingFallback? (default SkeletonRows), errorTitle?, errorDescription, errorDetail?, retryLabel?, onRetry, emptyText?, children}; migrate ladders screen-by-screen starting with the 9 files that repeat it 2-4 times, passing each site's current skeleton/copy through props so rendered DOM is unchanged.
- Payoff: ~43 ladder sites × ~12-20 scaffold lines (≈600-750 lines) collapse to ~4-6 lines each; future fail-close fixes (the F05/F08/F23-class false-empty bugs still being stamped out one-by-one) become 'wrap in QueryPanelBody' instead of hand-writing a ladder, and the loading-a11y (role=status/aria-label) + retry affordance become impossible to forget.
- Risk: Pure JSX re-parenting; per-site skeleton shape, error copy, and data-testid must be passed through verbatim or tests break — 71 test files under src/app/(dashboard) assert 再試行/再読み込み labels and several assert loading testids (e.g. handoff-action-rail-loading), which is also what locks behavior. Migrate incrementally, one screen per commit, diffing rendered output.
- Verifier (adversarial): Independently re-verified: PanelBody exists at external-viewer-content.tsx:432-473 with the exact described ladder shape and no hooks; re-ran greps and got exactly 84 non-test `isError ? (` sites in 45 files, exactly 91 role="status" blocks, and 25 files with combined isLoading?()/isError?() ternary ladders (candidate said 22 files — slight undercount, same magnitude). Read all three cited drift copies (handoff-workspace 553-566 aria-hidden skeleton + description-only ErrorState + 再読み込み; operating-hours 359-367 <Loading/> no title; handoff 1326-1337 title+detail+再試行) — real drift, not intentional decoupling. SkeletonRows at loading.tsx:14 confirmed as the existing a11y-correct primitive. Behavior lock confirmed: exactly 71 test files assert 再試行/再読み込み and handoff-action-rail-loading testid is asserted at handoff-workspace.test.tsx:513. Dedup: grepped both ops/refactor findings files — existing entries are per-site false-empty BUG fixes, none proposes the shared component extraction; different root cause, not on excluded list. Caveat: payoff is mildly overstated (per-site skeleton/copy survives as props, so real savings ~4-8 lines/site not 12-20), and per-site DOM subtleties (aria-hidden vs role=status, className placement) must pass through verbatim.

- [ ] **Codex cross-review**:

## R27 [19] query-helper (effort M) — 5 hand-rolled copies of the /api/dashboard/cockpit fetcher with drifted headers/error-handling and 3 different queryKey shapes — extract useDashboardCockpitQuery

- Anchor: `src/app/(dashboard)/handoff/handoff-workspace.tsx:92` (lens fe-panel-boilerplate)
- Files: `src/app/(dashboard)/handoff/handoff-workspace.tsx`, `src/app/(dashboard)/schedules/schedule-team-board.tsx`, `src/app/(dashboard)/settings/operational-policy-content.tsx`, `src/app/(dashboard)/prescriptions/intake/intake-triage-content.tsx`, `src/app/(dashboard)/reports/report-share-workspace.tsx`, `src/app/(dashboard)/dashboard/dashboard-cockpit.tsx`, `src/app/(dashboard)/my-day/my-day-content.tsx`
- Scores: payoff 4 / safety 3 / verify 5 / scope 4 / priority 3 = **19**
- Evidence: 7 screens query the same endpoint. A shared fetchDashboardCockpit is already exported from dashboard-cockpit.tsx:60 (reused only by my-day-content.tsx:39), while 5 files re-implement it: handoff-workspace.tsx:92 (raw `{'x-org-id': orgId}` header + bare `throw new Error`), schedule-team-board.tsx:79 (buildOrgHeaders + readApiJson), operational-policy-content.tsx:93, intake-triage-content.tsx:64, report-share-workspace.tsx:111 — ~9 lines each plus per-file useQuery wiring. queryKey drift: ['dashboard','cockpit',orgId] (handoff:1397, intake:309, my-day:198, report-share:599) vs ['dashboard','cockpit',orgId,viewScope] (dashboard-cockpit:627) vs ['schedule-rail-cockpit',orgId] (schedule-team-board:1744, +invalidations at 1754/1771) vs ['settings-rail-cockpit',orgId] (operational-policy:504) — same response cached under 3 unrelated keys, so cross-screen navigation refetches needlessly and invalidations don't propagate.
- Proposal: Move the fetcher out of dashboard-cockpit.tsx into src/lib/workspace/dashboard-cockpit-query.ts (or features/dashboard) exporting fetchDashboardCockpit + useDashboardCockpitQuery(orgId, {viewScope?, staleTime?}) with a single dashboardCockpitQueryKey(orgId) helper; delete the 5 local fetchers and point the 2 stray queryKeys at the shared key (or, for a strictly behavior-preserving first slice, keep per-screen keys via a key parameter and unify keys in a follow-up that also migrates the 2 invalidateQueries calls).
- Payoff: ~45 lines of drifted fetcher code deleted; one place to fix error copy/headers (handoff's copy currently bypasses the repo's readApiJson error envelope); key unification deduplicates network fetches of a heavy BFF endpoint across handoff/schedules/reports/my-day navigation.
- Risk: Key unification changes cache/invalidation semantics: schedule-team-board invalidates 'schedule-rail-cockpit' after task mutations (1754/1771) — those must move to the shared key in the same commit or rails elsewhere show stale data. Tests mocking fetch by URL (5 test files reference dashboard/cockpit) keep passing; 3 test files reference the rail-specific keys and need the key rename. Do the fetcher dedup (safe) separately from the key merge (semantic).
- Verifier (adversarial): Independently re-verified every claim. (1) Copies: grep confirms 7 screens hit /api/dashboard/cockpit; shared fetchDashboardCockpit is exported at dashboard-cockpit.tsx:60 and reused only by my-day-content.tsx:39/199, while 5 files re-implement ~8-line fetchers: handoff-workspace.tsx:92 (raw {'x-org-id'} header + bare throw), intake-triage-content.tsx:64 (raw header + bare throw), operational-policy-content.tsx:93 (buildOrgHeaders + bare throw), schedule-team-board.tsx:79 (buildOrgHeaders + readApiJson), report-share-workspace.tsx:111 (raw header + readApiJson) — real 3-way drift in headers AND error envelope, confirmed by reading each copy. (2) queryKey drift: all 8 cited lines reproduced exactly — ['dashboard','cockpit',orgId] at handoff:1397/intake:309/my-day:198/report-share:599, +viewScope at dashboard-cockpit:627, ['schedule-rail-cockpit',orgId] at 1744 with invalidations at 1754/1771, ['settings-rail-cockpit',orgId] at 504. (3) Test lock: 5 test files reference dashboard/cockpit as claimed; intake test mocks by queryKey prefix 'dashboard' (survives unification); 3 test files reference rail-cockpit keys and need renames, as the candidate stated. (4) Dedup: F14/F27 were fail-open error-handling bugs on these same queries (fixed in abde9163) — different root cause; no known finding covers fetcher/key dedup. NOT a duplicate. CAVEAT that lowers safety: the shared fetcher always sends ?scope=mine, but the 5 local copies send NO scope param, and route.ts:164 resolves absent scope to 'role_default' (= team for canViewAllDashboardWork users) — so naively pointing the 5 copies at fetchDashboardCockpit(orgId) CHANGES returned data for team-view roles; the shared helper must support an omitted-scope mode. This also exposes a live inconsistency the candidate strengthens: my-day caches scope=mine data under the same ['dashboard','cockpit',orgId] key that handoff/intake/report-share populate with role_default data — same key, different payloads today. The two-slice plan (fetcher dedup first, key merge second) is correct; add "preserve no-scope requests" to slice 1.

- [ ] **Codex cross-review**:

## R28 [19] query-helper (effort M) — Standardize query-param validation: parseSearchParams exists but 95 route files hand-roll searchParams->safeParse with 107-vs-44 message drift; add parseSearchParamsOrError (5 spread-idiom files are mechanical; the ~19-file has()&&!value read-helper family needs per-field-message-preserving migration)

- Anchor: `src/lib/api/validation.ts:3` (lens be-route-boilerplate)
- Files: `src/lib/api/validation.ts`, `src/app/api/billing-evidence/check/route.ts`, `src/app/api/business-holidays/route.ts`, `src/app/api/pharmacy-operating-hours/route.ts`, `src/app/api/patients/[id]/visit-records/pdf/route.ts`
- Scores: payoff 4 / safety 3 / verify 5 / scope 3 / priority 4 = **19**
- Evidence: 95 route files combine searchParams with safeParse, but parseSearchParams (validation.ts:3-11) is imported by only 25. Error-message drift across the hand-rolled sites: 107 copies of validationError('検索条件が不正です'...) vs 44 of 'クエリパラメータが不正です'/'クエリが不正です', with at least 4 detail-shape variants (flatten().fieldErrors; {[name]:[message]} x20; patientResult.fieldErrors x7; statusResult.fieldErrors x5). A second copied idiom feeds it: `...(searchParams.has('x') ? { x: searchParams.get('x') } : {})` spread-conditionals in 24 files (e.g. business-holidays/route.ts:33-35, pharmacy-operating-hours/route.ts:70-71, patients/[id]/visit-records/pdf/route.ts:40-41) — all working around the fact that no helper turns URLSearchParams + zod schema directly into a 400 response. Representative hand-rolled block: billing-evidence/check/route.ts:110-115.
- Proposal: Add `parseSearchParamsOrError<T>(schema, searchParams, message = '検索条件が不正です'): { ok: true; data: T } | { ok: false; response: NextResponse }` next to parseSearchParams in validation.ts, mapping absent params to undefined (subsuming the has()-spread idiom via null-entry filtering). Migrate the 24 spread-idiom files first (purely mechanical), then the remaining hand-rolled sites; keep per-route messages via the parameter where tests lock them.
- Payoff: ~150 validation-return sites converge on one shape (4-8 lines saved each, ~600+ lines), the two-message drift stops growing, and the F20-class from/to-date fixes get a single seam to attach dateKey schemas to instead of 95 bespoke parse blocks.
- Risk: Message/details payloads are asserted by route tests ('検索条件が不正です' appears in many route.test.ts files), so the helper must reproduce each route's current message via the parameter — routes on the minority message keep it explicitly. Multi-value params: Object.fromEntries-based parseSearchParams drops repeated keys; the new helper must preserve each route's current get() semantics (single value). Migrate per-route with its test green.
- Verifier (adversarial): Independently re-verified all core counts: 95 route.ts files combine searchParams+safeParse (grep -rl safeParse | xargs grep -l searchParams = 95) while parseSearchParams (validation.ts:3-11, Object.fromEntries-based) is imported by only 25; message drift is exactly 107 '検索条件が不正です' vs 44 'クエリパラメータが不正です/クエリが不正です'; detail shapes fragment as claimed (flatten().fieldErrors 270x, {[name]:[msg]} literal ~23x, patientResult.fieldErrors 8x, statusResult.fieldErrors 5x); re-read the cited blocks (billing-evidence/check/route.ts:109-115, business-holidays/route.ts:32-39, pharmacy-operating-hours/route.ts:68-76, visit-records/pdf/route.ts:39-47) and they match. ONE EVIDENCE ERROR: the conditional-spread has() idiom is in only 5 files, not 24 — the 24 counts every file containing searchParams.has(; ~19 of those use a different copied idiom (if (searchParams.has(name) && !value) local read-helpers with per-field Japanese messages), which strengthens the fragmentation case but is NOT purely mechanical to migrate. Dedup checked: F32 is QR draft-line readers (unrelated); no CE/N/X epic or F-finding covers query-validation convergence. Behavior is substantially test-locked: 30 route.test.ts files assert '検索条件が不正です', enabling per-route migration with its test green.

- [ ] **Codex cross-review**:

## R29 [19] oversized-split (effort L) — drug-master-content.tsx: 4,056-line single component (16 queries / 16 mutations / 33 useState) — extract detail Sheet, import-history section, and formulary-ops sections

- Anchor: `src/app/(dashboard)/admin/drug-masters/drug-master-content.tsx:1053` (lens oversized-split)
- Files: `src/app/(dashboard)/admin/drug-masters/drug-master-content.tsx`, `src/app/(dashboard)/admin/drug-masters/drug-master-formulary-view-model.ts`, `src/app/(dashboard)/admin/drug-masters/drug-master-content.test.tsx`
- Scores: payoff 4 / safety 4 / verify 4 / scope 4 / priority 3 = **19**
- Evidence: File is 5,112 lines (wc -l), largest in src/. DrugMasterOperationalContent spans 1053–5109 (~4,056 lines in ONE component) with 16 useQuery (1119–1456), 16 useMutation (1520–2111), 34 useState (1058–1099). Lines 96–1048 (~950 lines) are module-level types + pure formatters. JSX seams verified: formulary-ops Card 2316 (採用品変更申請 2427 / 処方・採用品不一致 2574 / 影響レビューキュー 2710 / 薬価改定差分レポート 2904 / 拠点間コピー 3026 / テンプレート 3191 / CSVプレビュー 3447), search+table ~3560–4020, import history data-testid at 4029 + official-import-preview 4221, detail Sheet 4319–5109 (~790 lines). Only 2 consumers: admin/drug-masters/page.tsx:9 (variant=master), admin/formulary/page.tsx:9 (variant=formulary).
- Proposal: Follow the file's own view-model precedent (drug-master-formulary-view-model.ts). (1) Move types (lines 98–603) to drug-master-content.types.ts and pure formatters (538–990: formatImport*, formatOfficialImportPreview*, readAuditObject, copyTextToClipboard, DrugNameCell, StructuredPayload) to drug-master-content-helpers.tsx (~950 lines out, zero behavior change — keep parseReorderPointInput export for tests). (2) Extract DrugDetailSheet (4319–5109, props: selectedDrugId, detailQuery/stockConfigQuery/stockHistoryQuery data + stock mutations). (3) Extract ImportHistorySection (4029–4318, props: import log query + filters). (4) Extract FormularyOperationsCard (2316–~3560) receiving the formulary queries/mutations as props. Hooks stay in the parent in original order (React Compiler safe); only JSX + pure code moves.
- Payoff: Main file drops from 5,112 to roughly 1,800 lines; each extracted section becomes independently reviewable/testable; the master vs formulary variant logic becomes visible instead of buried in one 2,800-line JSX return.
- Risk: Prop-threading mistakes between parent hooks and extracted sections. Locked by drug-master-content.test.tsx (2,875 lines) plus page.test.tsx variant wiring test and drug-master-formulary-view-model.test.ts. Do NOT reorder or conditionally extract the hook block (React Compiler + rules-of-hooks); extract JSX/pure code only.
- Verifier (adversarial): Re-ran all measurements: wc -l = 5,112 (largest non-test file in src/; next is 4,747); DrugMasterOperationalContent spans 1053–5109; grep confirms exactly 16 useQuery (1119–1456) and 16 useMutation (1520–2111); useState is 33 hooks not 34 (34 grep lines include the import) — immaterial. Re-read every claimed JSX seam: variant==='formulary' Card at 2316, 採用品変更申請 2427, 処方・採用品不一致 2574, CSVプレビュー 3447, import-history data-testid 4029, detail Sheet 4319–5109. Module-level pure types/formatters 96–1048 confirmed via declaration listing (types 98–603, formatters 538–990, DrugNameCell 991, StructuredPayload 1021, exported parseReorderPointInput 737). Exactly 2 consumers confirmed (admin/drug-masters/page.tsx:9 master, admin/formulary/page.tsx:9 formulary) plus page.test.tsx variant-wiring lock; behavior locked by 2,875-line drug-master-content.test.tsx and the existing view-model precedent (drug-master-formulary-view-model.ts, 506 lines, own test). Dedup: grepped ULTRACODE_FINDINGS_20260702.md and ULTRACODE_EXPANSION_MASTER_TARGETS.md — drug-master-content appears only in bug-class findings (data-table sorted row-click index, mhlw import perf), no oversized-split finding; not in the excluded F/CE list. Proposal is React Compiler-safe: hooks stay in parent in original order, only JSX subtrees and module-level pure code move. Residual risk is prop-threading across a very large JSX move, mitigated by the test suite and tsc.

- [ ] **Codex cross-review**:

## R30 [19] dup-helper (effort S) — formatFileSize duplicated byte-for-byte in two visit-attachment views — trivial SSOT extraction to src/lib/format/ (note: size rendering not currently test-locked; safety is from verbatim pure-function move + new unit test)

- Anchor: `src/components/features/visits/visit-attachments-field.tsx:25` (lens shared-helper-dupes)
- Files: `src/app/(dashboard)/visits/[id]/visit-record-detail.tsx`, `src/components/features/visits/visit-attachments-field.tsx`, `src/lib/format/currency.ts`
- Scores: payoff 2 / safety 5 / verify 5 / scope 5 / priority 2 = **19**
- Evidence: Two byte-identical 10-line formatFileSize copies: visit-record-detail.tsx:196-205 and visit-attachments-field.tsx:25-34 (same MB/KB/B thresholds, same toFixed(1)/Math.round rendering). No lib-level byte formatter exists (grep for formatBytes/formatFileSize under src/lib: 0 hits); src/lib/format/ currently holds only currency.ts, so the convention slot exists.
- Proposal: Extract to src/lib/format/file-size.ts (formatFileSize(sizeBytes: number): string) with a unit test, import in both components. Future attachment/evidence upload UIs (offline evidence quota messaging shows sizes too) reuse it.
- Payoff: Removes 10 duplicated lines now and pre-empts a third fork; gives file-size display a tested SSOT consistent with the repo's format/ convention (formatYen precedent F35).
- Risk: Zero behavior change (verbatim move); both host components have .test.tsx coverage rendering attachment rows.
- Verifier (adversarial): Re-verified independently: read both copies — visit-record-detail.tsx:196-205 and visit-attachments-field.tsx:25-34 are byte-identical 10-line pure functions (same MB toFixed(1)/KB Math.round/B thresholds). Grep confirms no formatFileSize/formatBytes anywhere else in src/ and src/lib/format/ holds only currency.ts, so the SSOT slot exists. Grepped all of ops/refactor/ (FINDINGS F01-F89 + EXPANSION targets) for formatFileSize/file-size: 0 hits — not a known finding; F35 is formatYen (different helper/file-set). One correction: the candidate's claim that both hosts have test coverage of attachment rows is false — visit-attachments-field has no test file, visit-record-form.test.tsx mocks it (line 201), and visit-record-detail.test.tsx uses attachments: []. Safety instead rests on the move being a verbatim pure-function extraction plus the proposed new unit test.

- [ ] **Codex cross-review**:

## R31 [19] test-harness-dup (effort L) — 364 API route tests each hand-roll the same vi.hoisted auth/RLS/logger/performance mock preamble (~90 lines/file, 32,919 total); 60 of 80 logger mocks are {error}-only

- Anchor: `src/app/api/comments/route.test.ts:48` (lens test-harness-dup)
- Files: `src/app/api/comments/route.test.ts`, `src/app/api/care-reports/route.test.ts`, `src/app/api/dashboard/cockpit/route.test.ts`
- Scores: payoff 4 / safety 4 / verify 5 / scope 3 / priority 3 = **19**
- Evidence: 364 route .test.ts files under src/app/api; awk-measured preamble before first describe() totals 32,919 lines (avg 90.4/file). Repeated fixtures: vi.mock('@/lib/db/rls') in 245 files, vi.mock('@/lib/db/client') in 336, requireAuthContextMock in 143, logger mock in 90 files (with drift: 29 mock only {error}, 15 mock error/warn/info/debug), withRoutePerformance passthrough in 33, runWithRequestAuthContext passthrough in 29. withOrgContextMock is a bare vi.fn() in 220+ files with 388 scattered per-test mockImplementation calls; auth fixtures drift (orgId 'org_1' x93 vs 'corg1234567890123456789012' x8, roles pharmacist/admin/clerk/trainee inlined 241 times via requireAuthContextMock.mockResolvedValue).
- Proposal: Add src/test/api-route-harness.ts exporting (a) buildAuthContext({role='pharmacist', orgId='org_1', userId='user_1'}) fixture builder, (b) passthroughRunWithRequestAuthContext / passthroughWithRoutePerformance factory fns and a complete loggerMock object usable inside each file's vi.mock factory, (c) withOrgContextPassthrough(tx) helper. vi.mock hoisting means files keep their own vi.mock() calls but the factory bodies become one-line imports. Adopt incrementally, newest-first; do NOT big-bang.
- Payoff: Cuts an estimated 20-30 boilerplate lines per file (7-10k lines across 364 files) and converges the drifting auth-context and logger fixtures; the incomplete logger mocks ({error} only) currently break whenever a route adds logger.warn — the shared complete mock removes that whole failure class.
- Risk: vi.mock hoisting: factories must not close over top-level test-file state, so the helper must be import-safe inside vi.mock callbacks (vitest allows imports inside factory). Behavior locked by the 364 suites themselves; migrate file-by-file with green runs. Largest-effort item here.
- Verifier (adversarial): Independently re-ran all counts: 364 API .test.ts files (find|wc), preamble-before-first-describe = 32,919 lines exactly (awk per-file sum, avg 90.4), requireAuthContextMock in 143 files + 241 mockResolvedValue calls (exact), withOrgContextMock 227 files / 388 mockImplementation (exact), withRoutePerformance 33 (exact), orgId 'org_1' x630 in 292 files vs corg123... in 5 files (drift confirmed). Re-read comments/care-reports/cockpit route.test.ts preambles: identical skeleton (vi.hoisted destructure, auth-context mock, runWithRequestAuthContext passthrough (\_ctx,cb)=>cb(), logger mock, withRoutePerformance passthrough, rls mock) with the claimed logger drift confirmed and worse than stated: of 80 files mocking @/lib/utils/logger, 60 mock only {error}, 20 include warn (claim said 90/29/15; direction right, magnitude off). Minor deviations: db/rls 234 files (claim 245), db/client 278 (claim 336) — immaterial to substance. Per-route prisma model mocks differ legitimately and are correctly excluded from the proposal, so this is not intentional decoupling. Deduped against ops/refactor/ULTRACODE_FINDINGS_20260702.md and ULTRACODE_EXPANSION_MASTER_TARGETS.md (grep harness/boilerplate/vi.mock/route.test: no hit). vi.mock hoisting constraint is real but solvable via async factory await import(); test-only change, each migration re-locked by its own green suite; completing the logger mock cannot break currently-green tests.

- [ ] **Codex cross-review**:

## R32 [19] test-harness-dup (effort S) — QueryClient test wrapper duplicated 73 times across 52 test files with retry-config drift (19 bare-default copies, ≥8 local renderWith\* helper reimplementations)

- Anchor: `src/app/(dashboard)/patients/[id]/patient-documents-panel.test.tsx:38` (lens test-harness-dup)
- Files: `src/app/(dashboard)/statistics/statistics-content.test.tsx`, `src/app/(dashboard)/admin/analytics/analytics-content.test.tsx`, `src/app/(dashboard)/patients/[id]/patient-documents-panel.test.tsx`, `src/components/features/comments/mention-input.test.tsx`, `src/components/features/comments/comment-thread.test.tsx`
- Scores: payoff 3 / safety 4 / verify 5 / scope 4 / priority 3 = **19**
- Evidence: 71 `new QueryClient(` occurrences in 52 test files; 72 QueryClientProvider JSX blocks in 50 files; 6 local renderWith\* helper reimplementations (statistics-content.test.tsx:56, analytics-content.test.tsx:48, mention-input.test.tsx:28, comment-thread.test.tsx:62, +2). Config drift: 35 set queries.retry:false only, 33 also mutations.retry:false, and patient-documents-panel.test.tsx:38/243/326 uses bare `new QueryClient()` with default retries (3 retries + backoff on error-path tests = slow/flaky).
- Proposal: Add src/test/query-client.tsx exporting createTestQueryClient() (retry:false for both queries and mutations, gcTime:0) and renderWithQueryClient(ui, {client?}) returning {client, ...renderResult}. Codemod the 6 local helpers and 71 instantiations; the bare-default copies in patient-documents-panel get the retry-disabled client.
- Payoff: ~300-400 lines deleted across 52 files; eliminates the queries-only vs queries+mutations retry drift; fixes the latent slowness of default-retry clients on error-path tests.
- Risk: Any test intentionally relying on retry or cache persistence would change timing — grep shows none configure retry:true, and each suite's green run locks behavior. Mutation-retry disable is a strict flake reduction.
- Verifier (adversarial): Re-ran all greps: 73 `new QueryClient(` in 52 test files, 72 `<QueryClientProvider` JSX in 50 files. Re-read the helper copies: renderWithClient (statistics-content.test.tsx:58, analytics-content.test.tsx:48), byte-identical renderWithQueryClient (mention-input.test.tsx:28, comment-thread.test.tsx:62), createTestQueryClient/createWrapper trio (print-hub-content.test.tsx:41-58), plus renderWith\* in medications/metrics/operational-policy tests — ≥8 local reimplementations. Drift confirmed and worse than claimed: 49 files queries-retry-off, 41 also mutations-off, and 19 bare `new QueryClient()` default-retry copies (patient-documents-panel.test.tsx x7, visit-brief-card.test.tsx x12; candidate only cited 3). Dedup: 0 QueryClient mentions in ULTRACODE_FINDINGS_20260702.md and ULTRACODE_EXPANSION_MASTER_TARGETS.md. Behavior lock: no test enables retry except query-provider.test.ts:12 (retry:1) which tests the production provider and must be excluded from the codemod; the bare-default files have no rejecting/throwing query paths, so retry:false swap is behavior-preserving (this also means the 'slow/flaky today' claim is overstated — latent only). Shared-util precedent exists (src/test/dom-test-utils.ts). Caveats for implementer: exclude query-provider.test.ts; reconsider gcTime:0 (possible within-test cache-persistence reliance) or verify each suite green.

- [ ] **Codex cross-review**:

## R33 [19] dup-helper (effort M) — Merge billing-evidence's private Japan month-range/JST-offset helpers into date-boundary SSOT

- Anchor: `src/server/services/billing-evidence/core.ts:279` (lens prisma-query-helpers)
- Files: `src/server/services/billing-evidence/core.ts`, `src/lib/utils/date-boundary.ts`, `src/server/services/pca-rental-billing.ts`, `src/server/services/billing-evidence/information-provision.ts`, `src/server/services/billing-evidence/duplicate-interaction.ts`, `src/server/services/conference-sync.ts`, `src/app/api/billing-evidence/check/route.ts`, `src/server/services/patient-mcs.ts` 他
- Scores: payoff 4 / safety 4 / verify 4 / scope 4 / priority 3 = **19**
- Evidence: billing-evidence/core.ts:264-290 re-implements Japan month boundaries with its own `JAPAN_TIME_ZONE_OFFSET_MS = 9*60*60*1000` (core.ts:264), `japanCivilMonthParts`, `billingMonthForJapanTimestamp` (core.ts:274), and `japanMonthRangeForBillingMonth` (core.ts:279, returns {start,nextStart,end}) — semantically the same `Date.UTC(y,m,1) - offset` math as date-boundary.ts japanMonthInstantRange:116 ({gte,lt}). Consumers of the private copy: pca-rental-billing.ts:80, information-provision.ts:152, duplicate-interaction.ts:195, conference-sync.ts:553, billing-evidence/check/route.ts:118. Two more parallel hand-rolls: patient-mcs.ts:27 `TOKYO_OFFSET_MS` + getTokyoCalendarParts:448-460 (Intl formatToParts, duplicating japanDateKey at date-boundary.ts:35), and admin/staff-metrics/route.ts:8-40 parseMonthRange building local-TZ `new Date(year, monthIndex, 1)` (correct only because prod pins TZ=Asia/Tokyo).
- Proposal: Add Date-sentinel adapters to src/lib/utils/date-boundary.ts (`japanMonthKeyForInstant(value: Date): string` + reuse japanMonthInstantRange; provide `{start,nextStart,end}` shim or migrate the 3 where-clauses to {gte,lt}); delete core.ts:264-290 private block; replace patient-mcs getTokyoCalendarParts with japanDateKey; converge staff-metrics parseMonthRange onto japanMonthInstantRange.
- Payoff: Single JST-offset constant and month-boundary formula in the codebase (currently 3 independent copies: date-boundary.ts:21, core.ts:264, patient-mcs.ts:27); billing month math becomes locked by date-boundary.test.ts (21 tests) instead of being tested only indirectly.
- Risk: The {start,nextStart,end} → {gte,lt} where-clause rewrite must keep `lt: nextStart` (not lte: end) semantics — the 3 consumers already use gte/lt so this is mechanical; staff-metrics convergence changes behavior only if server TZ != Asia/Tokyo (pinned in env). Billing-evidence has route tests (billing-evidence/check, stats, analytics route.test.ts).
- Verifier (adversarial): Re-read core.ts:256-290: private JAPAN_TIME_ZONE_OFFSET_MS=9*60*60\*1000 (line 264), japanCivilMonthParts (266-272), billingMonthForJapanTimestamp (274-277), japanMonthRangeForBillingMonth (279-290) — start/nextStart use the identical `Date.UTC(y,m,1) - offset` formula as date-boundary.ts japanMonthInstantRange:116-121 (offset constant duplicated at date-boundary.ts:21). Re-ran the consumer grep: japanMonthRangeForBillingMonth callers are pca-rental-billing.ts:80, information-provision.ts:152, duplicate-interaction.ts:195, core.ts:1258, plus billing-candidates/route.ts:355 (one MORE than the candidate listed); billingMonthForJapanTimestamp callers are conference-sync.ts:553, check/route.ts:118, plus stats/route.ts:22 and analytics/route.ts:30 (also missed — these use the sentinel as a `billing_month` column value, so a Date-sentinel adapter is genuinely required, as the proposal states). Verified the {gte,lt} migration is mechanical: every range consumer uses `{gte: monthRange.start, lt: monthRange.nextStart}` and grep for `monthRange.end|billingMonthRange.end` returns zero hits (exit 1) — the `end` field is dead. Verified third offset copy patient-mcs.ts:27 TOKYO_OFFSET_MS + getTokyoCalendarParts:448-465 (Intl formatToParts Asia/Tokyo, same pattern as japanDateKey at date-boundary.ts:35-44) and staff-metrics/route.ts:8-40 parseMonthRange building local-TZ `new Date(year, monthIndex, 1)` (TZ-dependent). Test lock verified: date-boundary.test.ts exists with 21 it() blocks, and every touched file has a test (core.test.ts, duplicate-interaction.test.ts, pca-rental-billing.test.ts, conference-sync.test.ts, patient-mcs.test.ts, staff-metrics/route.test.ts, billing-evidence check/stats/analytics route.test.ts). Dedup checked: ULTRACODE findings on core.ts date logic are different root-causes (startOfWeek/endOfWeek local-TZ bug at core.ts:292-304 and setHours cutoff at 489-496 — the doc explicitly calls billingMonthForJapanTimestamp the JST-aware correct code); EPIC 5 is a TZ bug-fix epic (CE03/N19/N20/N24... at other call sites) and does not cover merging these correct private helpers into the date-boundary SSOT. Caveats folded into scores: (1) patient-mcs getTokyoCalendarParts returns numeric parts consumed by createTokyoDate for MM/DD year inference, so a straight japanDateKey swap needs a parse step or numeric-parts helper — that leg is the weakest; (2) staff-metrics convergence changes behavior only on non-JST server TZ (a fix aligned with EPIC 5, but its route.test may pin local-TZ dates and would need adjusting); (3) sentinel semantics (billing_month column stores UTC-midnight-of-month Date) must be preserved by the adapter, not replaced with month-key strings.

- [ ] **Codex cross-review**:

## R34 [19] dup-helper (effort M) — Extract getErrorMessage(error, fallback?): the `x instanceof Error ? x.message : fallback` ternary is copied 192 times in 76 files (168 literal-fallback, 18 undefined-fallback); two divergent local helpers (queryErrorMessage / errorMessageFromUnknown) already exist and differ on empty-message handling

- Anchor: `src/components/features/patients/patient-form.tsx:151` (lens component-props-api)
- Files: `src/components/features/patients/patient-form.tsx`, `src/app/(dashboard)/schedules/route-compare/route-compare-content.tsx`, `src/app/(dashboard)/patients/[id]/patient-insurance-card.tsx`, `src/app/(dashboard)/patients/[id]/patient-contacts-panel.tsx`, `src/lib/utils`
- Scores: payoff 4 / safety 4 / verify 5 / scope 3 / priority 3 = **19**
- Evidence: grep -rEn "instanceof Error \? [a-zA-Z_.]+\.message :" over src/app+src/components+src/hooks (non-test) = 192 matches in 76 files, overwhelmingly in toast.error(...) and ErrorState detail= adapters. Two local helpers already exist with divergent names doing the identical thing: patient-form.tsx:151 `queryErrorMessage(errorValue, fallback)` and route-compare-content.tsx:113 `errorMessageFromUnknown(error, fallback)`. No shared helper exists in src/lib/utils (checked directory listing + symbol grep). Not covered by known findings F01-F89 / CE-N-X epics (grep of both ops/refactor files returned no hits for instanceof Error / getErrorMessage).
- Proposal: Add src/lib/utils/error-message.ts exporting `getErrorMessage(error: unknown, fallback: string): string`; first re-point the two existing local helpers at it, then mechanical sweep of the 192 ternaries (highest-density files first: patient-insurance-card.tsx, patient-care-team-panel.tsx).
- Payoff: ~190 duplicated ternaries collapse to one tested helper; single place to later harden non-Error throws (e.g. string throws, ApiError.detail) instead of 76 files.
- Risk: Near-zero: output is byte-identical for Error and non-Error inputs. Screen tests already lock the toast/detail strings (71 test files assert error copy), so any transcription slip fails loudly.
- Verifier (adversarial): Independently re-ran the greps: 192 matches in 76 non-test files (src/app+src/components; src/hooks doesn't exist). Re-read both local helpers: patient-form.tsx:151 queryErrorMessage and route-compare-content.tsx:113 errorMessageFromUnknown exist as claimed, but are NOT identical — the latter adds an `&& error.message` truthiness check (empty-message Error returns fallback vs ''), so the unified helper must pick one semantic. Confirmed no shared helper in src/lib (listed src/lib/utils, grepped src/lib exports). Variant breakdown: 168/192 literal-string fallback (exact getErrorMessage shape), 18 `: undefined`, 2 `String(err)`, ~4 constant/template/null — needs an optional-fallback signature for the tail. 141/192 are toast.error sites; 24 test files assert the error copy (candidate's '71' overstated, but the refactor is byte-identical per site since fallback strings pass through unchanged). Dedup grep of ops/refactor/ULTRACODE_FINDINGS_20260702.md and ULTRACODE_EXPANSION_MASTER_TARGETS.md for instanceof Error/getErrorMessage/errorMessage: zero hits.

- [ ] **Codex cross-review**:

## R35 [19] type-drift (effort S) — ErrorState/EmptyState duplicate heading-switch and href/onClick action renderer internally; ErrorState hand-copies the Button variant union

- Anchor: `src/components/ui/error-state.tsx:74` (lens component-props-api)
- Files: `src/components/ui/error-state.tsx`, `src/components/ui/empty-state.tsx`, `src/components/ui/button-variants.ts`
- Scores: payoff 3 / safety 4 / verify 4 / scope 5 / priority 3 = **19**
- Evidence: Three internal duplications between the two state components: (1) heading-level switch — ErrorStateHeading (error-state.tsx:100-119) vs EmptyStateHeading (empty-state.tsx:21-38), identical case 2/3/4 bodies; (2) href-vs-onClick Button rendering — renderAction (error-state.tsx:74-98) vs inline branch (empty-state.tsx:72-81); (3) action prop shape drift — ErrorStateAction {label, href?, onClick?, variant?, size?} (error-state.tsx:10-16) vs EmptyState action {label, href?, onClick?} that hardcodes size="sm" (empty-state.tsx:12-16,74,78). ErrorState's variant union at line 14 ('default'|'outline'|'secondary'|'ghost'|'destructive'|'link') is a hand copy of button-variants.ts and will silently drift if Button variants change (same hand-copied union also at visit-workflow-projection.ts:40, visit-proposal-diagnostics-card.tsx:13). headingLevel ranges also drift: 1-4 (ErrorState) vs 2-4 (EmptyState) vs 2|3 (page-section.tsx:14). Consumers: 84 non-test files import error-state, 27 use EmptyState — all untouched by this refactor.
- Proposal: Extract src/components/ui/state-parts.tsx (internal, not exported to screens): shared `StateAction` type using VariantProps<typeof buttonVariants>['variant'|'size'], a `StateActionButton` renderer, and a polymorphic `Heading level={1|2|3|4}` used by both components (and optionally PageSection). Keep EmptyState's default size 'sm' via a defaultSize arg so rendered output is unchanged.
- Payoff: ~70 lines of internal duplication removed; the Button variant union stops being maintained by hand in 3 places; both state components accept one action shape, so future props (e.g. the onRetry shorthand from the sibling candidate) land once.
- Risk: Zero consumer churn (84+27 importer files keep their props). error-state.test.tsx and empty-state.test.tsx lock heading tag, href-Link vs onClick-Button rendering, and button size — they pin the exact contract this must preserve.
- Verifier (adversarial): Re-read both components: ErrorStateHeading (error-state.tsx:100-119) and EmptyStateHeading (empty-state.tsx:21-38) have identical case 2/3/4 bodies; renderAction (error-state.tsx:74-98) and empty-state.tsx:72-81 duplicate the href-Link vs onClick-Button branch (diff: EmptyState hardcodes size="sm" and omits type="button" — shared renderer must preserve both). Re-read button-variants.ts:16-27: ErrorState's variant union at line 14 is an exact hand copy, and its size union at line 15 has ALREADY drifted (missing 'xs'), proving the drift risk. Sibling hand-copied unions confirmed at src/lib/visits/visit-workflow-projection.ts:40 (candidate's path src/features/visits was wrong) and visit-proposal-diagnostics-card.tsx:13. headingLevel drift 1-4/2-4/2|3 confirmed (page-section is in components/layout, not ui). Re-ran counts: 84 non-test error-state importers, 29 (not 27) EmptyState users — all untouched. Tests exist (4+2 cases) locking heading level and href rendering, though empty-state tests do NOT assert button size, so size="sm" preservation is implementation-checked only. Not a duplicate of any listed known finding (F30 is a different file-set/root-cause).

- [ ] **Codex cross-review**:

## R36 [18] type-drift (effort M) — Three overlapping bounded-integer query-param helper families (pagination.ts / validation.ts / search-params.ts) — converge on one

- Anchor: `src/lib/api/pagination.ts:23` (lens be-route-boilerplate)
- Files: `src/lib/api/pagination.ts`, `src/lib/api/validation.ts`, `src/lib/api/search-params.ts`
- Scores: payoff 3 / safety 4 / verify 4 / scope 4 / priority 3 = **18**
- Evidence: Three independently grown helper families parse the same 'bounded integer search param' concept with different error semantics: (1) pagination.ts imperative — parseBoundedInteger (26 route files, silently clamps/falls back), parseOptionalBoundedIntegerParam (3 files, {ok:false} on garbage), parsePaginationParams (33 files); (2) validation.ts zod — boundedIntegerSearchParam (13 files, 400 with Japanese field message), optionalBoundedIntegerSearchParam (7 files), optionalBlankableBoundedIntegerSearchParam; (3) search-params.ts — parseExactIntegerSearchParam (1 file) plus readSingleSearchParam/readStrictOptionalSearchParam. The same `/^-?\d+$/` regex + min/max logic is triplicated (pagination.ts:23-34, validation.ts:23-29, search-params.ts:33+). Net effect: ?limit=abc silently becomes the default on parseBoundedInteger routes but returns 400 on boundedIntegerSearchParam routes — an API-behavior inconsistency that grows with every new route choosing a family at random.
- Proposal: Pick the zod family (validation.ts) as the SSOT since it composes with the parseSearchParamsOrError candidate; reimplement parseBoundedInteger/parseOptionalBoundedIntegerParam as thin wrappers over it (preserving their silent-clamp semantics exactly, documented as the lenient variant), fold parseExactIntegerSearchParam's single consumer in, and add a JSDoc pointer steering new code to one entry point.
- Payoff: One integer-parsing implementation with two documented strictness modes instead of three drifting regex/clamp copies; new routes stop coin-flipping between silent-clamp and 400 semantics; combined with the search-params helper this completes a single documented entry point for query parsing.
- Risk: Must NOT unify observable semantics — parseBoundedInteger's silent fallback vs zod's 400 are both test-locked per route (pagination.test.ts, validation.test.ts, search-params.test.ts all exist and lock each family's edge cases); this is internal dedup behind unchanged signatures, so gates are the three helper test files plus typecheck.
- Verifier (adversarial): Re-read all three files: /^-?\d+$/ + min/max bounds logic confirmed at pagination.ts:30,48, validation.ts:26, search-params.ts:43 (4 copies, 3 files). Re-ran consumer greps (grep -rl minus defining file): parseBoundedInteger 26, parseOptionalBoundedIntegerParam 3, parsePaginationParams 33, boundedIntegerSearchParam 13, optionalBoundedIntegerSearchParam 7, parseExactIntegerSearchParam 1 — all match the candidate. Verified the behavior inconsistency is real (silent clamp/fallback vs zod 400 vs {ok:false,message}). All three test files exist, locking each family's semantics. Grepped both dedup docs (ULTRACODE_FINDINGS_20260702.md, ULTRACODE_EXPANSION_MASTER_TARGETS.md) for boundedInteger/parseExactInteger/lib-api paths: zero hits — distinct from F32 (readString) and CE18 (trimStringOrUndefined). Caveats: the families encode deliberate clamp-vs-reject policies so dedupable core is only ~10 lines each, and zod .int() vs Number.isSafeInteger differ on unsafe-integer edges (offset uses max=MAX_SAFE_INTEGER at pagination.ts:61) — wrappers must reproduce exact semantics; a shared low-level integer-token parser may be a cleaner SSOT than the proposed zod family.

- [ ] **Codex cross-review**:

## R37 [18] oversized-split (effort L) — prescription-intake-form.tsx: single 2,731-line component with six clean fieldset seams and an already-established extraction precedent in the same directory

- Anchor: `src/app/(dashboard)/prescriptions/new/prescription-intake-form.tsx:322` (lens oversized-split)
- Files: `src/app/(dashboard)/prescriptions/new/prescription-intake-form.tsx`, `src/app/(dashboard)/prescriptions/new/prescription-period-review.tsx`, `src/app/(dashboard)/prescriptions/new/prescription-intake-submit.ts`
- Scores: payoff 4 / safety 3 / verify 4 / scope 4 / priority 3 = **18**
- Evidence: File is 3,053 lines; PrescriptionIntakeForm spans 322–3053 (~2,731 lines) with 7 useQuery + 2 useMutation (208–863) and 14 useState. JSX fieldset seams verified by legend text: 患者・ケース 1833–1990, 処方箋情報 1992–2326, 処方明細 2328–2571, 施設まとめ処方 2574–2667, 疑義照会 2671–2770, 前回処方との差分 2779–2968. Directory precedent already exists: GenericCandidatePanel is an in-file extracted component (197–315), prescription-period-review.tsx is a separately-filed section component, and prescription-intake-submit.ts / prescription-intake-urls.ts are extracted pure logic with their own tests.
- Proposal: Extract the two largest, most self-contained fieldsets first: PreviousPrescriptionDiffSection (2779–2968 + PreviousPrescriptionLine/Intake types at 138–170) and FacilityBatchSection (2574–2667 + FacilityBatchEntryDraft type at 120), then PrescriptionLinesSection (2328–2571, receives lines state + line mutators). Move GenericCandidatePanel (197–315) to its own file at the same time. Parent keeps all queries/mutations/useState; sections receive values + setters as props (same pattern as prescription-period-review.tsx).
- Payoff: Main file drops to ~1,800 lines with the highest-churn intake screen split along the exact section boundaries reviewers already think in; diff-review section becomes unit-testable without mounting the whole form.
- Risk: 14 useState values threaded as props — typo-level risk. Locked by prescription-intake-form.test.tsx, prescription-intake-form.contract.test.ts, prescription-intake-submit.test.ts, prescription-period-review.shared.test.ts. Note pending F38 (clearDraft failure) touches the submit path — avoid overlapping that slice.
- Verifier (adversarial): Re-ran wc -l: prescription-intake-form.tsx is exactly 3,053 lines; PrescriptionIntakeForm opens at 322 and closes at EOF (tail confirms `</form>);}` at 3051-3053) = ~2,731 lines. Re-grepped hooks: 7 useQuery (208 in GenericCandidatePanel + 639/657/684/700/713/725 in the form), 2 useMutation (813, 863), 13 useState (331-530; candidate said 14, immaterial). All six fieldset legends re-verified at 1834 患者・ケース, 1993 処方箋情報, 2329 処方明細, 2574 施設まとめ処方, 2671 疑義照会, 2779 前回処方との差分 — candidate's line numbers off by at most 1. Types confirmed: FacilityBatchEntryDraft ~120, PreviousPrescriptionLine 138-155, PreviousPrescriptionIntake 157-170; GenericCandidatePanel spans 197-314. Precedent confirmed: prescription-period-review.tsx (174 lines) is an already-extracted section file, prescription-intake-submit.ts/urls.ts are extracted pure logic with tests, drug-master-formulary-view-model.ts exists. Self-containment re-read: diff section (2779+) consumes only prescriptionDiff (useMemo at 1414, stays in parent), SOURCE_LABELS (already in prescription-form.shared.ts), and buildLineBadges/badgeClassName; facility section consumes selectedPatientName/selectedCaseId/lines/addCurrentFacilityBatchEntry/facilityBatchEntries — all props-passable. Dedup: grepped both ops/refactor files — no oversized-split or component-split finding for this file; only F38 (clearDraft bug, line 348 of findings) and F32 (API-route reader dupes) touch prescription-intake paths, both different (file-set, root-cause). Two caveats the candidate under-states: (1) buildLineBadges (1051) and badgeClassName (1114) are defined inside the component and used by BOTH 処方明細 (2372-2377) and 差分 (2838) sections — they are pure over their args and must be hoisted to module scope as part of the extraction; (2) behavior lock is thinner than implied — prescription-intake-form.test.tsx is 248 lines / 5 tests (mostly error-affordance render checks + one submit-payload test), so the moved JSX is only partially test-locked; the contract/submit/period-review tests lock submit logic, not the extracted sections' render.

- [ ] **Codex cross-review**:

## R38 [18] oversized-split (effort M) — visit-record-form.tsx: 2,040-line component already wrapped in VisitRecordWorkflowSection step boundaries — extract per-step section components

- Anchor: `src/app/(dashboard)/visits/[id]/record/visit-record-form.tsx:453` (lens oversized-split)
- Files: `src/app/(dashboard)/visits/[id]/record/visit-record-form.tsx`, `src/app/(dashboard)/visits/[id]/record/visit-record-form.shared.ts`, `src/app/(dashboard)/visits/[id]/record/visit-step-nav.tsx`
- Scores: payoff 4 / safety 3 / verify 4 / scope 4 / priority 3 = **18**
- Evidence: File is 2,495 lines; VisitRecordForm spans 453–2495 (~2,040 lines). The JSX is already segmented by an in-file VisitRecordWorkflowSection wrapper (defined at 314) with verified step blocks: safety/CDS step 1569–1786, step 1788–1795, medication-status step 1797–1889, SOAP step 1891–2086 (S/O at 1958–2022, A/P at 2023–2086), and a 370-line combined block 2088–2457 (receipt record 2094, next-visit suggestion 2287, residual medications 2318, patient-info reflect 2357, submit 2427). Extraction precedent already in this directory: visit-step-nav.tsx, visit-completion-readiness-warning.tsx, visit-record-form.shared.ts, visit-mode-mobile.shared.ts. Pure builders buildStructuredSoap (362–425) and buildDraftMetadata (426–441) are also extractable to the .shared.ts file.
- Proposal: Extract SoapStepSection (1891–2086), ReceiptAndWrapUpSection (2088–2457, or split receipt/next-visit/residual into three), and SafetyAlertsSection (1569–1786) as sibling files each receiving the RHF form object + the step-visibility props VisitRecordWorkflowSection already takes; move buildStructuredSoap/buildDraftMetadata/buildPreviousVisitReuseSource (350–441) into visit-record-form.shared.ts where formatting helpers already live.
- Payoff: Main file drops to ~1,300 lines; the mobile step-mode logic (p0_22/p0_23 comments) becomes auditable per step; SOAP builders get direct unit tests in the shared module.
- Risk: RHF register/watch threading across component boundaries — must pass the form object, not destructured watch values, to avoid re-render behavior changes. Locked by visit-record-form.test.tsx (780 lines), visit-record-form.shared.test.ts, visit-mode-mobile.shared.test.ts, visit-step-nav.test.tsx. Pending F11 (schedule fetch failure suppresses CDS alerts) touches the safety section — land F11 first.
- Verifier (adversarial): Independently re-verified: (1) wc -l = 2,495 lines for visit-record-form.tsx; VisitRecordForm starts at 453 (grep), so ~2,040-line component. (2) grep confirms VisitRecordWorkflowSection defined at 314 and JSX blocks exactly at the claimed boundaries: 1569–1786 (訪問前確認, contains CDS visitAlerts/carryItemsWarning panel), 1788–1795 (入力状況), 1797–1889 (訪問結果), 1891–2086 (現地記録 SOAP), 2088–2457 (保存前チェック with receipt ~2094, next-visit ~2287, residual ~2318, patient-reflect ~2357, submit ~2427). Minor nit: 1797–1889 is the visit-result step, not "medication-status step", and 1569–1786 is readiness+CDS — labeling only, not a refutation. (3) Extraction is unusually low-friction: FormProvider already wraps the whole tree (lines 1525–2493), so extracted sections can use useFormContext instead of threading the form object; total RHF surface is small (13 form.register, 6 setValue, 1 getValues repo-grep). The SOAP section's closure surface is tiny — only structuredSoapDraft, handleStructuredSoapChange, mobileStepId, form.register. (4) Pure builders buildPreviousVisitReuseSource/buildStructuredSoap/buildDraftMetadata verified at 350–441, no hook usage, trivially movable to visit-record-form.shared.ts (exists, 134L, with 113L test). (5) Test locks confirmed by wc -l: visit-record-form.test.tsx 780L, visit-record-form.shared.test.ts 113L, visit-mode-mobile.shared.test.ts 146L, visit-step-nav.test.tsx 52L. (6) Precedent files exist in-dir (visit-step-nav.tsx 454L, visit-completion-readiness-warning.tsx). React Compiler is unaffected — extracting child components under FormProvider does not fight compiler memoization. Dedup check: grep of ULTRACODE_FINDINGS_20260702.md and ULTRACODE_EXPANSION_MASTER_TARGETS.md shows only F11 (schedule-fetch bug, different root cause) and the dead soap-step-wizard deletion cluster (dead-code removal, different file-set/root-cause) touch this area — no existing oversized-split finding for this file. Residual risks that cap safety: pending F11 lands in the safety section (candidate already sequences it first), and the file has active churn (0066a03f, 21c33840) plus concurrent-edit review-drift risk noted in project memory; the 2088–2457 block is 370 lines and may need the proposed 3-way sub-split to stay reviewable.

- [ ] **Codex cross-review**:

## R39 [18] oversized-split (effort S) — prescription-intake-service.ts: drug-identity resolution (~175 lines) and medication-profile sync (~215 lines) clusters are extractable submodules per this service's own classification/pair precedent

- Anchor: `src/server/services/prescription-intake-service.ts:1430` (lens oversized-split)
- Files: `src/server/services/prescription-intake-service.ts`, `src/server/services/prescription-intake-classification.ts`, `src/server/services/prescription-intake-pair.ts`
- Scores: payoff 3 / safety 4 / verify 4 / scope 4 / priority 3 = **18**
- Evidence: File is 1,644 lines with a 2,071-line test file; the codebase already split this service twice (prescription-intake-classification.ts 118 lines, prescription-intake-pair.ts 124 lines, each with own test). Two remaining coherent clusters: (a) drug-identity resolution 588–763 (normalizePrescriptionLineSourceDrugCodeType, readPrescriptionLineSourceDrugCode/DrugIdentityCodes, normalizePrescriptionLineDrugMasterId, resolveCreateIntakeLineDrugIdentities); (b) medication-profile sync 1430–1644 (detectIntakeChanges, syncMedicationProfiles, normalizePrescriptionDrugCode, profileKeys, incomingLineKeys, resolveDrugMasterIdsByPrescriptionCode). 4 non-test consumers of the service module.
- Proposal: Extract cluster (a) to prescription-intake-drug-identity.ts and cluster (b) to prescription-intake-profile-sync.ts, each with the matching slice of the 2,071-line test file moved alongside; the service re-exports ProfileSyncResult to keep its public surface stable.
- Payoff: Service drops to ~1,250 lines centered on the transaction orchestration (createPrescriptionIntakeInTx 944–1257); profile-sync — a known bug hotspot — gets a focused test module instead of being buried in a 2,071-line suite.
- Risk: Behavior-preserving move of module-private functions; locked by prescription-intake-service.test.ts. IMPORTANT: pending F09 fixes profileKeys' dead `code:` key inside cluster (b) — land F09 first or in the same slice, otherwise the move and the fix will collide.
- Verifier (adversarial): Re-ran wc -l: service 1,644 / test 2,071 / classification.ts 118 / pair.ts 124 (both prior splits have own tests — precedent real). Re-read declaration map: cluster (a) 588–763 (5 drug-identity functions, single orchestrator call at 1008) and cluster (b) 1430–1644 (detectIntakeChanges, syncMedicationProfiles, profileKeys, incomingLineKeys, resolveDrugMasterIdsByPrescriptionCode, invoked only via runPrescriptionIntakePostCreateHooks 1383) exist exactly as claimed; all module-private except ProfileSyncResult, so the move is behavior-preserving with stable public surface. Grepped ops/refactor findings: only F09 (incomingLineKeys dead code: key — a bug fix, different kind/root-cause) touches this file; no existing split candidate, so not a duplicate, and the candidate correctly flags the F09 collision. Corrections: 5 non-test consumers (not 4); clusters share tiny one-line normalizers (normalizePrescriptionDrugCode used by cluster a at 598/603-604 and by collectOutpatientInjectionBlockedLines 530/556/566 which stays behind; normalizePrescriptionLineDrugMasterId used by cluster b at 1488/1609), so the extraction must export/share these helpers — trivial but the clusters are not perfectly self-contained.

- [ ] **Codex cross-review**:

## R40 [18] query-helper (effort L) — 194 hand-rolled `res.json().catch(...)` + throw-error boilerplate sites across 86 files while the canonical readApiJson SSOT is used in only 14 files

- Anchor: `src/lib/api/client-json.ts:56` (lens shared-helper-dupes)
- Files: `src/lib/api/client-json.ts`, `src/app/(dashboard)/admin/drug-masters/drug-master-content.tsx`, `src/app/(dashboard)/patients/[id]/card-workspace.tsx`, `src/app/(dashboard)/admin/shifts/shifts-content.tsx`, `src/app/(dashboard)/handoff/handoff-workspace.tsx`, `src/app/(dashboard)/views/saved-views-content.tsx`, `src/components/features/dispense-workbench/dispensing-workbench.adapter.ts`
- Scores: payoff 4 / safety 3 / verify 4 / scope 3 / priority 4 = **18**
- Evidence: grep counts: 194 `json().catch` sites across 86 non-test files vs readApiJson (src/lib/api/client-json.ts:56 — parses body, extracts message/error key, throws with fallback, optional zod schema) imported in only 14 files. Top offenders: drug-master-content.tsx 16 sites, card-workspace.tsx 11, shifts-content.tsx 11, handoff-workspace.tsx 7. The pressure is visible: card-workspace.tsx:374 defines its own private readPatientShareApiJson wrapper, saved-views-content.tsx:213 defines readErrorMessage, dispensing-workbench.adapter.ts:142/416 define fetchJson/mutateJson — 4 independent local re-inventions of the same helper. Drift already produced a shipped bug of this class (F03: one site read body?.error against the {code,message} envelope); readApiJson's readApiErrorMessage (client-json.ts:12) handles both 'message' and 'error' keys, so it is envelope-compatible with response.ts and compatibilityError.
- Proposal: Incremental adoption sweep of readApiJson (do NOT add a new helper): start with the 5 files having >=5 sites (46 sites, ~180 boilerplate lines), fold the local wrappers (readPatientShareApiJson, readErrorMessage, mutateJson) into readApiJson calls, and add an ESLint restriction or review checklist entry against new `json().catch` in components.
- Payoff: Each site shrinks 4-5 lines to 1; error-key drift (the F03 class) becomes structurally impossible at converted sites; ~600-800 lines of boilerplate retired at full sweep.
- Risk: Per-site behavior nuance: some sites intentionally return {} instead of throwing, or read details (e.g. body?.details?.blocked_lines) — convert only plain throw-with-message sites mechanically, leave detail-consuming sites for manual passes. Existing content .test.tsx files mock fetch responses and lock toast/error messages per screen.
- Verifier (adversarial): Re-ran every count: `json().catch` = 194 sites / 86 non-test files (exact match); readApiJson imported in 14 files incl. client-json.ts itself. Top offenders reproduced exactly (drug-master-content 16, card-workspace 11, shifts-content 11, handoff-workspace 7). Re-read all 4 claimed local re-inventions and confirmed each exists: readPatientShareApiJson at card-workspace.tsx:374 (used :1006/:1031), readErrorMessage at saved-views-content.tsx:213 (used :260/:279), fetchJson at dispensing-workbench.adapter.ts:142 and mutateJson at :416. Re-read client-json.ts:12-22 — readApiErrorMessage handles both 'message' and 'error' keys, so it is envelope-compatible as claimed. Sampled drug-master-content sites (1500/1532/1578/1616/1657/1696/1725/1762): near-identical 4-line `json?.message ?? '...失敗しました'` throw boilerplate. ~116/194 sites are followed by `throw new Error` within 2 lines (mechanically convertible); the remainder matches the candidate's own carve-out (return-null / detail-consuming sites, e.g. fetchJson returns T|null). Dedup check: grepped both findings files — F03 is a single-site bug fix cited only as evidence of the drift class; no existing epic covers readApiJson adoption sweep. Caveats folded into safety score: (a) readApiJson throws fallbackMessage on 2xx-with-unparseable-body while some hand-rolled sites tolerate null json on ok responses — a real per-site semantic difference the risk section only partially names; (b) drug-master-content.test.tsx mocks fetch (48 refs) but greps 0 '失敗しました' strings, so error-message text is not uniformly test-locked per screen.

- [ ] **Codex cross-review**:

## R41 [18] type-drift (effort M) — Visit-schedule billing preview wire contract duplicated FE (day-view.shared.ts) vs server (billing-requirement-validator / visit-schedule-billing-preview) with type-widening drift; BillingCadencePreview is 9-field byte-identical copy

- Anchor: `src/app/(dashboard)/schedules/day-view.shared.ts:89` (lens type-contract-drift)
- Files: `src/app/(dashboard)/schedules/day-view.shared.ts`, `src/server/services/billing-requirement-validator.ts`, `src/server/services/visit-schedule-billing-preview.ts`, `src/app/api/visit-schedule-proposals/billing-preview/route.ts`
- Scores: payoff 3 / safety 4 / verify 4 / scope 4 / priority 3 = **18**
- Evidence: Three payload types are declared twice: BillingRequirementAlert at day-view.shared.ts:89 (type: string) vs billing-requirement-validator.ts:45 (type: BillingAlertType union, severity: BillingAlertSeverity); BillingCadencePreview at day-view.shared.ts:97 vs billing-requirement-validator.ts:151 (10 fields, byte-identical); VisitScheduleBillingPreview at day-view.shared.ts:124 vs visit-schedule-billing-preview.ts:30 with drift in 3 fields (FE recommended_visit_type: VisitType vs BE string; FE recommended_priority: VisitPriority vs BE 'normal'|'urgent'|'emergency'; FE site_config_status: local SiteConfigStatus vs BE BillingRuntimeSiteConfigStatus; FE HomeComprehensivePreview vs BE BillingRuntimeHomeComprehensive). Served by api/visit-schedule-proposals/billing-preview/route.ts; FE copy consumed by 5 files (day-view.shared.ts, schedule-day-planner.ts, schedule-day-planner-hooks.ts, schedule-proposals-content.tsx, schedule-weekly-optimizer.tsx). day-view.shared.ts is 909 lines with 23 importers.
- Proposal: Extract the wire contract (VisitScheduleBillingPreview + BillingRequirementAlert + BillingCadencePreview + SiteConfigStatus + HomeComprehensivePreview) to src/types/api/visit-schedule-billing-preview.ts following the existing src/types/schedule-day-board.ts precedent; server services declare their return types as the contract, day-view.shared.ts re-exports/imports it. Also trims the oversized day-view.shared.ts.
- Payoff: Removes ~50 duplicated lines and 6 union-mismatch drift points on a billing-critical payload; any future field addition to the preview is compile-checked FE and BE.
- Risk: Reconciling the widened FE unions (string vs BillingAlertType) could surface latent FE assumptions; pure type move otherwise. Locked by day-view.shared.test.ts, billing-preview route tests, and schedule proposal flow tests (proposal-human-decision-flow.test.tsx).
- Verifier (adversarial): Re-verified every claim. (1) wc -l: day-view.shared.ts=909, billing-requirement-validator.ts=829, visit-schedule-billing-preview.ts=1104, billing-preview route=88. (2) BillingRequirementAlert declared twice: day-view.shared.ts:89 with `type: string` vs billing-requirement-validator.ts:45 with `type: BillingAlertType` (8-member union at :33) — confirmed widening drift. (3) BillingCadencePreview byte-identical at day-view.shared.ts:97 and billing-requirement-validator.ts:151 — but it has 9 fields, not 10 as claimed (monthly_cap, current_month_count, remaining_month_count, weekly_cap, current_week_count, scheduled_dates_current_month, next_billable_date, suggested_dates, reason). (4) VisitScheduleBillingPreview drift confirmed by reading both: FE (day-view.shared.ts:124) recommended_visit_type: VisitType / recommended_priority: VisitPriority vs server (visit-schedule-billing-preview.ts:30) string / 'normal'|'urgent'|'emergency'. The priority difference is nominal-only (visitPriorityValues in src/lib/validations/visit-schedule.ts:81 is exactly ['normal','urgent','emergency']), but recommended_visit_type drift is REAL and unsound: the route (billing-preview/route.ts:21) passes raw unvalidated searchParams.get('visit_type') which the server echoes back at visit-schedule-billing-preview.ts:968, so the FE's VisitType narrowing is a lie for adversarial query params — unification must decide validate-at-boundary vs widen. (5) SiteConfigStatus (day-view.shared.ts:108) and BillingRuntimeSiteConfigStatus (billing-runtime-context.ts:22) are identical 5-member unions; HomeComprehensivePreview (day-view.shared.ts:115) and BillingRuntimeHomeComprehensive (billing-runtime-context.ts:36) are identical 6-field shapes — pure moves. (6) FE consumer count confirmed: exactly the 5 claimed files reference VisitScheduleBillingPreview in schedules/ (grep), and day-view.shared has exactly 23 non-test importers (grep -l | wc -l = 23). (7) Test locks exist: day-view.shared.test.ts, billing-preview/route.test.ts, billing-preview-batch/route.test.ts, proposal-human-decision-flow.test.tsx, visit-schedule-billing-preview.test.ts, billing-requirement-validator.test.ts. (8) src/types/schedule-day-board.ts precedent exists (ls src/types). Dedup: grep of ops/refactor/ULTRACODE_FINDINGS_20260702.md and ULTRACODE_EXPANSION_MASTER_TARGETS.md for day-view.shared/billing-preview/these type names hits only the visit-schedule-billing-preview N+1 perf finding (services-perf-n+1, :1031) — different root cause; F30 is workflow-dashboard drift, different file-set. Not a duplicate. Caveat lowering safety slightly: reconciling recommended_visit_type is not a pure type move — either the route gains visit_type validation (observable behavior change for invalid params) or FE consumers must handle string; the rest (cadence, alert, site-config, home-comprehensive) is mechanical and tsc-locked.

- [ ] **Codex cross-review**:

## R42 [18] dup-helper (effort S) — VisitVehicleResourceOption re-declared byte-identically in 2 FE files despite exported canonical copy; VisitVehicleResourcesResponse envelope triplicated (proposals-content, weekly-optimizer, admin vehicles)

- Anchor: `src/app/(dashboard)/schedules/proposals/schedule-proposals-content.tsx:232` (lens type-contract-drift)
- Files: `src/app/(dashboard)/schedules/schedule-day-planner-hooks.ts`, `src/app/(dashboard)/schedules/proposals/schedule-proposals-content.tsx`, `src/app/(dashboard)/schedules/proposals/schedule-weekly-optimizer.tsx`, `src/app/(dashboard)/admin/vehicles/vehicles-content.tsx`, `src/app/(dashboard)/schedules/day-view.shared.ts`
- Scores: payoff 2 / safety 4 / verify 5 / scope 5 / priority 2 = **18**
- Evidence: schedule-day-planner-hooks.ts:22 already EXPORTS VisitVehicleResourceOption (VisitVehicleResourceSummary & {available; site}), yet schedule-proposals-content.tsx:232 and schedule-weekly-optimizer.tsx:129 re-declare it byte-identically as local types. VisitVehicleResourcesResponse ({data; total_count?; visible_count?; hidden_count?; truncated?}) is declared 3x: those two files (:239, :129 block) plus vehicles-content.tsx:62 (over a locally-declared VisitVehicleResource row). All three mirror GET /api/visit-vehicle-resources (route.ts:99). The base VisitVehicleResourceSummary lives at day-view.shared.ts:72 and is imported by 4 files.
- Proposal: Move VisitVehicleResourceOption + VisitVehicleResourcesResponse next to VisitVehicleResourceSummary in day-view.shared.ts (or keep the hooks export) and import in the 3 consumers; vehicles-content's row type becomes the shared summary + admin extras. Subsumable under candidate 1's TruncatedListResponse<T> for the envelope part.
- Payoff: Deletes 3 duplicate envelope declarations and 2 duplicate Option declarations (~35 lines); vehicle payload changes propagate compile-checked to admin and both scheduling surfaces.
- Risk: Pure type dedup; locked by vehicles-content tests, schedule proposal tests (proposal-human-decision-flow.test.tsx), and visit-vehicle-resources route tests.
- Verifier (adversarial): Re-ran greps and re-read all copies. Confirmed: hooks.ts:22 exports VisitVehicleResourceOption; proposals-content.tsx:232 and weekly-optimizer.tsx:121 (not :129 as stated — that's the Response) re-declare it byte-identically. VisitVehicleResourcesResponse envelope {data; total_count?; visible_count?; hidden_count?; truncated?} declared 3x (proposals-content:239, weekly-optimizer:129, vehicles-content:62) matching route.ts response keys. VisitVehicleResourceSummary at day-view.shared.ts:72 has 4 importers. Tests exist (vehicles-content.test.tsx, schedule-proposals-content.test.tsx, schedule-weekly-optimizer.test.tsx, route.test.ts), though the cited proposal-human-decision-flow.test.tsx does not exist. Not in F01-F89/CE/N/X exclusions (grep of both ops/refactor docs found no vehicle-resource finding). Caveat: admin VisitVehicleResource has max_stops: number vs Summary's number | null, so folding the admin row into Summary+extras is not purely mechanical — the safe core is the 2 Option re-declarations + shared envelope generic; admin row unification is a second step.

- [ ] **Codex cross-review**:

## R43 [18] test-harness-dup (effort M) — stubFetch reimplemented 26 times in two divergent families (fake {ok:true} object vs real Response URL-dispatcher)

- Anchor: `src/app/(dashboard)/handoff/handoff-workspace.test.tsx:145` (lens test-harness-dup)
- Files: `src/app/(dashboard)/handoff/handoff-workspace.test.tsx`, `src/app/(dashboard)/patients/[id]/safety-check/safety-check-content.test.tsx`, `src/app/(dashboard)/patients/[id]/consent/consent-records-content.test.tsx`, `src/app/(dashboard)/settings/operational-policy-content.test.tsx`
- Scores: payoff 4 / safety 4 / verify 4 / scope 3 / priority 3 = **18**
- Evidence: 26 'function stubFetch\*' definitions across 23 files; 104 test files call vi.stubGlobal('fetch') (277 call sites); 37 files hand-roll `vi.fn(async (input: RequestInfo...)` URL dispatchers. Two drifting families: (A) fake object `{ ok: true, json: async () => ... } as Response` (safety-check-content.test.tsx:243, operational-policy-content.test.tsx:181, 23 occurrences) which cannot express status/headers/ok:false, and (B) real `new Response(JSON.stringify(...), {status})` dispatchers (handoff-workspace.test.tsx:145 = 96 lines, consent-records-content.test.tsx:128 = 55 lines, report-share-workspace.test.tsx:327 = 32 lines). Family-A drift is the exact mechanism behind the handoff-comments silent-bug precedent: handlers that always return ok:true hide error-path regressions.
- Proposal: Add src/test/fetch-stub.ts exporting stubFetchRoutes(routes: Array<{match: string|RegExp, method?, response: unknown, status?}>) that always returns real Response objects and throws on unmatched URLs (fail-loud instead of silent undefined), plus stubFetchOnce(json, {status=200}) for the single-endpoint case, both returning the vi.fn for call assertions and auto-registering vi.unstubAllGlobals cleanup. Migrate family-A files first since they are the drift hazard.
- Payoff: Removes ~600+ lines of per-file dispatcher code; unmatched-URL throw converts silent false-empty test passes into loud failures (matches the repo's fail-close FE pattern); a single Response-shape SSOT ends the ok:true-object family.
- Risk: Family-A tests currently pass a non-Response object; switching to real Response can change json()/ok semantics a component observes — each migrated suite must stay green as the behavior lock. Handlers with per-test conditional logic (handoff cockpitStatus option) need the route-list form, not a flag rewrite.
- Verifier (adversarial): Re-ran every grep myself: 26 `function stubFetch` definitions across 23 test files (exact match to claim); 104 files / 277 call sites of vi.stubGlobal('fetch'); 39 files (candidate said 37, actual slightly higher) hand-roll `vi.fn(async (input: RequestInfo...)` URL dispatchers. Re-read all four anchor copies at the exact claimed lines: handoff-workspace.test.tsx:145 is a real-Response URL dispatcher ending at line 240 (96 lines, as claimed); consent-records-content.test.tsx:128 and reports/report-share-workspace.test.tsx (stubFetch near line 327) are the same family-B `new Response(JSON.stringify(...), {status})` shape; safety-check-content.test.tsx:243 and operational-policy-content.test.tsx:181 are family-A fake objects `mockResolvedValue({ ok: true, json: async () => ... } as Response)` — that exact pattern actually appears 61 times across 48 files, larger than the candidate stated. The two families are genuinely divergent (family A cannot express status/headers/ok:false). Not intentional decoupling: src/test/dom-test-utils.ts already exists and is imported by 228 test files, so a shared src/test/fetch-stub.ts follows established repo precedent. Dedup check: grepped ops/refactor/ULTRACODE_FINDINGS_20260702.md and ULTRACODE_EXPANSION_MASTER_TARGETS.md for stubFetch/stubGlobal/test-harness/fetch-stub — zero hits, so this is a new (file-set, root-cause) pair. Test-only change, no React Compiler interaction, prod behavior untouched. Real residual risk correctly stated in the candidate: family-A migration swaps fake objects for real Response (JSON.stringify round-trip loses reference identity/non-serializable fixtures) and fail-loud unmatched-URL throw may surface routes tests silently ignored today — both are per-suite migration costs, caught by the suite staying green.

- [ ] **Codex cross-review**:

## R44 [18] pattern-inconsistency (effort L) — Converge client fetch error handling on readApiJson — 222 fixed-message `if (!res.ok) throw` sites and 66 hand-rolled envelope readers vs 13 adopters of the shared helper

- Anchor: `src/lib/api/client-json.ts:56` (lens state-ui-consistency)
- Files: `src/lib/api/client-json.ts`, `src/app/(dashboard)/admin/drug-masters/drug-master-content.tsx`, `src/app/(dashboard)/views/saved-views-content.tsx`, `src/app/(dashboard)/schedules/proposals/schedule-weekly-optimizer.tsx`, `src/app/(dashboard)/patients/[id]/medications/medications-content.tsx`, `src/app/(dashboard)/visits/[id]/visit-record-detail.tsx`, `src/app/(dashboard)/reports/[id]/share/interprofessional-share-content.tsx`, `src/app/(dashboard)/conferences/conferences-content.tsx` 他
- Scores: payoff 4 / safety 3 / verify 4 / scope 3 / priority 4 = **18**
- Evidence: `grep -rnE "if \(!(res|response)\.ok\) throw new Error\("` over src (excl. tests) = 222 sites / 100 files that discard the API error envelope and throw a fixed string (e.g. patients/compare/compare-board.tsx:35, top offender drug-master-content.tsx with 20 sites). A further 66 sites / 28 files hand-duplicate envelope reading via `payload?.message ?? ...` ternaries. The shared reader `readApiJson` (src/lib/api/client-json.ts:56, with fallbackMessage + optional zod schema, locked by client-json.test.ts) has only 13 non-test adopter files. Known findings F03/F69 cover the `.error`-vs-`.message` misread bug only, not this bypass/duplication cluster.
- Proposal: Per-screen slices: replace `const res = await fetch(...); if (!res.ok) throw new Error('固定文言'); return res.json()` with `return readApiJson(res, '固定文言')` (identical fallback string keeps the no-envelope path byte-identical), and replace the 66 manual `payload?.message ?? fallback` readers with the same call. Optionally add a `fetchApiJson(url, init, fallback)` wrapper next to readApiJson to also absorb the fetch line.
- Payoff: One envelope-reading code path instead of ~290 hand-rolled ones (~250 lines of boilerplate removed); actionable server messages (409 conflicts, validation details) surface in toasts instead of generic fallbacks, making the F03 bug class structurally impossible; schema option enables incremental response validation.
- Risk: Behavior changes only when the server supplies an envelope message: toasts show the server message instead of the fixed string (this is the repo's stated intended pattern per F69 reference reader schedule-team-board.tsx:108). Component tests that mock fetch rejections with envelope bodies and assert the fixed toast text will need updating; do it screen-by-screen with the existing per-screen suites as the gate. client-json.test.ts locks the helper itself.
- Verifier (adversarial): Re-ran every count myself: `grep -rnE "if \(!(res|response)\.ok\) throw new Error\("` over src excl. tests = exactly 222 sites / 100 files (90 under src/app/(dashboard), rest in components/phos/api); drug-master-content.tsx = 20 sites confirmed via grep -c; spot-read compare-board.tsx:35 confirms the exact shape (fetch → throw fixed string → res.json(), envelope discarded). readApiJson exists at src/lib/api/client-json.ts:56 with fallbackMessage + optional schema and reads both message/error keys (lines 12-22), locked by src/lib/api/client-json.test.ts (verified present); non-test adopters = 14 files minus the helper itself = 13, matching the claim. Hand-rolled envelope readers: my grep for `\?\.(message|error) \?\?` excl. tests/helper = 89 sites / 34 files — MORE than the claimed 66/28 (candidate undercounted; spot-reads of auth pages confirm they are byte-for-byte the pattern readApiJson replaces). Dedup: F03 (schedule drawer .error misread, already fixed as RR-FE-20260702-F03) and F69 (sweep note: that was the only misreader) cover the wrong-key bug class only; neither ULTRACODE_FINDINGS F01-F89 nor ULTRACODE_EXPANSION_MASTER_TARGETS contains any readApiJson/envelope-convergence item (grep for readApiJson/envelope/client-json in both files = no refactor epic). Different (file-set, root-cause) from all excluded findings. Caveats folded into scores: (1) not strictly behavior-preserving — when the server supplies an envelope message, toasts change from fixed string to server message (repo-intended per F03/F69 reference reader, but component tests asserting fixed toast text will need updates); (2) a minority of the 222 sites are not envelope endpoints or interpolate status (e.g. src/phos/api/offlineEvidenceQueue.ts:356 uses HTTP ${response.status}) so per-site judgment is required, and the ok-but-non-JSON path changes from SyntaxError to Error(fallback); (3) 100-file blast radius makes this an L, multi-slice program, not one PR.

- [ ] **Codex cross-review**:

## R45 [17] pattern-inconsistency (effort S) — Pre-ErrorState-era plain text-destructive error branches (raw error.message, no retry) survive in 5 sites (patient-insurance/labs/readiness/workflow-preview cards + admin settings) — align to ErrorState convention; medication-calendar already converged

- Anchor: `src/app/(dashboard)/patients/[id]/patient-insurance-card.tsx:728` (lens fe-panel-boilerplate)
- Files: `src/app/(dashboard)/patients/[id]/patient-insurance-card.tsx`, `src/app/(dashboard)/patients/[id]/patient-labs-card.tsx`, `src/app/(dashboard)/patients/[id]/medication-calendar/medication-calendar-content.tsx`, `src/app/(dashboard)/admin/settings/settings-content.tsx`
- Scores: payoff 3 / safety 4 / verify 4 / scope 4 / priority 2 = **17**
- Evidence: 4 sites still use the old ladder branch `query.error instanceof Error ? (<p role="status" aria-live="polite" className="text-sm text-destructive">{error.message}</p>)`: patient-insurance-card.tsx:728-731, patient-labs-card.tsx:417-420, medication-calendar-content.tsx:345, admin/settings/settings-content.tsx:313-314 (bare <div text-destructive>). All other ~84 ladder sites converged on ErrorState variant=server + 再試行/refetch (error-state.tsx is imported by 81 non-test files). These 4 render raw Error.message (leaks internal fetch copy), give no retry affordance, and misuse role=status for an error.
- Proposal: Replace each with the repo-standard ErrorState variant=server size=inline + refetch action (drops straight into candidate 1's QueryPanelBody if that lands first), keeping the current message as description.
- Payoff: Last 4 outliers of the fail-close migration converge; patients' insurance/labs cards (medical data) gain the retry affordance every sibling card already has; removes the only remaining raw error.message rendering in dashboard ladders.
- Risk: This is a small intentional UI change (adds retry button, swaps role=status→ErrorState markup), not pure consolidation — flagging per scout mandate. patient-insurance-card/patient-labs-card/medication-calendar tests exist; grep shows none assert the destructive <p> markup, but each test file should get the standard error-state assertion added in the same commit.
- Verifier (adversarial): Re-ran the greps and re-read every claimed site. Root cause (pre-ErrorState-era plain destructive error branches, raw error.message, no retry) is real and NOT a duplicate of the known FE false-empty fail-close findings (those are unhandled-isError fail-open bugs in different files; here errors ARE surfaced but with legacy markup). But the inventory needs correction: (1) medication-calendar-content.tsx:345 is ALREADY converged — lines 346-362 have fixed copy + role="alert" + AlertTriangle + 再試行 refetch button, with a comment explicitly stating raw error strings are not shown — drop it; (2) two sites were missed: patient-readiness-card.tsx:73-75 and patient-workflow-preview-card.tsx:79-83 use the identical <p role="status" text-destructive>{error.message}</p> markup. Corrected set is 5 files: patient-insurance-card.tsx:729, patient-labs-card.tsx:418, patient-readiness-card.tsx:73, patient-workflow-preview-card.tsx:79, admin/settings/settings-content.tsx:314. Verified error-state is imported by 84 non-test files, siblings pass error.message as ErrorState detail (patients-board:875, safety-check:499, visits-today:343), and settings-content.test.tsx already asserts ErrorState-style retryable errors for two sibling queries in the same file, making :314 an intra-file outlier. No test asserts the legacy markup (grep of all 5 test files), so the swap is not test-blocked, though it is a small intentional UI change (adds retry, role=status→alert semantics) requiring new test assertions per site.

- [ ] **Codex cross-review**:

## R46 [17] pattern-inconsistency (effort S) — Adopt buildCursorPage in the ~20 route files that hand-roll hasMore/slice/nextCursor (exclude conference-notes dual-overflow, external-access null-cursor, pharmacist-shifts optional-limit variants)

- Anchor: `src/app/api/medication-cycles/route.ts:104` (lens be-route-boilerplate)
- Files: `src/lib/api/pagination.ts`, `src/app/api/medication-cycles/route.ts`, `src/app/api/care-reports/route.ts`, `src/app/api/drug-masters/route.ts`, `src/app/api/visit-schedule-proposals/route.ts`, `src/app/api/external-access/route.ts`
- Scores: payoff 3 / safety 3 / verify 4 / scope 4 / priority 3 = **17**
- Evidence: buildCursorPage (src/lib/api/pagination.ts:8-21) is used by 21 route files, but 25 other route files containing `hasMore` never import it and re-implement the same take-limit+1/slice/nextCursor math inline (44 `take: ... + 1` sites overall). Example: medication-cycles/route.ts:104-112 — `const hasMore = cycles.length > limit; const page = hasMore ? cycles.slice(0, limit) : cycles; ... nextCursor: hasMore ? String(offset + limit) : undefined` — semantically identical to buildCursorPage with `cursorOf: () => String(offset + limit)`. Other hand-rollers include patients/[id]/prescriptions, external-access, drug-masters, conference-notes, pharmacy-drug-stocks, visit-schedule-proposals, care-reports (25 total via grep -L buildCursorPage over hasMore route files).
- Proposal: Mechanical adoption: replace each inline hasMore/slice/nextCursor block with buildCursorPage(rows, limit, cursorOf) — offset-cursor routes pass `() => String(offset + limit)`, id/keyset routes pass `(row) => row.id`. No signature or response-shape change (PaginatedResponse already matches the {data, hasMore, nextCursor} envelope these routes emit).
- Payoff: One pagination-boundary implementation instead of 26; kills the class of off-by-one/last-page drift bugs at the seam (each hand-rolled copy is 4-6 lines, ~120 lines deleted) and makes totalCount/nextCursor conventions greppable from a single helper.
- Risk: Pure expression-level substitution; each route's list test asserts hasMore/nextCursor on boundary pages (e.g. medication-cycles route tests), locking behavior. Watch the one variance: some copies slice with `[...rows]` vs in-place — buildCursorPage copies defensively, strictly safe.
- Verifier (adversarial): Independently re-ran greps: 21 route files use buildCursorPage, 28 contain hasMore, 25 lack the import (file list reproduced exactly); 50 take:+1 sites (candidate said 44, immaterial). Re-read src/lib/api/pagination.ts:8-21 and the copies: medication-cycles:104-112, drug-masters:162-240, consent-records:195-198, cases:214-221, billing-candidates:316-318, qr-scan-drafts:249-251, patients/[id]/prescriptions:377-379, visit-records:958-994 are genuine drop-ins (offset/id/keyset cursors all fit cursorOf). CONFIRMED with corrections: (1) ~5 of the 25 are variants, not pure substitutions — external-access:94-101 emits nextCursor:null (JSON-serialized, vs undefined omitted → shape change if swapped), conference-notes:292-301 has dual-overflow hasMore logic (not the same math), pharmacist-shifts:108-109 has an optional-limit branch, me/sites and pharmacy-drug-stocks are hasMore-only; (2) test-lock claim overstated — all 25 have route tests but 9/25 have zero hasMore/nextCursor boundary assertions (cases, billing-candidates, me/sites, pharmacy-drug-stocks, inquiry-records, facilities/[id]/patients, admin/uat-feedback, admin/external-professionals/[id]/patients, pharmacist-shifts x2). Dedup: grepped both ops/refactor findings files — existing cursor-pagination items (FE fetchAllCursorPages over-fetch, dispense-queue no-pagination) have different root causes. Realistic scope: ~20 mechanical adoptions, exclude or adapt the 3 variant routes, add boundary assertions where missing before swapping.

- [ ] **Codex cross-review**:

## R47 [17] type-drift (effort M) — Conference-note JSON column shapes (ActionItem, Participant) duplicated 4x/3x across FE, sync service, tasks route, and pdf record despite canonical zod schemas already existing in src/lib/validations/conference.ts — consolidate via z.infer exports

- Anchor: `src/server/services/conference-sync.ts:70` (lens type-contract-drift)
- Files: `src/app/(dashboard)/conferences/conferences-content.tsx`, `src/server/services/conference-sync.ts`, `src/app/api/conference-notes/[id]/tasks/route.ts`
- Scores: payoff 3 / safety 4 / verify 4 / scope 4 / priority 2 = **17**
- Evidence: ActionItem is declared 3x for the same conference_notes.action_items JSON column: conference-sync.ts:70 and tasks/route.ts:17 ({title?: string; assignee?; converted_task_id?; converted_at?}) vs conferences-content.tsx:105 which requires title: string. Participant is declared 2x with real shape drift: conferences-content.tsx:70 has required name/role plus external_professional_id (absent server-side), while conference-sync.ts:83 has all-optional fields plus organization_name (absent FE-side) — each copy under-describes what the other writes. action_items is touched by 4 API route files (conference-notes/route.ts, [id]/route.ts, [id]/tasks/route.ts, [id]/generate-report/route.ts). (src/types/dashboard-home.ts:13 ActionItem is an unrelated concept — name collision only.)
- Proposal: Define zod schemas (conferenceActionItemSchema, conferenceParticipantSchema) in src/lib/validations/ or a src/types/conference-notes.ts contract, with the union of observed fields, and z.infer types imported by all three sites; tasks/route.ts already parses with zod (convertActionItemSchema) so this extends an existing pattern.
- Payoff: Single source of truth for a schemaless JSON column read/written from 5 files; the external_professional_id / organization_name blind spots become visible and validated at the API boundary.
- Risk: Reconciling required-vs-optional title needs a deliberate decision (FE requires it, persistence layer does not) — narrowing could reject legacy rows, so the shared schema should keep BE optionality. Locked by conferences-content.test.tsx and the four conference-notes route tests.
- Verifier (adversarial): Independently re-verified all copies by reading them: ActionItem all-optional shape is byte-identical at conference-sync.ts:70 and tasks/route.ts:17, while conferences-content.tsx:105 requires title — and there is a 4th copy the candidate missed (pdf-conference-note-record.ts:29 ConferenceNoteActionItem). Participant drift confirmed exactly as claimed (FE :70 requires name/role + external_professional_id, sync :83 all-optional + organization_name), with a 3rd copy at pdf-conference-note-record.ts:20. grep -l confirms 4 conference-notes route files touch action_items, all test-covered. Material correction: the proposed conferenceActionItemSchema/conferenceParticipantSchema ALREADY EXIST at src/lib/validations/conference.ts:24/:13 with those exact names, already used by 3 of the 4 routes, and the participant schema already holds the union of drifted fields — so the refactor reduces to exporting z.infer types (plus a loose persisted-read type) and replacing 7 local declarations with imports, i.e. reuse-existing-Z, safer than stated. Not intentional decoupling (the canonical write schema proves SSOT intent). Dedup grep of ULTRACODE_FINDINGS/MASTER_TARGETS: conference hits are only F36/F37 (false-empty), F30 (workflow-dashboard types, different file-set), CXR2-TZ02 (TZ) — no overlap. Caveat: conferences-content.test.tsx is thin (path validation only per F36 verifier), so keep the change type-only and rely on typecheck + the four route suites.

- [ ] **Codex cross-review**:

## R48 [17] test-harness-dup (effort M) — next/navigation and use-org-id mocks duplicated across 40 and 123 test files with shape drift (61 useOrgIdMock vs 57 inline vs 5 other)

- Anchor: `src/app/(dashboard)/patients/[id]/consent/consent-records-content.test.tsx:11` (lens test-harness-dup)
- Files: `src/app/(dashboard)/patients/[id]/consent/consent-records-content.test.tsx`, `src/app/(dashboard)/tasks/tasks-content.test.tsx`, `src/app/(dashboard)/patients/[id]/card-workspace.test.tsx`, `src/app/(dashboard)/patients/[id]/medications/print/page.test.tsx`
- Scores: payoff 3 / safety 4 / verify 4 / scope 3 / priority 3 = **17**
- Evidence: vi.mock('next/navigation') in 40 test files, each exporting a different partial surface (useRouter only at card-workspace.test.tsx:28; useParams only at medications/print/page.test.tsx:16; useParams+useSearchParams; redirect only at visit-records/page.test.ts:6; router return-shape drift: {replace} x6 vs {replace,push} x4). vi.mock('@/lib/hooks/use-org-id') in 123 files split between `useOrgId: useOrgIdMock` (56) and inline `useOrgId: () => 'org_1'` (55) plus 4 other shapes.
- Proposal: Add src/test/next-mocks.ts exporting complete factory objects designed for use inside per-file vi.mock callbacks: createNavigationMock({params, searchParams}) returning the FULL surface (useRouter with push/replace/back/prefetch/refresh spies, useParams, usePathname, useSearchParams, redirect) plus a defaultUseOrgIdMock('org_1'). Files keep their one-line vi.mock('next/navigation', () => navMock) but the mock body converges.
- Payoff: Ends the partial-surface failure class: today a component adding router.push to a screen whose test mocked only {replace} throws 'push is not a function' and gets patched ad hoc in each file; ~400 lines of mock bodies converge to one SSOT.
- Risk: vi.mock hoisting means the factory must be imported inside the mock callback (supported by vitest via async factory or top-level import referenced lazily); tests asserting 'replace was called, push was NOT' still work since all fns are distinct spies. Migrate opportunistically, no big-bang.
- Verifier (adversarial): Re-ran all greps: exactly 40 files mock next/navigation and 123 mock @/lib/hooks/use-org-id. Re-read the cited copies and confirmed partial-surface drift (useRouter-only card-workspace.test.tsx:28; useParams-only medications/print/page.test.tsx:16; redirect-only visit-records/page.test.ts:6; useRouter+usePathname+useSearchParams tasks-content.test.tsx:32-36) and router return-shape drift ({replace} ~7 sites vs {replace,push} ~4 sites incl. search-content.test.tsx:136, notifications-content.test.tsx:95). My useOrgId tally is 61 useOrgIdMock / 57 inline () => 'org_1' / 3 orgIdMock.value / 2 other (candidate said 56/55/4 — minor count drift, same substance). src/test/dom-test-utils.ts already exists as shared-test-infra precedent. Dedup grep of ULTRACODE_FINDINGS/EXPANSION files shows only stale-vi.mock mentions inside dead-code findings — different file-set and root-cause, not a duplicate. Caveats verified but non-fatal: vi.mock hoisting requires async-factory import (candidate acknowledges); ~15 files reconfigure useRouterMock per-test so factory must expose spies; opportunistic migration risks a temporary third shape. Test-only, locked by suite green.

- [ ] **Codex cross-review**:

## R49 [17] pattern-inconsistency (effort S) — State-color stragglers never adjudicated by the migration ledger: patient-field-revision badges (sky/amber/slate, 2 consumers, only label 変更 test-locked) and settings-catalog scope badges (bg-red-100 text-red-800 family, 1 consumer)

- Anchor: `src/components/features/patients/patient-field-revision-presentation.ts:28` (lens state-ui-consistency)
- Files: `src/components/features/patients/patient-field-revision-presentation.ts`, `src/components/features/patients/patient-field-revision-timeline.tsx`, `src/app/(dashboard)/visits/[id]/visit-reflected-fields-card.tsx`, `src/lib/admin/settings-catalog.ts`, `docs/state-color-migration-map.md`
- Scores: payoff 3 / safety 3 / verify 4 / scope 5 / priority 2 = **17**
- Evidence: docs/state-color-migration-map.md claims 状態色ドリフト残ゼロ and lists every intentional raw-palette survivor, but neither file appears in the ledger. (1) patient-field-revision-presentation.ts:28-33 hand-rolls 追加=sky/解除=slate/変更=amber badge classes (2 consumers: patient-field-revision-timeline.tsx, visit-reflected-fields-card.tsx; zero test assertions on labels/colors — grep count 0 in both test files). These are change-kind states, the same boundary class the ledger already ruled on for card-workspace ACTIVITY_BADGE_CLASSES (S5: migrate to roles). (2) settings-catalog.ts:168-171 uses the literal 移行対象 pattern `bg-red-100 text-red-800` etc. for scope badges, where 赤=システム manufactures the exact false danger signal the ledger's KPI rule forbids.
- Proposal: Adjudicate both in docs/state-color-migration-map.md, then: revision badges → StateBadge/6-axis tokens per the ACTIVITY_BADGE_CLASSES precedent (追加→info, 解除→readonly, 変更→confirm or info); settings-catalog scope badges → either neutral default Badge (scope is a category, per the KPI/neutral rule) or an explicit カテゴリ型パレット exclusion entry — never leave them un-adjudicated.
- Payoff: Restores the ledger's 残ゼロ invariant (its core value is being exhaustive); removes 7 hand-rolled raw-palette class strings; revision badges gain icon+text non-color-dependent rendering via StateBadge (WCAG rule in CLAUDE.md).
- Risk: Purely visual; no tests lock the current colors/labels (verified 0 assertions), so regression risk is a design-judgment one: the 変更→role mapping and the category-vs-state call on settings-catalog need the ledger's boundary-case procedure (両 supervisor), not a unilateral sweep.
- Verifier (adversarial): Independently re-ran all greps: ledger (docs/state-color-migration-map.md, 360 lines, line 360 claims 残ゼロ) has 0 hits for revision/patient-field/settings-catalog; patient-field-revision-presentation.ts:28-33 confirmed hand-rolling sky/slate/amber badge classes with exactly 2 consumers (timeline.tsx:18, visit-reflected-fields-card.tsx:13,85); settings-catalog.ts:167-172 SCOPE_LABELS confirmed with bg-red-100 text-red-800 (システム=赤) family, sole consumer settings-content.tsx; repo-wide bg-red-100 text-red-800 grep = 3 lines in 2 files (only settings-catalog is unadjudicated). git log shows both files predate the ledger's last update (2026-06-17 and 2026-03-28 vs ledger 2026-06-21), so they were missed by the 残ゼロ audit. ACTIVITY_BADGE_CLASSES S5 precedent verified in ledger 境界事例 section; StateBadge exists with tests. One evidence correction: claim of zero label/color test assertions is slightly wrong — visit-reflected-fields-card.test.tsx:71 asserts getByText('変更'); no color/class assertions anywhere, so the label is test-locked and preserved by the proposal (colors unlocked). Dedup check: ops/refactor findings reference state-color only as dead-code context, not these files.

- [ ] **Codex cross-review**:

## R50 [17] dup-helper (effort S) — Converge 3 hand-rolled patientLabObservation latest-per-analyte query+loop copies onto listPatientLabSummary (coordinate cds/checker leg with F73's findFirst fix)

- Anchor: `src/server/services/patient-detail-labs.ts:15` (lens prisma-query-helpers)
- Files: `src/server/services/patient-detail-labs.ts`, `src/app/api/patients/[id]/route.ts`, `src/server/services/report-generator.ts`, `src/server/cds/checker.ts`
- Scores: payoff 3 / safety 3 / verify 4 / scope 4 / priority 3 = **17**
- Evidence: 4 copies of 'patientLabObservation.findMany measured_at desc → first-wins per analyte_code': the extracted service listPatientLabSummary (patient-detail-labs.ts:15-40, 3 consumers: patient-detail.ts:240, visit-brief.ts:361, patient-detail-workflow-preview.ts:206) plus 3 stragglers that hand-roll both query and loop: patients/[id]/route.ts:1501-1516 + 1543-1549 (adds id/value_text to select, else identical where/orderBy/take:50), report-generator.ts:345-361 + 447-452 (orderBy adds created_at desc tie-break; drift from the service's bare [measured_at desc]), cds/checker.ts:1555-1570 (dynamic analyteCodes, no take limit, first-wins at :1567).
- Proposal: Extend listPatientLabSummary with options `{ analyteCodes?: readonly LabAnalyteCode[]; select?: 'summary' | 'detail' }` (or add sibling `latestLabObservationsByAnalyte(db, {orgId, patientId, analyteCodes})` in patient-detail-labs.ts) and replace the 3 hand-rolled query+loop pairs; adopt the [measured_at desc, created_at desc] tie-break as the single ordering.
- Payoff: Lab 'latest value per analyte' — a clinically load-bearing selection — gets one definition instead of 4; the tie-break drift (report-generator has created_at fallback, the other 3 do not) is resolved explicitly instead of by accident; cds/checker gains the missing take bound.
- Risk: Unifying the tie-break changes which row wins when two observations share measured_at (currently insertion-order-dependent in 3 of 4 copies) — call it out and lock with a fixture; behavior otherwise preserved. Tests: cds/checker.test.ts, report-generator.test.ts, patients/[id]/route.test.ts + detail-slices.test.ts.
- Verifier (adversarial): Re-ran the greps and re-read all copies. grep 'patientLabObservation.findMany' (non-test) → exactly 4 latest-per-analyte sites + 1 correctly-excluded full-history route (patients/[id]/labs/route.ts:96). Verified: patient-detail-labs.ts:15-40 service (orderBy [measured_at desc], take 50, first-wins Map) with 3 consumers (patient-detail.ts:240, visit-brief.ts:361, patient-detail-workflow-preview.ts:206); patients/[id]/route.ts:1501-1518 + 1543-1549 identical query+loop (select adds id/value_text); report-generator.ts:345-362 + 447-452 with the claimed created_at-desc tie-break drift (real, and test-locked at report-generator.test.ts:437); cds/checker.ts:1555-1570 dynamic codes, no take, first-wins at 1566-1570. Not in the excluded dedup list; distinct (file-set, root-cause) from F73, though the checker leg overlaps F73 whose findFirst-per-analyte fix would diverge from the proposed findMany helper — the convergence should coordinate with F73. Corrections to the candidate: (1) patients/[id]/route.test.ts and detail-slices.test.ts contain zero lab_summary/analyte assertions (grep hits are label/action_label substrings only), and cds/checker.test.ts mocks findMany but never asserts the pt_inr/k monitoring path — so 2 of 4 sites are NOT test-locked, weaker than the risk section claims; new fixtures needed there. (2) The checker copy's 2-field select {analyte_code, value_numeric} is not covered by the proposed 'summary'|'detail' select option as written. Core duplication, drift, and concrete target shape all hold; refactor follows the existing extraction precedent.

- [ ] **Codex cross-review**:

## R51 [16] dup-component (effort M) — 3 near-identical icon-bubble KPI cards (staff SummaryCard, workflow MetricCard, performance KpiCard, ~87 lines) — extract one shared icon-stat-card and have external-viewer's variant adopt it; business-holidays (and optionally report-delivery) SummaryCards can adopt existing StatCard, whose docstring already claims this consolidation

- Anchor: `src/app/(dashboard)/admin/staff/staff-kpi-panel.tsx:223` (lens fe-panel-boilerplate)
- Files: `src/app/(dashboard)/admin/staff/staff-kpi-panel.tsx`, `src/app/(dashboard)/workflow/workflow-dashboard-view.tsx`, `src/app/(dashboard)/admin/operations-insights/operations-insights-content.tsx`, `src/app/(dashboard)/admin/performance/page.tsx`, `src/app/(dashboard)/admin/analytics/analytics-content.tsx`, `src/app/(dashboard)/admin/business-holidays/business-holidays-content.tsx`, `src/app/(dashboard)/reports/report-delivery-dashboard.tsx`, `src/app/(dashboard)/external/external-viewer-content.tsx` 他
- Scores: payoff 3 / safety 4 / verify 4 / scope 3 / priority 2 = **16**
- Evidence: src/components/ui/stat-card.tsx:32 docstring explicitly says 'KPI / 件数ストリップの共通カード。各画面で再実装されていた MetricCard/KpiCard/SummaryCard を統合する' yet ≥8 local re-implementations remain. Cluster A (same visual DNA — Card, muted label, `text-3xl font-bold tabular-nums` value, caption, `rounded-full border bg-background p-2` icon bubble): staff-kpi-panel.tsx:223-248 SummaryCard (26L), workflow-dashboard-view.tsx:1486-1511 MetricCard (26L), operations-insights-content.tsx:60-68 MetricCard (9L), performance/page.tsx:142-176 KpiCard (35L, +HelpPopover/unit/tone), analytics-content.tsx:548-581 MetricCard (34L, +isLoading pulse) = 130 lines / 5 copies. Cluster B (bare label+value, direct StatCard fit): business-holidays-content.tsx:727-736 SummaryCard (10L), report-delivery-dashboard.tsx:417-427 SummaryCard (11L), external-viewer-content.tsx:499-535 SummaryCard (37L, includes the isError→'—' degradation also inlined at business-holidays:594-601, billing-rules/page.tsx:555-558, medications-content.tsx:1100-1114). Excluded as genuinely different: metrics-dashboard-content.tsx:64-134 (progress/target, 71L), capacity KpiCard:83, billing-check KpiCard:58 (progress bars).
- Proposal: Cluster A: extract the icon-bubble card verbatim as one shared component (e.g. src/components/ui/icon-stat-card.tsx or a `size="lg" icon` variant of StatCard) and delete the 5 locals; Cluster B: replace the 3 trivial SummaryCards with existing StatCard, keeping the isError ? '—' : value expression at the call site (matching external-viewer's current behavior).
- Payoff: ~130+58 duplicated lines removed across 8 dashboards; KPI typography/spacing (currently drifting between text-2xl and text-3xl variants) gains one owner, and StatCard's docstring-stated consolidation intent is actually completed for the simple cases.
- Risk: Cluster A extraction is pixel-preserving (copies are near-identical; parameterize HelpPopover/unit/tone/isLoading). Cluster B StatCard adoption changes visuals slightly (2xl vs 3xl, left-border accent) — check each screen's .test.tsx (staff-kpi-panel, business-holidays, report-delivery, analytics have tests asserting labels/values, not markup) and get a quick /design-review pass; if pixel parity is required, keep Cluster B on the new Cluster-A component instead.
- Verifier (adversarial): Re-read every cited component. Confirmed: stat-card.tsx:32 docstring states the consolidation intent verbatim; only 3 real StatCard importers exist (grep). staff-kpi-panel.tsx:231-248 SummaryCard and workflow-dashboard-view.tsx:1486-1511 MetricCard are near-verbatim identical icon-bubble cards (only Card vs Card size="sm", string vs number value); performance/page.tsx:142-176 KpiCard is the same DNA + HelpPopover/unit/tone params. Inline isError→'—' verified at business-holidays:593-601 and billing-rules:555-558. Tests (staff-kpi-panel, business-holidays, workflow, performance, analytics, external-viewer) assert via getByText on labels/values, not markup, so extraction is behavior-locked. Dedup grep of ULTRACODE_FINDINGS/EXPANSION files finds no KPI-card consolidation item (F56 is unused-component dead code, different root cause). Corrections that shrink scope: operations-insights MetricCard (60-68) is a plain div/text-xl with NO icon — not the claimed DNA; analytics MetricCard (548-581) uses CardHeader inline icon, no bubble — different layout; external-viewer SummaryCard (499-535) is misfiled in Cluster B — it has the icon bubble + responsive sm: hiding + isError, so it must adopt the new Cluster-A component (with variants), not StatCard. Also report-delivery SummaryCard (417-427) carries an explicit design comment avoiding heavy-Card look (StatCard's calm div styling is compatible but a visible change). True verbatim Cluster A = 3 copies (~87 lines), Cluster B direct StatCard fit = business-holidays (+report-delivery with design-review pass).

- [ ] **Codex cross-review**:

## R52 [16] test-harness-dup (effort M) — createRequest NextRequest builder duplicated 255 times across API route tests

- Anchor: `src/app/api/visit-schedules/reorder/route.test.ts:75` (lens test-harness-dup)
- Files: `src/app/api/visit-schedules/reorder/route.test.ts`, `src/app/api/visit-schedules/[id]/conflict-reconfirmation/route.test.ts`, `src/app/api/visit-schedules/day-board/route.test.ts`
- Scores: payoff 3 / safety 4 / verify 4 / scope 3 / priority 2 = **16**
- Evidence: grep 'function createRequest' src/app/api --include='\*.test.ts' = 255 definitions (plus 1 makeRequest, 1 createRequestWithSearch), each 5-12 lines (~1,800 duplicated lines); 663 raw `new NextRequest(` call sites. All variants encode the same shape: localhost URL + {'content-type': 'application/json', 'x-org-id': 'org_1'} headers + JSON.stringify body, drifting only in method defaults and optional-header handling (compare reorder/route.test.ts:75 vs visit-schedules/route.test.ts:113 vs [id]/route.test.ts:143).
- Proposal: Add src/test/next-request.ts exporting createOrgRequest(url, {method='GET', body, orgId='org_1', headers}) returning NextRequest with the canonical header set, plus createAnonRequest for the no-header cases. Replace per-file definitions mechanically; per-route URL stays at the call site.
- Payoff: ~1,500+ lines deleted; a single place to change when request-building conventions change (e.g. new required header), instead of 255 copies; kills header-casing drift.
- Risk: Low — request construction only; the 364 route suites lock behavior. A few createRequest variants take positional args (body-only vs url+body) so the codemod needs per-file review, not blind sed.
- Verifier (adversarial): Re-ran all greps: 256 'function createRequest' definitions in 255 files under src/app/api/\*_/_.test.ts, plus 1 makeRequest and 1 createRequestWithSearch; exactly 663 'new NextRequest(' call sites. Re-read the 3 cited copies plus 8 random samples: all are 5-12 line wrappers of the same shape (localhost URL, content-type: application/json + JSON.stringify for bodies, optional x-org-id: org_1 — 107/256 windows include it), with drift limited to signature/method-default/header-optionality exactly as the candidate described. One sampled copy (pharmacy-visit-requests/[id]/decision) embeds expected_updated_at defaulting, confirming the per-file-review caveat is real. No existing shared builder (src/test has only dom-test-utils.ts). Dedup grep of ops/refactor/ULTRACODE_FINDINGS_20260702.md and ULTRACODE_EXPANSION_MASTER_TARGETS.md found zero createRequest/NextRequest mentions. Anchor file:line verified. Caveat on safety: this is test-only (prod behavior cannot regress), but a sloppy codemod could silently change which auth path a test exercises (e.g., adding x-org-id where deliberately omitted), so exact-header preservation per call site is required.

- [ ] **Codex cross-review**:

## R53 [16] query-helper (effort M) — Extract firstWinsByKey/groupIntoLists collection helpers for the 'findMany desc → keep newest per key' idiom (~21 first-wins sites + 62 grouping sites)

- Anchor: `src/server/services/pdf-visit-record.ts:183` (lens prisma-query-helpers)
- Files: `src/server/services/pdf-visit-record.ts`, `src/server/services/patient-status-tracker.ts`, `src/server/services/workflow-dashboard-sections.ts`, `src/server/services/prescription-intake-service.ts`, `src/server/services/patient-detail-documents.ts`, `src/server/services/patient-home-operations.ts`, `src/server/services/visit-brief.ts`, `src/server/services/visit-schedule-billing-preview.ts` 他
- Scores: payoff 3 / safety 4 / verify 4 / scope 3 / priority 2 = **16**
- Evidence: 25 copies of the first-wins loop `if (!map.has(k)) map.set(k, row)` after a desc-ordered findMany (grep for `if (!*.has(` followed by `.set(`), e.g. pdf-visit-record.ts:183 (auditLog created_at desc), patient-status-tracker.ts:196+242, workflow-dashboard-sections.ts:823 (latestCycleByCaseId AND latestCycleByPatientId in one loop), care-reports/route.ts:366 (deliveryRecord ordered [report_id asc, created_at desc, id desc]), day-board/route.ts:537, prescription-intake-service.ts:1479, jobs/daily/shared.ts:112, cds/checker.ts:1567, org/members/route.ts:41. Plus 61 occurrences in 27 files of the sibling grouping loop `map.get(k) ?? []` + push (e.g. visit-schedule-billing-preview.ts:272/335/348, day-board/route.ts:542). No collections helper exists in src/lib (checked src/lib/utils/, src/lib/collections.ts absent). Only 2 raw DISTINCT ON sites exist (patient-service.ts:537 precedent, patient-status-tracker.ts:220 deliberately avoids it).
- Proposal: Add src/lib/utils/collections.ts with `firstWinsByKey<T, K>(rows: Iterable<T>, keyOf: (row: T) => K | null | undefined): Map<K, T>` and `groupIntoLists<T, K>(rows: Iterable<T>, keyOf: (row: T) => K | null | undefined): Map<K, T[]>`; mechanically replace the 25 first-wins loops and highest-traffic grouping loops. Keep the patient-service.ts:537 DISTINCT ON as the documented DB-side escape hatch (`latestPerGroup` raw-SQL push-down stays a follow-up per-site perf decision, per CE11/N23/CXR2-PERF01 epics).
- Payoff: ~25+ hand-rolled loops (3-8 lines each, ~120 lines) collapse to one tested pure function; the 'is this latest-per-key or dedupe?' intent becomes explicit at each call-site; future DISTINCT ON push-downs get a single seam to target.
- Risk: Pure in-memory transform, behavior-preserving by construction as long as input iteration order is untouched; sites with multi-map single loops (workflow-dashboard-sections.ts:823) need two passes or a keep-loop. Locked by patient-status-tracker.test.ts, report-generator.test.ts, care-reports/route.test.ts, day-board route tests.
- Verifier (adversarial): Independently re-ran greps: dumped all 56 `if (!x.has(` sites in non-test src/server+src/app/api and classified them — ~21 genuine first-wins map-building sites (candidate said 25; a few listed sites are default-fills e.g. visit-schedule-billing-preview.ts:481, so slightly overstated but materially accurate). Grouping sibling `.get(k) ?? []` = 62 occurrences / 27 files (claimed 61/27). Re-read anchor copies confirming desc-ordered findMany + first-wins loop: pdf-visit-record.ts:167+183, care-reports/route.ts:358+366, workflow-dashboard-sections.ts:820-825 (dual-map, as disclosed), cds/checker.ts:1567, org/members/route.ts:41. No collections helper exists in src/lib (verified). Strong corroboration: pharmacists/route.ts:31-39 already hand-rolled a local generic dedupePharmacistsByUserId<T> = exactly firstWinsByKey. All 5 claimed lock-test files exist. Dedup: F34/F73/CXR2-PERF01/EPIC8 are perf/overfetch findings with DB push-down fixes — different root cause (loop duplication vs overfetch); candidate explicitly coordinates. Caveats: pdf-visit-record loop has a side effect (userIds.add) and patient-status-tracker:242 has a conditional transform, so ~4-5 sites are not purely mechanical; EPIC 8 push-downs would delete ~5 of the hottest loops, trimming payoff.

- [ ] **Codex cross-review**:

## R54 [15] dup-helper (effort M) — FE date-label formatting: formatOptionalDate duplicates lib SSOT formatDateLabel, plus 5 local formatter copies with real display drift (unpadded dates, UTC-slice clock leak, 4 empty-fallbacks) and 26 inline toLocaleDateString('ja-JP') sites — consolidate on src/lib/ui/date-format.ts, EXCLUDING formatDateDisplay (test-locked intentional UTC-slice semantics per ULTRACODE_FINDINGS_20260702.md:530 refutation)

- Anchor: `src/lib/ui/date-format.ts:24` (lens shared-helper-dupes)
- Files: `src/lib/ui/date-format.ts`, `src/lib/datetime/date-display.ts`, `src/lib/patient/home-visit-intake.ts`, `src/app/(dashboard)/workflow/pharmacy-cooperation/pharmacy-cooperation-workflow-content.tsx`, `src/components/features/visits/visit-medication-management-section.tsx`, `src/components/patient-mcs/patient-mcs-summary-card.tsx`, `src/app/(dashboard)/reports/report-share-workspace.tsx`, `src/app/(dashboard)/admin/pca-pumps/pca-pumps-content.tsx` 他
- Scores: payoff 4 / safety 2 / verify 4 / scope 3 / priority 2 = **15**
- Evidence: Three lib-level 'canonical' helpers coexist: formatDateLabel/formatDateTimeLabel (src/lib/ui/date-format.ts:24,35; 17 consumer files), formatDateDisplay (src/lib/datetime/date-display.ts:1; slice(0,10), fallback '-', 3 consumers), and formatOptionalDate (src/lib/patient/home-visit-intake.ts:389; semantically identical to formatDateLabel — parseISO→'yyyy/MM/dd', fallback '—', raw on unparseable — but lives in a patient-domain module; 6 consumer files). On top: local copies with behavioral drift — pharmacy-cooperation-workflow-content.tsx:695 formatDateTime does value.slice(0,16).replace('T',' ') (raw ISO slice: renders UTC clock time if the API returns a Z-suffixed instant, fallback '-'); visit-medication-management-section.tsx:173 formatDateLabel renders `${getFullYear()}/${getMonth()+1}/${getDate()}` (unpadded '2026/7/2', fallback '未設定'); patient-mcs-summary-card.tsx:8 formatDateTime (Intl MM/dd HH:mm, fallback '未記録'); report-share-workspace.tsx:89 formatDateTime (MM/dd + formatTimeOfDay); pca-pumps-content.tsx:363 formatDate (pins T00:00:00+09:00 then toLocaleDateString). Plus 27 inline `toLocaleDateString('ja-JP')` sites in 7 files (11 in admin/drug-masters/drug-master-content.tsx, 8 in reports/[id]/print/page.tsx).
- Proposal: Adopt src/lib/ui/date-format.ts as the single FE date-label SSOT (its pattern/fallback options already cover every variant above): (1) re-export/absorb formatOptionalDate and formatDateDisplay as thin pattern presets there, (2) replace the 5 local formatDate/formatDateTime copies with formatDateLabel/formatDateTimeLabel calls (explicit pattern 'MM/dd HH:mm' where screens intentionally use short form; keep per-screen fallback strings via the fallback option), (3) sweep the 27 inline toLocaleDateString sites file-by-file.
- Payoff: One place defines empty-fallback, padding, and TZ-parse policy for date labels; kills the drift class where the same timestamp renders as '2026/07/02', '2026/7/2', a UTC-shifted clock time, and four different empty markers depending on screen.
- Risk: Display-only, but drift IS the current behavior: the pharmacy-cooperation raw-slice copy and the unpadded copy will change rendered strings — component tests (e.g. visit-medication-management-section, pharmacy-cooperation-workflow-content tests) that assert current labels must be updated deliberately; do it screen-by-screen, not as one bulk regex.
- Verifier (adversarial): Independently re-verified every copy: date-format.ts:24/35 pattern+fallback API with 17 importer files (grep -rl = 17); home-visit-intake.ts:390 formatOptionalDate is near-identical to formatDateLabel defaults (parseISO→yyyy/MM/dd, '—', raw on unparseable; only edge diff is try/catch vs new Date re-parse), 6 referencing files; all 5 local copies re-read at claimed lines and drift is real — pharmacy-cooperation-workflow-content.tsx:695 slice(0,16) raw UTC slice (its own test fixtures feed Z-suffixed instants, so UTC clock leak is concrete), visit-medication-management-section.tsx:173 unpadded getMonth()+1 with '未設定', patient-mcs-summary-card.tsx:8 Intl MM/dd HH:mm '未記録', report-share-workspace.tsx:89 MM/dd+formatTimeOfDay, pca-pumps-content.tsx:363 +09:00-pinned toLocaleDateString; inline toLocaleDateString('ja-JP') = 26 sites in 7 files (12/8 in drug-master/print page incl. option-arg variants) — candidate's 27 is within rounding. Not a dup of F35/CE18/etc. HOWEVER two corrections: (1) proposal step absorbing formatDateDisplay duplicates an already-REFUTED adjudication at ops/refactor/ULTRACODE_FINDINGS_20260702.md:530 — date-display.test.ts locks slice(0,10)/'-' as intentional and slice=UTC-date vs parseISO+format=JST-date is a day-boundary semantic not expressible as a date-fns preset, so 'options already cover every variant' is false for that helper; carve it out, leaving 2 (not 3) consolidatable lib SSOTs. (2) The refactor is explicitly behavior-CHANGING at the 5 local-copy screens (unpadded→padded, UTC slice→JST render can shift the displayed day); tests exist for all 5 screens but assert current strings and must be deliberately rewritten, so this is display-normalization work, not a mechanical behavior-preserving extract.

- [ ] **Codex cross-review**:

## R55 [15] pattern-inconsistency (effort M) — 31 visible plain-text 「読み込み中...」 loading states (across 18 files) violate the skeleton guideline — replace with the existing Skeleton/SkeletonRows primitives

- Anchor: `src/app/(dashboard)/admin/uat/uat-content.tsx:463` (lens state-ui-consistency)
- Files: `src/components/ui/loading.tsx`, `src/app/(dashboard)/admin/uat/uat-content.tsx`, `src/app/(dashboard)/admin/drug-masters/drug-master-content.tsx`, `src/app/(dashboard)/patients/[id]/medications/medications-content.tsx`, `src/app/(dashboard)/schedules/proposals/schedule-proposals-content.tsx`, `src/app/(dashboard)/schedules/proposals/schedule-weekly-optimizer.tsx`, `src/app/(dashboard)/admin/pharmacy-sites/pharmacy-sites-content.tsx`, `src/app/(dashboard)/admin/data-explorer/data-explorer-content.tsx` 他
- Scores: payoff 3 / safety 3 / verify 4 / scope 3 / priority 2 = **15**
- Evidence: docs/ui-ux-design-guidelines.md:485 mandates 「ローディングはスケルトン（レイアウト形状を保つ）、スピナー単独は不可」. Grep for visible text nodes `>…読み込み中…<` (excluding aria-label/sr-only/Loading label props, excl. tests) = 31 sites across 21 files rendering a bare `<p|div className="text-sm text-muted-foreground">読み込み中...</p>` (uat-content has 4, drug-master-content 4, medications-content 4, schedule-proposals/weekly-optimizer 4, pharmacy-sites 2, data-explorer 2, ...). The compliant primitives already exist in src/components/ui/loading.tsx (Skeleton, SkeletonRows with role="status" + sr-only text) and are the majority pattern: 53 non-test files import SkeletonRows.
- Proposal: Replace each visible-text loading branch with `<SkeletonRows rows={n} cols={m} />` (list/table sections) or shaped `<Skeleton className=…/>` (cards/detail panes) from '@/components/ui/loading' — the exact pattern prescriptions-table.tsx:113-118 already uses. No new components.
- Payoff: Loading UX converges on the documented SSOT (layout-shape-preserving skeletons, no layout jump); a11y improves for free (SkeletonRows carries role=status/aria-label while most bare text nodes have neither); removes 31 ad-hoc one-off loading branches.
- Risk: Visual-only change; per repo convention several suites assert loading text (getByText('読み込み中...')) and will need to switch to role=status queries — run each screen's unit suite per slice. drug-master-content.tsx and its test are currently modified on this branch (concurrent-edit drift risk — re-diff before landing).
- Verifier (adversarial): Re-ran the greps myself: visible-text pattern (>読み込み中 / 読み込み中...<, excl. tests/aria-label/sr-only/label props/skeleton) = exactly 31 sites, across 18 unique files (not 21 as claimed — minor overcount). Re-read docs/ui-ux-design-guidelines.md motion section: 「ローディングはスケルトン（レイアウト形状を保つ）、スピナー単独は不可」 is binding. Re-read src/components/ui/loading.tsx: Skeleton + SkeletonRows (role=status, sr-only 読み込み中...) exist; 53 non-test files import SkeletonRows (majority pattern). Precedent confirmed at src/app/(dashboard)/prescriptions/prescriptions-table.tsx:116 (path corrected: no components/features prefix). Test lock: 7 test files assert loading text via getByText/queryByText (incl. calendar-view.test.tsx covering a target file) — risk note accurate. Dedup check against ULTRACODE_FINDINGS_20260702.md and EXPANSION_MASTER_TARGETS.md: their loading mentions are false-empty/isError bug findings (different root cause), no skeleton-conversion finding exists. Caveats: not strictly behavior-preserving (visible UI changes; per-site rows/cols shaping needs judgment), and schedules/proposals/loading.tsx:10 is a route-level loading file whose title text may be intentional alongside skeletons — implementer should exclude or handle specially. drug-master-content.tsx concurrent-edit risk correctly flagged.

- [ ] **Codex cross-review**:

## R56 [14] dup-component (effort M) — 7-11 screens hand-roll MetricCard/KpiCard/SummaryCard that StatCard's own doc comment says it replaces (3 adopters); 3-4 copies need StatCard API extension or exclusion (gauge/progress-bar/testid variants)

- Anchor: `src/components/ui/stat-card.tsx:32` (lens component-props-api)
- Files: `src/components/ui/stat-card.tsx`, `src/app/(dashboard)/admin/metrics/metrics-dashboard-content.tsx`, `src/app/(dashboard)/admin/operations-insights/operations-insights-content.tsx`, `src/app/(dashboard)/admin/analytics/analytics-content.tsx`, `src/app/(dashboard)/workflow/workflow-dashboard-view.tsx`, `src/app/(dashboard)/admin/capacity/capacity-content.tsx`, `src/app/(dashboard)/admin/performance/page.tsx`, `src/app/(dashboard)/billing/billing-check-content.tsx` 他
- Scores: payoff 3 / safety 2 / verify 4 / scope 3 / priority 2 = **14**
- Evidence: stat-card.tsx:32 doc comment states 'KPI/件数ストリップの共通カード。各画面で再実装されていた MetricCard/KpiCard/SummaryCard を統合する' — yet only 3 files import it (statistics-content, operating-hours-content, inventory-forecast-content) while 11 local re-implementations remain: MetricCard x4 (metrics-dashboard-content.tsx:64, operations-insights-content.tsx:60, analytics-content.tsx:548, workflow-dashboard-view.tsx:1486), KpiCard x3 (capacity-content.tsx:83, performance/page.tsx:142, billing-check-content.tsx:58), SummaryCard x4 (business-holidays-content.tsx:727, staff-kpi-panel.tsx:223, external-viewer-content.tsx:499, report-delivery-dashboard.tsx:417), each ~25 lines (~270 total) with drifting prop names (helper/caption/detail all = StatCard's hint). Distinct from known finding on the zero-importer Stage-0 component batch: StatCard IS wired; this is per-screen re-implementation by its intended consumers.
- Proposal: Migrate the 11 local cards to StatCard: label→label, value→value, helper/caption/detail→hint, icon→icon, state accents→role (STATUS_TOKENS). One screen per slice; delete each local component after swap.
- Payoff: ~270 lines of near-duplicate JSX deleted; KPI tiles converge on the tabular-nums/left-accent SSOT styling per docs/ui-ux-design-guidelines.md; role-based state colors replace ad-hoc classNames.
- Risk: NOT pixel-identical: locals use e.g. text-3xl values and Card chrome vs StatCard's compact typography — this is a deliberate visual normalization, so needs a design pass ack. Behavior (rendered label/value text) is locked by tests in 9/11 screens (staff-kpi-panel.test.tsx, metrics-dashboard-content.test.tsx, capacity-content.test.tsx, billing-check-content.test.tsx, analytics-content.test.tsx, performance/page.test.tsx, external-viewer-content.test.tsx, report-delivery-dashboard.test.tsx, business-holidays-content.test.tsx); workflow-dashboard-view and operations-insights lack tests — verify visually.
- Verifier (adversarial): Independently re-verified: (1) stat-card.tsx:31-36 doc comment states StatCard consolidates per-screen MetricCard/KpiCard/SummaryCard; (2) grep shows exactly 3 importers (statistics-content, operating-hours-content, inventory-forecast-content) plus its own test; (3) all 11 local definitions exist at the exact cited lines (re-ran the function/const grep); (4) ls confirms 9/11 screens have colocated test files, with workflow-dashboard-view and operations-insights missing, matching the claim. Not a duplicate of the known Stage-0 zero-importer finding (ULTRACODE_FINDINGS_20260702.md:511/514), which explicitly notes StatCard IS wired — different file-set and root cause. Caveat that lowers scope/safety: re-reading the 11 copies shows they are NOT all near-identical to StatCard — metrics-dashboard MetricCard (gauge: max/targetLine/alert/HelpPopover), capacity KpiCard (ProgressBar+StatusDot, comments mark design deliberate), billing-check KpiCard (progress bar, valueClassName, data-testid contract locked by 37 test assertions), performance KpiCard (HelpPopover+tone) carry extra features StatCard lacks; realistic clean-swap set is ~7 screens (~180 lines), and the migration is explicitly NOT pixel-preserving (deliberate normalization needing design ack), as the candidate itself discloses.

- [ ] **Codex cross-review**:

## dedup で除外した既知重複

- `src/components/ui/patient-pinned-header.tsx` — Six orphaned src/components/ui primitives (534 prod lines + 341 test lines, zero importers each) (= F56)
- `src/components/features/visits/visit-card-mobile.tsx` — Dead mobile visit UI remnants: visit-card-mobile.tsx + facility-patient-swipe-rail.tsx (455 prod + 2 (= F26)
- `src/components/features/workflow/reason-dialog.tsx` — Orphaned old workflow-detail widgets: reason-dialog, stage-timeline, previous-stage-summary (297 pro (= F26)
- `src/server/jobs/daily-helpers.ts` — Retire the 3 competing startOfDay implementations in jobs/services in favor of date-boundary helpers (= F66 (also F64, F60, F61, F67; ULTRACODE_EXPANSION_MASTER_TARGETS.md EPIC 5 「タイムゾーン / 日付境界」))
