# Context Snapshot: repo-audit

- Task statement:
  - Audit the entire CareViaX codebase iteratively, fix bugs/inconsistencies/security/performance issues, verify after each change, and continue until completion conditions are met.
- Desired outcome:
  - Whole repository reviewed as audit scope.
  - Necessary fixes applied without narrowing scope arbitrarily.
  - Available verification passes with evidence, or remaining blockers are explicitly documented as incomplete.
- Known facts / evidence:
  - Repo root contains application code in `src/`, tooling in `tools/`, docs in `docs/`, Prisma schema in `prisma/`.
  - Verification scripts in `package.json`: `pnpm lint`, `pnpm test`, `pnpm build`, Playwright audit variants, and several TSX operational scripts.
  - Worktree is already dirty across app routes, server services, tests, config files, and `.omx/` state/log files; avoid overwriting unrelated user changes.
  - Root `AGENTS.md` requires compact progress, evidence-backed completion, and repo-wide verification before claiming done.
  - This repo uses Next.js `16.2.1`; guidance requires reading relevant local Next docs before writing framework-sensitive code.
- Constraints:
  - No arbitrary scope reduction.
  - No destructive git operations or reverting unrelated edits.
  - No new dependencies without explicit request.
  - Must leave resumable records after each iteration.
- Unknowns / open questions:
  - Current baseline failures across lint, tests, and build.
  - Whether existing user changes are already mid-fix for some failures.
  - Which failures are root causes versus downstream symptoms.
- Likely codebase touchpoints:
  - `src/app/**`
  - `src/server/**`
  - `src/lib/**`
  - `src/components/**`
  - `src/types/**`
  - `tools/**`
  - root config (`package.json`, `eslint.config.mjs`, `next.config.ts`, `tsconfig.json`)
  - CI/workflow files if verification points there
