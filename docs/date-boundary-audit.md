# Date Boundary Audit

Audited on 2026-06-12 for `setHours(0, 0, 0, 0)` in non-test `src` files.

Rule: use `todayUtcRange` / `utcDateFromLocalKey` for `@db.Date` comparisons. Keep local-midnight `Date` logic for real `DateTime` columns and UI-only local date math.

| File                                                                 | Line | Usage                                | Compared value / column                                      | `@db.Date`?                                                             | Decision                             |
| -------------------------------------------------------------------- | ---: | ------------------------------------ | ------------------------------------------------------------ | ----------------------------------------------------------------------- | ------------------------------------ |
| `src/server/services/patient-status-tracker.ts`                      |  183 | Recent medication change window      | `MedicationCycle.created_at`                                 | No, `DateTime`                                                          | Keep local midnight.                 |
| `src/server/jobs/next-day.ts`                                        |    8 | Job-local business-day calculation   | `formatDateKey(candidate)` and local weekend checks          | No direct DB comparison                                                 | Keep local date math.                |
| `src/server/jobs/daily-helpers.ts`                                   |   26 | Job due-date helper                  | Generated operational task due dates                         | No direct `@db.Date` comparison                                         | Keep local date math.                |
| `src/server/jobs/daily-helpers.ts`                                   |   48 | Conference text date parsing         | Parsed date from text                                        | No direct `@db.Date` comparison                                         | Keep local date normalization.       |
| `src/server/jobs/daily-helpers.ts`                                   |   54 | Conference text date parsing         | Parsed date from text                                        | No direct `@db.Date` comparison                                         | Keep local date normalization.       |
| `src/app/api/settings/operational-policy/route.ts`                   |   70 | Month-start context                  | Operational policy change-log count context                  | No direct `@db.Date` comparison                                         | Keep local month boundary.           |
| `src/server/services/billing-evidence/core.ts`                       |  308 | Japan-local week start               | Billing evidence week grouping                               | No direct `@db.Date` comparison                                         | Keep local week boundary.            |
| `src/server/services/billing-evidence/core.ts`                       |  492 | Prior visit cutoff                   | `VisitRecord.visit_date`                                     | No, `DateTime`                                                          | Keep local DateTime boundary.        |
| `src/app/api/prescription-intakes/triage/route.ts`                   |  143 | Intake triage time window            | `PrescriptionIntake.prescribed_date` / created intake timing | No, `prescribed_date` is `DateTime`                                     | Keep local DateTime boundary.        |
| `src/app/(dashboard)/patients/[id]/patient-detail-tabs.tsx`          |  229 | Upcoming visit filter                | `VisitSchedule.scheduled_date`                               | Yes, `@db.Date`                                                         | Replaced with `todayUtcRange().gte`. |
| `src/app/api/medication-sets/workspace/set-derivations.ts`           |   30 | Set plan slot derivation             | `SetPlan.target_period_start/end`                            | No, `DateTime` in schema                                                | Keep local date math.                |
| `src/app/api/dashboard/cockpit/route.ts`                             |  131 | Dashboard created-at boundary        | `MedicationCycle.created_at` and other real timestamps       | No, `DateTime`; route already uses `todayUtcRange` for `scheduled_date` | Keep local DateTime boundary.        |
| `src/app/api/dashboard/home/patients/route.ts`                       |  385 | Recent medication change window      | `MedicationCycle.created_at`                                 | No, `DateTime`                                                          | Keep local midnight.                 |
| `src/app/(dashboard)/search/search-content.tsx`                      |  343 | Search filter preset                 | UI date-key generation only                                  | No direct DB comparison                                                 | Keep local UI date math.             |
| `src/app/(dashboard)/prescriptions/intake/intake-triage-content.tsx` |   78 | Received-at display                  | UI display formatting                                        | No DB comparison                                                        | Keep local display boundary.         |
| `src/app/api/admin/master-hub/route.ts`                              |   59 | Vehicle inspection deadline rollover | Parsed date from vehicle notes                               | No DB comparison                                                        | Keep local date math.                |
| `src/app/api/admin/master-hub/route.ts`                              |   68 | Days-until calculation               | UI readiness metric                                          | No DB comparison                                                        | Keep local date math.                |
| `src/app/api/admin/master-hub/route.ts`                              |   70 | Days-until calculation               | UI readiness metric                                          | No DB comparison                                                        | Keep local date math.                |

Non-actionable documentation hit:

- `src/lib/utils/date-boundary.ts:12` explains why local `setHours(0, 0, 0, 0)` must not be used for `@db.Date` comparisons.
