# High ROI Functional Proposals

作成日: 2026-06-18

対象は、現行 CareViaX の実機能に直接つながる改善だけに絞る。新規の大規模 redesign ではなく、既存 API / BFF / UI / service / test の接続を強める前提。

| #   | 領域           | 提案                                                                                      | 根拠となる現行機能                                                                                | 最小実装スライス                                                                                      |
| --- | -------------- | ----------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- |
| 1   | 訪問準備       | 訪問 ready 判定を 1 つの server helper に統一                                             | `visit-preparation-readiness.ts`, visit-preparation GET/PUT, day-board, schedule PATCH ready gate | `buildVisitReadyContext` を export し、day-board / prep pack / ready PATCH のテストを同じ期待へ寄せる |
| 2   | 訪問準備       | 「準備保存」と「ready 化」を atomic action にする                                         | `schedule-day-preparation.ts` は保存後に別 PATCH で ready 化                                      | PUT に `mark_ready` を追加し、ready 失敗時に成功 toast が出ないようにする                             |
| 3   | 訪問準備       | 準備 pack の過去 contact log を PHI 最小表示にする                                        | `visit-preparations/[scheduleId]/route.ts` は contact name / phone / note を扱う                  | method / outcome / due / has_note のみ返し、詳細は権限付き detail 取得へ分離                          |
| 4   | 日程ボード     | 同姓同名患者の識別 chip を day-board / 提案カードへ追加                                   | day-board DTO は `patient_name` 中心                                                              | kana / masked DOB / 施設 unit の compact chip を追加                                                  |
| 5   | 日程提案       | `change_requested` から focused reschedule task を起こす                                  | contact result は `reschedule_pending` に残るが task 化は未整備                                   | contact result 記録時に再提案 task を upsert し、候補詳細へ deep link                                 |
| 6   | 日程変更       | reschedule API の date-key を厳格化                                                       | reschedule route の日付境界が route ごとに散る                                                    | 既存 `visitScheduleDateKeySchema` / strict date helper を再利用                                       |
| 7   | ルート確定     | route confirmation に OCC / freshness を入れる                                            | route snapshot と vehicle assignment が stale でも通る余地                                        | prep GET に schedule version を返し、route confirm PUT で expected version を要求                     |
| 8   | 日程取消       | 日程 cancel 時に関連 task / proposal を掃除する                                           | schedule DELETE は schedule/audit 中心                                                            | visit_preparation / contact_followup task 解決、open proposal supersede を同 transaction へ           |
| 9   | 訪問記録       | handoff extraction を fire-and-forget から retryable task へ                              | visit-record 作成後の extraction 失敗が console warn 止まり                                       | extraction failure を task / status に保存し、retry endpoint を追加                                   |
| 10  | 訪問記録       | next-visit suggestion から日程 proposal を直接生成                                        | visit record は follow-up task を作るが proposal draft には直結しない                             | task action で `source_visit_record_id` 付き proposal 生成                                            |
| 11  | 残薬           | 残薬 issue の dedupe key を薬品コード / 単位込みにする                                    | drug name 中心 dedupe は同名・規格違いに弱い                                                      | residual schema と issue metadata に drug_code / unit を追加                                          |
| 12  | 処方差分       | 訪問準備の処方差分に line id / code / date delta を含める                                 | prep pack は display name 中心                                                                    | DTO に evidence fields を追加し、UI は compact 表示のまま保持                                         |
| 13  | 日程ボード     | schedule operational task count を当日関連に scope する                                   | board 上の task count が org-wide open task に寄りやすい                                          | due-today / overdue / schedule-related group count を追加                                             |
| 14  | 施設訪問       | facility parallel context を redacted packet summary と full packet に分ける              | prep pack が同施設文脈を広く扱う                                                                  | redacted summary DTO を day-board、full detail を facility-packet route に限定                        |
| 15  | ブラウザ検証   | 訪問準備 -> ready -> contact follow-up -> 再提案の route-mocked E2E を追加                | 単体テストは増えたがブラウザ proof が薄い                                                         | Playwright route-mock で schedule board の実導線を固定                                                |
| 16  | 調剤UI         | workbench の title/menu 旧 chrome を撤去し、PhaseTabs と前回処方/新規グループ作成だけ残す | `dispensing-workbench.tsx` にタイトル専用 bar と span menu が残る                                 | 旧 titleBar/menuBar を削除し、主操作を既存 toolbar へ再配置                                           |
| 17  | 調剤           | 調剤安全チェックを定数 ACK ではなく UI 実入力にする                                       | workbench adapter と dispense-results API で checklist 証跡の実入力性が弱い                       | UI checklist state を payload 化し、API で false / 欠落を拒否                                         |
| 18  | 調剤           | barcode verify を DispenseResult の証跡へ結合する                                         | barcode 照合 route と調剤完了記録が分離                                                           | line 別 verification snapshot を保存し、高リスク薬では必須化                                          |
| 19  | 調剤監査       | 監査待ち件数と再監査 queue 条件を共通化                                                   | workbench count と audit route の条件が drift しうる                                              | `dispenseAuditPendingWhere` helper を作り rejected / hold / approved count を固定                     |
| 20  | セット         | 手動 set batch / cell 作成 API に status と OCC guard を入れる                            | stale 画面や直接 API から set_audited 後に作成できる余地                                          | `expected_updated_at` と `cycle.overall_status === setting` を要求                                    |
| 21  | セット         | セットカレンダーの処方 line 取得を最新処方へ統一                                          | calendar 表示と generate-batches が異なる処方集合を見うる                                         | calendar も latest intake のみへ寄せ、2 intake fixture で表示/生成一致を確認                          |
| 22  | セット         | セット数量配分に不可分単位・端数ルールを入れる                                            | 錠/包など物理的に作れない小数セルが発生しうる                                                     | unit step allocator を追加し、錠/包は整数、g/mL は小数許可                                            |
| 23  | セット監査     | partial set audit の再作業 task を冪等化する                                              | 部分承認を繰り返すと高優先 task が重複しうる                                                      | plan_id scoped dedupe / upsert を導入                                                                 |
| 24  | セット監査     | `photo_asset_ids` の所有権を検証する                                                      | 任意 asset id を監査証跡に紐付けられる IDOR 余地                                                  | FileAsset を org / patient / cycle / purpose で照合し、foreign asset は拒否                           |
| 25  | 患者詳細       | 在宅支援サマリーの算定ブロッカーを BillingEvidence に接続                                 | `home-care-ops` の patient summary は task 数中心                                                 | `listBillingEvidenceBlockers` を患者別 summary に反映                                                 |
| 26  | 報告一覧       | CareReport 一覧を DB 側 pagination / filtering へ移す                                     | care-reports list はメモリ filter / cursor 処理が大きい                                           | Prisma `take: limit + 1` と DB where に移管                                                           |
| 27  | 報告           | CareReport list と detail の content 契約を分離                                           | list `include_content` で raw content が乗る                                                      | list は `content_summary` のみ、detail は internal/external preview を分離                            |
| 28  | 報告送付       | report send API に Idempotency-Key 契約を追加                                             | intent key は宛先材料中心                                                                         | header / payload key を保存し、same-key different-body は 409                                         |
| 29  | 報告送付       | channel ごとに `external_sent / manual_recorded / internal_share` を明示                  | fax / postal / phone は実送信と記録が混ざりやすい                                                 | DeliveryRecord metadata と response に delivery mode を追加                                           |
| 30  | PDF/共有       | メール PDF リンクを短命 external token 化                                                 | pdf_url をメール本文へ直接入れる経路がある                                                        | send 時に grant/token を発行し、メールには token URL のみ                                             |
| 31  | 報告生成       | source_provenance を internal と external summary に分ける                                | prescription line id / drug code が report content に残る                                         | PDF/送付は summary のみ参照する test を追加                                                           |
| 32  | PDF            | CareReport PDF に audience role gate を入れる                                             | PDF gate は status confirmed 中心                                                                 | `report_type -> audience` 不一致を 409                                                                |
| 33  | PDF            | CareReport 系で generic JSON flatten fallback を禁止する                                  | `pdf-documents.tsx` に汎用 flatten が残る                                                         | report_type unknown は出力不可、内部 JSON が PDF text に出ない test                                   |
| 34  | 請求           | BillingCandidate review に OCC を入れる                                                   | confirm/exclude が last-write-wins になりうる                                                     | `updated_at` / version を payload 要求し stale は 409                                                 |
| 35  | 請求月次       | close/export の外部送信を retryable job 化                                                | close route が webhook / claims export を同期実行                                                 | close は job 作成まで、送信は job runner へ                                                           |
| 36  | 請求連携       | claims export に site 境界を入れる                                                        | export adapter に `siteId: ''` が渡る箇所がある                                                   | site 未解決は 422、解決済みのみ adapter input                                                         |
| 37  | 請求CSV        | PHI 最小版 CSV export profile を追加                                                      | CSV に patient name / building / yj codes が含まれる                                              | `profile=summary/external` を追加し external は匿名化                                                 |
| 38  | 請求PDF        | receipt / invoice PDF の金額整合チェック                                                  | 発行状態中心で PDF 化される                                                                       | receipt は collected > 0、invoice は billed > 0 を gate                                               |
| 39  | 外部共有       | external access の閲覧監査を scope 別に記録                                               | grant は accessed_at 更新中心                                                                     | grant_id / scope / IP / UA / viewed_at の event を保存                                                |
| 40  | 外部共有       | self-report POST を冪等化                                                                 | mobile retry / double tap で報告が重複しうる                                                      | Idempotency-Key または body hash + grant short window                                                 |
| 41  | 患者一覧       | board payload から住所 PHI を落とす                                                       | UI 表示に使わない address が list cache に乗る                                                    | server-side search hint へ置換し、住所は detail のみに限定                                            |
| 42  | 患者一覧       | chip/count 集計を表示上限 slice から分離                                                  | 80件 slice 後の集計に引きずられる                                                                 | count / groupBy を別 query で返す                                                                     |
| 43  | 患者一覧       | 外部待ち / 照会待ちカードを communications / workflow へ直リンク                          | status は出るが link が患者詳細 fallback                                                          | `action_href` を該当 queue に設定                                                                     |
| 44  | 連絡先         | masked contact 値の overwrite を防ぐ                                                      | masked GET 値を UI state に入れて PUT しうる                                                      | masked role は編集不可、または unchanged masked fields を除外                                         |
| 45  | 連絡先         | delete/create 全置換を差分更新へ変える                                                    | contact PUT が全削除・全作成                                                                      | contact id 維持、diff audit、同時編集 conflict                                                        |
| 46  | 患者詳細       | communication panel を contacts / queue で error 分離                                     | 片方の API 失敗で panel 全体が落ちる                                                              | 独立 loading/error と retry button                                                                    |
| 47  | 連携依頼       | 緊急連絡 draft 作成を冪等化                                                               | patient detail から同じ draft を複数作れる                                                        | patient/template/source dedupe key で既存 draft を返す                                                |
| 48  | 連携依頼       | 返信待ち対応を transaction action にまとめる                                              | UI が response/task/status を複数 fetch で処理                                                    | `/resolve-followup` で response + task + close を transaction                                         |
| 49  | タスク         | `/api/tasks` に pagination と inline complete 可否を追加                                  | tasks list は全件取得、UI は部分失敗で知る                                                        | cursor pagination、`can_complete_inline` を返し UI で選択制御                                         |
| 50  | ダッシュボード | urgent card を task id 付き deep link と live clock にする                                | cockpit の urgent link は広い遷移、now は render 時固定                                           | `/audit?taskId=...` 等へ遷移し、clock は minute tick で更新                                           |
| 51  | 患者一覧       | residence label を粗い区分だけにする                                                      | board は施設名断片を `residence_label` と検索対象に載せうる                                       | `在宅/施設/入院中` に固定し、facility name/address が JSON に出ない test                              |
| 52  | 患者比較       | compare board の対象選定理由を PHI 最小サマリにする                                       | compare は patient board BFF を再利用しカード状態から候補を選ぶ                                   | candidate reason を attention / blocker count のみにし、住所・施設名を使わない                        |
| 53  | 患者正本       | 住所・連絡先差分履歴を値ではなく field-level change にする                                | patient detail は正本と履歴を同画面で扱う                                                         | address/phone は `変更あり` 表示、値は権限付き編集画面だけ                                            |
| 54  | 患者文書       | 文書 panel に source freshness を出す                                                     | patient documents / consent / management-plan が別々に更新される                                  | 最終確認日・期限切れ・未添付を compact badge 化                                                       |
| 55  | 管理計画       | 管理計画の期限切れを operational task に接続                                              | ready gate は approved plan を見るが、期限切れ前の予防 task が薄い                                | `management_plan_expiring` task を upsert し患者詳細/日程boardへ表示                                  |
| 56  | 同意           | 同意期限・撤回を患者 board / visit board の blocker に出す                                | consent records は ready 判定に効くが一覧で見えにくい                                             | PHI なしの `同意期限/撤回` count を readiness summary に追加                                          |
| 57  | 検査値         | eGFR など検査値の古さを安全タグと task にする                                             | patient board は lab 有無だけで renal tag を出す                                                  | 検査日 stale / missing を区別し、詳細へ deep link                                                     |
| 58  | 薬歴PDF        | 服薬カレンダー PDF に internal/external profile を分ける                                  | medication calendar PDF route が患者・薬剤情報を出力                                              | external profile では住所・内部メモ・line id を除外                                                   |
| 59  | MCS連携        | MCS link の同期状態を患者 workflow preview に出す                                         | MCS link card と communication workflow が別々                                                    | `linked/sync_failed/pending` を患者詳細 rail に表示                                                   |
| 60  | Collaboration  | room token を患者/ケース scope と短 TTL に限定                                            | collaboration room token route が患者 collaboration で使われる                                    | token payload に patient/case scope、TTL、監査 event を追加                                           |
| 61  | 連絡一覧       | communication export に redaction profile を追加                                          | communication-requests export route がある                                                        | `profile=internal/external` で電話・住所・note の出力を切替                                           |
| 62  | 連絡依頼       | request queue に cursor と stale update guard を入れる                                    | requests content は queue 操作と一覧更新を同時に扱う                                              | `cursor` / `expected_updated_at` を追加し、409 を UI で再読込                                         |
| 63  | 連絡履歴       | communication event attachment の所有権を検証                                             | communication-events / files routes が別々                                                        | file asset を org/patient/thread/purpose で照合してから紐付け                                         |
| 64  | 外部専門職     | suggestions を施設/患者関係で tenant-safe に絞る                                          | external-professionals suggestions が入力補完に使われる                                           | org + facility/case relation で候補制限、理由 badge を返す                                            |
| 65  | 連絡先マスタ   | contact profile の重複候補 merge queue を作る                                             | admin contact-profiles と患者 care-team link が分かれる                                           | phone/fax/name 正規化で候補提示、merge は手動承認                                                     |
| 66  | ダッシュボード | cockpit card の stale data indicator を追加                                               | dashboard cockpit は複数 BFF の集計を表示                                                         | `generated_at` / source age をカード単位で出し古い集計を明示                                          |
| 67  | Workflow       | workflow dashboard cache invalidation を domain event に寄せる                            | workflow-dashboard-cache と複数 API mutation がある                                               | mutation 後に対象 section key を明示 invalidate                                                       |
| 68  | 通知           | notifications に cursor pagination と既読 bulk action を追加                              | notifications content/query-state が一覧を扱う                                                    | `cursor` / `mark_read_bulk`、未読 count を optimistic ではなく response で確定                        |
| 69  | 監査ログ       | audit-log export を job 化して範囲と redaction を固定                                     | audit-logs export route が同期出力                                                                | export request -> job -> downloadable manifest                                                        |
| 70  | 管理分析       | operations insights から該当 queue へ deep link する                                      | admin operations-insights は傾向を出すが実作業導線が薄い                                          | metric card に `/tasks?type=...` / `/billing?...` link を付ける                                       |
| 71  | 算定根拠       | visit record 保存時に billing evidence check を inline 表示                               | billing-evidence check route と visit record UI が分離                                            | 保存後 response に blocker summary を返し、訪問記録画面に出す                                         |
| 72  | 算定候補       | exclude/confirm に理由必須と audit snapshot を入れる                                      | billing-candidates review 操作が状態変更中心                                                      | reason code / expected version / before-after audit を必須化                                          |
| 73  | 入金           | collection receipt を冪等化                                                               | billing-candidates collection route が入金記録を扱う                                              | Idempotency-Key と same-body replay を実装                                                            |
| 74  | 月次請求       | close/export job の進捗を billing UI に表示                                               | close/export route と billing candidates UI が分離                                                | job status panel と retry/cancel を追加                                                               |
| 75  | 算定ルール     | billing rule revision の dry-run diff を UI に出す                                        | billing-rules revisions と admin billing-rules page がある                                        | 旧/新 revision の候補差分を patient count だけで preview                                              |
| 76  | 算定分析       | billing evidence stats を patient/service scope で drilldown                              | billing-evidence stats/analytics route がある                                                     | blocker type -> patient list -> detail link の 3段階                                                  |
| 77  | 請求CSV        | export 前 preview と downloadable manifest を作る                                         | billing-candidates export route が直接ファイル生成                                                | 件数・金額・除外理由集計を確認してから export                                                         |
| 78  | PCA            | PCA rental billing の月跨ぎ重複 guard を UI に出す                                        | pca-rental-billing service と admin PCA pump UI がある                                            | overlap warning と対象契約 link を candidates UI に表示                                               |
| 79  | 報告reminder   | care report reminders を dedupe / snooze 可能にする                                       | care-reports reminders route がある                                                               | report/patient/month dedupe key、snooze_until を追加                                                  |
| 80  | 今日の報告     | today-workspace に report readiness と算定 blocker を同時表示                             | care-reports today-workspace と billing evidence が分離                                           | patientごとに `report_missing / evidence_blocked` を compact 表示                                     |
| 81  | 外部共有       | external access token を document scope 単位に分ける                                      | external-access grant と report/patient share がある                                              | grant scope に `report/pdf/self_report` を持たせ、UIでscope表示                                       |
| 82  | 外部自己報告   | self-report draft autosave と duplicate submit guard                                      | external viewer self-report POST がある                                                           | local draft + body hash idempotency + submit disabled while pending                                   |
| 83  | PDF一括        | bulk PDF export に manifest と retention を追加                                           | pdf-bulk-export service/job がある                                                                | zip 内 manifest、期限切れ cleanup、失敗 item list                                                     |
| 84  | 報告送付       | delivery failure retry を share workspace に戻す                                          | report share workspace と send route が分離                                                       | DeliveryRecord failed state から再送 action を表示                                                    |
| 85  | 会議録PDF      | conference-note PDF に audience gate と参加者 redaction                                   | conference notes PDF route がある                                                                 | patient/family/external audience profile を分ける                                                     |
| 86  | 薬品マスタ     | import conflict review queue を作る                                                       | drug-master-imports と import logs がある                                                         | conflict rows を保存し admin/drug-masters で承認/却下                                                 |
| 87  | 薬品マスタ     | import logs から影響薬剤へ deep link                                                      | drug-master-import-logs route と drug master admin UI がある                                      | log row に changed drug count と filtered link                                                        |
| 88  | 後発品         | generic recommendation の承認 workflow を入れる                                           | drug-masters generic-recommendations route がある                                                 | 推奨を draft/approved に分け、処方入力は approved のみ使用                                            |
| 89  | 在庫           | usage mismatch の解決を stock history と transaction 化                                   | pharmacy-drug-stocks usage-mismatch / history routes がある                                       | resolve action で adjustment + audit + mismatch close                                                 |
| 90  | 在庫           | bulk copy / template apply を dry-run から確定へ二段階化                                  | stock copy/template/apply routes がある                                                           | preview diff -> confirm token -> apply                                                                |
| 91  | 在庫CSV        | stock export に internal/external profile を追加                                          | pharmacy-drug-stocks export route がある                                                          | external は薬局内部原価・ロット詳細を除外                                                             |
| 92  | 在庫予測       | inventory forecast から補充 request を直接作る                                            | inventory-forecast analytics と stock request route がある                                        | forecast row action で request draft 作成                                                             |
| 93  | 安全follow-up  | pharmacy stock safety follow-up task を冪等化                                             | safety-follow-up route と operational tasks が分離                                                | stock_id/type/month dedupe key で task upsert                                                         |
| 94  | 薬剤警告       | alert rule impact preview を保存前に表示                                                  | drug-alert-rules admin route がある                                                               | 対象薬剤件数・直近処方件数・想定alert数を preview                                                     |
| 95  | 施設マスタ     | facility unit の重複 guard と merge workflow                                              | admin facilities units routes がある                                                              | normalized unit name unique guard、manual merge                                                       |
| 96  | サービス範囲   | service area geocode failure queue を作る                                                 | service-area/admin analytics と geocode fields がある                                             | failed geocode を admin task として表示                                                               |
| 97  | スタッフ       | pharmacist credential expiry を operational task 化                                       | pharmacist-credentials admin page/route がある                                                    | expiry window で task upsert、更新後 close                                                            |
| 98  | 車両           | vehicle maintenance / max_stops policy を schedule blocker に接続                         | admin vehicles と schedule capacity check がある                                                  | unavailable/maintenance vehicle を提案生成・day-boardで blocker表示                                   |
| 99  | 文書テンプレ   | template publish を version/OCC 付きにする                                                | document-template editor と delivery-rule manager がある                                          | draft -> publish、expected version、rollback link                                                     |
| 100 | UAT/改善       | UAT feedback を PHI redacted task に変換                                                  | admin UAT feedback route/content がある                                                           | feedback -> triage task 作成、patient-like text redaction、owner assignment                           |

## 先に実装したい順

1. 患者詳細・訪問準備・日程ボードにまたがる安全/請求 blocker の接続強化。
2. PHI が一覧 cache に広く乗る箇所の projection 縮小。
3. contact / report / external self-report の idempotency と partial failure 対策。
4. ready / review / close などの状態遷移に OCC を追加。
5. route-mocked E2E で、訪問準備から報告・算定までの実導線を固定。
6. 実ワークフロー証明として、訪問記録 -> 報告下書き -> 送付、外部 self-report -> 内部 queue、offline draft -> replay を優先して固定。
7. reports / billing / document-template は、送付前 checklist 永続化、回答記録 endpoint、請求 export preflight、template version/OCC から着手。
8. master / inventory は、import conflict review、bulk apply dry-run token、stock safety follow-up、inventory forecast -> request の順に実装。
